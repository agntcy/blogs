<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/feed.xml" rel="self" type="application/atom+xml" /><link href="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/" rel="alternate" type="text/html" /><updated>2026-04-02T15:02:19+00:00</updated><id>https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/feed.xml</id><title type="html">AGNTCY Blogs</title><subtitle>Building infrastructure for the Internet of Agents</subtitle><entry><title type="html">SLIM for Observability and Remediation: Beyond Agentic AI</title><link href="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/04/02/transporting-opentelemetry-over-slim.html" rel="alternate" type="text/html" title="SLIM for Observability and Remediation: Beyond Agentic AI" /><published>2026-04-02T07:00:00+00:00</published><updated>2026-04-02T07:00:00+00:00</updated><id>https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/04/02/transporting-opentelemetry-over-slim</id><content type="html" xml:base="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/04/02/transporting-opentelemetry-over-slim.html"><![CDATA[<p><a href="https://github.com/agntcy/slim">SLIM (Secure Low-Latency Interactive Messaging)</a> was designed as the transport layer for agentic AI protocols like <a href="https://a2a-protocol.org/latest/">A2A (Agent-to-Agent)</a>. While SLIM was built to enable AI agents to communicate securely across network boundaries, its core capabilities—network traversal, end-to-end encryption, and dynamic channel management—also apply to standard distributed applications. This post explores its use for an <strong>observability and remediation</strong> use case.</p>

<p>Modern distributed systems face critical challenges when transporting telemetry data. Applications run behind firewalls, in different cloud regions, at the edge, or across organizational boundaries. Traditional observability requires exposed collector endpoints and complex firewall management. Telemetry contains sensitive information yet TLS terminates at load balancers, leaving data exposed through intermediate infrastructure. Furthermore, hardcoded endpoint configurations break as systems scale elastically across regions.</p>

<p>SLIM addresses these challenges by enabling remote components to communicate without requiring exposed endpoints or complex NAT/firewall configurations. <a href="https://www.rfc-editor.org/rfc/rfc9420.txt">Message Layer Security (MLS)</a> protects telemetry from source to destination, and dynamic channel management allows you to reconfigure topologies at runtime without restarting applications.</p>

<h2 id="the-observability-and-remediation-use-case">The Observability and Remediation Use Case</h2>

<p>Consider a modern distributed application running across multiple environments—cloud, edge, and on-premises—often spanning separate Kubernetes clusters in different regions or cloud providers. When incidents occur, you need real-time monitoring with continuous telemetry streaming to dashboards and storage. Different stakeholders—operations teams in one cluster, SREs in another, executives from headquarters—need visibility into the same data from different network segments. AI agents should monitor telemetry streams for anomalies, and when problems arise, specialized diagnostic agents running in separate clusters should join dynamically to investigate root causes while engineers observe live agent analysis and approve remediation actions.</p>

<p>Traditional observability architectures struggle with this scenario. Hardcoded collector addresses make it difficult to add consumers dynamically across network segments. Sharing telemetry between clusters requires VPNs or complex tunneling. Broadcasting to multiple consumers requires intermediate message queues. Most critically, standard telemetry pipelines weren’t designed for AI agents to join channels dynamically and collaborate.</p>

<h2 id="why-use-slim-for-observability">Why Use SLIM for Observability?</h2>

<p>SLIM provides several capabilities that address common challenges in distributed observability.</p>

<h3 id="network-traversal-without-exposed-endpoints">Network Traversal Without Exposed Endpoints</h3>

<p>When working across Kubernetes clusters—whether in different AWS regions, separate GCP projects, or hybrid cloud/on-premises environments—each cluster boundary becomes a network boundary requiring careful firewall configuration, load balancer setup, and security group management.</p>

<p>SLIM simplifies these requirements. Applications and collectors connect outbound to SLIM nodes, meaning only the SLIM routing infrastructure needs exposed addresses. Components behind firewalls and NATs can communicate without VPN tunnels or complex ingress configurations. A workload in your AWS EKS cluster can send telemetry to a collector in your on-premises Kubernetes cluster without port forwarding or firewall exceptions.</p>

<h3 id="end-to-end-encryption-for-sensitive-telemetry">End-to-End Encryption for Sensitive Telemetry</h3>

<p>While standard OTLP over TLS protects point-to-point connections, TLS frequently terminates at load balancers or proxies. This means your telemetry—containing API keys, business metrics, and internal system details—flows in plaintext through infrastructure you may not fully control.</p>

<p>SLIM uses Message Layer Security (MLS) to provide true end-to-end encryption from application to collector. Messages remain encrypted even when traversing SLIM nodes, load balancers, or proxies. Only authorized channel participants possess the keys to decrypt telemetry, ensuring confidentiality across untrusted infrastructure.</p>

<h3 id="dynamic-channel-management">Dynamic Channel Management</h3>

<p>Traditional observability architectures are largely static. Routing telemetry to a new collector typically requires application restarts. Adding a monitoring agent to investigate an incident requires reconfiguration and redeployment. Pre-configured topologies can’t easily adapt at runtime.</p>

<p>SLIM’s Channel Manager API enables runtime topology changes without restarts or downtime. When a monitor agent detects elevated latency, it can create an incident-specific channel and invite specialized diagnostic agents to join the telemetry stream. Once the investigation completes, it removes participants dynamically via API calls.</p>

<h3 id="broadcast-telemetry-to-multiple-consumers">Broadcast Telemetry to Multiple Consumers</h3>

<p>Point-to-point OTLP forces an awkward choice: either implement multiple exporters in your applications (adding complexity and resource consumption) or introduce intermediate message queues (adding latency and operational complexity). Neither option is ideal when you want collectors, dashboards, and AI agents all receiving the same telemetry simultaneously.</p>

<p>SLIM’s broadcast channels support multiple simultaneous consumers. A single application publishes to a channel, and all participants—traditional collectors storing to Prometheus, real-time Grafana dashboards, AI agents performing anomaly detection, and specialized diagnostic agents analyzing patterns—receive the same data stream.</p>

<h3 id="ai-agent-integration">AI Agent Integration</h3>

<p>Standard telemetry pipelines were designed for collectors and storage systems, not AI agents. Agents that need to analyze telemetry typically must poll storage backends (introducing latency) and have limited ways to coordinate with other agents.</p>

<p>SLIM treats AI agents as channel participants. Agents receive live telemetry streams alongside collectors, eliminating polling latency. They can use the A2A protocol on separate SLIM channels to coordinate their analysis and share findings. Agents join and leave channels dynamically based on incident needs, with both telemetry and agent communication using the same MLS security model.</p>

<h2 id="demo-scenario-intelligent-incident-response">Demo Scenario: Intelligent Incident Response</h2>

<p>The following demo demonstrates SLIM’s capabilities for dynamic observability and AI-powered incident response.</p>

<p>The monitor application starts up and creates a SLIM channel for telemetry. It invites two participants: the monitored application (which generates metrics using the SLIM OpenTelemetry SDK) and an OpenTelemetry Collector (configured with a SLIM receiver to export metrics to Prometheus and Grafana). The monitor app itself also joins the channel to watch the telemetry stream for anomalies.</p>

<p>During normal operation, the monitored app continuously sends metrics—active connections and service latency. The collector stores these metrics for dashboards while the monitor app observes in the background.</p>

<p>When the application enters a high load period where the number of active connections increases and processing latency exceeds 200ms (the threshold used by the monitoring application), the monitor app triggers an alert.</p>

<p>The monitor app then invites a specialized diagnostic agent to join the telemetry channel. This agent collects metrics for 10 seconds, performs statistical analysis, and queries Azure OpenAI’s GPT-4 to diagnose the root cause. After completing the analysis, the agent prints the findings and signals completion.</p>

<p>The monitor app receives the completion notification, removes the special agent from the channel, and waits for the next incident. In the demo, this flow repeats in a cycle, simulating periodic issues. While the demo runs locally for simplicity, the same architecture works across cluster boundaries.</p>

<p>Here’s the architecture:</p>

<pre><code class="language-mermaid">graph TB
    APP[Monitored App&lt;br/&gt;Metrics: connections, latency]
    
    subgraph "SLIM Node"
        CHANNEL[SLIM Broadcast Channel&lt;br/&gt;MLS Encrypted]
    end
    
    subgraph "Monitoring &amp; Storage"
        COLLECTOR[OTel Collector&lt;br/&gt;SLIM Receiver]
        PROM[Prometheus]
        GRAF[Grafana Dashboard]
    end
    
    MONITOR[Monitor App&lt;br/&gt;Creates Channel &amp; Detects Anomalies]
    SPECIAL[Special Agent&lt;br/&gt;AI-Powered Diagnosis]
    
    MONITOR --&gt;|0. Create channel &amp; invite| CHANNEL
    APP --&gt;|1. Publish metrics| CHANNEL
    CHANNEL --&gt;|Broadcast metrics| COLLECTOR
    CHANNEL --&gt;|Broadcast metrics| MONITOR
    CHANNEL -.-&gt;|Broadcast metrics - after invite| SPECIAL
    COLLECTOR --&gt; PROM
    PROM --&gt; GRAF
    
    MONITOR --&gt;|2. Detect high latency| MONITOR
    MONITOR -.-&gt;|3. Invite special agent| SPECIAL
    SPECIAL --&gt;|4. Analyze with GPT-4| SPECIAL
    
    style APP fill:#4a90e2,color:#fff
    style CHANNEL fill:#f39c12,color:#fff
    style COLLECTOR fill:#27ae60,color:#fff
    style MONITOR fill:#9b59b6,color:#fff
    style SPECIAL fill:#e74c3c,color:#fff
    style PROM fill:#95a5a6,color:#fff
    style GRAF fill:#95a5a6,color:#fff
</code></pre>

<p>The architecture shows: a single source (the monitored app) broadcasts telemetry to multiple consumers (collector and monitor app). The monitor app creates the channel and manages participant lifecycles. The special agent joins when needed, with no pre-configuration. LLM-powered diagnostics operate on live telemetry streams. Standard OpenTelemetry metrics flow through SLIM to Prometheus/Grafana. All communication is encrypted with MLS.</p>

<p>The building blocks for observability over SLIM—including the SLIM receiver and exporter for collectors and SDK exporter for applications—are available in the <a href="https://github.com/agntcy/slim-otel">slim-otel repository</a>. 
The complete working demo instead is available in the <a href="https://github.com/agntcy/agentic-apps/tree/main/observability_app">agentic-apps repository</a> in the observability_app folder.</p>

<h2 id="running-the-demo">Running the Demo</h2>

<p>This section provides step-by-step instructions to run the incident response demo.</p>

<h3 id="step-1-clone-and-build-the-collector">Step 1: Clone and Build the Collector</h3>

<p>Clone the repository and build the custom OpenTelemetry Collector with SLIM components:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/agntcy/agentic-apps.git
<span class="nb">cd </span>agentic-apps/observability_app

<span class="c"># Build the collector with SLIM receiver/exporter as Docker image</span>
task collector:docker:build
</code></pre></div></div>

<p>This will install the <a href="https://opentelemetry.io/docs/collector/extend/ocb/">OpenTelemetry Collector Builder (OCB)</a>, generate the collector code with SLIM components using <code class="language-plaintext highlighter-rouge">builder-config.yaml</code>, and create a Docker image.</p>

<h3 id="step-2-start-infrastructure">Step 2: Start Infrastructure</h3>

<p>Start all infrastructure services (SLIM node, OTel Collector, Prometheus, and Grafana):</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>task infra:start
</code></pre></div></div>

<p>Verify all services are running:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>task infra:status
</code></pre></div></div>

<h3 id="step-3-configure-grafana-dashboard">Step 3: Configure Grafana Dashboard</h3>

<p>Import the pre-configured dashboard:</p>
<ol>
  <li>Open http://localhost:3000 and login with <code class="language-plaintext highlighter-rouge">admin</code> / <code class="language-plaintext highlighter-rouge">admin</code></li>
  <li>Navigate to Dashboards → Import</li>
  <li>Upload the <code class="language-plaintext highlighter-rouge">grafana-dashboard.json</code> file available in the repo</li>
</ol>

<h3 id="step-4-run-the-applications">Step 4: Run the Applications</h3>

<p>Open three separate terminal windows and run each application:</p>

<p><strong>Terminal 1 - Monitored Application</strong> (generates metrics):</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>task monitored-application:run
</code></pre></div></div>

<p><strong>Terminal 2 - Monitor Application</strong> (creates channel, detects anomalies):</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>task monitor-application:run
</code></pre></div></div>
<p>This will invite both the <strong>monitored-application</strong> and the <strong>OTel Collector</strong> to the same SLIM channel.
This channel is used to distribute telemetry</p>

<p><strong>Terminal 3 - Special Agent</strong> (AI-powered diagnostics):</p>

<p>First, set your Azure OpenAI credentials:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">AZURE_OPENAI_API_KEY</span><span class="o">=</span><span class="s2">"your-api-key"</span>
<span class="nb">export </span><span class="nv">AZURE_OPENAI_ENDPOINT</span><span class="o">=</span><span class="s2">"https://your-endpoint.openai.azure.com/"</span>
<span class="nb">export </span><span class="nv">AZURE_OPENAI_DEPLOYMENT</span><span class="o">=</span><span class="s2">"gpt-4o"</span>  <span class="c"># Optional, defaults to gpt-4o</span>
</code></pre></div></div>

<p>Then run the agent:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>task special-agent:run
</code></pre></div></div>

<p>The <strong>special-agent</strong> will start and wait to be invited by the <strong>monitor-application</strong></p>

<h3 id="step-5-observe-the-demo-flow">Step 5: Observe the Demo Flow</h3>

<p>Watch the terminal outputs to see the incident response cycle:</p>

<p><strong>Low Load Period (20 seconds)</strong></p>
<ul>
  <li>Monitored app sends metrics (connections: ~50, latency: ~50ms)</li>
  <li>Monitor app observes quietly</li>
  <li>Metrics flow to Collector → Prometheus → Grafana</li>
</ul>

<p><strong>High Load Period (20 seconds) - Incident Detected</strong></p>
<ul>
  <li>Connections increase and Service Latency spikes</li>
  <li>Monitor app detects consecutive samples above 200ms threshold and triggers the alert</li>
  <li>Monitor app invites the special agent</li>
</ul>

<p><strong>AI Analysis (10 seconds)</strong></p>
<ul>
  <li>Special agent joins the channel and collects telemetry</li>
  <li>GPT-4 analyzes the metrics and produces diagnostic insights</li>
  <li>Special agent reports findings</li>
</ul>

<p><strong>Cleanup and Reset</strong></p>
<ul>
  <li>Monitor app receives completion notification</li>
  <li>Monitor app removes the special agent from the channel</li>
  <li>Cycle repeats as app alternates between low and high load</li>
</ul>

<h3 id="step-6-view-dashboards">Step 6: View Dashboards</h3>

<p>View real-time metrics in Grafana:</p>

<ol>
  <li>Navigate to http://localhost:3000</li>
  <li>Open the metrics on the imported dashboard</li>
</ol>

<h3 id="step-7-clean-up">Step 7: Clean Up</h3>

<p>Stop all applications with <code class="language-plaintext highlighter-rouge">Ctrl+C</code> in each terminal.</p>

<p>Stop infrastructure:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>task infra:stop
</code></pre></div></div>

<p>This stops and removes all Docker containers (SLIM node, collector, Prometheus, Grafana).</p>

<h2 id="conclusion">Conclusion</h2>

<p>SLIM extends beyond agentic AI to support observability and remediation in distributed applications. The demo shows how AI agents can join telemetry channels dynamically to diagnose incidents using LLMs, collaborating with traditional collectors over MLS-encrypted channels that work across cluster boundaries.</p>

<p>The <a href="https://github.com/agntcy/slim-otel">SLIM-OTel code</a> and <a href="https://github.com/agntcy/agentic-apps/tree/main/observability_app">observability demo</a> are open source. You can try it in your environment, adapt it for your setup, or contribute to the codebase.</p>

<hr />

<p><em>SLIM and SLIM OpenTelemetry components are developed by AGNTCY Contributors and released under the Apache 2.0 License.</em></p>]]></content><author><name>Michele Papalini</name></author><category term="technical" /><category term="slim" /><category term="opentelemetry" /><category term="observability" /><category term="telemetry" /><category term="mls" /><category term="encryption" /><category term="agents" /><summary type="html"><![CDATA[SLIM (Secure Low-Latency Interactive Messaging) was designed as the transport layer for agentic AI protocols like A2A (Agent-to-Agent). While SLIM was built to enable AI agents to communicate securely across network boundaries, its core capabilities—network traversal, end-to-end encryption, and dynamic channel management—also apply to standard distributed applications. This post explores its use for an observability and remediation use case. Modern distributed systems face critical challenges when transporting telemetry data. Applications run behind firewalls, in different cloud regions, at the edge, or across organizational boundaries. Traditional observability requires exposed collector endpoints and complex firewall management. Telemetry contains sensitive information yet TLS terminates at load balancers, leaving data exposed through intermediate infrastructure. Furthermore, hardcoded endpoint configurations break as systems scale elastically across regions. SLIM addresses these challenges by enabling remote components to communicate without requiring exposed endpoints or complex NAT/firewall configurations. Message Layer Security (MLS) protects telemetry from source to destination, and dynamic channel management allows you to reconfigure topologies at runtime without restarting applications. The Observability and Remediation Use Case Consider a modern distributed application running across multiple environments—cloud, edge, and on-premises—often spanning separate Kubernetes clusters in different regions or cloud providers. When incidents occur, you need real-time monitoring with continuous telemetry streaming to dashboards and storage. Different stakeholders—operations teams in one cluster, SREs in another, executives from headquarters—need visibility into the same data from different network segments. AI agents should monitor telemetry streams for anomalies, and when problems arise, specialized diagnostic agents running in separate clusters should join dynamically to investigate root causes while engineers observe live agent analysis and approve remediation actions. Traditional observability architectures struggle with this scenario. Hardcoded collector addresses make it difficult to add consumers dynamically across network segments. Sharing telemetry between clusters requires VPNs or complex tunneling. Broadcasting to multiple consumers requires intermediate message queues. Most critically, standard telemetry pipelines weren’t designed for AI agents to join channels dynamically and collaborate. Why Use SLIM for Observability? SLIM provides several capabilities that address common challenges in distributed observability. Network Traversal Without Exposed Endpoints When working across Kubernetes clusters—whether in different AWS regions, separate GCP projects, or hybrid cloud/on-premises environments—each cluster boundary becomes a network boundary requiring careful firewall configuration, load balancer setup, and security group management. SLIM simplifies these requirements. Applications and collectors connect outbound to SLIM nodes, meaning only the SLIM routing infrastructure needs exposed addresses. Components behind firewalls and NATs can communicate without VPN tunnels or complex ingress configurations. A workload in your AWS EKS cluster can send telemetry to a collector in your on-premises Kubernetes cluster without port forwarding or firewall exceptions. End-to-End Encryption for Sensitive Telemetry While standard OTLP over TLS protects point-to-point connections, TLS frequently terminates at load balancers or proxies. This means your telemetry—containing API keys, business metrics, and internal system details—flows in plaintext through infrastructure you may not fully control. SLIM uses Message Layer Security (MLS) to provide true end-to-end encryption from application to collector. Messages remain encrypted even when traversing SLIM nodes, load balancers, or proxies. Only authorized channel participants possess the keys to decrypt telemetry, ensuring confidentiality across untrusted infrastructure. Dynamic Channel Management Traditional observability architectures are largely static. Routing telemetry to a new collector typically requires application restarts. Adding a monitoring agent to investigate an incident requires reconfiguration and redeployment. Pre-configured topologies can’t easily adapt at runtime. SLIM’s Channel Manager API enables runtime topology changes without restarts or downtime. When a monitor agent detects elevated latency, it can create an incident-specific channel and invite specialized diagnostic agents to join the telemetry stream. Once the investigation completes, it removes participants dynamically via API calls. Broadcast Telemetry to Multiple Consumers Point-to-point OTLP forces an awkward choice: either implement multiple exporters in your applications (adding complexity and resource consumption) or introduce intermediate message queues (adding latency and operational complexity). Neither option is ideal when you want collectors, dashboards, and AI agents all receiving the same telemetry simultaneously. SLIM’s broadcast channels support multiple simultaneous consumers. A single application publishes to a channel, and all participants—traditional collectors storing to Prometheus, real-time Grafana dashboards, AI agents performing anomaly detection, and specialized diagnostic agents analyzing patterns—receive the same data stream. AI Agent Integration Standard telemetry pipelines were designed for collectors and storage systems, not AI agents. Agents that need to analyze telemetry typically must poll storage backends (introducing latency) and have limited ways to coordinate with other agents. SLIM treats AI agents as channel participants. Agents receive live telemetry streams alongside collectors, eliminating polling latency. They can use the A2A protocol on separate SLIM channels to coordinate their analysis and share findings. Agents join and leave channels dynamically based on incident needs, with both telemetry and agent communication using the same MLS security model. Demo Scenario: Intelligent Incident Response The following demo demonstrates SLIM’s capabilities for dynamic observability and AI-powered incident response. The monitor application starts up and creates a SLIM channel for telemetry. It invites two participants: the monitored application (which generates metrics using the SLIM OpenTelemetry SDK) and an OpenTelemetry Collector (configured with a SLIM receiver to export metrics to Prometheus and Grafana). The monitor app itself also joins the channel to watch the telemetry stream for anomalies. During normal operation, the monitored app continuously sends metrics—active connections and service latency. The collector stores these metrics for dashboards while the monitor app observes in the background. When the application enters a high load period where the number of active connections increases and processing latency exceeds 200ms (the threshold used by the monitoring application), the monitor app triggers an alert. The monitor app then invites a specialized diagnostic agent to join the telemetry channel. This agent collects metrics for 10 seconds, performs statistical analysis, and queries Azure OpenAI’s GPT-4 to diagnose the root cause. After completing the analysis, the agent prints the findings and signals completion. The monitor app receives the completion notification, removes the special agent from the channel, and waits for the next incident. In the demo, this flow repeats in a cycle, simulating periodic issues. While the demo runs locally for simplicity, the same architecture works across cluster boundaries. Here’s the architecture: graph TB APP[Monitored App&lt;br/&gt;Metrics: connections, latency] subgraph "SLIM Node" CHANNEL[SLIM Broadcast Channel&lt;br/&gt;MLS Encrypted] end subgraph "Monitoring &amp; Storage" COLLECTOR[OTel Collector&lt;br/&gt;SLIM Receiver] PROM[Prometheus] GRAF[Grafana Dashboard] end MONITOR[Monitor App&lt;br/&gt;Creates Channel &amp; Detects Anomalies] SPECIAL[Special Agent&lt;br/&gt;AI-Powered Diagnosis] MONITOR --&gt;|0. Create channel &amp; invite| CHANNEL APP --&gt;|1. Publish metrics| CHANNEL CHANNEL --&gt;|Broadcast metrics| COLLECTOR CHANNEL --&gt;|Broadcast metrics| MONITOR CHANNEL -.-&gt;|Broadcast metrics - after invite| SPECIAL COLLECTOR --&gt; PROM PROM --&gt; GRAF MONITOR --&gt;|2. Detect high latency| MONITOR MONITOR -.-&gt;|3. Invite special agent| SPECIAL SPECIAL --&gt;|4. Analyze with GPT-4| SPECIAL style APP fill:#4a90e2,color:#fff style CHANNEL fill:#f39c12,color:#fff style COLLECTOR fill:#27ae60,color:#fff style MONITOR fill:#9b59b6,color:#fff style SPECIAL fill:#e74c3c,color:#fff style PROM fill:#95a5a6,color:#fff style GRAF fill:#95a5a6,color:#fff The architecture shows: a single source (the monitored app) broadcasts telemetry to multiple consumers (collector and monitor app). The monitor app creates the channel and manages participant lifecycles. The special agent joins when needed, with no pre-configuration. LLM-powered diagnostics operate on live telemetry streams. Standard OpenTelemetry metrics flow through SLIM to Prometheus/Grafana. All communication is encrypted with MLS. The building blocks for observability over SLIM—including the SLIM receiver and exporter for collectors and SDK exporter for applications—are available in the slim-otel repository. The complete working demo instead is available in the agentic-apps repository in the observability_app folder. Running the Demo This section provides step-by-step instructions to run the incident response demo. Step 1: Clone and Build the Collector Clone the repository and build the custom OpenTelemetry Collector with SLIM components: git clone https://github.com/agntcy/agentic-apps.git cd agentic-apps/observability_app # Build the collector with SLIM receiver/exporter as Docker image task collector:docker:build This will install the OpenTelemetry Collector Builder (OCB), generate the collector code with SLIM components using builder-config.yaml, and create a Docker image. Step 2: Start Infrastructure Start all infrastructure services (SLIM node, OTel Collector, Prometheus, and Grafana): task infra:start Verify all services are running: task infra:status Step 3: Configure Grafana Dashboard Import the pre-configured dashboard: Open http://localhost:3000 and login with admin / admin Navigate to Dashboards → Import Upload the grafana-dashboard.json file available in the repo Step 4: Run the Applications Open three separate terminal windows and run each application: Terminal 1 - Monitored Application (generates metrics): task monitored-application:run Terminal 2 - Monitor Application (creates channel, detects anomalies): task monitor-application:run This will invite both the monitored-application and the OTel Collector to the same SLIM channel. This channel is used to distribute telemetry Terminal 3 - Special Agent (AI-powered diagnostics): First, set your Azure OpenAI credentials: export AZURE_OPENAI_API_KEY="your-api-key" export AZURE_OPENAI_ENDPOINT="https://your-endpoint.openai.azure.com/" export AZURE_OPENAI_DEPLOYMENT="gpt-4o" # Optional, defaults to gpt-4o Then run the agent: task special-agent:run The special-agent will start and wait to be invited by the monitor-application Step 5: Observe the Demo Flow Watch the terminal outputs to see the incident response cycle: Low Load Period (20 seconds) Monitored app sends metrics (connections: ~50, latency: ~50ms) Monitor app observes quietly Metrics flow to Collector → Prometheus → Grafana High Load Period (20 seconds) - Incident Detected Connections increase and Service Latency spikes Monitor app detects consecutive samples above 200ms threshold and triggers the alert Monitor app invites the special agent AI Analysis (10 seconds) Special agent joins the channel and collects telemetry GPT-4 analyzes the metrics and produces diagnostic insights Special agent reports findings Cleanup and Reset Monitor app receives completion notification Monitor app removes the special agent from the channel Cycle repeats as app alternates between low and high load Step 6: View Dashboards View real-time metrics in Grafana: Navigate to http://localhost:3000 Open the metrics on the imported dashboard Step 7: Clean Up Stop all applications with Ctrl+C in each terminal. Stop infrastructure: task infra:stop This stops and removes all Docker containers (SLIM node, collector, Prometheus, Grafana). Conclusion SLIM extends beyond agentic AI to support observability and remediation in distributed applications. The demo shows how AI agents can join telemetry channels dynamically to diagnose incidents using LLMs, collaborating with traditional collectors over MLS-encrypted channels that work across cluster boundaries. The SLIM-OTel code and observability demo are open source. You can try it in your environment, adapt it for your setup, or contribute to the codebase. SLIM and SLIM OpenTelemetry components are developed by AGNTCY Contributors and released under the Apache 2.0 License.]]></summary></entry><entry><title type="html">SlimRPC Multicast: One Call, Every Agent</title><link href="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/slim/agents/2026/03/31/slimrpc-multicast.html" rel="alternate" type="text/html" title="SlimRPC Multicast: One Call, Every Agent" /><published>2026-03-31T11:00:00+00:00</published><updated>2026-03-31T11:00:00+00:00</updated><id>https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/slim/agents/2026/03/31/slimrpc-multicast</id><content type="html" xml:base="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/slim/agents/2026/03/31/slimrpc-multicast.html"><![CDATA[<p>Standard RPC is point-to-point: one client talks to one server.
But agentic AI workloads rarely fit that mold. A coordinator agent might need to
fan out a task to a pool of workers and merge their results. A monitoring service
might need to poll every node in a cluster with a single call. A voting protocol
might need every participant to weigh in before a decision is made.</p>

<p>Today we are releasing <strong>SlimRPC Multicast</strong> — also called <strong>Group RPC</strong> — as part
of <a href="https://github.com/agntcy/slim">SLIM v1.3</a>. It extends the familiar
protobuf-based RPC model with native one-to-many semantics: broadcast a request
to a group of servers, collect every response, and know exactly which member
sent what. The best part? <strong>Your server code doesn’t change at all.</strong></p>

<!--more-->

<h2 id="a-quick-recap-what-is-slimrpc">A Quick Recap: What Is SlimRPC?</h2>

<p><a href="https://docs.agntcy.org/slim/slim-rpc/">SlimRPC</a> is a protobuf-based RPC
framework that uses <a href="https://github.com/agntcy/slim">SLIM</a> as the transport
layer instead of HTTP/2. You write a <code class="language-plaintext highlighter-rouge">.proto</code> file, run the
<a href="https://docs.agntcy.org/slim/slim-slimrpc-compiler/">SlimRPC compiler</a>, and get
type-safe client stubs and server servicers. Because the transport is SLIM,
communication benefits from end-to-end MLS encryption, automatic service
discovery, and reliable delivery with per-message acknowledgments.</p>

<p>SlimRPC supports all four gRPC interaction patterns:</p>

<table>
  <thead>
    <tr>
      <th>Pattern</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Unary-Unary</strong></td>
      <td>Single request → single response</td>
    </tr>
    <tr>
      <td><strong>Unary-Stream</strong></td>
      <td>Single request → stream of responses</td>
    </tr>
    <tr>
      <td><strong>Stream-Unary</strong></td>
      <td>Stream of requests → single response</td>
    </tr>
    <tr>
      <td><strong>Stream-Stream</strong></td>
      <td>Bidirectional streaming</td>
    </tr>
  </tbody>
</table>

<p>Group RPC adds a multicast variant of each one.</p>

<h2 id="the-problem-with-point-to-point">The Problem with Point-to-Point</h2>

<p>Consider a simple protobuf service:</p>

<div class="language-proto highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">service</span> <span class="n">Test</span> <span class="p">{</span>
  <span class="k">rpc</span> <span class="n">ExampleUnaryUnary</span><span class="p">(</span><span class="n">ExampleRequest</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">ExampleResponse</span><span class="p">);</span>
  <span class="k">rpc</span> <span class="n">ExampleUnaryStream</span><span class="p">(</span><span class="n">ExampleRequest</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">stream</span> <span class="n">ExampleResponse</span><span class="p">);</span>
  <span class="k">rpc</span> <span class="n">ExampleStreamUnary</span><span class="p">(</span><span class="n">stream</span> <span class="n">ExampleRequest</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">ExampleResponse</span><span class="p">);</span>
  <span class="k">rpc</span> <span class="n">ExampleStreamStream</span><span class="p">(</span><span class="n">stream</span> <span class="n">ExampleRequest</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">stream</span> <span class="n">ExampleResponse</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p>With standard SlimRPC (or gRPC), calling <code class="language-plaintext highlighter-rouge">ExampleUnaryUnary</code> talks to <strong>one</strong>
server. Each server instance registers under its own distinct SLIM name
(e.g. <code class="language-plaintext highlighter-rouge">agntcy/grpc/server1</code>, <code class="language-plaintext highlighter-rouge">agntcy/grpc/server2</code>), and the client already
knows these names — but it still has to call each one individually.</p>

<p>You could loop over servers and call each one sequentially, but that means:</p>

<ul>
  <li><strong>N serial round-trips</strong> instead of one broadcast.</li>
  <li><strong>N separate sessions</strong> instead of one shared group session.</li>
  <li><strong>No built-in correlation</strong> between responses and their source.</li>
  <li><strong>Manual bookkeeping</strong> for tracking which servers have replied and which
have failed.</li>
</ul>

<p>Group RPC solves all of this at the transport level.</p>

<h2 id="architecture-overview">Architecture Overview</h2>

<pre><code class="language-mermaid">%%{init: {"theme": "base", "themeVariables": {"primaryColor": "#ffffff", "primaryTextColor": "#1a1a1a", "primaryBorderColor": "#333333", "lineColor": "#555555", "edgeLabelBackground": "#ffffff"}}}%%
flowchart LR
    Client --&gt;|"one broadcast"| G(("GROUP&lt;br/&gt;session"))
    G --&gt; S0["Server 0"]
    G --&gt; S1["Server 1"]
    G --&gt; S2["Server N"]
    S0 -.-&gt;|"unicast reply"| G
    S1 -.-&gt;|"unicast reply"| G
    S2 -.-&gt;|"unicast reply"| G
    G -.-&gt; Client

    style Client fill:#d4edda,stroke:#2d6a4f,stroke-width:2px,color:#1a1a1a
    style G fill:#ffd166,stroke:#555,stroke-width:2px,color:#1a1a1a
    style S0 fill:#e9ecef,stroke:#333,stroke-width:2px,color:#1a1a1a
    style S1 fill:#e9ecef,stroke:#333,stroke-width:2px,color:#1a1a1a
    style S2 fill:#e9ecef,stroke:#333,stroke-width:2px,color:#1a1a1a
</code></pre>

<p>The design is straightforward:</p>

<ol>
  <li>The client creates a <strong>group channel</strong> via <code class="language-plaintext highlighter-rouge">Channel.new_group(app, members)</code>
instead of the usual <code class="language-plaintext highlighter-rouge">Channel.new(app, remote)</code>.</li>
  <li>Under the hood, the channel holds a single <strong>SLIM Multicast session</strong>. All
members are auto-invited on the first RPC call.</li>
  <li>Every request is <strong>broadcast</strong> to all members via the group session.</li>
  <li>Each server runs the <strong>exact same handler</strong> it would run for a point-to-point
call — it doesn’t know (or care) that it’s part of a group.</li>
  <li>Responses are sent <strong>unicast</strong> directly back to the client, so members never
see each other’s replies.</li>
  <li>The client’s <code class="language-plaintext highlighter-rouge">ResponseDispatcher</code> multiplexes incoming messages by <code class="language-plaintext highlighter-rouge">rpc-id</code>,
tagging each response with the identity of the member that sent it.</li>
</ol>

<h3 id="session-naming">Session Naming</h3>

<p>The group session name is derived from the client’s own name to avoid
collisions:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>client name:   org / namespace / my-client
group name:    org / namespace / &lt;uuid-v4&gt;
</code></pre></div></div>

<p>This scopes the group address to the same org/namespace as the client.</p>

<h2 id="what-changes-for-the-developer">What Changes for the Developer?</h2>

<h3 id="server-nothing">Server: Nothing</h3>

<p>The same <code class="language-plaintext highlighter-rouge">TestServicer</code> (Python) or <code class="language-plaintext highlighter-rouge">TestServer</code> (Go) implementation works
for both point-to-point and multicast. Each server in the group receives the
broadcast request and processes it through the same handler — it is not aware
of being part of a group.</p>

<h3 id="client-one-constructor-swap">Client: One Constructor Swap</h3>

<p>The only change is how you create the channel. Instead of pointing at a single
remote name, you supply a list of members:</p>

<p><strong>Python — Point-to-Point:</strong></p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">remote_name</span> <span class="o">=</span> <span class="n">slim_bindings</span><span class="p">.</span><span class="n">Name</span><span class="p">(</span><span class="s">"agntcy"</span><span class="p">,</span> <span class="s">"grpc"</span><span class="p">,</span> <span class="s">"server"</span><span class="p">),</span>

<span class="n">channel</span> <span class="o">=</span> <span class="n">slim_bindings</span><span class="p">.</span><span class="n">Channel</span><span class="p">.</span><span class="n">new_with_connection</span><span class="p">(</span>
    <span class="n">local_app</span><span class="p">,</span> <span class="n">remote_name</span><span class="p">,</span> <span class="n">conn_id</span>
<span class="p">)</span>
<span class="n">stub</span> <span class="o">=</span> <span class="n">TestStub</span><span class="p">(</span><span class="n">channel</span><span class="p">)</span>
<span class="n">response</span> <span class="o">=</span> <span class="k">await</span> <span class="n">stub</span><span class="p">.</span><span class="n">ExampleUnaryUnary</span><span class="p">(</span><span class="n">request</span><span class="p">)</span>
</code></pre></div></div>

<p><strong>Python — Multicast:</strong></p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">remote_names</span> <span class="o">=</span> <span class="p">[</span>
    <span class="n">slim_bindings</span><span class="p">.</span><span class="n">Name</span><span class="p">(</span><span class="s">"agntcy"</span><span class="p">,</span> <span class="s">"grpc"</span><span class="p">,</span> <span class="s">"server1"</span><span class="p">),</span>
    <span class="n">slim_bindings</span><span class="p">.</span><span class="n">Name</span><span class="p">(</span><span class="s">"agntcy"</span><span class="p">,</span> <span class="s">"grpc"</span><span class="p">,</span> <span class="s">"server2"</span><span class="p">),</span>
<span class="p">]</span>

<span class="n">channel</span> <span class="o">=</span> <span class="n">slim_bindings</span><span class="p">.</span><span class="n">Channel</span><span class="p">.</span><span class="n">new_group_with_connection</span><span class="p">(</span>
    <span class="n">local_app</span><span class="p">,</span> <span class="n">server_names</span><span class="p">,</span> <span class="n">conn_id</span>
<span class="p">)</span>
<span class="n">stub</span> <span class="o">=</span> <span class="n">TestGroupStub</span><span class="p">(</span><span class="n">channel</span><span class="p">)</span>
<span class="k">async</span> <span class="k">for</span> <span class="n">context</span><span class="p">,</span> <span class="n">response</span> <span class="ow">in</span> <span class="n">stub</span><span class="p">.</span><span class="n">ExampleUnaryUnary</span><span class="p">(</span><span class="n">request</span><span class="p">):</span>
    <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"[</span><span class="si">{</span><span class="n">context</span><span class="si">}</span><span class="s">] </span><span class="si">{</span><span class="n">response</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
</code></pre></div></div>

<p>The key differences:</p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">Channel.new_group_with_connection(app, members, conn_id)</code> replaces
<code class="language-plaintext highlighter-rouge">Channel.new_with_connection(app, remote, conn_id)</code>.</li>
  <li><code class="language-plaintext highlighter-rouge">TestGroupStub</code> replaces <code class="language-plaintext highlighter-rouge">TestStub</code> — both are auto-generated by the
SlimRPC compiler from the same <code class="language-plaintext highlighter-rouge">.proto</code> file.</li>
  <li>Every method returns an <strong>async generator</strong> of <code class="language-plaintext highlighter-rouge">(context, response)</code> tuples
instead of a single response. The <code class="language-plaintext highlighter-rouge">context</code> tells you which member sent the response.</li>
</ol>

<h2 id="multicast-interaction-patterns">Multicast Interaction Patterns</h2>

<p>All four standard patterns have multicast equivalents. The mental model is
always the same: <strong>broadcast the request side, collect tagged responses from
every member.</strong></p>

<h3 id="multicast-unary-unary">Multicast Unary-Unary</h3>

<p>One request in, one response per member out.</p>

<pre><code class="language-mermaid">sequenceDiagram
    participant C as Client
    participant M0 as Server 0
    participant M1 as Server 1

    par broadcast
        C-&gt;&gt;M0: request
    and
        C-&gt;&gt;M1: request
    end

    M0-&gt;&gt;C: response (unicast)
    M1-&gt;&gt;C: response (unicast)

    Note over C: Stream ends when all members reply
</code></pre>

<h3 id="multicast-unary-stream">Multicast Unary-Stream</h3>

<p>One request in, each member streams multiple responses back.</p>

<pre><code class="language-mermaid">sequenceDiagram
    participant C as Client
    participant M0 as Server 0
    participant M1 as Server 1

    par broadcast
        C-&gt;&gt;M0: request
    and
        C-&gt;&gt;M1: request
    end

    M0-&gt;&gt;C: item 1
    M0-&gt;&gt;C: item 2
    M0-&gt;&gt;C: EOS

    M1-&gt;&gt;C: item 1
    M1-&gt;&gt;C: EOS

    Note over C: Stream ends when all EOS received
</code></pre>

<h3 id="multicast-stream-unary">Multicast Stream-Unary</h3>

<p>A stream of requests is broadcast to all; each member accumulates them and replies once.</p>

<pre><code class="language-mermaid">sequenceDiagram
    participant C as Client
    participant M0 as Server 0
    participant M1 as Server 1

    par broadcast
        C-&gt;&gt;M0: msg 1
    and
        C-&gt;&gt;M1: msg 1
    end

    par broadcast
        C-&gt;&gt;M0: msg 2
    and
        C-&gt;&gt;M1: msg 2
    end

    par broadcast
        C-&gt;&gt;M0: EOS (end of requests)
    and
        C-&gt;&gt;M1: EOS (end of requests)
    end

    M0-&gt;&gt;C: response (unicast)
    M0-&gt;&gt;C: EOS

    M1-&gt;&gt;C: response (unicast)
    M1-&gt;&gt;C: EOS

    Note over C: Stream ends when all EOS received
</code></pre>

<h3 id="multicast-stream-stream">Multicast Stream-Stream</h3>

<p>A stream of requests is broadcast to all; each member streams its own responses.
Client sends and receives concurrently.</p>

<pre><code class="language-mermaid">sequenceDiagram
    participant C as Client
    participant M0 as Server 0
    participant M1 as Server 1

    par broadcast
        C-&gt;&gt;M0: msg 1
    and
        C-&gt;&gt;M1: msg 1
    end

    M0-&gt;&gt;C: item 1 (unicast)
    M1-&gt;&gt;C: item 1 (unicast)

    par broadcast
        C-&gt;&gt;M0: msg 2
    and
        C-&gt;&gt;M1: msg 2
    end

    M0-&gt;&gt;C: item 2 (unicast)
    M1-&gt;&gt;C: item 2 (unicast)

    par broadcast
        C-&gt;&gt;M0: EOS (end of requests)
    and
        C-&gt;&gt;M1: EOS (end of requests)
    end

    M0-&gt;&gt;C: EOS
    M1-&gt;&gt;C: EOS

    Note over C: 4 items total, stream ends when all EOS received
</code></pre>

<h2 id="the-eos-protocol">The EOS Protocol</h2>

<p>Knowing when <em>all</em> members are done is critical. SlimRPC uses an explicit
<strong>End-Of-Stream (EOS)</strong> marker — an empty-payload message with status code <code class="language-plaintext highlighter-rouge">Ok</code>.
Every server handler sends an EOS after its last response, regardless of
the interaction pattern:</p>

<table>
  <thead>
    <tr>
      <th>Pattern</th>
      <th>Server sends</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Unary-Unary</td>
      <td>response → EOS</td>
    </tr>
    <tr>
      <td>Unary-Stream</td>
      <td>item… → EOS</td>
    </tr>
    <tr>
      <td>Stream-Unary</td>
      <td>response → EOS</td>
    </tr>
    <tr>
      <td>Stream-Stream</td>
      <td>item… → EOS</td>
    </tr>
  </tbody>
</table>

<p>The client counts EOS markers. When <code class="language-plaintext highlighter-rouge">eos_count == num_members</code>, the response
stream terminates. If a member sends an error instead, that error counts as
its EOS — the stream won’t hang waiting for a member that has already failed.</p>

<pre><code class="language-mermaid">%%{init: {"theme": "base", "themeVariables": {"primaryColor": "#ffffff", "primaryTextColor": "#1a1a1a", "primaryBorderColor": "#333333", "lineColor": "#555555", "edgeLabelBackground": "#ffffff"}}}%%
flowchart LR
    A[receive message] --&gt; B{status code?}
    B --&gt;|Ok + empty| C[EOS: count++]
    B --&gt;|Ok + payload| D[yield MulticastItem]
    B --&gt;|error| E[yield Err, count++]
    C --&gt; F{count == N?}
    E --&gt; F
    F --&gt;|yes| G[stream ends]
    F --&gt;|no| A

    style A fill:#e9ecef,stroke:#333,stroke-width:2px,color:#1a1a1a
    style B fill:#ffd166,stroke:#555,stroke-width:2px,color:#1a1a1a
    style C fill:#d4edda,stroke:#2d6a4f,stroke-width:2px,color:#1a1a1a
    style D fill:#d4edda,stroke:#2d6a4f,stroke-width:2px,color:#1a1a1a
    style E fill:#f8d7da,stroke:#721c24,stroke-width:2px,color:#1a1a1a
    style F fill:#ffd166,stroke:#555,stroke-width:2px,color:#1a1a1a
    style G fill:#e9ecef,stroke:#333,stroke-width:2px,color:#1a1a1a
</code></pre>

<h2 id="a-complete-example">A Complete Example</h2>

<p>Let’s walk through a full working example. We’ll use the same protobuf
definition from above and run <strong>two server instances</strong> with <strong>one multicast
client</strong>.</p>

<h3 id="the-proto-file">The Proto File</h3>

<p>Create a file called <code class="language-plaintext highlighter-rouge">example.proto</code>. This is a standard protobuf service
definition — nothing multicast-specific here:</p>

<div class="language-proto highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">syntax</span> <span class="o">=</span> <span class="s">"proto3"</span><span class="p">;</span>
<span class="kn">package</span> <span class="nn">example_service</span><span class="p">;</span>

<span class="kd">service</span> <span class="n">Test</span> <span class="p">{</span>
  <span class="k">rpc</span> <span class="n">ExampleUnaryUnary</span><span class="p">(</span><span class="n">ExampleRequest</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">ExampleResponse</span><span class="p">);</span>
  <span class="k">rpc</span> <span class="n">ExampleUnaryStream</span><span class="p">(</span><span class="n">ExampleRequest</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">stream</span> <span class="n">ExampleResponse</span><span class="p">);</span>
  <span class="k">rpc</span> <span class="n">ExampleStreamUnary</span><span class="p">(</span><span class="n">stream</span> <span class="n">ExampleRequest</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">ExampleResponse</span><span class="p">);</span>
  <span class="k">rpc</span> <span class="n">ExampleStreamStream</span><span class="p">(</span><span class="n">stream</span> <span class="n">ExampleRequest</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">stream</span> <span class="n">ExampleResponse</span><span class="p">);</span>
<span class="p">}</span>

<span class="kd">message</span> <span class="nc">ExampleRequest</span> <span class="p">{</span>
  <span class="kt">string</span> <span class="na">example_string</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="kt">int64</span>  <span class="na">example_integer</span> <span class="o">=</span> <span class="mi">2</span><span class="p">;</span>
<span class="p">}</span>

<span class="kd">message</span> <span class="nc">ExampleResponse</span> <span class="p">{</span>
  <span class="kt">string</span> <span class="na">example_string</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="kt">int64</span>  <span class="na">example_integer</span> <span class="o">=</span> <span class="mi">2</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="generating-the-stubs">Generating the Stubs</h3>

<p>We use <a href="https://buf.build/">buf</a> to compile the proto file. First, download
the SlimRPC protoc plugin (version <strong>&gt;= 1.3.0</strong>) for your language from the
<a href="https://github.com/agntcy/slim/releases/tag/protoc-slimrpc-plugin-v1.3.0">releases page</a>
and place the binary on your <code class="language-plaintext highlighter-rouge">PATH</code>.</p>

<p>Then create a <code class="language-plaintext highlighter-rouge">buf.gen.yaml</code> alongside <code class="language-plaintext highlighter-rouge">example.proto</code>.</p>

<p><strong>Python</strong> — generates <code class="language-plaintext highlighter-rouge">*_pb2.py</code> (protobuf types), <code class="language-plaintext highlighter-rouge">*_pb2.pyi</code> (type stubs),
and <code class="language-plaintext highlighter-rouge">*_pb2_slimrpc.py</code> (SlimRPC stubs):</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">version</span><span class="pi">:</span> <span class="s">v2</span>
<span class="na">managed</span><span class="pi">:</span>
  <span class="na">enabled</span><span class="pi">:</span> <span class="no">true</span>
<span class="na">inputs</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">proto_file</span><span class="pi">:</span> <span class="s">example.proto</span>
<span class="na">plugins</span><span class="pi">:</span>
  <span class="c1"># SlimRPC stubs (TestStub + TestGroupStub + TestServicer)</span>
  <span class="pi">-</span> <span class="na">local</span><span class="pi">:</span> <span class="s">protoc-gen-slimrpc-python</span>
    <span class="na">out</span><span class="pi">:</span> <span class="s">types</span>
  <span class="c1"># Standard protobuf types</span>
  <span class="pi">-</span> <span class="na">remote</span><span class="pi">:</span> <span class="s">buf.build/protocolbuffers/python:v29.3</span>
    <span class="na">out</span><span class="pi">:</span> <span class="s">types</span>
  <span class="c1"># Type stubs for IDE support</span>
  <span class="pi">-</span> <span class="na">remote</span><span class="pi">:</span> <span class="s">buf.build/protocolbuffers/pyi:v31.1</span>
    <span class="na">out</span><span class="pi">:</span> <span class="s">types</span>
</code></pre></div></div>

<p><strong>Go</strong> — generates <code class="language-plaintext highlighter-rouge">*.pb.go</code> (protobuf types) and <code class="language-plaintext highlighter-rouge">*_slimrpc.pb.go</code>
(SlimRPC stubs):</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">version</span><span class="pi">:</span> <span class="s">v2</span>
<span class="na">managed</span><span class="pi">:</span>
  <span class="na">enabled</span><span class="pi">:</span> <span class="no">true</span>
<span class="na">plugins</span><span class="pi">:</span>
  <span class="c1"># Standard protobuf types</span>
  <span class="pi">-</span> <span class="na">remote</span><span class="pi">:</span> <span class="s">buf.build/protocolbuffers/go</span>
    <span class="na">out</span><span class="pi">:</span> <span class="s">types</span>
    <span class="na">opt</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">paths=source_relative</span>
  <span class="c1"># SlimRPC stubs (TestClient + TestGroupClient + TestServer)</span>
  <span class="pi">-</span> <span class="na">local</span><span class="pi">:</span> <span class="s">protoc-gen-slimrpc-go</span>
    <span class="na">out</span><span class="pi">:</span> <span class="s">types</span>
    <span class="na">opt</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">paths=source_relative</span>
</code></pre></div></div>

<p>Run the code generation:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>buf generate
</code></pre></div></div>

<p>The SlimRPC compiler produces <strong>both</strong> the standard point-to-point stubs
(<code class="language-plaintext highlighter-rouge">TestStub</code> / <code class="language-plaintext highlighter-rouge">TestClient</code>) <strong>and</strong> the multicast stubs (<code class="language-plaintext highlighter-rouge">TestGroupStub</code> /
<code class="language-plaintext highlighter-rouge">TestGroupClient</code>) from the same proto definition — no extra annotations
needed.</p>

<h3 id="server-python--unchanged">Server (Python) — Unchanged</h3>

<p>The server implementation is identical to a standard SlimRPC server. It
doesn’t know whether it’s serving a point-to-point or multicast client.
The handlers are the same you’d write for any SlimRPC service:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">TestService</span><span class="p">(</span><span class="n">TestServicer</span><span class="p">):</span>
    <span class="k">async</span> <span class="k">def</span> <span class="nf">ExampleUnaryUnary</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">request</span><span class="p">,</span> <span class="n">context</span><span class="p">):</span>
        <span class="k">return</span> <span class="n">ExampleResponse</span><span class="p">(</span>
            <span class="n">example_integer</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span>
            <span class="n">example_string</span><span class="o">=</span><span class="s">"Hello, World!"</span>
        <span class="p">)</span>

    <span class="k">async</span> <span class="k">def</span> <span class="nf">ExampleStreamStream</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">request_iterator</span><span class="p">,</span> <span class="n">context</span><span class="p">):</span>
        <span class="k">async</span> <span class="k">for</span> <span class="n">request</span> <span class="ow">in</span> <span class="n">request_iterator</span><span class="p">:</span>
            <span class="k">yield</span> <span class="n">ExampleResponse</span><span class="p">(</span>
                <span class="n">example_integer</span><span class="o">=</span><span class="n">request</span><span class="p">.</span><span class="n">example_integer</span> <span class="o">*</span> <span class="mi">100</span><span class="p">,</span>
                <span class="n">example_string</span><span class="o">=</span><span class="sa">f</span><span class="s">"Echo: </span><span class="si">{</span><span class="n">request</span><span class="p">.</span><span class="n">example_string</span><span class="si">}</span><span class="s">"</span><span class="p">,</span>
            <span class="p">)</span>
</code></pre></div></div>

<p>The only thing we parameterise is the <strong>instance name</strong>, which becomes the
third component of the SLIM name. This lets us run multiple copies of the
same server side by side:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># The instance name becomes the SLIM address:
# "server1" → agntcy/grpc/server1,  "server2" → agntcy/grpc/server2
</span><span class="n">local_name</span> <span class="o">=</span> <span class="n">slim_bindings</span><span class="p">.</span><span class="n">Name</span><span class="p">(</span><span class="s">"agntcy"</span><span class="p">,</span> <span class="s">"grpc"</span><span class="p">,</span> <span class="n">instance</span><span class="p">)</span>
</code></pre></div></div>

<p>Launch two instances in separate terminals:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Terminal 1 — registers as agntcy/grpc/server1</span>
python server.py <span class="nt">--instance</span> server1

<span class="c"># Terminal 2 — registers as agntcy/grpc/server2</span>
python server.py <span class="nt">--instance</span> server2
</code></pre></div></div>

<blockquote>
  <p>📄 Full server source:
<a href="https://github.com/agntcy/slim/blob/slim-v1.3.0/data-plane/bindings/python/examples/slimrpc/simple/server.py">server.py</a></p>
</blockquote>

<h3 id="multicast-client-python">Multicast Client (Python)</h3>

<p>The client creates a <strong>group channel</strong> targeting both servers — this is the
only line that differs from a regular point-to-point client:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># P2P:   Channel.new_with_connection(app, one_server, conn_id)
# Group:
</span><span class="n">server_names</span> <span class="o">=</span> <span class="p">[</span>
    <span class="n">slim_bindings</span><span class="p">.</span><span class="n">Name</span><span class="p">(</span><span class="s">"agntcy"</span><span class="p">,</span> <span class="s">"grpc"</span><span class="p">,</span> <span class="s">"server1"</span><span class="p">),</span>
    <span class="n">slim_bindings</span><span class="p">.</span><span class="n">Name</span><span class="p">(</span><span class="s">"agntcy"</span><span class="p">,</span> <span class="s">"grpc"</span><span class="p">,</span> <span class="s">"server2"</span><span class="p">),</span>
<span class="p">]</span>
<span class="n">channel</span> <span class="o">=</span> <span class="n">slim_bindings</span><span class="p">.</span><span class="n">Channel</span><span class="p">.</span><span class="n">new_group_with_connection</span><span class="p">(</span>
    <span class="n">local_app</span><span class="p">,</span> <span class="n">server_names</span><span class="p">,</span> <span class="n">conn_id</span>
<span class="p">)</span>
</code></pre></div></div>

<p>Then use the generated <code class="language-plaintext highlighter-rouge">TestGroupStub</code> instead of <code class="language-plaintext highlighter-rouge">TestStub</code>. Every method
becomes an async generator that yields <code class="language-plaintext highlighter-rouge">(context, response)</code> tuples — one
per member:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">stub</span> <span class="o">=</span> <span class="n">TestGroupStub</span><span class="p">(</span><span class="n">channel</span><span class="p">)</span>

<span class="c1"># === Multicast Unary-Unary ===
</span><span class="n">request</span> <span class="o">=</span> <span class="n">ExampleRequest</span><span class="p">(</span><span class="n">example_integer</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">example_string</span><span class="o">=</span><span class="s">"hello"</span><span class="p">)</span>
<span class="k">async</span> <span class="k">for</span> <span class="n">context</span><span class="p">,</span> <span class="n">resp</span> <span class="ow">in</span> <span class="n">stub</span><span class="p">.</span><span class="n">ExampleUnaryUnary</span><span class="p">(</span>
    <span class="n">request</span><span class="p">,</span> <span class="n">timeout</span><span class="o">=</span><span class="n">timedelta</span><span class="p">(</span><span class="n">seconds</span><span class="o">=</span><span class="mi">5</span><span class="p">)</span>
<span class="p">):</span>
    <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"  [</span><span class="si">{</span><span class="n">context</span><span class="si">}</span><span class="s">] </span><span class="si">{</span><span class="n">resp</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
<span class="c1"># Output:
#   [agntcy/grpc/server1] example_integer: 1, example_string: "Hello, World!"
#   [agntcy/grpc/server2] example_integer: 1, example_string: "Hello, World!"
</span></code></pre></div></div>

<p>Streaming works the same way — responses from all members are interleaved
and tagged:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># === Multicast Stream-Stream ===
</span><span class="k">async</span> <span class="k">def</span> <span class="nf">stream_requests</span><span class="p">():</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="mi">3</span><span class="p">):</span>
        <span class="k">yield</span> <span class="n">ExampleRequest</span><span class="p">(</span><span class="n">example_integer</span><span class="o">=</span><span class="n">i</span><span class="p">,</span> <span class="n">example_string</span><span class="o">=</span><span class="sa">f</span><span class="s">"item </span><span class="si">{</span><span class="n">i</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>

<span class="k">async</span> <span class="k">for</span> <span class="n">context</span><span class="p">,</span> <span class="n">resp</span> <span class="ow">in</span> <span class="n">stub</span><span class="p">.</span><span class="n">ExampleStreamStream</span><span class="p">(</span>
    <span class="n">stream_requests</span><span class="p">(),</span> <span class="n">timeout</span><span class="o">=</span><span class="n">timedelta</span><span class="p">(</span><span class="n">seconds</span><span class="o">=</span><span class="mi">5</span><span class="p">)</span>
<span class="p">):</span>
    <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"  [</span><span class="si">{</span><span class="n">context</span><span class="si">}</span><span class="s">] </span><span class="si">{</span><span class="n">resp</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
<span class="c1"># Each server echoes all 3 items → 6 responses total, tagged by source
</span></code></pre></div></div>

<blockquote>
  <p>📄 Full client source:
<a href="https://github.com/agntcy/slim/blob/slim-v1.3.0/data-plane/bindings/python/examples/slimrpc/simple/client_group.py">client_group.py</a></p>
</blockquote>

<h3 id="multicast-client-go">Multicast Client (Go)</h3>

<p>The same pattern in Go — <code class="language-plaintext highlighter-rouge">ChannelNewGroupWithConnection</code> plus the generated
<code class="language-plaintext highlighter-rouge">TestGroupClient</code>:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">serverNames</span> <span class="o">:=</span> <span class="p">[]</span><span class="o">*</span><span class="n">slim_bindings</span><span class="o">.</span><span class="n">Name</span><span class="p">{</span>
    <span class="n">slim_bindings</span><span class="o">.</span><span class="n">NewName</span><span class="p">(</span><span class="s">"agntcy"</span><span class="p">,</span> <span class="s">"grpc"</span><span class="p">,</span> <span class="s">"server1"</span><span class="p">),</span>
    <span class="n">slim_bindings</span><span class="o">.</span><span class="n">NewName</span><span class="p">(</span><span class="s">"agntcy"</span><span class="p">,</span> <span class="s">"grpc"</span><span class="p">,</span> <span class="s">"server2"</span><span class="p">),</span>
<span class="p">}</span>
<span class="n">channel</span><span class="p">,</span> <span class="n">_</span> <span class="o">:=</span> <span class="n">slim_bindings</span><span class="o">.</span><span class="n">ChannelNewGroupWithConnection</span><span class="p">(</span><span class="n">app</span><span class="p">,</span> <span class="n">serverNames</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">connId</span><span class="p">)</span>
<span class="n">client</span> <span class="o">:=</span> <span class="n">pb</span><span class="o">.</span><span class="n">NewTestGroupClient</span><span class="p">(</span><span class="n">channel</span><span class="p">)</span>

<span class="n">stream</span><span class="p">,</span> <span class="n">_</span> <span class="o">:=</span> <span class="n">client</span><span class="o">.</span><span class="n">ExampleUnaryUnary</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">pb</span><span class="o">.</span><span class="n">ExampleRequest</span><span class="p">{</span>
    <span class="n">ExampleInteger</span><span class="o">:</span> <span class="m">1</span><span class="p">,</span>
    <span class="n">ExampleString</span><span class="o">:</span>  <span class="s">"hello"</span><span class="p">,</span>
<span class="p">})</span>
<span class="k">for</span> <span class="p">{</span>
    <span class="n">item</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">stream</span><span class="o">.</span><span class="n">Recv</span><span class="p">()</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span> <span class="n">log</span><span class="o">.</span><span class="n">Fatal</span><span class="p">(</span><span class="n">err</span><span class="p">)</span> <span class="p">}</span>
    <span class="k">if</span> <span class="n">item</span> <span class="o">==</span> <span class="no">nil</span> <span class="p">{</span> <span class="k">break</span> <span class="p">}</span>
    <span class="n">log</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"[%s] %+v"</span><span class="p">,</span> <span class="n">item</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">item</span><span class="o">.</span><span class="n">Value</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<blockquote>
  <p>📄 Full client source:
<a href="https://github.com/agntcy/slim/blob/slim-v1.3.0/data-plane/bindings/go/examples/slimrpc/simple/cmd/client_group/client_group.go">client_group.go</a></p>
</blockquote>

<p>Both the Go <code class="language-plaintext highlighter-rouge">TestGroupClient</code> and the Python <code class="language-plaintext highlighter-rouge">TestGroupStub</code> are generated
from the same <code class="language-plaintext highlighter-rouge">.proto</code> file by the SlimRPC compiler.</p>

<h2 id="multiplexing-concurrent-rpcs">Multiplexing Concurrent RPCs</h2>

<p>Every outgoing request carries a unique <code class="language-plaintext highlighter-rouge">rpc-id</code> (UUID). Each server echoes it
back on its responses. A single background task reads all incoming messages
from the group session and dispatches them to per-RPC channels keyed by
<code class="language-plaintext highlighter-rouge">rpc-id</code>. When an RPC caller is done (or dropped), a <code class="language-plaintext highlighter-rouge">DispatcherGuard</code>
automatically unregisters its channel.</p>

<p>This means <strong>multiple concurrent RPCs can share the same persistent group
session</strong> — no session-per-call overhead.</p>

<h2 id="message-metadata">Message Metadata</h2>

<p>SlimRPC uses a small set of metadata keys to coordinate the protocol:</p>

<table>
  <thead>
    <tr>
      <th>Key</th>
      <th>Set by</th>
      <th>Purpose</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">service</code></td>
      <td>Client</td>
      <td>Service name (absent on responses)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">method</code></td>
      <td>Client</td>
      <td>Method name (absent on responses)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">rpc-id</code></td>
      <td>Client</td>
      <td>UUID identifying the call; echoed on responses</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">slimrpc-code</code></td>
      <td>Server</td>
      <td>Integer status code; present on every response</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">slimrpc-timeout</code></td>
      <td>Client</td>
      <td>Deadline as Unix timestamp</td>
    </tr>
  </tbody>
</table>

<p>A message is an <strong>EOS</strong> when <code class="language-plaintext highlighter-rouge">slimrpc-code = 0</code> (Ok) and the payload is empty.
A message is a <strong>request</strong> when <code class="language-plaintext highlighter-rouge">service</code> is present. A message is a <strong>response</strong>
when <code class="language-plaintext highlighter-rouge">slimrpc-code</code> is present and <code class="language-plaintext highlighter-rouge">service</code> is absent.</p>

<h2 id="error-handling">Error Handling</h2>

<p>In a multicast scenario, one member might fail while others succeed. SlimRPC
represents this with dedicated error variants so the client always knows
<strong>what</strong> went wrong and <strong>who</strong> caused it:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">enum</span> <span class="n">RpcError</span> <span class="p">{</span>
    <span class="cd">/// Standard RPC error (same as point-to-point)</span>
    <span class="n">Rpc</span> <span class="p">{</span> <span class="n">code</span><span class="p">:</span> <span class="n">RpcCode</span><span class="p">,</span> <span class="n">message</span><span class="p">:</span> <span class="nb">String</span><span class="p">,</span> <span class="n">details</span><span class="p">:</span> <span class="nb">Option</span><span class="o">&lt;</span><span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">u8</span><span class="o">&gt;&gt;</span> <span class="p">},</span>

    <span class="cd">/// Error from a specific group member — `origin` is the member's SLIM name</span>
    <span class="n">MulticastRpc</span> <span class="p">{</span> <span class="n">origin</span><span class="p">:</span> <span class="nb">String</span><span class="p">,</span> <span class="n">code</span><span class="p">:</span> <span class="n">RpcCode</span><span class="p">,</span> <span class="n">message</span><span class="p">:</span> <span class="nb">String</span><span class="p">,</span> <span class="n">details</span><span class="p">:</span> <span class="nb">Option</span><span class="o">&lt;</span><span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">u8</span><span class="o">&gt;&gt;</span> <span class="p">},</span>

    <span class="cd">/// The session died before all members replied</span>
    <span class="n">MulticastSessionClosed</span> <span class="p">{</span> <span class="n">completed</span><span class="p">:</span> <span class="nb">u64</span><span class="p">,</span> <span class="n">total</span><span class="p">:</span> <span class="nb">u64</span><span class="p">,</span> <span class="n">received</span><span class="p">:</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">missing</span><span class="p">:</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span> <span class="p">},</span>
<span class="p">}</span>
</code></pre></div></div>

<p>The three variants cover every failure mode:</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">MulticastRpc</code></strong> — a single member returned an error. The <code class="language-plaintext highlighter-rouge">origin</code> field
identifies which one. This error is yielded as an <code class="language-plaintext highlighter-rouge">Err</code> item in the
response stream, <strong>counts as that member’s EOS</strong>, and does not affect
other members — they continue sending responses normally.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">MulticastSessionClosed</code></strong> — the underlying group session dropped before
all members finished. The <code class="language-plaintext highlighter-rouge">received</code> and <code class="language-plaintext highlighter-rouge">missing</code> lists tell you exactly
which members completed and which didn’t.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">Rpc</code></strong> — a transport-level or client-side error (e.g. timeout,
serialisation failure) that is not tied to a specific member.</li>
</ul>

<h2 id="cross-language-support">Cross-Language Support</h2>

<p>SlimRPC Multicast works across all languages supported by the SLIM bindings,
thanks to the <a href="https://mozilla.github.io/uniffi-rs/">UniFFI</a> binding layer.
The core implementation is in Rust, with UniFFI-compatible wrappers for
types that don’t map directly to foreign languages (like async streams):</p>

<table>
  <thead>
    <tr>
      <th>UniFFI Type</th>
      <th>Role</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">MulticastResponseReader</code></td>
      <td>Pull-based reader for unary-input multicast results</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">MulticastBidiStreamHandler</code></td>
      <td>Concurrent send/receive for stream-input multicast</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">MulticastStreamMessage</code></td>
      <td><code class="language-plaintext highlighter-rouge">Data(RpcMulticastItem) \| Error \| End</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">RpcMulticastItem</code></td>
      <td>Payload + <code class="language-plaintext highlighter-rouge">RpcMessageContext{source: Name}</code></td>
    </tr>
  </tbody>
</table>

<p>This means the same multicast semantics are available in <strong>Python, Go, Kotlin,
Java, .NET</strong>, and of course <strong>Rust</strong> natively.</p>

<h2 id="key-design-decisions">Key Design Decisions</h2>

<h3 id="servers-dont-know-about-groups">Servers Don’t Know About Groups</h3>

<p>This is perhaps the most important design choice. Servers run the <strong>exact same
handlers</strong> for both point-to-point and multicast calls. The multicast
coordination — broadcasting, EOS counting, response tagging — is entirely a
client-side and transport-level concern. This means:</p>

<ul>
  <li>Existing services gain multicast support for free.</li>
  <li>No server-side code changes or redeployment needed.</li>
  <li>The same server can serve both P2P and multicast clients simultaneously.</li>
</ul>

<h3 id="unicast-responses">Unicast Responses</h3>

<p>Responses travel <strong>directly</strong> from each member to the client via unicast publish.
Members never see each other’s responses. This keeps each member’s session
free of cross-member traffic and avoids O(N²) message amplification.</p>

<h3 id="persistent-session-with-lazy-initialization">Persistent Session with Lazy Initialization</h3>

<p>The group session is created on the first RPC call and reused for all
subsequent calls. If the session dies, it’s transparently recreated. Members
are re-invited on every session creation. This amortizes the session
handshake cost across many RPC calls.</p>

<h3 id="smart-constructor">Smart Constructor</h3>

<p>The <code class="language-plaintext highlighter-rouge">Channel</code> constructor is smart about the member list:</p>

<ul>
  <li><strong>1 member</strong>: creates a standard P2P channel (no multicast overhead).</li>
  <li><strong>Many members</strong>: creates a GROUP channel with a generated session name.</li>
</ul>

<p>This means library code can accept a member list without special-casing the
single-server scenario.</p>

<h2 id="getting-started">Getting Started</h2>

<ol>
  <li>
    <p><strong>Install the SlimRPC compiler</strong> — download pre-built binaries (version
<strong>&gt;= 1.3.0</strong>) for your platform from the
<a href="https://github.com/agntcy/slim/releases/tag/protoc-slimrpc-plugin-v1.3.0">releases page</a>
and place them on your <code class="language-plaintext highlighter-rouge">PATH</code>.</p>
  </li>
  <li>
    <p><strong>Generate stubs</strong> — the compiler now produces both <code class="language-plaintext highlighter-rouge">TestStub</code> and
<code class="language-plaintext highlighter-rouge">TestGroupStub</code> (or <code class="language-plaintext highlighter-rouge">TestClient</code> and <code class="language-plaintext highlighter-rouge">TestGroupClient</code> in Go) from
the same <code class="language-plaintext highlighter-rouge">.proto</code> file:</p>

    <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>buf generate
</code></pre></div>    </div>
  </li>
  <li>
    <p><strong>Write your server</strong> — implement the servicer exactly as you would for
standard SlimRPC or gRPC.</p>
  </li>
  <li>
    <p><strong>Write your client</strong> — use <code class="language-plaintext highlighter-rouge">Channel.new_group_with_connection</code> and the
generated <code class="language-plaintext highlighter-rouge">GroupStub</code>/<code class="language-plaintext highlighter-rouge">GroupClient</code>.</p>
  </li>
  <li>
    <p><strong>Run it</strong> — start a SLIM node, launch your servers, and run the client.
Complete examples are in the repository:</p>
    <ul>
      <li><a href="https://github.com/agntcy/slim/tree/slim-v1.3.0/data-plane/bindings/python/examples/slimrpc/simple">Python example</a></li>
      <li><a href="https://github.com/agntcy/slim/tree/slim-v1.3.0/data-plane/bindings/go/examples/slimrpc/simple">Go example</a></li>
    </ul>
  </li>
</ol>

<h2 id="whats-next">What’s Next</h2>

<p>Group RPC in SLIM v1.3 lays the foundation for a number of exciting
extensions we’re exploring:</p>

<ul>
  <li><strong>Context propagation</strong>: allowing group members to observe each other’s
responses within the same session, enabling collaborative reasoning patterns.</li>
  <li><strong>Dynamic membership</strong>: inviting and removing members from a live group
session without recreating it.</li>
  <li><strong>Aggregation primitives</strong>: built-in reducers (majority vote, first-response,
quorum) that collapse multicast responses into a single result.</li>
</ul>

<p>If you’re building multi-agent systems, we’d love to hear your use cases.
Join our <a href="https://join.slack.com/t/agntcy/shared_invite/zt-3hb4p7bo0-5H2otGjxGt9OQ1g5jzK_GQ">Slack community</a>
or open an issue on <a href="https://github.com/agntcy/slim">GitHub</a>.</p>

<hr />

<p><em>Have questions? Join our <a href="https://join.slack.com/t/agntcy/shared_invite/zt-3hb4p7bo0-5H2otGjxGt9OQ1g5jzK_GQ">Slack community</a> or check out our <a href="https://github.com/agntcy">GitHub</a>.</em></p>]]></content><author><name>Mauro Sardara</name></author><category term="technical" /><category term="slim" /><category term="agents" /><category term="slim" /><category term="slimrpc" /><category term="multicast" /><category term="group" /><category term="rust" /><category term="python" /><category term="go" /><category term="tutorial" /><summary type="html"><![CDATA[Standard RPC is point-to-point: one client talks to one server. But agentic AI workloads rarely fit that mold. A coordinator agent might need to fan out a task to a pool of workers and merge their results. A monitoring service might need to poll every node in a cluster with a single call. A voting protocol might need every participant to weigh in before a decision is made. Today we are releasing SlimRPC Multicast — also called Group RPC — as part of SLIM v1.3. It extends the familiar protobuf-based RPC model with native one-to-many semantics: broadcast a request to a group of servers, collect every response, and know exactly which member sent what. The best part? Your server code doesn’t change at all.]]></summary></entry><entry><title type="html">Announcing slim-a2a-go 0.1.0: Native A2A over SLIM for Go</title><link href="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/agents/go/slim/a2a/announcements/2026/03/24/slim-a2a-go-release.html" rel="alternate" type="text/html" title="Announcing slim-a2a-go 0.1.0: Native A2A over SLIM for Go" /><published>2026-03-24T11:00:00+00:00</published><updated>2026-03-24T11:00:00+00:00</updated><id>https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/agents/go/slim/a2a/announcements/2026/03/24/slim-a2a-go-release</id><content type="html" xml:base="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/agents/go/slim/a2a/announcements/2026/03/24/slim-a2a-go-release.html"><![CDATA[<p>We’re excited to announce the initial release of
<a href="https://github.com/agntcy/slim-a2a-go"><strong>slim-a2a-go</strong></a> (v0.1.0): a Go library
that lets any <a href="https://a2a-protocol.org/latest/">A2A</a> agent communicate over
<a href="https://github.com/agntcy/slim">SLIM</a> instead of HTTP or gRPC — in just a few
lines of code.</p>

<!--more-->

<h2 id="why-slim-a2a-go">Why slim-a2a-go?</h2>

<p>The <a href="https://a2a-protocol.org/latest/">A2A (Agent-to-Agent) protocol</a> defines a standard interface
for AI agents to discover and communicate with each other. Its reference Go SDK,
<a href="https://github.com/a2aproject/a2a-go">a2a-go</a>, ships with two transports out of
the box: HTTP/JSON-RPC and gRPC. Both are great for many scenarios, but they
carry the usual baggage of TCP-based networking: you need to know the remote
agent’s hostname and port, manage TLS certificates, deal with firewalls and
NAT traversal, and so on.</p>

<p><strong>SLIM</strong> (<a href="https://github.com/agntcy/slim">Secure Low-Latency Interactive Messaging</a>)
takes a different approach. Instead of addressing agents by IP and port, SLIM
uses a hierarchical naming scheme — think <code class="language-plaintext highlighter-rouge">agntcy/demo/weather_agent</code> — and
routes messages through lightweight SLIM nodes that act as brokers. Your agents
subscribe to a name, and peers reach them by that name, regardless of where they
physically run. SLIM also provides end-to-end MLS encryption and session-layer
reliability out of the box.</p>

<p><code class="language-plaintext highlighter-rouge">slim-a2a-go</code> is the bridge between these two worlds. It adds SLIM as a third
transport option for <code class="language-plaintext highlighter-rouge">a2a-go</code>, letting you swap HTTP for SLIM with minimal
changes to your agent code.</p>

<h2 id="architecture">Architecture</h2>

<p>The library has two packages that mirror the client/server split in <code class="language-plaintext highlighter-rouge">a2a-go</code>:</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">a2aclient</code></strong> — wraps a <code class="language-plaintext highlighter-rouge">slim_bindings.Channel</code> and implements the full
<code class="language-plaintext highlighter-rouge">a2aclient.Transport</code> interface, so every A2A operation (send message, get
task, streaming, push notifications…) works over SLIM.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">a2aserver</code></strong> — wraps an <code class="language-plaintext highlighter-rouge">a2asrv.RequestHandler</code> and registers it with a
<code class="language-plaintext highlighter-rouge">slim_bindings.Server</code>, converting incoming SLIM RPC calls back into A2A
domain types.</li>
</ul>

<p>The generated protobuf/SLIM-RPC stubs in the <code class="language-plaintext highlighter-rouge">a2apb</code> package provide the
wire-format layer, produced by the
<a href="https://github.com/agntcy/slim"><code class="language-plaintext highlighter-rouge">protoc-gen-slimrpc-go</code></a> plugin from the
official <a href="https://github.com/a2aproject/A2A">A2A gRPC specification</a>.</p>

<pre><code class="language-mermaid">sequenceDiagram
    participant C as Client&lt;br/&gt;a2aclient.Transport
    participant SN as SLIM Node
    participant S as Server&lt;br/&gt;a2aserver.Handler

    C-&gt;&gt;SN: subscribe(agntcy/demo/client)
    S-&gt;&gt;SN: subscribe(agntcy/demo/echo_agent)

    C-&gt;&gt;SN: SendMessage (protobuf over SLIM RPC)
    SN-&gt;&gt;S: routed by name
    S-&gt;&gt;SN: TaskStatusUpdateEvent + ArtifactUpdateEvent
    SN-&gt;&gt;C: streamed response
</code></pre>

<p>Under the hood, messages are serialised to protobuf (the same wire format as the
gRPC transport) and then carried over SLIM’s own binary transport layer. The
application code only ever sees <code class="language-plaintext highlighter-rouge">a2a-go</code>’s domain types — <code class="language-plaintext highlighter-rouge">*a2a.Message</code>,
<code class="language-plaintext highlighter-rouge">*a2a.Task</code>, etc.</p>

<hr />

<h2 id="getting-started">Getting Started</h2>

<p>Add <code class="language-plaintext highlighter-rouge">slim-a2a-go</code> to your module:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>go get github.com/agntcy/slim-a2a-go@v0.1.0
</code></pre></div></div>

<p>Then download the pre-compiled SLIM native library:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>go run github.com/agntcy/slim-bindings-go/cmd/slim-bindings-setup
</code></pre></div></div>

<p>This installs a platform-specific shared library that the Go bindings link
against at runtime. It is a one-time step per developer machine and is already
baked into the CI workflow in the <code class="language-plaintext highlighter-rouge">slim-a2a-go</code> repository.</p>

<h3 id="prerequisites">Prerequisites</h3>

<table>
  <thead>
    <tr>
      <th>Requirement</th>
      <th>Version</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Go</td>
      <td>1.23+</td>
    </tr>
    <tr>
      <td>SLIM node</td>
      <td>any release from <a href="https://github.com/agntcy/slim/releases">agntcy/slim</a></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">slimctl</code> (optional)</td>
      <td>same release</td>
    </tr>
  </tbody>
</table>

<p>A SLIM node can be started locally in seconds:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Option 1 – via slimctl</span>
slimctl slim start <span class="nt">--endpoint</span> 127.0.0.1:46357
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Option 2 – via Docker</span>
docker run <span class="nt">-p</span> 46357:46357 ghcr.io/agntcy/slim:latest <span class="se">\</span>
  /slim <span class="nt">--config</span> /config.yaml
</code></pre></div></div>

<h3 id="repository-layout">Repository layout</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>slim-a2a-go/
├── a2aclient/          # Client transport (slim_bindings.Channel → a2aclient.Transport)
├── a2aserver/          # Server handler  (a2asrv.RequestHandler → slim_bindings.Server)
├── a2apb/              # Generated SLIM RPC stubs (protoc-gen-slimrpc-go)
└── examples/
    └── echo_agent/
        ├── cmd/server/ # Echo agent server
        └── cmd/client/ # Echo agent client
</code></pre></div></div>

<hr />

<h2 id="example-1-a-simple-native-a2a-agent-over-slim">Example 1: A Simple Native A2A Agent over SLIM</h2>

<p>Let’s walk through the echo-agent example that ships with the library. It
demonstrates the full server + client lifecycle in about 80 lines each.</p>

<h3 id="the-server">The server</h3>

<p>An A2A server over SLIM has four steps:</p>

<ol>
  <li>Initialise the runtime</li>
  <li>Create an application identity</li>
  <li>Build the A2A handler stack</li>
  <li>Call <code class="language-plaintext highlighter-rouge">Serve()</code></li>
</ol>

<p>The full source is at <a href="https://github.com/agntcy/slim-a2a-go/blob/main/examples/echo_agent/cmd/server/main.go"><code class="language-plaintext highlighter-rouge">examples/echo_agent/cmd/server/main.go</code></a>.</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// examples/echo_agent/cmd/server/main.go</span>
<span class="k">package</span> <span class="n">main</span>

<span class="k">import</span> <span class="p">(</span>
    <span class="s">"context"</span>
    <span class="s">"fmt"</span>
    <span class="s">"log/slog"</span>

    <span class="s">"github.com/a2aproject/a2a-go/a2a"</span>
    <span class="s">"github.com/a2aproject/a2a-go/a2asrv"</span>
    <span class="s">"github.com/a2aproject/a2a-go/a2asrv/eventqueue"</span>
    <span class="s">"github.com/agntcy/slim-a2a-go/a2aserver"</span>
    <span class="n">slim_bindings</span> <span class="s">"github.com/agntcy/slim-bindings-go"</span>
<span class="p">)</span>

<span class="k">func</span> <span class="n">run</span><span class="p">(</span><span class="n">endpoint</span> <span class="kt">string</span><span class="p">)</span> <span class="kt">error</span> <span class="p">{</span>
    <span class="c">// 1. Initialise the global SLIM runtime (Rust library via CGo).</span>
    <span class="n">slim_bindings</span><span class="o">.</span><span class="n">InitializeWithDefaults</span><span class="p">()</span>
    <span class="n">svc</span> <span class="o">:=</span> <span class="n">slim_bindings</span><span class="o">.</span><span class="n">GetGlobalService</span><span class="p">()</span>

    <span class="c">// 2. Create this agent's SLIM identity using a hierarchical name (org, namespace, service).</span>
    <span class="n">name</span> <span class="o">:=</span> <span class="n">slim_bindings</span><span class="o">.</span><span class="n">NewName</span><span class="p">(</span><span class="s">"agntcy"</span><span class="p">,</span> <span class="s">"demo"</span><span class="p">,</span> <span class="s">"echo_agent"</span><span class="p">)</span>
    <span class="n">app</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">svc</span><span class="o">.</span><span class="n">CreateAppWithSecret</span><span class="p">(</span><span class="n">name</span><span class="p">,</span> <span class="s">"my_shared_secret_for_testing_purposes_only"</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"create app: %w"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>

    <span class="c">// 3. Connect to the SLIM routing node and subscribe to our name.</span>
    <span class="n">connID</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">svc</span><span class="o">.</span><span class="n">Connect</span><span class="p">(</span><span class="n">slim_bindings</span><span class="o">.</span><span class="n">NewInsecureClientConfig</span><span class="p">(</span><span class="n">endpoint</span><span class="p">))</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"connect: %w"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">app</span><span class="o">.</span><span class="n">Subscribe</span><span class="p">(</span><span class="n">name</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">connID</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"subscribe: %w"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>

    <span class="c">// 4. Build the A2A handler stack and register it with the SLIM server.</span>
    <span class="n">requestHandler</span> <span class="o">:=</span> <span class="n">a2asrv</span><span class="o">.</span><span class="n">NewHandler</span><span class="p">(</span><span class="o">&amp;</span><span class="n">echoExecutor</span><span class="p">{})</span>
    <span class="n">slimHandler</span>    <span class="o">:=</span> <span class="n">a2aserver</span><span class="o">.</span><span class="n">NewHandler</span><span class="p">(</span><span class="n">requestHandler</span><span class="p">)</span>
    <span class="n">server</span>         <span class="o">:=</span> <span class="n">slim_bindings</span><span class="o">.</span><span class="n">ServerNewWithConnection</span><span class="p">(</span><span class="n">app</span><span class="p">,</span> <span class="n">name</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">connID</span><span class="p">)</span>
    <span class="n">slimHandler</span><span class="o">.</span><span class="n">RegisterWith</span><span class="p">(</span><span class="n">server</span><span class="p">)</span>

    <span class="n">slog</span><span class="o">.</span><span class="n">Info</span><span class="p">(</span><span class="s">"echo agent ready"</span><span class="p">,</span> <span class="s">"slim_name"</span><span class="p">,</span> <span class="s">"agntcy/demo/echo_agent"</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">server</span><span class="o">.</span><span class="n">Serve</span><span class="p">()</span>
<span class="p">}</span>
</code></pre></div></div>

<p>The agent logic lives in an <code class="language-plaintext highlighter-rouge">AgentExecutor</code>, identical to any <code class="language-plaintext highlighter-rouge">a2a-go</code> executor
regardless of transport:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">type</span> <span class="n">echoExecutor</span> <span class="k">struct</span><span class="p">{}</span>

<span class="k">func</span> <span class="p">(</span><span class="n">e</span> <span class="o">*</span><span class="n">echoExecutor</span><span class="p">)</span> <span class="n">Execute</span><span class="p">(</span>
    <span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span>
    <span class="n">reqCtx</span> <span class="o">*</span><span class="n">a2asrv</span><span class="o">.</span><span class="n">RequestContext</span><span class="p">,</span>
    <span class="n">queue</span> <span class="n">eventqueue</span><span class="o">.</span><span class="n">Queue</span><span class="p">,</span>
<span class="p">)</span> <span class="kt">error</span> <span class="p">{</span>
    <span class="k">if</span> <span class="n">reqCtx</span><span class="o">.</span><span class="n">StoredTask</span> <span class="o">==</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">queue</span><span class="o">.</span><span class="n">Write</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">a2a</span><span class="o">.</span><span class="n">NewStatusUpdateEvent</span><span class="p">(</span><span class="n">reqCtx</span><span class="p">,</span> <span class="n">a2a</span><span class="o">.</span><span class="n">TaskStateSubmitted</span><span class="p">,</span> <span class="no">nil</span><span class="p">));</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
            <span class="k">return</span> <span class="n">err</span>
        <span class="p">}</span>
    <span class="p">}</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">queue</span><span class="o">.</span><span class="n">Write</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">a2a</span><span class="o">.</span><span class="n">NewStatusUpdateEvent</span><span class="p">(</span><span class="n">reqCtx</span><span class="p">,</span> <span class="n">a2a</span><span class="o">.</span><span class="n">TaskStateWorking</span><span class="p">,</span> <span class="no">nil</span><span class="p">));</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">err</span>
    <span class="p">}</span>

    <span class="n">text</span> <span class="o">:=</span> <span class="n">extractText</span><span class="p">(</span><span class="n">reqCtx</span><span class="o">.</span><span class="n">Message</span><span class="p">)</span>

    <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">queue</span><span class="o">.</span><span class="n">Write</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">a2a</span><span class="o">.</span><span class="n">NewArtifactEvent</span><span class="p">(</span><span class="n">reqCtx</span><span class="p">,</span> <span class="n">a2a</span><span class="o">.</span><span class="n">TextPart</span><span class="p">{</span><span class="n">Text</span><span class="o">:</span> <span class="n">text</span><span class="p">}));</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">err</span>
    <span class="p">}</span>

    <span class="n">completed</span> <span class="o">:=</span> <span class="n">a2a</span><span class="o">.</span><span class="n">NewStatusUpdateEvent</span><span class="p">(</span><span class="n">reqCtx</span><span class="p">,</span> <span class="n">a2a</span><span class="o">.</span><span class="n">TaskStateCompleted</span><span class="p">,</span> <span class="no">nil</span><span class="p">)</span>
    <span class="n">completed</span><span class="o">.</span><span class="n">Final</span> <span class="o">=</span> <span class="no">true</span>
    <span class="k">return</span> <span class="n">queue</span><span class="o">.</span><span class="n">Write</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">completed</span><span class="p">)</span>
<span class="p">}</span>

<span class="k">func</span> <span class="p">(</span><span class="n">e</span> <span class="o">*</span><span class="n">echoExecutor</span><span class="p">)</span> <span class="n">Cancel</span><span class="p">(</span>
    <span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span>
    <span class="n">reqCtx</span> <span class="o">*</span><span class="n">a2asrv</span><span class="o">.</span><span class="n">RequestContext</span><span class="p">,</span>
    <span class="n">queue</span> <span class="n">eventqueue</span><span class="o">.</span><span class="n">Queue</span><span class="p">,</span>
<span class="p">)</span> <span class="kt">error</span> <span class="p">{</span>
    <span class="n">evt</span> <span class="o">:=</span> <span class="n">a2a</span><span class="o">.</span><span class="n">NewStatusUpdateEvent</span><span class="p">(</span><span class="n">reqCtx</span><span class="p">,</span> <span class="n">a2a</span><span class="o">.</span><span class="n">TaskStateCanceled</span><span class="p">,</span> <span class="no">nil</span><span class="p">)</span>
    <span class="n">evt</span><span class="o">.</span><span class="n">Final</span> <span class="o">=</span> <span class="no">true</span>
    <span class="k">return</span> <span class="n">queue</span><span class="o">.</span><span class="n">Write</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">evt</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Notice that nothing in the executor knows about SLIM — it just writes events to
the queue, exactly as it would for an HTTP or gRPC server.</p>

<h3 id="the-client">The client</h3>

<p>On the client side, swap the usual HTTP/gRPC transport for <code class="language-plaintext highlighter-rouge">slima2aclient.NewTransport</code>:</p>

<p>The full source is at <a href="https://github.com/agntcy/slim-a2a-go/blob/main/examples/echo_agent/cmd/client/main.go"><code class="language-plaintext highlighter-rouge">examples/echo_agent/cmd/client/main.go</code></a>.</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// examples/echo_agent/cmd/client/main.go</span>
<span class="k">package</span> <span class="n">main</span>

<span class="k">import</span> <span class="p">(</span>
    <span class="s">"context"</span>
    <span class="s">"fmt"</span>

    <span class="s">"github.com/a2aproject/a2a-go/a2a"</span>
    <span class="n">slima2aclient</span> <span class="s">"github.com/agntcy/slim-a2a-go/a2aclient"</span>
    <span class="n">slim_bindings</span> <span class="s">"github.com/agntcy/slim-bindings-go"</span>
<span class="p">)</span>

<span class="k">func</span> <span class="n">run</span><span class="p">(</span><span class="n">endpoint</span><span class="p">,</span> <span class="n">text</span> <span class="kt">string</span><span class="p">)</span> <span class="kt">error</span> <span class="p">{</span>
    <span class="n">slim_bindings</span><span class="o">.</span><span class="n">InitializeWithDefaults</span><span class="p">()</span>
    <span class="n">svc</span> <span class="o">:=</span> <span class="n">slim_bindings</span><span class="o">.</span><span class="n">GetGlobalService</span><span class="p">()</span>

    <span class="c">// The client needs its own SLIM identity so it can receive replies.</span>
    <span class="c">// The org and namespace don't have to match the server's — any unique</span>
    <span class="c">// name on the SLIM node works.</span>
    <span class="n">localName</span> <span class="o">:=</span> <span class="n">slim_bindings</span><span class="o">.</span><span class="n">NewName</span><span class="p">(</span><span class="s">"agntcy"</span><span class="p">,</span> <span class="s">"demo"</span><span class="p">,</span> <span class="s">"client"</span><span class="p">)</span>
    <span class="n">app</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">svc</span><span class="o">.</span><span class="n">CreateAppWithSecret</span><span class="p">(</span><span class="n">localName</span><span class="p">,</span> <span class="s">"my_shared_secret_for_testing_purposes_only"</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"create app: %w"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>

    <span class="n">connID</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">svc</span><span class="o">.</span><span class="n">Connect</span><span class="p">(</span><span class="n">slim_bindings</span><span class="o">.</span><span class="n">NewInsecureClientConfig</span><span class="p">(</span><span class="n">endpoint</span><span class="p">))</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"connect: %w"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">app</span><span class="o">.</span><span class="n">Subscribe</span><span class="p">(</span><span class="n">localName</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">connID</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"subscribe: %w"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>

    <span class="c">// Open a channel to the echo agent by its SLIM name.</span>
    <span class="n">remoteName</span> <span class="o">:=</span> <span class="n">slim_bindings</span><span class="o">.</span><span class="n">NewName</span><span class="p">(</span><span class="s">"agntcy"</span><span class="p">,</span> <span class="s">"demo"</span><span class="p">,</span> <span class="s">"echo_agent"</span><span class="p">)</span>
    <span class="n">channel</span>    <span class="o">:=</span> <span class="n">slim_bindings</span><span class="o">.</span><span class="n">ChannelNewWithConnection</span><span class="p">(</span><span class="n">app</span><span class="p">,</span> <span class="n">remoteName</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">connID</span><span class="p">)</span>
    <span class="k">defer</span> <span class="n">channel</span><span class="o">.</span><span class="n">Destroy</span><span class="p">()</span>

    <span class="c">// Wrap the channel in the SLIM A2A transport.</span>
    <span class="n">transport</span> <span class="o">:=</span> <span class="n">slima2aclient</span><span class="o">.</span><span class="n">NewTransport</span><span class="p">(</span><span class="n">channel</span><span class="p">)</span>
    <span class="k">defer</span> <span class="n">transport</span><span class="o">.</span><span class="n">Destroy</span><span class="p">()</span> <span class="c">//nolint:errcheck</span>

    <span class="n">result</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">transport</span><span class="o">.</span><span class="n">SendMessage</span><span class="p">(</span><span class="n">context</span><span class="o">.</span><span class="n">Background</span><span class="p">(),</span> <span class="o">&amp;</span><span class="n">a2a</span><span class="o">.</span><span class="n">MessageSendParams</span><span class="p">{</span>
        <span class="n">Message</span><span class="o">:</span> <span class="o">&amp;</span><span class="n">a2a</span><span class="o">.</span><span class="n">Message</span><span class="p">{</span>
            <span class="n">ID</span><span class="o">:</span>    <span class="n">a2a</span><span class="o">.</span><span class="n">NewMessageID</span><span class="p">(),</span>
            <span class="n">Role</span><span class="o">:</span>  <span class="n">a2a</span><span class="o">.</span><span class="n">MessageRoleUser</span><span class="p">,</span>
            <span class="n">Parts</span><span class="o">:</span> <span class="p">[]</span><span class="n">a2a</span><span class="o">.</span><span class="n">Part</span><span class="p">{</span><span class="n">a2a</span><span class="o">.</span><span class="n">TextPart</span><span class="p">{</span><span class="n">Text</span><span class="o">:</span> <span class="n">text</span><span class="p">}},</span>
        <span class="p">},</span>
    <span class="p">})</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"send message: %w"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>

    <span class="n">fmt</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"&gt; %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">text</span><span class="p">)</span>
    <span class="n">printResult</span><span class="p">(</span><span class="n">result</span><span class="p">)</span>
    <span class="k">return</span> <span class="no">nil</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="running-the-example">Running the example</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Terminal 1 – server</span>
go run ./examples/echo_agent/cmd/server

<span class="c"># Terminal 2 – client</span>
go run ./examples/echo_agent/cmd/client <span class="nt">--text</span> <span class="s2">"hello from SLIM"</span>
<span class="c"># Output:</span>
<span class="c"># &gt; hello from SLIM</span>
<span class="c"># hello from SLIM</span>
</code></pre></div></div>

<hr />

<h2 id="example-2-integrating-slim-a2a-go-with-google-adk-go">Example 2: Integrating slim-a2a-go with Google ADK-Go</h2>

<p><a href="https://github.com/google/adk-go">ADK-Go</a> is Google’s open-source framework
for building AI agents backed by Gemini. Its <code class="language-plaintext highlighter-rouge">examples/a2a</code> example exposes an
ADK agent as an A2A server over HTTP/JSON-RPC and connects to it via a
<code class="language-plaintext highlighter-rouge">remoteagent.NewA2A</code> client. Let’s replace the HTTP transport with SLIM.</p>

<h3 id="the-original-http-based-example">The original HTTP-based example</h3>

<p>The original <code class="language-plaintext highlighter-rouge">startWeatherAgentServer</code> function looks like this:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// original: google/adk-go/examples/a2a/main.go (HTTP version)</span>
<span class="k">func</span> <span class="n">startWeatherAgentServer</span><span class="p">()</span> <span class="kt">string</span> <span class="p">{</span>
    <span class="n">listener</span><span class="p">,</span> <span class="n">_</span> <span class="o">:=</span> <span class="n">net</span><span class="o">.</span><span class="n">Listen</span><span class="p">(</span><span class="s">"tcp"</span><span class="p">,</span> <span class="s">"127.0.0.1:0"</span><span class="p">)</span>
    <span class="n">baseURL</span> <span class="o">:=</span> <span class="o">&amp;</span><span class="n">url</span><span class="o">.</span><span class="n">URL</span><span class="p">{</span><span class="n">Scheme</span><span class="o">:</span> <span class="s">"http"</span><span class="p">,</span> <span class="n">Host</span><span class="o">:</span> <span class="n">listener</span><span class="o">.</span><span class="n">Addr</span><span class="p">()</span><span class="o">.</span><span class="n">String</span><span class="p">()}</span>

    <span class="k">go</span> <span class="k">func</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">adkAgent</span> <span class="o">:=</span> <span class="n">newWeatherAgent</span><span class="p">(</span><span class="n">context</span><span class="o">.</span><span class="n">Background</span><span class="p">())</span>
        <span class="n">agentCard</span> <span class="o">:=</span> <span class="o">&amp;</span><span class="n">a2a</span><span class="o">.</span><span class="n">AgentCard</span><span class="p">{</span>
            <span class="n">Name</span><span class="o">:</span>               <span class="n">adkAgent</span><span class="o">.</span><span class="n">Name</span><span class="p">(),</span>
            <span class="n">Skills</span><span class="o">:</span>             <span class="n">adka2a</span><span class="o">.</span><span class="n">BuildAgentSkills</span><span class="p">(</span><span class="n">adkAgent</span><span class="p">),</span>
            <span class="n">PreferredTransport</span><span class="o">:</span> <span class="n">a2a</span><span class="o">.</span><span class="n">TransportProtocolJSONRPC</span><span class="p">,</span>
            <span class="n">URL</span><span class="o">:</span>                <span class="n">baseURL</span><span class="o">.</span><span class="n">JoinPath</span><span class="p">(</span><span class="s">"/invoke"</span><span class="p">)</span><span class="o">.</span><span class="n">String</span><span class="p">(),</span>
        <span class="p">}</span>

        <span class="n">mux</span> <span class="o">:=</span> <span class="n">http</span><span class="o">.</span><span class="n">NewServeMux</span><span class="p">()</span>
        <span class="n">mux</span><span class="o">.</span><span class="n">Handle</span><span class="p">(</span><span class="n">a2asrv</span><span class="o">.</span><span class="n">WellKnownAgentCardPath</span><span class="p">,</span> <span class="n">a2asrv</span><span class="o">.</span><span class="n">NewStaticAgentCardHandler</span><span class="p">(</span><span class="n">agentCard</span><span class="p">))</span>

        <span class="n">executor</span>       <span class="o">:=</span> <span class="n">adka2a</span><span class="o">.</span><span class="n">NewExecutor</span><span class="p">(</span><span class="n">adka2a</span><span class="o">.</span><span class="n">ExecutorConfig</span><span class="p">{</span><span class="o">...</span><span class="p">})</span>
        <span class="n">requestHandler</span> <span class="o">:=</span> <span class="n">a2asrv</span><span class="o">.</span><span class="n">NewHandler</span><span class="p">(</span><span class="n">executor</span><span class="p">)</span>
        <span class="n">mux</span><span class="o">.</span><span class="n">Handle</span><span class="p">(</span><span class="s">"/invoke"</span><span class="p">,</span> <span class="n">a2asrv</span><span class="o">.</span><span class="n">NewJSONRPCHandler</span><span class="p">(</span><span class="n">requestHandler</span><span class="p">))</span>

        <span class="n">http</span><span class="o">.</span><span class="n">Serve</span><span class="p">(</span><span class="n">listener</span><span class="p">,</span> <span class="n">mux</span><span class="p">)</span>
    <span class="p">}()</span>

    <span class="k">return</span> <span class="n">baseURL</span><span class="o">.</span><span class="n">String</span><span class="p">()</span>
<span class="p">}</span>

<span class="k">func</span> <span class="n">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">a2aServerAddress</span> <span class="o">:=</span> <span class="n">startWeatherAgentServer</span><span class="p">()</span>
    <span class="n">remoteAgent</span><span class="p">,</span> <span class="n">_</span> <span class="o">:=</span> <span class="n">remoteagent</span><span class="o">.</span><span class="n">NewA2A</span><span class="p">(</span><span class="n">remoteagent</span><span class="o">.</span><span class="n">A2AConfig</span><span class="p">{</span>
        <span class="n">Name</span><span class="o">:</span>            <span class="s">"A2A Weather agent"</span><span class="p">,</span>
        <span class="n">AgentCardSource</span><span class="o">:</span> <span class="n">a2aServerAddress</span><span class="p">,</span>   <span class="c">// fetches the card over HTTP</span>
    <span class="p">})</span>
    <span class="c">// ...</span>
<span class="p">}</span>
</code></pre></div></div>

<p>The key components we’ll replace:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">http.NewServeMux()</code> + <code class="language-plaintext highlighter-rouge">a2asrv.NewJSONRPCHandler()</code> + <code class="language-plaintext highlighter-rouge">http.Serve()</code> → SLIM server</li>
  <li>The HTTP agent-card URL lookup → pass the <code class="language-plaintext highlighter-rouge">*a2a.AgentCard</code> directly</li>
  <li>The default <code class="language-plaintext highlighter-rouge">a2aclient.Factory</code> (JSON-RPC/gRPC) → a SLIM-transport factory</li>
</ul>

<h3 id="the-slim-based-server">The SLIM-based server</h3>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">(</span>
    <span class="s">"github.com/agntcy/slim-a2a-go/a2aserver"</span>
    <span class="n">slima2aclient</span> <span class="s">"github.com/agntcy/slim-a2a-go/a2aclient"</span>
    <span class="n">slim_bindings</span>  <span class="s">"github.com/agntcy/slim-bindings-go"</span>
<span class="p">)</span>

<span class="k">const</span> <span class="p">(</span>
    <span class="n">slimEndpoint</span> <span class="o">=</span> <span class="s">"http://127.0.0.1:46357"</span>
    <span class="n">sharedSecret</span> <span class="o">=</span> <span class="s">"my_shared_secret_for_testing_purposes_only"</span>
<span class="p">)</span>

<span class="c">// startWeatherAgentServer registers the ADK weather agent as an A2A service</span>
<span class="c">// over SLIM and returns its agent card.</span>
<span class="k">func</span> <span class="n">startWeatherAgentServer</span><span class="p">(</span><span class="n">svc</span> <span class="o">*</span><span class="n">slim_bindings</span><span class="o">.</span><span class="n">Service</span><span class="p">)</span> <span class="o">*</span><span class="n">a2a</span><span class="o">.</span><span class="n">AgentCard</span> <span class="p">{</span>
    <span class="c">// NewName takes (org, namespace, service).</span>
    <span class="n">agentSLIMName</span> <span class="o">:=</span> <span class="n">slim_bindings</span><span class="o">.</span><span class="n">NewName</span><span class="p">(</span><span class="s">"agntcy"</span><span class="p">,</span> <span class="s">"demo"</span><span class="p">,</span> <span class="s">"weather_agent"</span><span class="p">)</span>
    <span class="n">app</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">svc</span><span class="o">.</span><span class="n">CreateAppWithSecret</span><span class="p">(</span><span class="n">agentSLIMName</span><span class="p">,</span> <span class="n">sharedSecret</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="n">log</span><span class="o">.</span><span class="n">Fatalf</span><span class="p">(</span><span class="s">"create server app: %v"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>

    <span class="n">connID</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">svc</span><span class="o">.</span><span class="n">Connect</span><span class="p">(</span><span class="n">slim_bindings</span><span class="o">.</span><span class="n">NewInsecureClientConfig</span><span class="p">(</span><span class="n">slimEndpoint</span><span class="p">))</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="n">log</span><span class="o">.</span><span class="n">Fatalf</span><span class="p">(</span><span class="s">"connect: %v"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">app</span><span class="o">.</span><span class="n">Subscribe</span><span class="p">(</span><span class="n">agentSLIMName</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">connID</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="n">log</span><span class="o">.</span><span class="n">Fatalf</span><span class="p">(</span><span class="s">"subscribe: %v"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>

    <span class="n">adkAgent</span> <span class="o">:=</span> <span class="n">newWeatherAgent</span><span class="p">(</span><span class="n">context</span><span class="o">.</span><span class="n">Background</span><span class="p">())</span>

    <span class="n">agentCard</span> <span class="o">:=</span> <span class="o">&amp;</span><span class="n">a2a</span><span class="o">.</span><span class="n">AgentCard</span><span class="p">{</span>
        <span class="n">Name</span><span class="o">:</span>               <span class="n">adkAgent</span><span class="o">.</span><span class="n">Name</span><span class="p">(),</span>
        <span class="n">Skills</span><span class="o">:</span>             <span class="n">adka2a</span><span class="o">.</span><span class="n">BuildAgentSkills</span><span class="p">(</span><span class="n">adkAgent</span><span class="p">),</span>
        <span class="n">PreferredTransport</span><span class="o">:</span> <span class="n">slima2aclient</span><span class="o">.</span><span class="n">SLIMProtocol</span><span class="p">,</span>
        <span class="c">// URL must be the SLIM name in "org/namespace/service" form and must</span>
        <span class="c">// match the fields used in agentSLIMName — the client factory uses it</span>
        <span class="c">// to resolve the remote endpoint.</span>
        <span class="n">URL</span><span class="o">:</span>                <span class="s">"agntcy/demo/weather_agent"</span><span class="p">,</span>
        <span class="n">Capabilities</span><span class="o">:</span>       <span class="n">a2a</span><span class="o">.</span><span class="n">AgentCapabilities</span><span class="p">{</span><span class="n">Streaming</span><span class="o">:</span> <span class="no">true</span><span class="p">},</span>
    <span class="p">}</span>

    <span class="n">executor</span> <span class="o">:=</span> <span class="n">adka2a</span><span class="o">.</span><span class="n">NewExecutor</span><span class="p">(</span><span class="n">adka2a</span><span class="o">.</span><span class="n">ExecutorConfig</span><span class="p">{</span>
        <span class="n">RunnerConfig</span><span class="o">:</span> <span class="n">runner</span><span class="o">.</span><span class="n">Config</span><span class="p">{</span>
            <span class="n">AppName</span><span class="o">:</span>        <span class="n">adkAgent</span><span class="o">.</span><span class="n">Name</span><span class="p">(),</span>
            <span class="n">Agent</span><span class="o">:</span>          <span class="n">adkAgent</span><span class="p">,</span>
            <span class="n">SessionService</span><span class="o">:</span> <span class="n">session</span><span class="o">.</span><span class="n">InMemoryService</span><span class="p">(),</span>
        <span class="p">},</span>
    <span class="p">})</span>

    <span class="n">requestHandler</span> <span class="o">:=</span> <span class="n">a2asrv</span><span class="o">.</span><span class="n">NewHandler</span><span class="p">(</span><span class="n">executor</span><span class="p">)</span>
    <span class="n">slimHandler</span>    <span class="o">:=</span> <span class="n">a2aserver</span><span class="o">.</span><span class="n">NewHandler</span><span class="p">(</span><span class="n">requestHandler</span><span class="p">)</span>
    <span class="n">server</span>         <span class="o">:=</span> <span class="n">slim_bindings</span><span class="o">.</span><span class="n">ServerNewWithConnection</span><span class="p">(</span><span class="n">app</span><span class="p">,</span> <span class="n">agentSLIMName</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">connID</span><span class="p">)</span>
    <span class="n">slimHandler</span><span class="o">.</span><span class="n">RegisterWith</span><span class="p">(</span><span class="n">server</span><span class="p">)</span>

    <span class="n">log</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"A2A/SLIM server starting at agntcy/demo/weather_agent"</span><span class="p">)</span>
    <span class="k">go</span> <span class="k">func</span><span class="p">()</span> <span class="p">{</span>
        <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">server</span><span class="o">.</span><span class="n">Serve</span><span class="p">();</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
            <span class="n">log</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"SLIM server stopped: %v"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
        <span class="p">}</span>
    <span class="p">}()</span>

    <span class="k">return</span> <span class="n">agentCard</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="the-slim-client-factory">The SLIM client factory</h3>

<p><code class="language-plaintext highlighter-rouge">remoteagent.NewA2A</code> accepts a <code class="language-plaintext highlighter-rouge">ClientFactory</code> field that tells ADK how to build
an <code class="language-plaintext highlighter-rouge">a2aclient.Client</code> for the remote agent. We create one that knows how to open
a SLIM channel given the hierarchical SLIM name stored in the agent card’s URL:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">newSLIMClientFactory</span><span class="p">(</span>
    <span class="n">app</span>  <span class="o">*</span><span class="n">slim_bindings</span><span class="o">.</span><span class="n">App</span><span class="p">,</span>
    <span class="n">conn</span> <span class="o">*</span><span class="n">slim_bindings</span><span class="o">.</span><span class="n">ConnectionID</span><span class="p">,</span>
<span class="p">)</span> <span class="o">*</span><span class="n">a2aclient</span><span class="o">.</span><span class="n">Factory</span> <span class="p">{</span>
    <span class="n">slimTransportFactory</span> <span class="o">:=</span> <span class="n">a2aclient</span><span class="o">.</span><span class="n">TransportFactoryFn</span><span class="p">(</span>
        <span class="k">func</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">url</span> <span class="kt">string</span><span class="p">,</span> <span class="n">_</span> <span class="o">*</span><span class="n">a2a</span><span class="o">.</span><span class="n">AgentCard</span><span class="p">)</span> <span class="p">(</span><span class="n">a2aclient</span><span class="o">.</span><span class="n">Transport</span><span class="p">,</span> <span class="kt">error</span><span class="p">)</span> <span class="p">{</span>
            <span class="c">// url is the SLIM name in "org/project/component" form.</span>
            <span class="n">remoteName</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">slim_bindings</span><span class="o">.</span><span class="n">NewNameFromString</span><span class="p">(</span><span class="n">url</span><span class="p">)</span>
            <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
                <span class="k">return</span> <span class="no">nil</span><span class="p">,</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"invalid SLIM name %q: %w"</span><span class="p">,</span> <span class="n">url</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
            <span class="p">}</span>
            <span class="n">channel</span> <span class="o">:=</span> <span class="n">slim_bindings</span><span class="o">.</span><span class="n">ChannelNewWithConnection</span><span class="p">(</span><span class="n">app</span><span class="p">,</span> <span class="n">remoteName</span><span class="p">,</span> <span class="n">conn</span><span class="p">)</span>
            <span class="k">return</span> <span class="n">slima2aclient</span><span class="o">.</span><span class="n">NewTransport</span><span class="p">(</span><span class="n">channel</span><span class="p">),</span> <span class="no">nil</span>
        <span class="p">},</span>
    <span class="p">)</span>

    <span class="k">return</span> <span class="n">a2aclient</span><span class="o">.</span><span class="n">NewFactory</span><span class="p">(</span>
        <span class="n">a2aclient</span><span class="o">.</span><span class="n">WithDefaultsDisabled</span><span class="p">(),</span>
        <span class="n">a2aclient</span><span class="o">.</span><span class="n">WithTransport</span><span class="p">(</span><span class="n">slima2aclient</span><span class="o">.</span><span class="n">SLIMProtocol</span><span class="p">,</span> <span class="n">slimTransportFactory</span><span class="p">),</span>
    <span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">WithDefaultsDisabled()</code> prevents the factory from registering the built-in
JSON-RPC and gRPC transports — we only want SLIM here.</p>

<h3 id="putting-it-all-together">Putting it all together</h3>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">ctx</span> <span class="o">:=</span> <span class="n">context</span><span class="o">.</span><span class="n">Background</span><span class="p">()</span>

    <span class="c">// Initialise the SLIM runtime once for the whole process.</span>
    <span class="n">slim_bindings</span><span class="o">.</span><span class="n">InitializeWithDefaults</span><span class="p">()</span>
    <span class="n">svc</span> <span class="o">:=</span> <span class="n">slim_bindings</span><span class="o">.</span><span class="n">GetGlobalService</span><span class="p">()</span>

    <span class="c">// Start the weather agent over SLIM.</span>
    <span class="n">agentCard</span> <span class="o">:=</span> <span class="n">startWeatherAgentServer</span><span class="p">(</span><span class="n">svc</span><span class="p">)</span>

    <span class="c">// Create a separate SLIM identity for the ADK client.</span>
    <span class="n">clientName</span> <span class="o">:=</span> <span class="n">slim_bindings</span><span class="o">.</span><span class="n">NewName</span><span class="p">(</span><span class="s">"agntcy"</span><span class="p">,</span> <span class="s">"demo"</span><span class="p">,</span> <span class="s">"adk_client"</span><span class="p">)</span>
    <span class="n">clientApp</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">svc</span><span class="o">.</span><span class="n">CreateAppWithSecret</span><span class="p">(</span><span class="n">clientName</span><span class="p">,</span> <span class="n">sharedSecret</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="n">log</span><span class="o">.</span><span class="n">Fatalf</span><span class="p">(</span><span class="s">"create client app: %v"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="n">clientConn</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">svc</span><span class="o">.</span><span class="n">Connect</span><span class="p">(</span><span class="n">slim_bindings</span><span class="o">.</span><span class="n">NewInsecureClientConfig</span><span class="p">(</span><span class="n">slimEndpoint</span><span class="p">))</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="n">log</span><span class="o">.</span><span class="n">Fatalf</span><span class="p">(</span><span class="s">"connect client: %v"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">clientApp</span><span class="o">.</span><span class="n">Subscribe</span><span class="p">(</span><span class="n">clientName</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">clientConn</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="n">log</span><span class="o">.</span><span class="n">Fatalf</span><span class="p">(</span><span class="s">"subscribe client: %v"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>

    <span class="c">// Wire up the remote ADK agent backed by the SLIM client factory.</span>
    <span class="n">remoteAgent</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">remoteagent</span><span class="o">.</span><span class="n">NewA2A</span><span class="p">(</span><span class="n">remoteagent</span><span class="o">.</span><span class="n">A2AConfig</span><span class="p">{</span>
        <span class="n">Name</span><span class="o">:</span>          <span class="s">"A2A Weather agent"</span><span class="p">,</span>
        <span class="n">AgentCard</span><span class="o">:</span>     <span class="n">agentCard</span><span class="p">,</span>  <span class="c">// no HTTP lookup needed</span>
        <span class="n">ClientFactory</span><span class="o">:</span> <span class="n">newSLIMClientFactory</span><span class="p">(</span><span class="n">clientApp</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">clientConn</span><span class="p">),</span>
    <span class="p">})</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="n">log</span><span class="o">.</span><span class="n">Fatalf</span><span class="p">(</span><span class="s">"create remote agent: %v"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>

    <span class="n">config</span> <span class="o">:=</span> <span class="o">&amp;</span><span class="n">launcher</span><span class="o">.</span><span class="n">Config</span><span class="p">{</span>
        <span class="n">AgentLoader</span><span class="o">:</span> <span class="n">agent</span><span class="o">.</span><span class="n">NewSingleLoader</span><span class="p">(</span><span class="n">remoteAgent</span><span class="p">),</span>
    <span class="p">}</span>
    <span class="n">l</span> <span class="o">:=</span> <span class="n">full</span><span class="o">.</span><span class="n">NewLauncher</span><span class="p">()</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">=</span> <span class="n">l</span><span class="o">.</span><span class="n">Execute</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">config</span><span class="p">,</span> <span class="n">os</span><span class="o">.</span><span class="n">Args</span><span class="p">[</span><span class="m">1</span><span class="o">:</span><span class="p">]);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="n">log</span><span class="o">.</span><span class="n">Fatalf</span><span class="p">(</span><span class="s">"run failed: %v</span><span class="se">\n\n</span><span class="s">%s"</span><span class="p">,</span> <span class="n">err</span><span class="p">,</span> <span class="n">l</span><span class="o">.</span><span class="n">CommandLineSyntax</span><span class="p">())</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>The ADK launcher, the <code class="language-plaintext highlighter-rouge">remoteagent</code>, and all the ADK session/runner machinery
are completely unchanged — only the transport wiring is different. The key
differences from the original HTTP example are summarised in the diagram below:</p>

<pre><code class="language-mermaid">flowchart LR
    subgraph original["Original (HTTP)"]
        direction LR
        A1[ADK Launcher] --&gt;|remoteagent.NewA2A| B1[HTTP JSON-RPC Client]
        B1 --&gt;|HTTP POST /invoke| C1[http.ServeMux]
        C1 --&gt; D1[a2asrv.JSONRPCHandler]
        D1 --&gt; E1[adka2a.Executor]
    end

    subgraph slim["SLIM Transport"]
        direction LR
        A2[ADK Launcher] --&gt;|remoteagent.NewA2A| B2[SLIM Transport Client]
        B2 --&gt;|SLIM RPC protobuf| C2[SLIM Node]
        C2 --&gt;|routed by name| D2[slim_bindings.Server]
        D2 --&gt; E2[a2aserver.Handler]
        E2 --&gt; F2[adka2a.Executor]
    end
</code></pre>

<hr />

<h2 id="whats-next">What’s Next</h2>

<p>This 0.1.0 release establishes the core transport layer. On the roadmap:</p>

<ul>
  <li><strong>A2A v1.0 support</strong> — the A2A specification has just releases v1.0.0.
We will update the library to v1.0 release once a2a-go supports it.</li>
  <li><strong>More ADK-Go examples</strong> — multi-agent scenarios where ADK agents discover
and call each other purely by SLIM name</li>
</ul>

<hr />

<h2 id="references">References</h2>

<ul>
  <li><a href="https://github.com/agntcy/slim-a2a-go">slim-a2a-go on GitHub</a></li>
  <li><a href="https://github.com/agntcy/slim">SLIM project</a></li>
  <li><a href="https://docs.agntcy.org/slim/overview/">SLIM documentation</a></li>
  <li><a href="https://a2a-protocol.org/latest/">A2A protocol specification</a></li>
  <li><a href="https://github.com/a2aproject/a2a-go">a2a-go SDK</a></li>
  <li><a href="https://github.com/google/adk-go">Google ADK-Go</a></li>
  <li><a href="https://github.com/agntcy/slim-bindings-go">slim-bindings-go</a></li>
</ul>

<hr />

<p><em>Have questions? Join our <a href="https://join.slack.com/t/agntcy/shared_invite/zt-3hb4p7bo0-5H2otGjxGt9OQ1g5jzK_GQ">Slack community</a> or check out our <a href="https://github.com/agntcy">GitHub</a>.</em></p>]]></content><author><name>Sam Betts</name></author><category term="agents" /><category term="go" /><category term="slim" /><category term="a2a" /><category term="announcements" /><category term="a2a" /><category term="slim" /><category term="go" /><category term="adk-go" /><category term="tutorial" /><category term="release" /><summary type="html"><![CDATA[We’re excited to announce the initial release of slim-a2a-go (v0.1.0): a Go library that lets any A2A agent communicate over SLIM instead of HTTP or gRPC — in just a few lines of code.]]></summary></entry><entry><title type="html">Directory v1.x: Unified OIDC Authentication for Humans, Machines, and CI</title><link href="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/security/authentication/directory/2026/03/16/directory-oidc-authentication-unified-access.html" rel="alternate" type="text/html" title="Directory v1.x: Unified OIDC Authentication for Humans, Machines, and CI" /><published>2026-03-16T09:00:00+00:00</published><updated>2026-03-16T09:00:00+00:00</updated><id>https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/security/authentication/directory/2026/03/16/directory-oidc-authentication-unified-access</id><content type="html" xml:base="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/security/authentication/directory/2026/03/16/directory-oidc-authentication-unified-access.html"><![CDATA[<p>Authentication in Directory has evolved significantly. We moved from a mixed model centered on GitHub OAuth for user access to a <strong>unified OIDC-first model</strong> that supports three real-world access patterns with one consistent trust and authorization pipeline:</p>

<ol>
  <li>Human interactive login</li>
  <li>Service user (machine-to-machine) login</li>
  <li>GitHub Actions workload identity</li>
</ol>

<p>This post explains the architecture, security model, migration path, and practical usage patterns for the new OIDC-based setup.</p>

<!--more-->

<h2 id="why-we-changed">Why We Changed</h2>

<p>The previous model solved an important problem, but it became limiting as usage expanded:</p>

<ul>
  <li>Human operators needed browser-based login and cached session behavior</li>
  <li>Service users needed one-step non-interactive auth without manual token curl flows</li>
  <li>CI pipelines needed short-lived workload identity without long-lived PAT secrets</li>
</ul>

<p>A single OIDC model gives us:</p>

<ul>
  <li>✅ One identity protocol across humans, machines, and workflows</li>
  <li>✅ Short-lived bearer tokens instead of long-lived static credentials</li>
  <li>✅ Cleaner RBAC principal mapping in <code class="language-plaintext highlighter-rouge">envoy-authz</code></li>
  <li>✅ Better security posture for automation and public gateway usage</li>
</ul>

<h2 id="the-new-model-at-a-glance">The New Model at a Glance</h2>

<p>Directory now uses <strong>OIDC for caller identity</strong>, while preserving <strong>SPIFFE-based service trust</strong> inside the platform.</p>

<pre><code class="language-mermaid">flowchart LR
    classDef actor fill:#03142b,stroke:#0251af,stroke-width:2px,color:#f3f6fd;
    classDef idp fill:#0251af,stroke:#f3f6fd,stroke-width:2px,color:#f3f6fd;
    classDef gateway fill:#0251af,stroke:#f3f6fd,stroke-width:3px,color:#f3f6fd;
    classDef api fill:#03142b,stroke:#0251af,stroke-width:3px,color:#f3f6fd;

    H[Human Operator&lt;br/&gt;dirctl auth login]:::actor
    SU[Service User&lt;br/&gt;pre-issued token]:::actor
    GHA[GitHub Actions&lt;br/&gt;OIDC token]:::actor

    Dex[Dex OIDC&lt;br/&gt;Token Broker]:::idp
    GitHubOIDC[GitHub OIDC&lt;br/&gt;token.actions.githubusercontent.com]:::idp

    Envoy[Envoy Gateway&lt;br/&gt;jwt_authn + ext_authz]:::gateway
    API[Directory API&lt;br/&gt;SPIFFE-trusted backend]:::api

    H --&gt;|OIDC token| Envoy
    SU --&gt;|OIDC token| Envoy
    GHA --&gt;|OIDC token| Envoy

    Envoy --&gt;|JWKS verify| Dex
    Envoy --&gt;|JWKS verify| GitHubOIDC

    Envoy --&gt;|Authorized request| API
</code></pre>

<p>Key idea: <strong>identity is OIDC at the edge, authorization is policy-driven in ext-authz, backend trust remains SPIFFE-aware.</strong></p>

<hr />

<h1 id="part-1-human-interactive-oidc">Part 1: Human Interactive OIDC</h1>

<p>Human users authenticate with OIDC PKCE via <code class="language-plaintext highlighter-rouge">dirctl auth login</code>.</p>

<p>In practice this is the “developer laptop” path: interactive browser login once, then cached token reuse for normal CLI operations.</p>

<h2 id="flow">Flow</h2>

<pre><code class="language-mermaid">sequenceDiagram
    participant User as Developer
    participant CLI as dirctl
    participant IdP as Dex OIDC
    participant Envoy as Envoy Gateway
    participant Authz as ext-authz
    participant API as Directory API

    User-&gt;&gt;CLI: dirctl auth login
    CLI-&gt;&gt;IdP: PKCE auth code flow
    IdP-&gt;&gt;CLI: access token
    CLI-&gt;&gt;CLI: cache token (~/.config/dirctl/auth-token.json)

    User-&gt;&gt;CLI: dirctl search --auth-mode=oidc
    CLI-&gt;&gt;Envoy: gRPC + Bearer token
    Envoy-&gt;&gt;Envoy: jwt_authn validates issuer/signature
    Envoy-&gt;&gt;Authz: principal + method check
    Authz-&gt;&gt;Envoy: allow/deny
    Envoy-&gt;&gt;API: forward authorized call
    API-&gt;&gt;Envoy: response
    Envoy-&gt;&gt;CLI: response
</code></pre>

<h2 id="request-and-dataflow-human-path">Request and dataflow (human path)</h2>

<ol>
  <li><code class="language-plaintext highlighter-rouge">dirctl</code> gets an access token from Dex (PKCE) and caches it locally.</li>
  <li>CLI sends gRPC request with <code class="language-plaintext highlighter-rouge">Authorization: Bearer &lt;token&gt;</code> to Envoy.</li>
  <li>Envoy <code class="language-plaintext highlighter-rouge">jwt_authn</code> validates issuer/signature and routes only verified JWTs forward.</li>
  <li><code class="language-plaintext highlighter-rouge">ext_authz</code> extracts canonical principal (typically <code class="language-plaintext highlighter-rouge">user:{iss}:{email}</code>) and checks method permission in Casbin.</li>
  <li>On allow, request is forwarded and backend sees the authorized identity context.</li>
</ol>

<p>This keeps authn/authz logic centralized at the edge while keeping backend behavior consistent.</p>

<h2 id="example">Example</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">DIRECTORY_CLIENT_OIDC_ISSUER</span><span class="o">=</span><span class="s2">"https://prod.idp.ads.outshift.io"</span>
<span class="nb">export </span><span class="nv">DIRECTORY_CLIENT_OIDC_CLIENT_ID</span><span class="o">=</span><span class="s2">"dirctl"</span>

<span class="c"># Interactive login (opens browser)</span>
dirctl auth login

<span class="c"># Or device flow (no browser needed)</span>
dirctl auth login <span class="nt">--device</span>

dirctl auth status

dirctl search <span class="nt">--version</span> <span class="s2">"v1.*"</span> <span class="se">\</span>
  <span class="nt">--server-addr</span> <span class="s2">"prod.gateway.ads.outshift.io:443"</span> <span class="se">\</span>
  <span class="nt">--auth-mode</span><span class="o">=</span>oidc <span class="se">\</span>
  <span class="nt">--output</span> json
</code></pre></div></div>

<hr />

<h1 id="part-2-service-user--machine-authentication">Part 2: Service User / Machine Authentication</h1>

<p>Service users (bots, automation, MCP agents) authenticate using pre-issued tokens passed via <code class="language-plaintext highlighter-rouge">--auth-token</code> or the <code class="language-plaintext highlighter-rouge">DIRECTORY_CLIENT_AUTH_TOKEN</code> environment variable.</p>

<p>This is the preferred path for non-interactive automation outside GitHub Actions (for example bots, scheduled jobs, and service integrations).</p>

<h2 id="pre-issued-token-usage">Pre-issued token usage</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">DIRECTORY_CLIENT_AUTH_TOKEN</span><span class="o">=</span><span class="s2">"&lt;service-user-jwt&gt;"</span>
<span class="nb">export </span><span class="nv">DIRECTORY_CLIENT_SERVER_ADDRESS</span><span class="o">=</span><span class="s2">"prod.gateway.ads.outshift.io:443"</span>

dirctl search <span class="nt">--version</span> <span class="s2">"v1.*"</span> <span class="se">\</span>
  <span class="nt">--auth-mode</span><span class="o">=</span>oidc <span class="se">\</span>
  <span class="nt">--output</span> json
</code></pre></div></div>

<h2 id="request-and-dataflow-service-user-path">Request and dataflow (service user path)</h2>

<ol>
  <li>The pre-issued JWT is passed directly to Envoy via <code class="language-plaintext highlighter-rouge">Authorization: Bearer</code> header.</li>
  <li>Runtime requests go through the same edge chain: <code class="language-plaintext highlighter-rouge">jwt_authn</code> -&gt; <code class="language-plaintext highlighter-rouge">ext_authz</code> -&gt; backend.</li>
  <li>Principal is resolved as <code class="language-plaintext highlighter-rouge">client:{iss}:{email}</code> based on the token’s claims and issuer configuration.</li>
</ol>

<p>This keeps service user auth consistent with human OIDC transport and policy enforcement, while supporting long-lived tokens for automation that cannot use interactive login.</p>

<hr />

<h1 id="part-3-github-actions-oidc-no-pat">Part 3: GitHub Actions OIDC (No PAT)</h1>

<p>CI now uses workload identity tokens from GitHub OIDC instead of <code class="language-plaintext highlighter-rouge">DIRECTORY_CLIENT_GITHUB_TOKEN</code> PAT flows.</p>

<p>This gives per-run, short-lived identity and avoids storing long-lived credentials for Directory access in CI.</p>

<h2 id="reusable-workflow-actions">Reusable workflow actions</h2>

<p>We introduced reusable workflow actions to simplify secure CI usage:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">./.github/actions/build-dirctl</code></li>
  <li><code class="language-plaintext highlighter-rouge">./.github/actions/fetch-oidc-token</code></li>
</ul>

<h2 id="minimal-pattern">Minimal pattern</h2>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">permissions</span><span class="pi">:</span>
  <span class="na">id-token</span><span class="pi">:</span> <span class="s">write</span>
  <span class="na">contents</span><span class="pi">:</span> <span class="s">read</span>

<span class="na">steps</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v4</span>

  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Build dirctl from branch source</span>
    <span class="na">id</span><span class="pi">:</span> <span class="s">build-dirctl</span>
    <span class="na">uses</span><span class="pi">:</span> <span class="s">./.github/actions/build-dirctl</span>
    <span class="na">with</span><span class="pi">:</span>
      <span class="na">go_version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">1.26.1"</span>

  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Fetch OIDC token</span>
    <span class="na">id</span><span class="pi">:</span> <span class="s">oidc</span>
    <span class="na">uses</span><span class="pi">:</span> <span class="s">./.github/actions/fetch-oidc-token</span>
    <span class="na">with</span><span class="pi">:</span>
      <span class="na">audience</span><span class="pi">:</span> <span class="s">dir</span>

  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Run search</span>
    <span class="na">env</span><span class="pi">:</span>
      <span class="na">DIRECTORY_CLIENT_AUTH_TOKEN</span><span class="pi">:</span> <span class="s">$</span>
      <span class="na">DIRCTL_PATH</span><span class="pi">:</span> <span class="s">$</span>
    <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
      <span class="s">"${DIRCTL_PATH}" search --name "*" \</span>
        <span class="s">--server-addr "prod.gateway.ads.outshift.io:443" \</span>
        <span class="s">--auth-mode=oidc \</span>
        <span class="s">--output json</span>
</code></pre></div></div>

<p>This removes long-lived PAT dependence for workflow access to Directory.</p>

<h2 id="request-and-dataflow-github-actions-path">Request and dataflow (GitHub Actions path)</h2>

<ol>
  <li>Workflow requests OIDC token from GitHub (<code class="language-plaintext highlighter-rouge">id-token: write</code> permission).</li>
  <li>Token is passed to <code class="language-plaintext highlighter-rouge">dirctl</code> via <code class="language-plaintext highlighter-rouge">DIRECTORY_CLIENT_AUTH_TOKEN</code>.</li>
  <li>Envoy validates GitHub issuer/JWKS and expected audience.</li>
  <li><code class="language-plaintext highlighter-rouge">ext_authz</code> maps trusted workflow claims to canonical workflow principal (<code class="language-plaintext highlighter-rouge">ghwf:...</code>) and applies Casbin role checks.</li>
  <li>Request is allowed only if workflow identity and method permissions match policy.</li>
</ol>

<p>This is why CI identity is treated as workload identity, not human federation identity.</p>

<hr />

<h1 id="part-4-why-this-ci-model-decision-record">Part 4: Why This CI Model (Decision Record)</h1>

<p>Before landing on the current setup, we evaluated multiple GitHub Actions integration patterns.</p>

<h2 id="options-we-considered">Options we considered</h2>

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>Security</th>
      <th>Effort</th>
      <th>Operational Complexity</th>
      <th>Final decision</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Trust GitHub Actions OIDC directly at edge</td>
      <td>High (short-lived, workflow identity)</td>
      <td>Medium</td>
      <td>Medium</td>
      <td>✅ Chosen</td>
    </tr>
    <tr>
      <td>GitHub OIDC -&gt; broker/token exchange -&gt; internal token</td>
      <td>Very high</td>
      <td>High</td>
      <td>High</td>
      <td>Later candidate</td>
    </tr>
    <tr>
      <td>Pre-issued service tokens from dedicated IdP</td>
      <td>Medium (long-lived token)</td>
      <td>Low</td>
      <td>Low/Medium</td>
      <td>Useful fallback</td>
    </tr>
  </tbody>
</table>

<h2 id="why-direct-github-oidc-trust-won-for-now">Why direct GitHub OIDC trust won for now</h2>

<ul>
  <li>It removes long-lived PAT/static-secret dependency in CI</li>
  <li>It maps naturally to workflow-level identity claims</li>
  <li>It fits our current Envoy + <code class="language-plaintext highlighter-rouge">jwt_authn</code> + <code class="language-plaintext highlighter-rouge">ext_authz</code> architecture</li>
  <li>It delivered strong security quickly without introducing a new broker service</li>
</ul>

<h2 id="important-distinction">Important distinction</h2>

<p>GitHub appears in two very different roles:</p>

<ul>
  <li><strong>GitHub federation for humans</strong> (browser login UX)</li>
  <li><strong>GitHub Actions OIDC for workloads</strong> (ephemeral workflow identity)</li>
</ul>

<p>Treating workflow identity as machine/workload auth (not human federation) was a key design choice.</p>

<hr />

<h1 id="part-5-authorization-model-ext-authz--casbin">Part 5: Authorization Model (ext-authz + Casbin)</h1>

<p>Identity and authorization are intentionally separated:</p>

<ol>
  <li>Envoy <code class="language-plaintext highlighter-rouge">jwt_authn</code> validates token issuer/signature/audience</li>
  <li><code class="language-plaintext highlighter-rouge">envoy-authz</code> extracts canonical principal</li>
  <li>Casbin enforces method-level role permissions</li>
</ol>

<h2 id="request-pipeline-details">Request pipeline details</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client (Bearer JWT)
  -&gt; Envoy jwt_authn
      - validates token (iss, signature, aud)
      - forwards verified payload
  -&gt; ext_authz
      - computes canonical principal
      - enforces Casbin role/method policy
      - sets authorization identity headers
  -&gt; backend
      - receives request already authorized with canonical identity
</code></pre></div></div>

<h2 id="canonical-identity-consistency">Canonical identity consistency</h2>

<p>A subtle but important behavior: the backend should see the <strong>same principal</strong> that authorization evaluated.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">jwt_authn</code> can expose identity from standard claims (e.g. <code class="language-plaintext highlighter-rouge">sub</code>)</li>
  <li><code class="language-plaintext highlighter-rouge">ext_authz</code> computes canonical principal (<code class="language-plaintext highlighter-rouge">user:...</code>, <code class="language-plaintext highlighter-rouge">client:...</code>, <code class="language-plaintext highlighter-rouge">ghwf:...</code>)</li>
  <li><code class="language-plaintext highlighter-rouge">ext_authz</code> returns headers so the backend gets that canonical identity, avoiding “authorized one identity, logged another” drift</li>
</ul>

<p>This improves auditability and reduces policy confusion.</p>

<h2 id="forwarded-x--headers-for-backend-features">Forwarded <code class="language-plaintext highlighter-rouge">x-</code> headers for backend features</h2>

<p>These identity headers are forwarded downstream after authorization and can be used by backend services for rate limits, audit logs, and feature controls:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">x-authorized-principal</code>: canonical identity used by Casbin (for example <code class="language-plaintext highlighter-rouge">user:...</code>, <code class="language-plaintext highlighter-rouge">client:...</code>, <code class="language-plaintext highlighter-rouge">ghwf:...</code>)</li>
  <li><code class="language-plaintext highlighter-rouge">x-user-id</code>: set to the same canonical principal value for consistent identity handling</li>
  <li><code class="language-plaintext highlighter-rouge">x-principal-type</code>: principal class (for example <code class="language-plaintext highlighter-rouge">user</code>, <code class="language-plaintext highlighter-rouge">service</code>, <code class="language-plaintext highlighter-rouge">github</code>, <code class="language-plaintext highlighter-rouge">public</code>)</li>
  <li><code class="language-plaintext highlighter-rouge">x-user-issuer</code>: issuer claim projected by <code class="language-plaintext highlighter-rouge">jwt_authn</code> (<code class="language-plaintext highlighter-rouge">iss</code>)</li>
</ul>

<p>The verified JWT payload header (<code class="language-plaintext highlighter-rouge">x-jwt-payload</code>) is used by <code class="language-plaintext highlighter-rouge">ext_authz</code> for extraction/evaluation; client-supplied values are stripped at the edge before JWT validation.</p>

<h2 id="principal-types">Principal types</h2>

<ul>
  <li>Human users: <code class="language-plaintext highlighter-rouge">user:{iss}:{email}</code></li>
  <li>Machine clients: <code class="language-plaintext highlighter-rouge">client:{iss}:{email}</code></li>
  <li>GitHub workflows: <code class="language-plaintext highlighter-rouge">ghwf:repo:{repo}:workflow:{file}:ref:{ref}[:env:{env}]</code></li>
</ul>

<h2 id="github-workflow-wildcard-support">GitHub workflow wildcard support</h2>

<p>To avoid per-branch role churn, workflow principals support constrained wildcard matching:</p>

<ul>
  <li>Allowed: one trailing <code class="language-plaintext highlighter-rouge">*</code> in branch suffix under <code class="language-plaintext highlighter-rouge">:ref:refs/heads/</code></li>
  <li>Example: <code class="language-plaintext highlighter-rouge">ghwf:repo:agntcy/dir:workflow:oidc-test.yml:ref:refs/heads/*</code></li>
  <li>Not allowed: multiple wildcards, internal wildcards, wildcard outside branch segment</li>
</ul>

<p>This keeps matching practical while avoiding regex complexity and policy ambiguity.</p>

<hr />

<h1 id="part-6-claim-validation-and-policy-boundaries">Part 6: Claim Validation and Policy Boundaries</h1>

<p>For GitHub workflow identity, trust is not just “issuer is GitHub.” We also bind authorization to stable workflow context claims.</p>

<p>Typical checks include:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">iss == https://token.actions.githubusercontent.com</code></li>
  <li><code class="language-plaintext highlighter-rouge">aud == &lt;expected audience&gt;</code></li>
  <li><code class="language-plaintext highlighter-rouge">repository</code> and <code class="language-plaintext highlighter-rouge">repository_owner</code></li>
  <li><code class="language-plaintext highlighter-rouge">ref</code> (branch/tag)</li>
  <li><code class="language-plaintext highlighter-rouge">job_workflow_ref</code> (workflow file + ref)</li>
  <li>optional <code class="language-plaintext highlighter-rouge">environment</code></li>
</ul>

<p>Design boundary we keep strict:</p>

<ul>
  <li><strong>Authentication claims</strong> prove identity context</li>
  <li><strong>Roles and permissions</strong> come from deployment config/Casbin policy</li>
  <li>We do not treat JWT role claims as authorization truth</li>
</ul>

<hr />

<h1 id="part-7-security-hardening-improvements">Part 7: Security Hardening Improvements</h1>

<p>The OIDC rollout included additional hardening:</p>

<ul>
  <li>Removed <code class="language-plaintext highlighter-rouge">grpc.reflection</code> from public auth server paths</li>
  <li>Added ingress rate limits for publicly exposed gateway endpoints</li>
</ul>

<p>Combined with short-lived OIDC tokens and explicit RBAC roles, this improves default security for both human and CI traffic.</p>

<hr />

<h1 id="part-8-migration-notes">Part 8: Migration Notes</h1>

<p>If you still have legacy GitHub auth config/docs, this is the practical migration path:</p>

<ol>
  <li>Switch CLI and workflows to <code class="language-plaintext highlighter-rouge">--auth-mode=oidc</code></li>
  <li>Replace <code class="language-plaintext highlighter-rouge">DIRECTORY_CLIENT_GITHUB_TOKEN</code> with <code class="language-plaintext highlighter-rouge">DIRECTORY_CLIENT_AUTH_TOKEN</code> in CI</li>
  <li>Configure OIDC issuer/JWKS trust in Envoy</li>
  <li>Migrate RBAC principals from <code class="language-plaintext highlighter-rouge">github:&lt;user&gt;</code> to OIDC principal forms (<code class="language-plaintext highlighter-rouge">user:{iss}:{email}</code>, <code class="language-plaintext highlighter-rouge">ghwf:...</code>)</li>
  <li>Add dedicated least-privilege roles for CI workflows</li>
</ol>

<hr />

<h2 id="what-this-unlocks-next">What This Unlocks Next</h2>

<p>Unified OIDC gives us a cleaner base for:</p>

<ul>
  <li>Consistent policy semantics across user and workload identities</li>
  <li>Better auditability of authenticated principals</li>
  <li>Easier enterprise IdP federation and rollout</li>
  <li>Safer CI automation through ephemeral identities</li>
  <li>Optional IdP role-claim mapping for user/service tokens to reduce per-principal config churn</li>
</ul>

<h3 id="future-direction-optional-role-claim-mapping">Future direction: optional role-claim mapping</h3>

<p>A practical next step is to support an opt-in mode where trusted IdP role claims (for human or service-user tokens) can map directly to ext-authz roles.</p>

<p>Potential benefits:</p>

<ul>
  <li>Faster onboarding for new users or service accounts</li>
  <li>Fewer manual role principal entries in static config</li>
  <li>Better alignment with enterprise IAM role management</li>
</ul>

<p>Guardrails we would keep:</p>

<ul>
  <li>Disabled by default (explicit opt-in)</li>
  <li>Issuer-scoped trust policy (only configured issuers/claims accepted)</li>
  <li>Strict allowlist mapping (<code class="language-plaintext highlighter-rouge">token-role</code> -&gt; <code class="language-plaintext highlighter-rouge">ext-authz role</code>) instead of free-form trust</li>
  <li>Existing deny-list and method-level Casbin checks remain authoritative</li>
  <li>Auditable logs showing claim-derived role resolution decisions</li>
</ul>

<h2 id="conclusion">Conclusion</h2>

<p>Directory’s authentication model is now simpler and stronger:</p>

<ul>
  <li>✅ One OIDC-first model for humans, machines, and CI</li>
  <li>✅ Explicit role-based authorization with method-level control</li>
  <li>✅ Reduced secret sprawl by removing PAT dependence in CI</li>
  <li>✅ Preserved service-to-service trust boundaries in backend infrastructure</li>
</ul>

<p>This is a meaningful step toward secure-by-default Directory operations at scale.</p>

<h2 id="-references">📚 References</h2>

<ul>
  <li><a href="https://github.com/agntcy/dir">Directory GitHub Repository</a></li>
  <li><a href="https://dexidp.io/docs/">Dex OIDC Provider</a></li>
  <li><a href="https://openid.net/specs/openid-connect-core-1_0.html">OpenID Connect Core</a></li>
  <li><a href="https://datatracker.ietf.org/doc/html/rfc6749">OAuth 2.0 Authorization Framework (RFC 6749)</a></li>
  <li><a href="https://docs.github.com/en/actions/security-for-github-actions/security-hardening-your-deployments/about-security-hardening-with-openid-connect">GitHub Actions OIDC Documentation</a></li>
  <li><a href="https://www.envoyproxy.io/docs/envoy/latest/configuration/http/http_filters/jwt_authn_filter">Envoy JWT Authentication Filter</a></li>
  <li><a href="https://www.envoyproxy.io/docs/envoy/latest/configuration/http/http_filters/ext_authz_filter">Envoy External Authorization Filter</a></li>
  <li><a href="https://spiffe.io">SPIFFE/SPIRE Documentation</a></li>
</ul>]]></content><author><name>Tibor Kircsi</name></author><category term="security" /><category term="authentication" /><category term="directory" /><category term="oidc" /><category term="dex" /><category term="github-actions" /><category term="envoy" /><category term="rbac" /><category term="spiffe" /><category term="zero-trust" /><summary type="html"><![CDATA[Authentication in Directory has evolved significantly. We moved from a mixed model centered on GitHub OAuth for user access to a unified OIDC-first model that supports three real-world access patterns with one consistent trust and authorization pipeline: Human interactive login Service user (machine-to-machine) login GitHub Actions workload identity This post explains the architecture, security model, migration path, and practical usage patterns for the new OIDC-based setup.]]></summary></entry><entry><title type="html">Write Once, Run Everywhere: Why Rust + UniFFI is the Future of Multi-Language Libraries</title><link href="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/03/13/rust-uniffi-multilanguage-strategy.html" rel="alternate" type="text/html" title="Write Once, Run Everywhere: Why Rust + UniFFI is the Future of Multi-Language Libraries" /><published>2026-03-13T12:00:00+00:00</published><updated>2026-03-13T12:00:00+00:00</updated><id>https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/03/13/rust-uniffi-multilanguage-strategy</id><content type="html" xml:base="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/03/13/rust-uniffi-multilanguage-strategy.html"><![CDATA[<p>Imagine writing your core business logic once and having it automatically available in Python, Swift, Kotlin, Ruby, and Go. No rewrites, no version drift, no maintaining parallel implementations. This isn’t a pipe dream—it’s exactly what Rust + UniFFI enables. In this post, we’ll explore why choosing Rust as your base language with UniFFI for language bindings is a game-changer for teams supporting multiple platforms.</p>

<!--more-->

<h2 id="the-multi-language-problem">The Multi-Language Problem</h2>

<p>If you’re building a library or SDK that needs to work across multiple languages, you’ve likely faced this dilemma:</p>

<p><strong>Option 1: Rewrite for each language</strong></p>
<ul>
  <li>Write the same logic in Python, JavaScript, Java, Swift, etc.</li>
  <li>Result: N implementations to maintain, N sets of bugs, version drift nightmares</li>
</ul>

<p><strong>Option 2: Pick one language and force everyone to use it</strong></p>
<ul>
  <li>Standardize on one language for all platforms</li>
  <li>Result: Can’t support iOS (needs Swift/Obj-C), can’t support Android (needs Kotlin/Java), can’t support Python users, etc.</li>
</ul>

<p><strong>Option 3: Use C as a common denominator</strong></p>
<ul>
  <li>Write once in C, bind everywhere</li>
  <li>Result: Memory unsafety, tedious FFI code, no modern language features</li>
</ul>

<p>None of these options are great. But there’s a fourth way.</p>

<h2 id="enter-rust--uniffi">Enter Rust + UniFFI</h2>

<p>Rust offers a compelling foundation for multi-language libraries:</p>

<p><strong>Rust’s Strategic Advantages:</strong></p>
<ul>
  <li><strong>Memory safety without garbage collection</strong> - Works on any platform, no runtime required</li>
  <li><strong>Zero-cost abstractions</strong> - Performance comparable to C/C++</li>
  <li><strong>Modern language features</strong> - Algebraic data types, pattern matching, traits</li>
  <li><strong>Excellent error handling</strong> - Result types and comprehensive error propagation</li>
  <li><strong>Strong ecosystem</strong> - Mature cryptography, networking, parsing libraries</li>
  <li><strong>C-compatible FFI</strong> - Can expose a stable C API when needed</li>
</ul>

<p>But raw Rust FFI is still tedious. You need to manually:</p>
<ul>
  <li>Create C-compatible functions with <code class="language-plaintext highlighter-rouge">extern "C"</code></li>
  <li>Write header files</li>
  <li>Handle memory allocation and deallocation across boundaries</li>
  <li>Convert between Rust and C types</li>
  <li>Write bindings for each target language</li>
</ul>

<p>This is where <strong>UniFFI</strong> comes in.</p>

<h2 id="what-is-uniffi">What is UniFFI?</h2>

<p><a href="https://mozilla.github.io/uniffi-rs">UniFFI</a> (Unified Foreign Function Interface) is Mozilla’s solution for generating language bindings from Rust. You define your API once using either a UDL (UniFFI Definition Language) file or Rust procedural macros—we prefer the macro approach since it keeps everything directly in the code—and UniFFI automatically generates:</p>

<ul>
  <li>C-compatible FFI layer in Rust</li>
  <li>Python bindings</li>
  <li>Swift bindings</li>
  <li>Kotlin bindings</li>
  <li>Ruby bindings</li>
  <li>Go bindings (via community extensions)</li>
</ul>

<p><strong>The result?</strong> Write your core logic once in Rust, add a few annotations, and get native-feeling bindings in multiple languages automatically.</p>

<h2 id="real-world-benefits">Real-World Benefits</h2>

<h3 id="1-single-source-of-truth">1. Single Source of Truth</h3>

<p>Your business logic lives in one place. When you fix a bug or add a feature, it’s immediately available to all languages after regenerating bindings.</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Core Rust implementation</span>
<span class="nd">#[uniffi::export]</span>
<span class="k">pub</span> <span class="k">fn</span> <span class="nf">validate_transaction</span><span class="p">(</span><span class="n">tx</span><span class="p">:</span> <span class="n">Transaction</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="n">ValidationResult</span><span class="p">,</span> <span class="n">ValidationError</span><span class="o">&gt;</span> <span class="p">{</span>
    <span class="c1">// Complex validation logic here - write once</span>
    <span class="c1">// Automatically available in Python, Swift, Kotlin, Go, Ruby</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="2-native-performance-everywhere">2. Native Performance Everywhere</h3>

<p>Because Rust compiles to native code without a runtime, your library performs well on:</p>
<ul>
  <li>iOS and Android mobile apps</li>
  <li>Python data science tools</li>
  <li>Go microservices</li>
  <li>Ruby web applications</li>
</ul>

<p>No GIL issues in Python, no garbage collection pauses, no warm-up time.</p>

<h3 id="3-type-safety-across-languages">3. Type Safety Across Languages</h3>

<p>UniFFI preserves your Rust types across language boundaries:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">#[derive(uniffi::Record)]</span>
<span class="k">pub</span> <span class="k">struct</span> <span class="n">User</span> <span class="p">{</span>
    <span class="k">pub</span> <span class="n">id</span><span class="p">:</span> <span class="nb">u64</span><span class="p">,</span>
    <span class="k">pub</span> <span class="n">name</span><span class="p">:</span> <span class="nb">String</span><span class="p">,</span>
    <span class="k">pub</span> <span class="n">email</span><span class="p">:</span> <span class="nb">Option</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span><span class="p">,</span>
<span class="p">}</span>

<span class="nd">#[derive(uniffi::Enum)]</span>
<span class="k">pub</span> <span class="k">enum</span> <span class="n">UserStatus</span> <span class="p">{</span>
    <span class="n">Active</span><span class="p">,</span>
    <span class="n">Suspended</span> <span class="p">{</span> <span class="n">reason</span><span class="p">:</span> <span class="nb">String</span> <span class="p">},</span>
    <span class="n">Deleted</span><span class="p">,</span>
<span class="p">}</span>
</code></pre></div></div>

<p>This becomes:</p>
<ul>
  <li>Python: dataclasses with proper types</li>
  <li>Swift: native structs and enums with associated values</li>
  <li>Kotlin: data classes and sealed classes</li>
  <li>Go: structs with proper types</li>
</ul>

<h3 id="4-consistent-error-handling">4. Consistent Error Handling</h3>

<p>Define errors once in Rust:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">#[derive(uniffi::Error)]</span>
<span class="k">pub</span> <span class="k">enum</span> <span class="n">ApiError</span> <span class="p">{</span>
    <span class="n">NetworkError</span> <span class="p">{</span> <span class="n">message</span><span class="p">:</span> <span class="nb">String</span> <span class="p">},</span>
    <span class="n">InvalidCredentials</span><span class="p">,</span>
    <span class="n">RateLimitExceeded</span> <span class="p">{</span> <span class="n">retry_after</span><span class="p">:</span> <span class="nb">u64</span> <span class="p">},</span>
<span class="p">}</span>
</code></pre></div></div>

<p>UniFFI converts these to idiomatic exceptions or Result types in each target language.</p>

<h3 id="5-drastically-reduced-maintenance">5. Drastically Reduced Maintenance</h3>

<p>Consider the maintenance burden:</p>

<p><strong>Without UniFFI:</strong></p>
<ul>
  <li>5 language implementations = 5× the code</li>
  <li>5× the testing surface</li>
  <li>5× the bug fixes</li>
  <li>Constant version drift</li>
</ul>

<p><strong>With Rust + UniFFI:</strong></p>
<ul>
  <li>1 core implementation + thin binding layer</li>
  <li>Test the core once, integration test the bindings</li>
  <li>Fix bugs once, works everywhere</li>
  <li>Versions stay in sync automatically</li>
</ul>

<h2 id="the-architecture">The Architecture</h2>

<p>Here’s how we approach laying out a multi-language library with Rust + UniFFI:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>my-library/
├── core/              # Core Rust implementation
│   ├── src/
│   │   ├── lib.rs
│   │   └── api.udl   # UniFFI interface definition
│   └── Cargo.toml
├── bindings/
│   ├── python/        # Python bindings + packaging
│   │   ├── src/       # Generated Python code (from UniFFI)
│   │   ├── pyproject.toml
│   │   └── setup.py
│   ├── swift/         # Swift bindings + packaging
│   │   ├── Sources/   # Generated Swift code (from UniFFI)
│   │   └── Package.swift
│   ├── kotlin/        # Kotlin bindings + packaging
│   │   ├── src/       # Generated Kotlin code (from UniFFI)
│   │   ├── build.gradle.kts
│   │   └── pom.xml
│   └── go/            # Go bindings + packaging
│       ├── *.go       # Generated Go code (from UniFFI)
│       └── go.mod
└── examples/          # Usage examples in each language
</code></pre></div></div>

<p><strong>Important:</strong> UniFFI generates the language-specific binding code, but it doesn’t create the packaging and build files needed for each ecosystem. You’ll need to provide:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">pyproject.toml</code> or <code class="language-plaintext highlighter-rouge">setup.py</code> for Python (PyPI)</li>
  <li><code class="language-plaintext highlighter-rouge">Package.swift</code> for Swift (Swift Package Manager)</li>
  <li><code class="language-plaintext highlighter-rouge">build.gradle.kts</code> or <code class="language-plaintext highlighter-rouge">pom.xml</code> for Kotlin (Maven/Gradle)</li>
  <li><code class="language-plaintext highlighter-rouge">go.mod</code> for Go (Go modules)</li>
  <li><code class="language-plaintext highlighter-rouge">Cargo.toml</code> configuration for the bindings</li>
  <li>Platform-specific build scripts or CI/CD workflows</li>
</ul>

<p>The workflow:</p>
<ol>
  <li>Implement features in Rust</li>
  <li>Define public API in <code class="language-plaintext highlighter-rouge">.udl</code> or use macros</li>
  <li>Run UniFFI code generation</li>
  <li>Update version numbers in language-specific package files</li>
  <li>Package bindings for each language’s ecosystem (PyPI, CocoaPods, Maven, go modules)</li>
</ol>

<h2 id="when-to-choose-rust--uniffi">When to Choose Rust + UniFFI</h2>

<p>This approach is ideal when:</p>

<ul>
  <li><strong>You’re building a library or SDK</strong> used by multiple languages</li>
  <li><strong>Performance matters</strong> - mobile apps, data processing, cryptography</li>
  <li><strong>Correctness is critical</strong> - financial systems, security tools, infrastructure</li>
  <li><strong>You need mobile support</strong> - iOS and Android apps</li>
  <li><strong>You want to avoid vendor lock-in</strong> - not tied to a specific runtime or platform</li>
  <li><strong>Your team values maintainability</strong> - over initial development speed</li>
</ul>

<p>It’s <strong>less ideal</strong> when:</p>
<ul>
  <li>You only need to support one language</li>
  <li>Your logic is primarily I/O-bound with no computation</li>
  <li>You need extremely rapid prototyping (though Rust has gotten much faster to develop in)</li>
  <li>Your team has no one comfortable with Rust (though the learning investment pays off)</li>
</ul>

<h2 id="getting-started">Getting Started</h2>

<p>If you’re convinced, here’s how to start:</p>

<ol>
  <li><strong>Learn Rust basics</strong> - Focus on ownership, borrowing, and error handling</li>
  <li><strong>Build a small Rust library</strong> - Implement core logic</li>
  <li><strong>Add UniFFI</strong> - Start with one target language using the <a href="https://mozilla.github.io/uniffi-rs">UniFFI documentation</a></li>
  <li><strong>Generate bindings</strong> - See your Rust code callable from Python/Swift/Kotlin</li>
  <li><strong>Expand</strong> - Add more languages as needed</li>
</ol>

<p>We’ve found this approach transformative for our own work. Our <a href="https://github.com/agntcy/slim">SLIM messaging library</a> uses Rust + UniFFI to provide consistent, high-performance messaging across Go, Python, and mobile platforms—all maintained by a small team.</p>

<h2 id="conclusion">Conclusion</h2>

<p>For teams building libraries that need to support multiple languages, Rust + UniFFI offers a practical solution to the maintenance burden of parallel implementations.</p>

<p>By choosing Rust as your base language and UniFFI for bindings, you get:</p>
<ul>
  <li>✅ Write logic once</li>
  <li>✅ Native performance everywhere</li>
  <li>✅ Type safety across language boundaries</li>
  <li>✅ Memory safety without garbage collection</li>
  <li>✅ Dramatically reduced maintenance burden</li>
</ul>

<p>The initial investment in learning Rust and setting up UniFFI pays off when you add features, fix bugs, or expand to new languages. For teams with limited engineering resources, consolidating your core logic into a single, well-tested implementation makes a lot of sense.</p>

<hr />

<p><em>Interested in seeing Rust + UniFFI in action? Check out our <a href="https://blogs.agntcy.org/technical/2026/01/27/integrating-rust-and-go-with-uniffi.html">complete guide to integrating Rust and Go</a> or explore the <a href="https://github.com/agntcy/slim">SLIM messaging library</a>. Have questions? Join our <a href="https://join.slack.com/t/agntcy/shared_invite/zt-3hb4p7bo0-5H2otGjxGt9OQ1g5jzK_GQ">Slack community</a> or check out our <a href="https://github.com/agntcy">GitHub</a>.</em></p>]]></content><author><name>Sam Betts</name></author><category term="technical" /><category term="rust" /><category term="uniffi" /><category term="multilanguage" /><category term="bindings" /><category term="interop" /><category term="architecture" /><summary type="html"><![CDATA[Imagine writing your core business logic once and having it automatically available in Python, Swift, Kotlin, Ruby, and Go. No rewrites, no version drift, no maintaining parallel implementations. This isn’t a pipe dream—it’s exactly what Rust + UniFFI enables. In this post, we’ll explore why choosing Rust as your base language with UniFFI for language bindings is a game-changer for teams supporting multiple platforms.]]></summary></entry><entry><title type="html">Directory Federation Hands-On: SPIRE and SPIFFE in a Local Kind Environment</title><link href="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/security/directory/2026/02/25/directory-federation.html" rel="alternate" type="text/html" title="Directory Federation Hands-On: SPIRE and SPIFFE in a Local Kind Environment" /><published>2026-02-25T09:00:00+00:00</published><updated>2026-02-25T09:00:00+00:00</updated><id>https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/security/directory/2026/02/25/directory-federation</id><content type="html" xml:base="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/security/directory/2026/02/25/directory-federation.html"><![CDATA[<p><a href="https://docs.agntcy.org/dir/overview/">Agent Directory</a> is a secure, scalable, decentralized service that holds agent records. The software allows users to publish and discover agent records across the network of decentralized, federated directories. Agent Directory provides a cryptographic trust model that ensures authenticity and provenance of each record. This component is available as open-source software. The Agent Directory enables secure, decentralized agent discovery across organizational boundaries. To join the public Directory network or federate your own instance, you need SPIFFE federation: cryptographic trust between SPIRE servers through trust bundle exchange.</p>

<p>This post walks you through a hands-on local setup using two Kind clusters: SPIRE federation with the <code class="language-plaintext highlighter-rouge">https_spiffe</code> profile, then Directory and dirctl deployed across clusters. No cert-manager or public DNS required—ideal for learning and experimentation. When you’re ready for production, see <a href="https://docs.agntcy.org/dir/getting-started/">Getting Started</a> and <a href="https://docs.agntcy.org/dir/partner-prod-federation/">Partner Federation with Prod</a> for the <code class="language-plaintext highlighter-rouge">https_web</code> profile and joining the public Directory.</p>

<!--more-->

<p><strong>TL;DR:</strong> Run two Kind clusters with SPIRE federation (<code class="language-plaintext highlighter-rouge">https_spiffe</code>) and cross-cluster Directory + dirctl in about 15–20 minutes. No cert-manager or public DNS required—everything runs locally with MetalLB.</p>

<h3 id="what-youll-learn">What You’ll Learn</h3>

<p>In this post, you’ll learn:</p>

<ul>
  <li>How SPIRE servers exchange trust bundles for cross-cluster authentication</li>
  <li>The difference between <code class="language-plaintext highlighter-rouge">https_spiffe</code> (local/air-gapped) and <code class="language-plaintext highlighter-rouge">https_web</code> (production) federation profiles</li>
  <li>How the Directory and dirctl charts create <code class="language-plaintext highlighter-rouge">ClusterFederatedTrustDomain</code> automatically</li>
  <li>How to verify mTLS-authenticated calls between dirctl (partner cluster) and the Directory API (testbed cluster)</li>
</ul>

<h2 id="why-try-federation-locally">Why Try Federation Locally?</h2>

<ul>
  <li><strong>Understand the flow</strong> — Trust bundle exchange, workload identity across clusters</li>
  <li><strong>No production dependencies</strong> — MetalLB provides LoadBalancer IPs; no Let’s Encrypt or DNS</li>
  <li><strong>Reproducible</strong> — Same steps work on any machine with Docker and kind</li>
  <li><strong>Foundation for prod</strong> — Concepts transfer directly to production; profile differs (<code class="language-plaintext highlighter-rouge">https_web</code>)</li>
</ul>

<h2 id="federation-profiles-at-a-glance">Federation Profiles at a Glance</h2>

<table>
  <thead>
    <tr>
      <th>Profile</th>
      <th>Certificates</th>
      <th>Bootstrap</th>
      <th>Best for</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>https_spiffe</strong></td>
      <td>SPIRE SVIDs</td>
      <td>Manual exchange</td>
      <td>Local Kind, air-gapped</td>
    </tr>
    <tr>
      <td><strong>https_web</strong></td>
      <td>Let’s Encrypt/CA</td>
      <td>None</td>
      <td>Production, public cloud</td>
    </tr>
  </tbody>
</table>

<p>We use <code class="language-plaintext highlighter-rouge">https_spiffe</code> here because it needs no cert-manager or public DNS. For production federation with the public Directory, use <code class="language-plaintext highlighter-rouge">https_web</code>. See <a href="https://docs.agntcy.org/dir/federation-profiles/">Federation Profiles</a>.</p>

<h2 id="prerequisites">Prerequisites</h2>

<ul>
  <li><a href="https://kind.sigs.k8s.io/">kind</a></li>
  <li><a href="https://kubernetes.io/docs/tasks/tools/">kubectl</a></li>
  <li><a href="https://helm.sh/">Helm</a> 3.x</li>
  <li><a href="https://www.docker.com/">Docker</a> (with Buildx)</li>
</ul>

<h2 id="architecture-overview">Architecture Overview</h2>

<pre><code class="language-mermaid">flowchart LR
    classDef testbed fill:#0251af,stroke:#f3f6fd,stroke-width:2px,color:#f3f6fd;
    classDef partner fill:#03142b,stroke:#0251af,stroke-width:2px,color:#f3f6fd;
    classDef feder fill:#0251af,stroke:#f3f6fd,stroke-width:2px,color:#f3f6fd;

    subgraph Testbed["Testbed cluster"]
        Dir[Directory API]:::testbed
        Zot[Zot Registry]:::testbed
        SPIRE_TB[SPIRE Server]:::testbed
    end

    subgraph Feder["Trust bundle exchange"]
        TB[Trust bundles]:::feder
    end

    subgraph Partner["Partner cluster"]
        Dirctl[dirctl]:::partner
        SPIRE_PT[SPIRE Server]:::partner
    end

    SPIRE_TB --&gt; TB
    TB --&gt; SPIRE_PT
    SPIRE_PT --&gt; TB
    TB --&gt; SPIRE_TB
    Dirctl --&gt;|"mTLS (X.509-SVID)"| Dir
</code></pre>

<p><strong>Trust domains:</strong></p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">testbed.internal.local</code> — Directory cluster</li>
  <li><code class="language-plaintext highlighter-rouge">partner.testbed.local</code> — dirctl cluster</li>
</ul>

<hr />

<h2 id="step-1-create-two-kind-clusters-and-install-metallb">Step 1: Create Two Kind Clusters and Install MetalLB</h2>

<p>Create two Kind clusters and install MetalLB so LoadBalancer services receive stable IPs (required for SPIRE federation and the Directory API).</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kind create cluster <span class="nt">--name</span> testbed
kind create cluster <span class="nt">--name</span> partner
</code></pre></div></div>

<p>MetalLB provides LoadBalancer IPs in Kind. Inspect your Docker network and pick IPs from the kind subnet:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker network inspect kind <span class="nt">--format</span> <span class="s1">'{{range .IPAM.Config}}{{.Subnet}} {{end}}'</span>
</code></pre></div></div>

<p><strong>Testbed</strong> (e.g. 172.18.255.1–172.18.255.9):</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context kind-testbed
helm repo add metallb https://metallb.github.io/metallb
helm upgrade <span class="nt">--install</span> metallb metallb/metallb <span class="nt">-n</span> metallb-system <span class="nt">--create-namespace</span> <span class="nt">--wait</span>

kubectl apply <span class="nt">-f</span> - <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: testbed-pool
  namespace: metallb-system
spec:
  addresses: ["172.18.255.1-172.18.255.9"]
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: testbed-l2
  namespace: metallb-system
spec:
  ipAddressPools: [testbed-pool]
</span><span class="no">EOF
</span></code></pre></div></div>

<p><strong>Partner</strong> (e.g. 172.18.255.11–172.18.255.19):</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context kind-partner
helm upgrade <span class="nt">--install</span> metallb metallb/metallb <span class="nt">-n</span> metallb-system <span class="nt">--create-namespace</span> <span class="nt">--wait</span>

kubectl apply <span class="nt">-f</span> - <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: partner-pool
  namespace: metallb-system
spec:
  addresses: ["172.18.255.11-172.18.255.19"]
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: partner-l2
  namespace: metallb-system
spec:
  ipAddressPools: [partner-pool]
</span><span class="no">EOF
</span></code></pre></div></div>

<hr />

<h2 id="step-2-deploy-spire-on-both-clusters-https_spiffe">Step 2: Deploy SPIRE on Both Clusters (https_spiffe)</h2>

<p><strong>Testbed:</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context kind-testbed

helm upgrade <span class="nt">--install</span> <span class="nt">-n</span> spire-server spire-crds spire-crds <span class="nt">--repo</span> https://spiffe.github.io/helm-charts-hardened/ <span class="nt">--create-namespace</span>
helm upgrade <span class="nt">--install</span> <span class="nt">-n</span> spire-server spire spire <span class="nt">--repo</span> https://spiffe.github.io/helm-charts-hardened/ <span class="se">\</span>
  <span class="nt">-f</span> - <span class="o">&lt;&lt;</span><span class="sh">'</span><span class="no">EOF</span><span class="sh">'
global:
  spire:
    trustDomain: testbed.internal.local
    clusterName: testbed
    namespaces:
      create: false
    ingressControllerType: other
  installAndUpgradeHooks:
    enabled: false
  deleteHooks:
    enabled: false

spire-server:
  federation:
    enabled: true
    tls:
      spire:
        enabled: true
      certManager:
        enabled: false
    ingress:
      enabled: false
  controllerManager:
    watchClassless: true
    className: spire
    identities:
      clusterFederatedTrustDomain:
        enabled: true
      clusterSPIFFEIDs:
        default:
          federatesWith: [partner.testbed.local]
</span><span class="no">EOF
</span></code></pre></div></div>

<p>Create LoadBalancer for federation endpoint:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> - <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
apiVersion: v1
kind: Service
metadata:
  name: spire-server-federation
  namespace: spire-server
  annotations:
    metallb.io/address-pool: testbed-pool
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: server
    app.kubernetes.io/instance: spire
  ports:
  - port: 8443
    targetPort: 8443
    name: federation
</span><span class="no">EOF

</span><span class="c"># Wait for SPIRE server, agent, and OIDC discovery provider (agent and OIDC are slowest)</span>
kubectl <span class="nb">wait</span> <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span>ready pod <span class="nt">-n</span> spire-server <span class="nt">-l</span> app.kubernetes.io/instance<span class="o">=</span>spire <span class="nt">--timeout</span><span class="o">=</span>300s
</code></pre></div></div>

<p><strong>Partner:</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context kind-partner

helm upgrade <span class="nt">--install</span> <span class="nt">-n</span> spire-server spire-crds spire-crds <span class="nt">--repo</span> https://spiffe.github.io/helm-charts-hardened/ <span class="nt">--create-namespace</span>
helm upgrade <span class="nt">--install</span> <span class="nt">-n</span> spire-server spire spire <span class="nt">--repo</span> https://spiffe.github.io/helm-charts-hardened/ <span class="se">\</span>
  <span class="nt">-f</span> - <span class="o">&lt;&lt;</span><span class="sh">'</span><span class="no">EOF</span><span class="sh">'
global:
  spire:
    trustDomain: partner.testbed.local
    clusterName: partner
    namespaces:
      create: false
    ingressControllerType: other
  installAndUpgradeHooks:
    enabled: false
  deleteHooks:
    enabled: false

spire-server:
  federation:
    enabled: true
    tls:
      spire:
        enabled: true
      certManager:
        enabled: false
    ingress:
      enabled: false
  controllerManager:
    watchClassless: true
    className: spire
    identities:
      clusterFederatedTrustDomain:
        enabled: true
      clusterSPIFFEIDs:
        default:
          federatesWith: [testbed.internal.local]
</span><span class="no">EOF
</span></code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> - <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
apiVersion: v1
kind: Service
metadata:
  name: spire-server-federation
  namespace: spire-server
  annotations:
    metallb.io/address-pool: partner-pool
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: server
    app.kubernetes.io/instance: spire
  ports:
  - port: 8443
    targetPort: 8443
    name: federation
</span><span class="no">EOF

</span><span class="c"># Wait for SPIRE server, agent, and OIDC discovery provider (agent and OIDC are slowest)</span>
kubectl <span class="nb">wait</span> <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span>ready pod <span class="nt">-n</span> spire-server <span class="nt">-l</span> app.kubernetes.io/instance<span class="o">=</span>spire <span class="nt">--timeout</span><span class="o">=</span>300s
</code></pre></div></div>

<p><strong>Verify:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get svc <span class="nt">-n</span> spire-server spire-server-federation
<span class="c"># testbed EXTERNAL-IP: 172.18.255.1, partner: 172.18.255.11</span>
</code></pre></div></div>

<hr />

<h2 id="step-3-export-trust-bundles">Step 3: Export Trust Bundles</h2>

<p>With <code class="language-plaintext highlighter-rouge">https_spiffe</code>, each SPIRE server needs the other’s trust bundle to bootstrap the TLS connection. The <strong>Directory</strong> and <strong>dirctl</strong> charts create <code class="language-plaintext highlighter-rouge">ClusterFederatedTrustDomain</code> automatically from their <code class="language-plaintext highlighter-rouge">spire.federation</code> config—you don’t need to apply them manually. We export the bundles now so we can embed them in the Helm values.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context kind-partner
kubectl <span class="nb">exec</span> <span class="nt">-n</span> spire-server spire-server-0 <span class="nt">-c</span> spire-server <span class="nt">--</span> <span class="se">\</span>
  spire-server bundle show <span class="nt">-format</span> spiffe <span class="o">&gt;</span> ./partner.bundle

kubectl config use-context kind-testbed
kubectl <span class="nb">exec</span> <span class="nt">-n</span> spire-server spire-server-0 <span class="nt">-c</span> spire-server <span class="nt">--</span> <span class="se">\</span>
  spire-server bundle show <span class="nt">-format</span> spiffe <span class="o">&gt;</span> ./testbed.bundle
</code></pre></div></div>

<hr />

<h2 id="step-4-deploy-directory-on-testbed">Step 4: Deploy Directory on Testbed</h2>

<p>Deploy the Agent Directory chart (API server, Zot registry, PostgreSQL) with SPIRE enabled and federation to <code class="language-plaintext highlighter-rouge">partner.testbed.local</code>. The <strong>Directory chart creates <code class="language-plaintext highlighter-rouge">ClusterFederatedTrustDomain</code></strong> from <code class="language-plaintext highlighter-rouge">apiserver.spire.federation</code>, so the testbed SPIRE server fetches the partner bundle and the API server can verify dirctl’s X.509-SVID over mTLS.</p>

<p>Ensure <code class="language-plaintext highlighter-rouge">./partner.bundle</code> exists before running (from Step 3).</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context kind-testbed

helm <span class="nb">install dir </span>oci://ghcr.io/agntcy/dir/helm-charts/dir <span class="se">\</span>
  <span class="nt">--version</span> v1.0.0 <span class="se">\</span>
  <span class="nt">--namespace</span> <span class="nb">dir</span> <span class="se">\</span>
  <span class="nt">--create-namespace</span> <span class="se">\</span>
  <span class="nt">-f</span> - <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
apiserver:
  image:
    repository: ghcr.io/agntcy/dir-apiserver
    tag: v1.0.0
    pullPolicy: IfNotPresent
  service:
    type: ClusterIP
  metrics:
    enabled: false
  routingService:
    type: NodePort
    nodePort: 30555
  spire:
    enabled: true
    className: spire
    trustDomain: testbed.internal.local
    useCSIDriver: true
    federation:
      - trustDomain: partner.testbed.local
        className: spire
        bundleEndpointURL: https://172.18.255.11:8443
        bundleEndpointProfile:
          type: https_spiffe
          endpointSPIFFEID: spiffe://partner.testbed.local/spire/server
        trustDomainBundle: |
</span><span class="si">$(</span><span class="nb">cat</span> ./partner.bundle | <span class="nb">sed</span> <span class="s1">'s/^/          /'</span><span class="si">)</span><span class="sh">
  config:
    listen_address: "0.0.0.0:8888"
    oasf_api_validation:
      disable: true
    authn:
      enabled: true
      mode: "x509"
      socket_path: "unix:///run/spire/agent-sockets/api.sock"
      audiences:
        - "spiffe://testbed.internal.local/spire/server"
    authz:
      enabled: true
      enforcer_policy_file_path: "/etc/agntcy/dir/authz_policies.csv"
    ratelimit:
      enabled: false
    store:
      provider: "oci"
      oci:
        registry_address: "dir-zot.dir.svc.cluster.local:5000"
        auth_config:
          insecure: "true"
          username: "admin"
          password: "admin"
    routing:
      listen_address: "/ip4/0.0.0.0/tcp/5555"
      datastore_dir: /etc/routing/datastore
      directory_api_address: "dir-apiserver.dir.svc.cluster.local:8888"
      gossipsub:
        enabled: false
    sync:
      auth_config:
        username: "user"
        password: "user"
    publication:
      scheduler_interval: "1h"
      worker_count: 1
      worker_timeout: "30m"
    database:
      type: "postgres"
      postgres:
        host: ""
        port: 5432
        database: "dir"
        ssl_mode: "disable"
  authz_policies_csv: |
    p,testbed.internal.local,*
    p,partner.testbed.local,*
    p,*,/agntcy.dir.store.v1.StoreService/Push
    p,*,/agntcy.dir.store.v1.StoreService/Pull
    p,*,/agntcy.dir.store.v1.StoreService/PullReferrer
    p,*,/agntcy.dir.store.v1.StoreService/Lookup
    p,*,/agntcy.dir.store.v1.SyncService/RequestRegistryCredentials
  secrets:
    ociAuth:
      username: "admin"
      password: "admin"
  reconciler:
    config:
      regsync:
        enabled: false
      indexer:
        enabled: false
  postgresql:
    enabled: true
    auth:
      username: "dir"
      password: "dir"
      database: "dir"
  strategy:
    type: Recreate
  extraVolumeMounts:
    - name: dir-routing-storage
      mountPath: /etc/routing
  extraVolumes:
    - name: dir-routing-storage
      emptyDir: {}
  zot:
    resources: {}
    mountSecret: true
    authHeader: "admin:admin"
    secretFiles:
      htpasswd: |-
        admin:</span><span class="se">\$</span><span class="sh">2y</span><span class="se">\$</span><span class="sh">05</span><span class="se">\$</span><span class="sh">vmiurPmJvHylk78HHFWuruFFVePlit9rZWGA/FbZfTEmNRneGJtha
        user:</span><span class="se">\$</span><span class="sh">2y</span><span class="se">\$</span><span class="sh">05</span><span class="se">\$</span><span class="sh">L86zqQDfH5y445dcMlwu6uHv.oXFgT6AiJCwpv3ehr7idc0rI3S2G
    mountConfig: true
    configFiles:
      config.json: |-
        {
          "distSpecVersion": "1.1.1",
          "storage": {"rootDirectory": "/var/lib/registry"},
          "http": {
            "address": "0.0.0.0",
            "port": "5000",
            "auth": {"htpasswd": {"path": "/secret/htpasswd"}},
            "accessControl": {
              "adminPolicy": {"users": ["admin"], "actions": ["read", "create", "update", "delete"]},
              "repositories": {"**": {"anonymousPolicy": [], "defaultPolicy": ["read"]}}
            }
          },
          "log": {"level": "info"},
          "extensions": {"search": {"enable": true}, "trust": {"enable": true, "cosign": true, "notation": false}}
        }
</span><span class="no">EOF
</span></code></pre></div></div>

<p>Wait for the API server:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">wait</span> <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span>ready pod <span class="nt">-n</span> <span class="nb">dir</span> <span class="nt">-l</span> app.kubernetes.io/name<span class="o">=</span>apiserver <span class="nt">--timeout</span><span class="o">=</span>240s
</code></pre></div></div>

<p>Expose Directory API via LoadBalancer:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> - <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
apiVersion: v1
kind: Service
metadata:
  name: dir-apiserver-external
  namespace: dir
  annotations:
    metallb.io/address-pool: testbed-pool
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: apiserver
    app.kubernetes.io/instance: dir
  ports:
  - port: 8888
    targetPort: 8888
    name: grpc
</span><span class="no">EOF
</span><span class="c"># EXTERNAL-IP will be 172.18.255.2</span>
</code></pre></div></div>

<hr />

<h2 id="step-5-deploy-dirctl-on-partner">Step 5: Deploy dirctl on Partner</h2>

<p>Deploy the dirctl chart configured to call the testbed API at <code class="language-plaintext highlighter-rouge">172.18.255.2:8888</code>. The <strong>dirctl chart creates <code class="language-plaintext highlighter-rouge">ClusterFederatedTrustDomain</code></strong> from <code class="language-plaintext highlighter-rouge">spire.federation</code>, so the partner SPIRE server fetches the testbed bundle and dirctl can verify the API server’s certificate over mTLS. The search cronjob runs every minute to demonstrate cross-cluster connectivity.</p>

<p>Ensure <code class="language-plaintext highlighter-rouge">./testbed.bundle</code> exists before running (from Step 3).</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context kind-partner

helm <span class="nb">install </span>dirctl oci://ghcr.io/agntcy/dir/helm-charts/dirctl <span class="se">\</span>
  <span class="nt">--version</span> v1.0.0 <span class="se">\</span>
  <span class="nt">--namespace</span> dir-admin <span class="se">\</span>
  <span class="nt">--create-namespace</span> <span class="se">\</span>
  <span class="nt">-f</span> - <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
global:
  cronjob:
    failedJobsHistoryLimit: 10
    successfulJobsHistoryLimit: 10
    concurrencyPolicy: "Forbid"

image:
  repository: ghcr.io/agntcy/dir-ctl
  tag: v1.0.0
  pullPolicy: IfNotPresent

spire:
  enabled: true
  trustDomain: partner.testbed.local
  useCSIDriver: true
  className: spire
  federation:
    - trustDomain: testbed.internal.local
      className: spire
      bundleEndpointURL: https://172.18.255.1:8443
      bundleEndpointProfile:
        type: https_spiffe
        endpointSPIFFEID: spiffe://testbed.internal.local/spire/server
      trustDomainBundle: |
</span><span class="si">$(</span><span class="nb">cat</span> ./testbed.bundle | <span class="nb">sed</span> <span class="s1">'s/^/        /'</span><span class="si">)</span><span class="sh">

env:
  - name: DIRECTORY_CLIENT_SERVER_ADDRESS
    value: "172.18.255.2:8888"
  - name: DIRECTORY_CLIENT_AUTH_MODE
    value: "x509"
  - name: DIRECTORY_CLIENT_TLS_SKIP_VERIFY
    value: "true"

cronjobs:
  search:
    enabled: true
    schedule: "* * * * *"
    args: ["search"]
</span><span class="no">EOF
</span></code></pre></div></div>

<blockquote>
  <p><strong>Note:</strong> <code class="language-plaintext highlighter-rouge">DIRECTORY_CLIENT_TLS_SKIP_VERIFY: "true"</code> is used because the API server is reached via MetalLB IP (no matching cert SAN). For production, use a proper DNS name and valid certificate.</p>
</blockquote>

<hr />

<h2 id="step-6-verify">Step 6: Verify</h2>

<p><strong>Optional: verify SPIRE federation first.</strong> Confirm each cluster has the other’s trust bundle—helps debug auth failures before running dirctl:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># On testbed – partner's bundle should appear</span>
kubectl config use-context kind-testbed
kubectl <span class="nb">exec</span> <span class="nt">-n</span> spire-server spire-server-0 <span class="nt">-c</span> spire-server <span class="nt">--</span> <span class="se">\</span>
  spire-server bundle list <span class="nt">-id</span> spiffe://partner.testbed.local <span class="nt">-format</span> spiffe

<span class="c"># On partner – testbed's bundle should appear</span>
kubectl config use-context kind-partner
kubectl <span class="nb">exec</span> <span class="nt">-n</span> spire-server spire-server-0 <span class="nt">-c</span> spire-server <span class="nt">--</span> <span class="se">\</span>
  spire-server bundle list <span class="nt">-id</span> spiffe://testbed.internal.local <span class="nt">-format</span> spiffe
</code></pre></div></div>

<p><strong>Verify dirctl can call the Directory API:</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context kind-partner
kubectl create job <span class="nt">-n</span> dir-admin dirctl-verify <span class="nt">--from</span><span class="o">=</span>cronjob/dirctl-search <span class="nt">--dry-run</span><span class="o">=</span>client <span class="nt">-o</span> yaml | kubectl apply <span class="nt">-f</span> -
kubectl <span class="nb">wait</span> <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span><span class="nb">complete </span>job/dirctl-verify <span class="nt">-n</span> dir-admin <span class="nt">--timeout</span><span class="o">=</span>120s
kubectl logs <span class="nt">-n</span> dir-admin job/dirctl-verify
</code></pre></div></div>

<p>Expected: successful <code class="language-plaintext highlighter-rouge">search</code> output (list of records or empty). If you see <code class="language-plaintext highlighter-rouge">remote error: tls: bad certificate</code>, ensure the Directory API server has <code class="language-plaintext highlighter-rouge">apiserver.spire.federation</code> for <code class="language-plaintext highlighter-rouge">partner.testbed.local</code> and <code class="language-plaintext highlighter-rouge">authz_policies_csv</code> includes <code class="language-plaintext highlighter-rouge">p,partner.testbed.local,*</code>.</p>

<hr />

<h2 id="cleanup">Cleanup</h2>

<p>When finished, tear down both clusters:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Testbed</span>
kubectl config use-context kind-testbed
helm uninstall <span class="nb">dir</span> <span class="nt">-n</span> <span class="nb">dir
</span>helm uninstall spire <span class="nt">-n</span> spire-server
helm uninstall spire-crds <span class="nt">-n</span> spire-server

<span class="c"># Partner</span>
kubectl config use-context kind-partner
helm uninstall dirctl <span class="nt">-n</span> dir-admin
helm uninstall spire <span class="nt">-n</span> spire-server
helm uninstall spire-crds <span class="nt">-n</span> spire-server

<span class="c"># Delete clusters</span>
kind delete cluster <span class="nt">--name</span> testbed
kind delete cluster <span class="nt">--name</span> partner
</code></pre></div></div>

<hr />

<h2 id="troubleshooting">Troubleshooting</h2>

<p>For federation-specific issues (trust bundles, certificates, SSL passthrough), see <a href="https://docs.agntcy.org/dir/federation-troubleshooting/">Federation Troubleshooting</a> in the docs.</p>

<table>
  <thead>
    <tr>
      <th>Issue</th>
      <th>Check</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>dirctl auth failure</td>
      <td><code class="language-plaintext highlighter-rouge">authz_policies_csv</code> has <code class="language-plaintext highlighter-rouge">p,partner.testbed.local,*</code>; dirctl pods get SVIDs</td>
    </tr>
    <tr>
      <td>“no X.509 bundle for trust domain”</td>
      <td>dirctl needs testbed bundle via <code class="language-plaintext highlighter-rouge">spire.federation</code>; reinstall and retry</td>
    </tr>
    <tr>
      <td>“remote error: tls: bad certificate”</td>
      <td>Directory needs <code class="language-plaintext highlighter-rouge">apiserver.spire.federation</code> for partner; restart apiserver</td>
    </tr>
    <tr>
      <td>Connection refused</td>
      <td>MetalLB pools; LoadBalancer selector; Docker network</td>
    </tr>
    <tr>
      <td>Zot 401 / hash errors</td>
      <td>Use <code class="language-plaintext highlighter-rouge">\$</code> in bcrypt hashes in heredocs to prevent shell expansion</td>
    </tr>
    <tr>
      <td>Reconciler 503</td>
      <td>Wait for PostgreSQL and Zot to be ready; reconciler needs both for readiness</td>
    </tr>
    <tr>
      <td>SPIFFE CSI driver missing</td>
      <td>SPIRE chart should provide it; if pods fail, check <code class="language-plaintext highlighter-rouge">spire.useCSIDriver</code> and agent</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="conclusion">Conclusion</h2>

<p>This hands-on setup demonstrates that <strong>SPIFFE federation doesn’t require production infrastructure</strong>. With two Kind clusters, MetalLB, and the <code class="language-plaintext highlighter-rouge">https_spiffe</code> profile, you can run cross-cluster mTLS in under 20 minutes. The Agent Directory and dirctl charts handle trust bundle configuration automatically—no manual <code class="language-plaintext highlighter-rouge">ClusterFederatedTrustDomain</code> applies needed. The concepts (trust bundles, workload identity, federation profiles) transfer directly to production; only the profile (<code class="language-plaintext highlighter-rouge">https_web</code>) and infrastructure (Ingress, cert-manager, DNS) change.</p>

<h2 id="next-steps-production-and-public-directory">Next Steps: Production and Public Directory</h2>

<p>For <strong>production</strong> and joining the <strong>public Directory network</strong> at <code class="language-plaintext highlighter-rouge">prod.api.ads.outshift.io</code>:</p>

<ol>
  <li><strong><a href="https://docs.agntcy.org/dir/prod-deployment/">Production Deployment</a></strong> — EKS, NGINX Ingress, SSL passthrough, persistent storage, credential management.</li>
  <li><strong><a href="https://docs.agntcy.org/dir/partner-prod-federation/">Partner Federation with Prod</a></strong> — Step-by-step guide using the <code class="language-plaintext highlighter-rouge">https_web</code> profile (Let’s Encrypt), public endpoints, and contributing your federation file to the Directory staging repository.</li>
</ol>

<h2 id="references">References</h2>

<ul>
  <li><a href="https://github.com/agntcy/dir">Directory GitHub Repository</a></li>
  <li><a href="https://docs.agntcy.org/dir/getting-started/">Getting Started</a> — local Kind deployment</li>
  <li><a href="https://docs.agntcy.org/dir/prod-deployment/">Production Deployment</a> — EKS, NGINX Ingress</li>
  <li><a href="https://docs.agntcy.org/dir/partner-prod-federation/">Partner Federation with Prod</a> — join the public Directory</li>
  <li><a href="https://docs.agntcy.org/dir/federation-profiles/">Federation Profiles</a> — https_web vs https_spiffe</li>
  <li><a href="https://docs.agntcy.org/dir/federation-troubleshooting/">Federation Troubleshooting</a> — best practices and common errors</li>
  <li><a href="https://spiffe.io">SPIFFE / SPIRE</a></li>
  <li><a href="https://kind.sigs.k8s.io/">Kind</a></li>
  <li><a href="https://metallb.org/">MetalLB</a></li>
</ul>

<hr />

<p><em>Have questions about Agent Directory federation or SPIFFE? Join our <a href="https://join.slack.com/t/agntcy/shared_invite/zt-3hb4p7bo0-5H2otGjxGt9OQ1g5jzK_GQ">Slack community</a> or check out our <a href="https://github.com/agntcy">GitHub</a>.</em></p>]]></content><author><name>Tibor Kircsi</name></author><category term="technical" /><category term="security" /><category term="directory" /><category term="spiffe" /><category term="spire" /><category term="federation" /><category term="mTLS" /><category term="zero-trust" /><category term="kind" /><category term="kubernetes" /><summary type="html"><![CDATA[Agent Directory is a secure, scalable, decentralized service that holds agent records. The software allows users to publish and discover agent records across the network of decentralized, federated directories. Agent Directory provides a cryptographic trust model that ensures authenticity and provenance of each record. This component is available as open-source software. The Agent Directory enables secure, decentralized agent discovery across organizational boundaries. To join the public Directory network or federate your own instance, you need SPIFFE federation: cryptographic trust between SPIRE servers through trust bundle exchange. This post walks you through a hands-on local setup using two Kind clusters: SPIRE federation with the https_spiffe profile, then Directory and dirctl deployed across clusters. No cert-manager or public DNS required—ideal for learning and experimentation. When you’re ready for production, see Getting Started and Partner Federation with Prod for the https_web profile and joining the public Directory.]]></summary></entry><entry><title type="html">Directory MCP Server: Bringing AI Agent Discovery to Your IDE</title><link href="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/02/19/directory-mcp-server.html" rel="alternate" type="text/html" title="Directory MCP Server: Bringing AI Agent Discovery to Your IDE" /><published>2026-02-19T08:00:00+00:00</published><updated>2026-02-19T08:00:00+00:00</updated><id>https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/02/19/directory-mcp-server</id><content type="html" xml:base="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/02/19/directory-mcp-server.html"><![CDATA[<p>The Model Context Protocol (MCP) has emerged as a powerful standard for connecting AI assistants with external tools and data sources. In this post, we’ll explore the Directory MCP server—a feature that brings AI agent discovery and management directly into your IDE, enabling seamless interaction with the Directory ecosystem.</p>

<!--more-->

<p><strong>TL;DR:</strong> The Directory MCP server lets your AI-powered IDE discover, validate, and use AI agents from the Directory network—without leaving your editor. It bridges Directory’s decentralized agent registry with MCP-enabled tools like Cursor, VS Code, and Codex.</p>

<h3 id="what-youll-learn">What You’ll Learn</h3>

<p>In this post, you’ll learn:</p>

<ul>
  <li>What Directory is and why MCP matters</li>
  <li>How to install and configure the Directory MCP server in your IDE</li>
  <li>How to distribute and sign OASF agent records through CI/CD</li>
  <li>How to discover, import, and audit agents using natural language inside your IDE</li>
  <li>How Directory enables secure agent ecosystems</li>
</ul>

<h2 id="what-is-agent-directory">What is Agent Directory?</h2>

<p><a href="https://docs.agntcy.org/dir/overview/">Agent Directory</a> is a secure, scalable, decentralized service that holds agent records. The software allows users to publish and discover agent records across the network of decentralized, federated directories. Agent Directory provides a cryptographic trust model that ensures authenticity and provenance of each record. This component is available as open-source software. The Directory provides a distributed peer-to-peer network for publishing, exchanging, and discovering information about AI agents. It uses <a href="https://github.com/agntcy/oasf">OASF (Open Agentic Schema Framework)</a> to describe AI agents with structured metadata, enabling:</p>

<ul>
  <li><strong>Capability-Based Discovery</strong>: Find agents based on their functional characteristics using hierarchical skill and domain taxonomies</li>
  <li><strong>Verifiable Claims</strong>: Cryptographic mechanisms ensure data integrity and provenance tracking</li>
  <li><strong>Distributed Architecture</strong>: Content-addressing and distributed hash tables (DHT) for scalable discovery across decentralized networks</li>
  <li><strong>Multi-Format Support</strong>: Import and export between OASF, A2A (Agent-to-Agent), and MCP formats</li>
</ul>

<h2 id="why-an-mcp-server">Why an MCP Server?</h2>

<p>While Directory provides CLI tools and SDKs for interacting with the network, developers increasingly work within AI-assisted IDEs like <a href="https://cursor.com">Cursor</a>, <a href="https://code.visualstudio.com">VS Code</a> or <a href="https://openai.com/codex/">Codex</a>. The MCP server bridges this gap by exposing Directory functionality directly to AI assistants, allowing you to:</p>

<ul>
  <li>Search for agents using natural language</li>
  <li>Validate and push agent records without leaving your editor</li>
  <li>Generate OASF records by analyzing your codebase</li>
  <li>Import and export between different agent formats</li>
</ul>

<p>Instead of context-switching to the terminal, you can simply ask your AI assistant to perform these operations.</p>

<h2 id="architecture">Architecture</h2>

<p>The MCP server acts as a bridge between your AI assistant and the Directory ecosystem:</p>

<pre><code class="language-mermaid">flowchart LR
    classDef ide fill:#0251af,stroke:#f3f6fd,stroke-width:2px,color:#f3f6fd;
    classDef mcp fill:#03142b,stroke:#0251af,stroke-width:2px,color:#f3f6fd;
    classDef external fill:#0251af,stroke:#f3f6fd,stroke-width:2px,color:#f3f6fd;

    subgraph IDE["IDE (Cursor, VS Code, Codex)"]
        AI[AI Assistant]:::ide
    end

    subgraph MCP["MCP Server (dirctl mcp serve)"]
        Tools[Tools]:::mcp
        Prompts[Prompts]:::mcp
    end

    subgraph External["External Services"]
        Dir[Directory Server&lt;br/&gt;Agent Records]:::external
        Schema[OASF Schema Server&lt;br/&gt;Validation]:::external
    end

    AI --&gt;|"Tool calls&lt;br/&gt;(search, push, pull)"| Tools
    AI --&gt;|"Prompt invocations&lt;br/&gt;(create_record, search_records)"| Prompts
    Tools --&gt; Dir
    Tools --&gt; Schema
    Prompts --&gt; Tools
</code></pre>

<p>The server communicates via stdin/stdout or streamable HTTP using the MCP protocol, making it compatible with any MCP-enabled IDE.</p>

<h2 id="prerequisites">Prerequisites</h2>

<p>Before setting up the MCP server, you’ll need:</p>

<ol>
  <li>
    <p><strong>dirctl CLI</strong> - Install via <a href="https://github.com/agntcy/dir/tree/main/HomebrewFormula">Homebrew</a> or download from <a href="https://github.com/agntcy/dir/releases">GitHub Releases</a>. Alternatively, use the Docker image.</p>
  </li>
  <li>
    <p><strong>An MCP-enabled IDE</strong></p>
  </li>
  <li>
    <p><strong>A running Directory server</strong> - Either:</p>
    <ul>
      <li><strong>Local development</strong>: Start one with <code class="language-plaintext highlighter-rouge">task server:start</code> in the <a href="https://github.com/agntcy/dir">Directory repository</a>, or use Docker Compose:
        <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd install</span>/docker <span class="o">&amp;&amp;</span> docker compose up <span class="nt">-d</span>
</code></pre></div>        </div>
      </li>
      <li><strong>Remote server</strong>: Use an existing Directory deployment (e.g., the <a href="https://blogs.agntcy.org/technical/2026/02/19/dir-v1.html">AGNTCY public testbed</a> at <code class="language-plaintext highlighter-rouge">prod.gateway.ads.outshift.io:443</code>)</li>
    </ul>
  </li>
</ol>

<h2 id="setup">Setup</h2>

<p>The MCP server runs via the <code class="language-plaintext highlighter-rouge">dirctl</code> CLI tool. Below are configuration instructions for some AI IDEs, with both binary and Docker options.</p>

<h3 id="cursor">Cursor</h3>

<p>Add the MCP server to your Cursor configuration at <code class="language-plaintext highlighter-rouge">~/.cursor/mcp.json</code>:</p>

<p><strong>Using the binary:</strong></p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"mcpServers"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"dir-mcp-server"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
      </span><span class="nl">"command"</span><span class="p">:</span><span class="w"> </span><span class="s2">"/path/to/dirctl"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"args"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"mcp"</span><span class="p">,</span><span class="w"> </span><span class="s2">"serve"</span><span class="p">],</span><span class="w">
      </span><span class="nl">"env"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"OASF_API_VALIDATION_SCHEMA_URL"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://schema.oasf.outshift.com"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"DIRECTORY_CLIENT_SERVER_ADDRESS"</span><span class="p">:</span><span class="w"> </span><span class="s2">"localhost:8888"</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p><strong>Using Docker:</strong></p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"mcpServers"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"dir-mcp-server"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
      </span><span class="nl">"command"</span><span class="p">:</span><span class="w"> </span><span class="s2">"docker"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"args"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="s2">"run"</span><span class="p">,</span><span class="w"> </span><span class="s2">"--rm"</span><span class="p">,</span><span class="w"> </span><span class="s2">"-i"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"-e"</span><span class="p">,</span><span class="w"> </span><span class="s2">"OASF_API_VALIDATION_SCHEMA_URL=https://schema.oasf.outshift.com"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"-e"</span><span class="p">,</span><span class="w"> </span><span class="s2">"DIRECTORY_CLIENT_SERVER_ADDRESS=localhost:8888"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"ghcr.io/agntcy/dir-ctl:latest"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"mcp"</span><span class="p">,</span><span class="w"> </span><span class="s2">"serve"</span><span class="w">
      </span><span class="p">]</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>After saving, restart Cursor for the changes to take effect.</p>

<h3 id="vs-code">VS Code</h3>

<p>For VS Code with GitHub Copilot, add the MCP server to your workspace configuration at <code class="language-plaintext highlighter-rouge">.vscode/mcp.json</code>, or to your user settings at <code class="language-plaintext highlighter-rouge">~/.vscode/mcp.json</code> for global availability:</p>

<p><strong>Using the binary:</strong></p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"servers"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"dir-mcp-server"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
      </span><span class="nl">"command"</span><span class="p">:</span><span class="w"> </span><span class="s2">"/path/to/dirctl"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"args"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"mcp"</span><span class="p">,</span><span class="w"> </span><span class="s2">"serve"</span><span class="p">],</span><span class="w">
      </span><span class="nl">"env"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"OASF_API_VALIDATION_SCHEMA_URL"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://schema.oasf.outshift.com"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"DIRECTORY_CLIENT_SERVER_ADDRESS"</span><span class="p">:</span><span class="w"> </span><span class="s2">"localhost:8888"</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p><strong>Using Docker:</strong></p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"servers"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"dir-mcp-server"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
      </span><span class="nl">"command"</span><span class="p">:</span><span class="w"> </span><span class="s2">"docker"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"args"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="s2">"run"</span><span class="p">,</span><span class="w"> </span><span class="s2">"--rm"</span><span class="p">,</span><span class="w"> </span><span class="s2">"-i"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"-e"</span><span class="p">,</span><span class="w"> </span><span class="s2">"OASF_API_VALIDATION_SCHEMA_URL=https://schema.oasf.outshift.com"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"-e"</span><span class="p">,</span><span class="w"> </span><span class="s2">"DIRECTORY_CLIENT_SERVER_ADDRESS=localhost:8888"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"ghcr.io/agntcy/dir-ctl:latest"</span><span class="p">,</span><span class="w">
        </span><span class="s2">"mcp"</span><span class="p">,</span><span class="w"> </span><span class="s2">"serve"</span><span class="w">
      </span><span class="p">]</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<h3 id="codex">Codex</h3>

<p>For OpenAI’s Codex, you can also add the MCP server config to your workspace configuration at <code class="language-plaintext highlighter-rouge">.codex/config.toml</code> or to your settings at <code class="language-plaintext highlighter-rouge">~/.codex/config.toml</code>:</p>

<p><strong>Using the binary:</strong></p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[mcp_servers.dir-mcp-server]</span>
<span class="py">command</span> <span class="p">=</span> <span class="s">"/path/to/dirctl"</span>
<span class="py">args</span> <span class="p">=</span> <span class="p">[</span><span class="s">"mcp"</span><span class="p">,</span> <span class="s">"serve"</span><span class="p">]</span>

<span class="nn">[mcp_servers.dir-mcp-server.env]</span>
<span class="py">OASF_API_VALIDATION_SCHEMA_URL</span> <span class="p">=</span> <span class="s">"https://schema.oasf.outshift.com"</span>
<span class="py">DIRECTORY_CLIENT_SERVER_ADDRESS</span> <span class="p">=</span> <span class="s">"localhost:8888"</span>
</code></pre></div></div>

<p><strong>Using Docker:</strong></p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[mcp_servers.dir-mcp-server]</span>
<span class="py">command</span> <span class="p">=</span> <span class="s">"docker"</span>
<span class="py">args</span> <span class="p">=</span> <span class="p">[</span>
  <span class="s">"run"</span><span class="p">,</span> <span class="s">"--rm"</span><span class="p">,</span> <span class="s">"-i"</span><span class="p">,</span>
  <span class="s">"-e"</span><span class="p">,</span> <span class="py">"OASF_API_VALIDATION_SCHEMA_URL</span><span class="p">=</span><span class="err">https://schema.oasf.outshift.com</span><span class="s">",</span><span class="err">
</span>  <span class="s">"-e"</span><span class="p">,</span> <span class="py">"DIRECTORY_CLIENT_SERVER_ADDRESS</span><span class="p">=</span><span class="err">localhost:</span><span class="mi">8888</span><span class="s">",</span><span class="err">
</span>  <span class="s">"ghcr.io/agntcy/dir-ctl:latest"</span><span class="p">,</span>
  <span class="s">"mcp"</span><span class="p">,</span> <span class="s">"serve"</span>
<span class="p">]</span>
</code></pre></div></div>

<p><strong>Note:</strong> When using Docker, environment variables must be passed via <code class="language-plaintext highlighter-rouge">-e</code> flags in the args array, not in the <code class="language-plaintext highlighter-rouge">env</code> section.</p>

<h3 id="connecting-to-a-remote-directory-server">Connecting to a Remote Directory Server</h3>

<p>To connect to a remote Directory server (e.g., the production gateway), update the environment variables to include the server address and GitHub authentication:</p>

<table>
  <thead>
    <tr>
      <th>Variable</th>
      <th>Value</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">OASF_API_VALIDATION_SCHEMA_URL</code></td>
      <td><code class="language-plaintext highlighter-rouge">https://schema.oasf.outshift.com</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">DIRECTORY_CLIENT_SERVER_ADDRESS</code></td>
      <td><code class="language-plaintext highlighter-rouge">prod.gateway.ads.outshift.io:443</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">DIRECTORY_CLIENT_AUTH_MODE</code></td>
      <td><code class="language-plaintext highlighter-rouge">github</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">DIRECTORY_CLIENT_GITHUB_TOKEN</code></td>
      <td>Your GitHub PAT</td>
    </tr>
  </tbody>
</table>

<p>Generate a PAT at <a href="https://github.com/settings/tokens">GitHub Settings &gt; Developer settings &gt; Personal access tokens</a> with <code class="language-plaintext highlighter-rouge">user:email</code> and <code class="language-plaintext highlighter-rouge">read:org</code> scopes.</p>

<h2 id="usage-examples">Usage Examples</h2>

<p>Once the Directory MCP server is installed, you can use it for a variety of tasks. Here are three real-world scenarios covering the full lifecycle: distributing agents, discovering and installing them, and auditing their provenance.</p>

<h3 id="scenario-1-distributing-your-agents-to-directory">Scenario 1: Distributing Your Agents to Directory</h3>

<p>In this scenario, you’ve built a multi-agent system and want to distribute each agent to Directory so others can discover them. We’ll use <a href="https://github.com/agntcy/coffeeAgntcy">CoffeeAgntcy</a>—AGNTCY’s reference application demonstrating a coffee supply chain with multiple collaborating agents.</p>

<p>For simplicity, we’ll use the <strong>Corto</strong> setup which has two agents:</p>
<ul>
  <li><strong>Exchange Agent</strong> - The coffee buyer that initiates purchases</li>
  <li><strong>Farm Agent</strong> - The coffee farm that responds to purchase requests</li>
</ul>

<p><strong>Step 1: Generate OASF records for each agent</strong></p>

<p>Open your project in Cursor and ask the AI to create records for each agent:</p>

<blockquote>
  <p>“Analyze the Exchange and Farm agents in coffeeAGNTCY/coffee_agents/corto/ and create OASF records (schema_version 1.0.0) for each. Use the Directory MCP tools to fetch valid skills and domains from the OASF schema. Save as agents/coffee-exchange-agent.json and agents/coffee-farm-agent.json”</p>
</blockquote>

<p>The AI analyzes your codebase, queries the OASF schema for valid skills and domains, and generates complete records.</p>

<p><strong>Exchange Agent</strong> - The coffee buyer:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"schema_version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"1.0.0"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"coffee-exchange-agent"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"1.0.0"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"created_at"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2026-02-18T10:00:00Z"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Coffee exchange agent that initiates purchases from farm agents. Orchestrates the buying workflow using LangGraph, communicates via A2A protocol over SLIM transport, and integrates with the Observe SDK for tracing."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"authors"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"AGNTCY Contributors"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"skills"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">1004</span><span class="p">,</span><span class="w"> </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"agent_orchestration/agent_coordination"</span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">1005</span><span class="p">,</span><span class="w"> </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"agent_orchestration/negotiation_resolution"</span><span class="p">}</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"domains"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">1801</span><span class="p">,</span><span class="w"> </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"retail_and_ecommerce/supply_chain_management"</span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">2301</span><span class="p">,</span><span class="w"> </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"agriculture/crop_management"</span><span class="p">}</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"locators"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"source_code"</span><span class="p">,</span><span class="w"> </span><span class="nl">"urls"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"https://github.com/agntcy/coffeeAgntcy/tree/main/coffeeAGNTCY/coffee_agents/corto/exchange"</span><span class="p">]}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p><strong>Farm Agent</strong> - The coffee seller:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"schema_version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"1.0.0"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"coffee-farm-agent"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"1.0.0"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"created_at"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2026-02-18T10:00:00Z"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Coffee farm agent that responds to purchase requests from the exchange. Manages inventory, processes orders, and communicates via A2A protocol. Exposes an A2A server endpoint for incoming requests."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"authors"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"AGNTCY Contributors"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"skills"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">1004</span><span class="p">,</span><span class="w"> </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"agent_orchestration/agent_coordination"</span><span class="p">}</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"domains"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">2301</span><span class="p">,</span><span class="w"> </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"agriculture/crop_management"</span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">1801</span><span class="p">,</span><span class="w"> </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"retail_and_ecommerce/supply_chain_management"</span><span class="p">}</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"locators"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"source_code"</span><span class="p">,</span><span class="w"> </span><span class="nl">"urls"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"https://github.com/agntcy/coffeeAgntcy/tree/main/coffeeAGNTCY/coffee_agents/corto/farm"</span><span class="p">]}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p><strong>Step 2: Set up GitHub Actions for CI/CD</strong></p>

<p>Directory provides <a href="https://github.com/agntcy/dir/tree/main/.github/actions">GitHub Actions</a> that automate the entire distribution workflow: validation, pushing, signing, and DHT publication.</p>

<p>Create <code class="language-plaintext highlighter-rouge">.github/workflows/publish-agents.yaml</code> in your repository:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">Distributing Agents to Directory</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>
    <span class="na">paths</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s1">'</span><span class="s">agents/*.json'</span>

<span class="na">permissions</span><span class="pi">:</span>
  <span class="na">id-token</span><span class="pi">:</span> <span class="s">write</span>  <span class="c1"># Required for keyless signing</span>
  <span class="na">contents</span><span class="pi">:</span> <span class="s">read</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">publish</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v4</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Setup dirctl</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">agntcy/dir/.github/actions/setup-dirctl@main</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">version</span><span class="pi">:</span> <span class="s">latest</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Validate OASF records</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">agntcy/dir/.github/actions/validate-record@main</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">record_paths</span><span class="pi">:</span> <span class="pi">|</span>
            <span class="s">agents/*.json</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Push to Directory</span>
        <span class="na">id</span><span class="pi">:</span> <span class="s">push</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">agntcy/dir/.github/actions/push-record@main</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">record_paths</span><span class="pi">:</span> <span class="s">agents/*.json</span>
          <span class="na">server_addr</span><span class="pi">:</span> <span class="s">${{ vars.DIRECTORY_ADDRESS }}</span>
          <span class="na">github_token</span><span class="pi">:</span> <span class="s">${{ secrets.DIRECTORY_TOKEN }}</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Sign records (keyless with GitHub OIDC)</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">agntcy/dir/.github/actions/sign-record@main</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">cids</span><span class="pi">:</span> <span class="s">${{ steps.push.outputs.cids }}</span>
          <span class="na">server_addr</span><span class="pi">:</span> <span class="s">${{ vars.DIRECTORY_ADDRESS }}</span>
          <span class="na">github_token</span><span class="pi">:</span> <span class="s">${{ secrets.DIRECTORY_TOKEN }}</span>
          <span class="na">oidc_client_id</span><span class="pi">:</span> <span class="s">https://github.com/${{ github.repository }}/.github/workflows/publish-agents.yaml@${{ github.ref }}</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Publish to DHT</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">agntcy/dir/.github/actions/publish-record@main</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">cids</span><span class="pi">:</span> <span class="s">${{ steps.push.outputs.cids }}</span>
          <span class="na">server_addr</span><span class="pi">:</span> <span class="s">${{ vars.DIRECTORY_ADDRESS }}</span>
          <span class="na">github_token</span><span class="pi">:</span> <span class="s">${{ secrets.DIRECTORY_TOKEN }}</span>
</code></pre></div></div>

<p>This workflow:</p>

<ol>
  <li><strong>Validates</strong> all agent records against the OASF schema, failing early if any record is invalid</li>
  <li><strong>Pushes</strong> records to Directory, outputting CIDs for each file</li>
  <li><strong>Signs</strong> records using GitHub OIDC (keyless)—no secrets to manage, signatures tied to your GitHub identity</li>
  <li><strong>Publishes</strong> to the DHT, making agents discoverable across the Directory network</li>
</ol>

<p>The signing step uses <a href="https://www.sigstore.dev/">Sigstore</a> with GitHub’s OIDC tokens—Sigstore issues a short-lived certificate from <a href="https://docs.sigstore.dev/certificate_authority/overview/">Fulcio</a> tied to your workflow identity, signs the records, and logs signatures to the <a href="https://docs.sigstore.dev/logging/overview/">Rekor</a> transparency log.</p>

<p><strong>Step 3: Commit and push</strong></p>

<p>Once your records are ready, commit them to your repository:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add agents/
git commit <span class="nt">-m</span> <span class="s2">"Add OASF records for Exchange and Farm agents"</span>
git push origin main
</code></pre></div></div>

<p>The GitHub Actions workflow triggers automatically and handles everything:</p>
<ol>
  <li><strong>Validates</strong> records against the OASF schema</li>
  <li><strong>Pushes</strong> to Directory (outputs CIDs)</li>
  <li><strong>Signs</strong> with GitHub OIDC (keyless)</li>
  <li><strong>Publishes</strong> to the DHT</li>
</ol>

<p><strong>Step 4: Verify the distributed records</strong></p>

<p>After the workflow completes, anyone can verify a record’s signature using the MCP server:</p>

<blockquote>
  <p>“Search Directory for coffee-exchange-agent, then verify its signature and show the signer identity”</p>
</blockquote>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"success"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
  </span><span class="nl">"message"</span><span class="p">:</span><span class="w"> </span><span class="s2">"trusted"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"signers"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"identity"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://github.com/agntcy/coffeeAgntcy/.github/workflows/publish-agents.yaml@refs/heads/main"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"issuer"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://token.actions.githubusercontent.com"</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>The signature shows it was signed by a specific GitHub Actions workflow, providing full provenance tracking:</p>
<ul>
  <li><strong>Who</strong> signed it (the workflow identity)</li>
  <li><strong>When</strong> it was signed (Rekor transparency log)</li>
  <li><strong>What</strong> was signed (content-addressed CID)</li>
</ul>

<p>Your agents are now fully distributed to the Directory network—signed, verified, and discoverable by anyone!</p>

<blockquote>
  <p>For a more complex multi-agent example, check out the <a href="https://github.com/agntcy/coffeeAgntcy/tree/main/coffeeAGNTCY/coffee_agents/lungo">Lungo setup</a> which includes farm agents for Brazil, Colombia, and Vietnam, plus logistics agents (shipper, accountant) and supervisor agents for auction orchestration.</p>
</blockquote>

<h3 id="scenario-2-discovering-and-installing-an-agent">Scenario 2: Discovering and Installing an Agent</h3>

<p>You need GitHub integration in your IDE—listing issues, searching code, managing pull requests—without leaving your editor. Instead of manually configuring MCP servers, you ask your AI assistant to find one in Directory.</p>

<p><strong>Step 1: Search for an agent with the capability you need</strong></p>

<blockquote>
  <p>“Search Directory for MCP servers with GitHub API integration skills”</p>
</blockquote>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"count"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w">
  </span><span class="nl">"has_more"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w">
  </span><span class="nl">"record_cids"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"baeareiarov6l6hcb274t5gxkwujbmcizi4ba5ullqoeyqrhlqc6ymz6fsm"</span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>Found a match! Let’s see what it offers.</p>

<p><strong>Step 2: Pull the record to inspect it</strong></p>

<blockquote>
  <p>“Pull record baeareiarov6l6hcb274t5gxkwujbmcizi4ba5ullqoeyqrhlqc6ymz6fsm and show me its capabilities”</p>
</blockquote>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"github-mcp"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"1.0.0"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Official GitHub MCP Server providing repository management, issue tracking, pull request workflows, code search, and GitHub API access for AI assistants."</span><span class="p">,</span><span class="w">
  </span><span class="nl">"authors"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"GitHub"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"skills"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"tool_interaction/api_schema_understanding"</span><span class="p">,</span><span class="w"> </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">1401</span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"tool_interaction/workflow_automation"</span><span class="p">,</span><span class="w"> </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">1402</span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"devops_mlops/ci_cd_configuration"</span><span class="p">,</span><span class="w"> </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">1203</span><span class="p">}</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"domains"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"technology/software_engineering"</span><span class="p">,</span><span class="w"> </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">102</span><span class="p">}</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"locators"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"source_code"</span><span class="p">,</span><span class="w"> </span><span class="nl">"urls"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"https://github.com/github/github-mcp-server"</span><span class="p">]}</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"modules"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"integration/mcp"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"data"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"github"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"GitHub API access for repository management, issues, PRs, and code search"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"connections"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
          </span><span class="p">{</span><span class="w">
            </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"streamable-http"</span><span class="p">,</span><span class="w">
            </span><span class="nl">"url"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://api.githubcopilot.com/mcp/"</span><span class="p">,</span><span class="w">
            </span><span class="nl">"headers"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
              </span><span class="nl">"Authorization"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Bearer ${GITHUB_PAT}"</span><span class="w">
            </span><span class="p">}</span><span class="w">
          </span><span class="p">}</span><span class="w">
        </span><span class="p">],</span><span class="w">
        </span><span class="nl">"tools"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
          </span><span class="p">{</span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"search_repositories"</span><span class="p">,</span><span class="w"> </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Search for GitHub repositories"</span><span class="p">},</span><span class="w">
          </span><span class="p">{</span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"search_code"</span><span class="p">,</span><span class="w"> </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Search code across repositories"</span><span class="p">},</span><span class="w">
          </span><span class="p">{</span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"get_file_contents"</span><span class="p">,</span><span class="w"> </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Get contents of a file from a repository"</span><span class="p">},</span><span class="w">
          </span><span class="p">{</span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"list_issues"</span><span class="p">,</span><span class="w"> </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"List issues in a repository"</span><span class="p">},</span><span class="w">
          </span><span class="p">{</span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"get_issue"</span><span class="p">,</span><span class="w"> </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Get details of a specific issue"</span><span class="p">},</span><span class="w">
          </span><span class="p">{</span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"create_issue"</span><span class="p">,</span><span class="w"> </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Create a new issue"</span><span class="p">},</span><span class="w">
          </span><span class="p">{</span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"list_pull_requests"</span><span class="p">,</span><span class="w"> </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"List pull requests in a repository"</span><span class="p">},</span><span class="w">
          </span><span class="p">{</span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"get_pull_request"</span><span class="p">,</span><span class="w"> </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Get details of a specific pull request"</span><span class="p">},</span><span class="w">
          </span><span class="p">{</span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"create_pull_request"</span><span class="p">,</span><span class="w"> </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Create a new pull request"</span><span class="p">},</span><span class="w">
          </span><span class="p">{</span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"get_me"</span><span class="p">,</span><span class="w"> </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Get the authenticated user's profile"</span><span class="p">}</span><span class="w">
        </span><span class="p">]</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>This is exactly what we need—GitHub’s official MCP server. The <code class="language-plaintext highlighter-rouge">modules</code> field contains the MCP configuration: a remote server (<code class="language-plaintext highlighter-rouge">streamable-http</code>) requiring a GitHub Personal Access Token for authentication.</p>

<p><strong>Step 3: Configure the MCP server in your IDE</strong></p>

<p>Here’s the key insight: <strong>the AI assistant can update your IDE configuration directly</strong>. Ask it to add the MCP server:</p>

<blockquote>
  <p>“Add this GitHub MCP server to my Cursor configuration at ~/.cursor/mcp.json using the connection details from the record”</p>
</blockquote>

<p>The assistant reads the connection details from the OASF record and updates <code class="language-plaintext highlighter-rouge">~/.cursor/mcp.json</code>:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"mcpServers"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"github"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
      </span><span class="nl">"url"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://api.githubcopilot.com/mcp/"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"headers"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">"Authorization"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Bearer ${GITHUB_PAT}"</span><span class="w">
      </span><span class="p">}</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>You’ll need a GitHub PAT with appropriate scopes (repo, read:user). Generate one at <a href="https://github.com/settings/tokens">GitHub Settings &gt; Developer settings &gt; Personal access tokens</a>.</p>

<p><strong>Step 4: Start using the new capability</strong></p>

<p>After restarting Cursor, you have full GitHub access. Let’s try it:</p>

<blockquote>
  <p>“List all open GitHub issues assigned to me across all repositories”</p>
</blockquote>

<p>The assistant uses the <code class="language-plaintext highlighter-rouge">search_issues</code> tool:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"total_count"</span><span class="p">:</span><span class="w"> </span><span class="mi">5</span><span class="p">,</span><span class="w">
  </span><span class="nl">"items"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"number"</span><span class="p">:</span><span class="w"> </span><span class="mi">863</span><span class="p">,</span><span class="w">
      </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Name resolution + verified record name on remote nodes"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"repository"</span><span class="p">:</span><span class="w"> </span><span class="s2">"agntcy/dir"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"state"</span><span class="p">:</span><span class="w"> </span><span class="s2">"open"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"labels"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"go"</span><span class="p">],</span><span class="w">
      </span><span class="nl">"milestone"</span><span class="p">:</span><span class="w"> </span><span class="s2">"DIR v1.1.0"</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"number"</span><span class="p">:</span><span class="w"> </span><span class="mi">39</span><span class="p">,</span><span class="w">
      </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Blog: How to use AGNTCY MCP server"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"repository"</span><span class="p">:</span><span class="w"> </span><span class="s2">"agntcy/blogs"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"state"</span><span class="p">:</span><span class="w"> </span><span class="s2">"open"</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"number"</span><span class="p">:</span><span class="w"> </span><span class="mi">24</span><span class="p">,</span><span class="w">
      </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Gather requirements for Migration SDK"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"repository"</span><span class="p">:</span><span class="w"> </span><span class="s2">"agntcy/oasf-sdk"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"state"</span><span class="p">:</span><span class="w"> </span><span class="s2">"open"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"labels"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"enhancement"</span><span class="p">]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="err">...</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<blockquote>
  <p>“List the 5 most recent open issues in the agntcy/dir repository”</p>
</blockquote>

<p>The assistant uses the <code class="language-plaintext highlighter-rouge">list_issues</code> tool:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"issues"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"number"</span><span class="p">:</span><span class="w"> </span><span class="mi">942</span><span class="p">,</span><span class="w">
      </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"[Feature]: Resolve and improve test coverage"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"state"</span><span class="p">:</span><span class="w"> </span><span class="s2">"OPEN"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"labels"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"kind/test"</span><span class="p">],</span><span class="w">
      </span><span class="nl">"created_at"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2026-02-12T13:59:57Z"</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"number"</span><span class="p">:</span><span class="w"> </span><span class="mi">929</span><span class="p">,</span><span class="w">
      </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"[Feature]: Remove sync monitor in favor of reconciler indexer task"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"state"</span><span class="p">:</span><span class="w"> </span><span class="s2">"OPEN"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"created_at"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2026-02-10T15:59:58Z"</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"number"</span><span class="p">:</span><span class="w"> </span><span class="mi">926</span><span class="p">,</span><span class="w">
      </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Revisit local deployment configuration"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"state"</span><span class="p">:</span><span class="w"> </span><span class="s2">"OPEN"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"created_at"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2026-02-10T11:56:23Z"</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="err">...</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"totalCount"</span><span class="p">:</span><span class="w"> </span><span class="mi">29</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<blockquote>
  <p>“Get the full details for issue #942 in agntcy/dir including the description and comments”</p>
</blockquote>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"number"</span><span class="p">:</span><span class="w"> </span><span class="mi">942</span><span class="p">,</span><span class="w">
  </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"[Feature]: Resolve and improve test coverage"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"state"</span><span class="p">:</span><span class="w"> </span><span class="s2">"open"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"author"</span><span class="p">:</span><span class="w"> </span><span class="s2">"ramizpolic"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"labels"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"kind/test"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"milestone"</span><span class="p">:</span><span class="w"> </span><span class="s2">"DIR v1.2.0"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"created_at"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2026-02-12T13:59:57Z"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"body"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Currently, the test coverage results are not properly covering certain components (e.g. for apiserver, we are dropping the unit test coverage for the sake of e2e test coverage). Proposed solution: Make sure that all test results are properly combined/merged across different test-cases (unit, e2e, ...)."</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>The assistant provides context about the work needed—all without leaving your IDE.</p>

<p>This is the power of Directory: discoverable agents with standardized metadata that the AI assistant can use to <strong>automatically configure and start using new capabilities</strong>.</p>

<h3 id="scenario-3-auditing-agent-provenance-itops">Scenario 3: Auditing Agent Provenance (ITOps)</h3>

<p>As an IT operations or security team member, you need to audit the agents deployed in your environment to ensure they’re from trusted sources and properly signed. Directory’s verification capabilities make this straightforward.</p>

<p><strong>Step 1: List all agents in your environment</strong></p>

<blockquote>
  <p>“Search Directory for all agent records and list their CIDs”</p>
</blockquote>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"count"</span><span class="p">:</span><span class="w"> </span><span class="mi">9</span><span class="p">,</span><span class="w">
  </span><span class="nl">"has_more"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w">
  </span><span class="nl">"record_cids"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="s2">"baeareifmeckgczj35wcy6fpdhwttdbkjkskbcktarufpgfvc4xcmhjc2k4"</span><span class="p">,</span><span class="w">
    </span><span class="s2">"baeareiarov6l6hcb274t5gxkwujbmcizi4ba5ullqoeyqrhlqc6ymz6fsm"</span><span class="p">,</span><span class="w">
    </span><span class="s2">"baeareigdnp76ayuix5467b7ixjrwwfio2dhnusqxpafidie2py2yw7dwla"</span><span class="p">,</span><span class="w">
    </span><span class="s2">"baeareihg2hdnkfhdtkermrw7fohbgjffwx3c6jn3zqy4vwsbrru5vyj4tm"</span><span class="p">,</span><span class="w">
    </span><span class="s2">"..."</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p><strong>Step 2: Verify signatures for each agent</strong></p>

<blockquote>
  <p>“For each of these CIDs, verify the signature and report which agents are signed vs unsigned”</p>
</blockquote>

<p>The assistant checks each record:</p>

<table>
  <thead>
    <tr>
      <th>CID</th>
      <th>Agent Name</th>
      <th>Status</th>
      <th>Signer</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">baeareifmeck...</code></td>
      <td>coffee-exchange-agent</td>
      <td>Signed</td>
      <td><code class="language-plaintext highlighter-rouge">agntcy/coffeeAgntcy/.github/workflows/publish-agents.yaml@main</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">baeareicotuif...</code></td>
      <td>coffee-farm-agent</td>
      <td>Signed</td>
      <td><code class="language-plaintext highlighter-rouge">agntcy/coffeeAgntcy/.github/workflows/publish-agents.yaml@main</code></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">baeareigdnp7...</code></td>
      <td>fetch-mcp-server</td>
      <td><strong>Not signed</strong></td>
      <td>-</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">baeareihg2hd...</code></td>
      <td>memory-mcp-server</td>
      <td><strong>Not signed</strong></td>
      <td>-</td>
    </tr>
  </tbody>
</table>

<p><strong>Step 3: Investigate unsigned agents</strong></p>

<p>The audit reveals that <code class="language-plaintext highlighter-rouge">fetch-mcp-server</code> and <code class="language-plaintext highlighter-rouge">memory-mcp-server</code> are not signed. Let’s get more details:</p>

<blockquote>
  <p>“Pull record baeareigdnp76ayuix5467b7ixjrwwfio2dhnusqxpafidie2py2yw7dwla and show its authors and locators”</p>
</blockquote>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"fetch-mcp-server"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"version"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2025.3.28"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"authors"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"Anthropic"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"created_at"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2026-02-17T10:00:00Z"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"locators"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"source_code"</span><span class="p">,</span><span class="w"> </span><span class="nl">"urls"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"https://github.com/modelcontextprotocol/servers/tree/main/src/fetch"</span><span class="p">]}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>The record claims to be from Anthropic but has no cryptographic proof. This is a potential security risk—anyone could have pushed this record claiming to be from that organization.</p>

<p><strong>Step 4: Compare with signed records</strong></p>

<p>For comparison, let’s look at a properly signed record:</p>

<blockquote>
  <p>“Verify the signature for coffee-exchange-agent (CID baeareifmeck…) and show full signer details”</p>
</blockquote>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"success"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
  </span><span class="nl">"message"</span><span class="p">:</span><span class="w"> </span><span class="s2">"trusted"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"signers"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"identity"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://github.com/agntcy/coffeeAgntcy/.github/workflows/publish-agents.yaml@refs/heads/main"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"issuer"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://token.actions.githubusercontent.com"</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>This signature confirms:</p>
<ul>
  <li><strong>Identity</strong>: Signed by the <code class="language-plaintext highlighter-rouge">agntcy/coffeeAgntcy</code> CI/CD workflow</li>
  <li><strong>Issuer</strong>: GitHub Actions OIDC provider</li>
  <li><strong>Integrity</strong>: The record hasn’t been modified since signing</li>
</ul>

<p><strong>Audit recommendations</strong></p>

<p>Based on this audit, you might:</p>

<ol>
  <li><strong>Flag unsigned agents</strong> for review before use in production</li>
  <li><strong>Require signatures</strong> from specific identities (e.g., <code class="language-plaintext highlighter-rouge">@yourcompany.com</code> emails or <code class="language-plaintext highlighter-rouge">https://github.com/yourorg/...</code> workflows)</li>
  <li><strong>Set up alerts</strong> for new unsigned records appearing in your environment</li>
  <li><strong>Establish a policy</strong> that all production agents must be signed by CI/CD workflows (not individual developers)</li>
</ol>

<p>This provenance tracking is what makes Directory suitable for enterprise environments—you always know where an agent came from and whether you can trust it.</p>

<h2 id="video-walkthrough">Video Walkthrough</h2>

<p>If you prefer a visual demonstration, check out this video where I walk through the Directory MCP server in action:</p>

<div style="position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden; max-width: 100%; margin-bottom: 1.5em;">
  <iframe src="https://www.youtube.com/embed/pdVqhlyJHo4" style="position: absolute; top: 0; left: 0; width: 100%; height: 100%;" frameborder="0" allowfullscreen=""></iframe>
</div>

<h2 id="the-bigger-picture">The Bigger Picture</h2>

<p>The Directory MCP server is part of a broader vision for AI agent interoperability. By standardizing how agents describe their capabilities (OASF), how they’re discovered (Directory), and how they communicate (A2A), we’re building infrastructure for an ecosystem where:</p>

<ul>
  <li>Agents can find and collaborate with other agents</li>
  <li>Developers can discover pre-built agents for their needs</li>
  <li>Organizations can distribute and share agents securely</li>
</ul>

<p>The MCP server brings this infrastructure directly into developer workflows, reducing friction and enabling AI-assisted agent development.</p>

<p>Check out the <a href="https://docs.agntcy.org/dir/overview/">Directory documentation</a> for a complete walkthrough, and the <a href="https://github.com/agntcy/dir/tree/main/mcp">MCP server README</a> for detailed configuration options.</p>

<h2 id="references">References</h2>

<ul>
  <li><a href="https://github.com/agntcy/dir">Directory GitHub Repository</a></li>
  <li><a href="https://github.com/agntcy/dir/tree/main/mcp">Directory MCP Server README</a></li>
  <li><a href="https://docs.agntcy.org/dir/overview/">Directory Documentation</a></li>
  <li><a href="https://github.com/agntcy/oasf">OASF (Open Agentic Schema Framework)</a></li>
  <li><a href="https://modelcontextprotocol.io/">Model Context Protocol</a></li>
  <li><a href="https://github.com/modelcontextprotocol/go-sdk">MCP Go SDK</a></li>
</ul>

<hr />

<p><em>Have questions about Directory or the MCP server? Join our <a href="https://join.slack.com/t/agntcy/shared_invite/zt-3hb4p7bo0-5H2otGjxGt9OQ1g5jzK_GQ">Slack community</a> or check out our <a href="https://github.com/agntcy">GitHub</a>.</em></p>]]></content><author><name>Adam Tagscherer</name></author><category term="technical" /><category term="mcp" /><category term="directory" /><category term="oasf" /><category term="ai-agents" /><category term="ide-integration" /><summary type="html"><![CDATA[The Model Context Protocol (MCP) has emerged as a powerful standard for connecting AI assistants with external tools and data sources. In this post, we’ll explore the Directory MCP server—a feature that brings AI agent discovery and management directly into your IDE, enabling seamless interaction with the Directory ecosystem.]]></summary></entry><entry><title type="html">Agent Directory v1.0: Distributed Announce and Discovery of Multi-Agentic-Systems</title><link href="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/02/19/dir-v1.html" rel="alternate" type="text/html" title="Agent Directory v1.0: Distributed Announce and Discovery of Multi-Agentic-Systems" /><published>2026-02-19T07:00:00+00:00</published><updated>2026-02-19T07:00:00+00:00</updated><id>https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/02/19/dir-v1</id><content type="html" xml:base="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/02/19/dir-v1.html"><![CDATA[<p>As AI systems evolve from isolated models into interconnected networks of specialized agents, several challenges emerge: How do agents find each other? How does an AI application discover which agents can perform specific actions driven by the current context or task requirements? How do developers publish their agents so others can find and use them?</p>

<p>Traditional service discovery systems weren’t designed for the agentic AI ecosystem. They lack semantic understanding of agent capabilities, don’t support capability-based discovery, and can’t handle the rich metadata that describes what agents can do. Developer platforms often resort to centralized marketplaces or proprietary directories, fragmenting the ecosystem and limiting agent reusability.</p>

<h2 id="the-problem-agent-discovery-at-scale">The Problem: Agent Discovery at Scale</h2>

<p>Modern AI applications increasingly rely on specialized agents working together:</p>

<ul>
  <li><strong>Multi-agent Workflows</strong> where different agents handle specific tasks (data processing, analysis, synthesis)</li>
  <li><strong>Agent Marketplaces</strong> where developers publish agents for others to discover and use</li>
  <li><strong>Dynamic Agent Coordination</strong> where systems discover and invoke agents based on current requirements</li>
  <li><strong>Federated AI Services</strong> spanning organizations, platforms, and deployment environments</li>
</ul>

<p>These scenarios require sophisticated discovery capabilities:</p>

<ul>
  <li><strong>Capability-based Search</strong> to find agents by what they <em>can do</em>, not just by name or identifier</li>
  <li><strong>Semantic Discovery</strong> to match agent capabilities to requirements using hierarchical taxonomies</li>
  <li><strong>Verifiable Metadata</strong> to trust that agent descriptions are authentic and haven’t been tampered with</li>
  <li><strong>Distributed Architecture</strong> to enable discovery across organizational boundaries without centralized control</li>
  <li><strong>Protocol Integrations</strong> to support various agent protocols (A2A, MCP) while maintaining semantic consistency</li>
  <li><strong>Developer Experience</strong> to make it simple to publish and discover agents programmatically</li>
</ul>

<p>Existing solutions fall short. DNS-based service discovery provides naming but lacks semantic understanding. Cloud-native service meshes focus on network routing, not capability matching. Container registries store artifacts but don’t organize by functional attributes. Centralized marketplaces work within single platforms but don’t enable cross-platform discovery.</p>

<p>The <a href="https://docs.agntcy.org/dir/overview/">Agent Directory</a> was built to solve exactly these challenges, providing a distributed, capability-aware directory service purpose-built for the agentic AI ecosystem. Agents announce their capabilities through structured metadata, while consumers discover them through rich capability-based queries. Whether you’re building a multi-agent AI system, an agent marketplace, or a platform that coordinates specialized AI services, Agent Directory provides the discovery infrastructure you need.</p>

<p>Agent Directory is a secure, scalable, decentralized service that holds agent records. The software allows users to publish and discover agent records across the network of decentralized, federated directories. Agent Directory provides a cryptographic trust model that ensures authenticity and provenance of each record. It is available as open-source software and can be found on <a href="https://github.com/agntcy/dir">GitHub</a>.</p>

<h2 id="what-is-agent-directory">What is Agent Directory?</h2>

<p>Agent Directory is a distributed directory service that enables agents to announce their capabilities and allows others to discover them through capability-based queries. Built on proven distributed systems principles, Directory uses content-addressing for global uniqueness, hierarchical taxonomies for semantic organization, and distributed hash tables (DHT) for scalable discovery across decentralized networks.</p>

<p>At its core, Agent Directory combines:</p>

<ul>
  <li><strong>Capability-based Discovery</strong> - Agents publish structured metadata describing their functional characteristics</li>
  <li><strong>Hierarchical Taxonomies</strong> - Capabilities are organized through semantic taxonomies for efficient matching</li>
  <li><strong>Content-addressed Naming</strong> - Cryptographic hashes (CIDs) ensure global uniqueness and data integrity</li>
  <li><strong>Distributed Architecture</strong> - DHT-based routing enables scalable discovery without centralized control</li>
  <li><strong>Verifiable Claims</strong> - Cryptographic signing provides data integrity and provenance tracking</li>
  <li><strong>OCI Integration</strong> - Standard OCI distribution protocol for interoperable storage and retrieval</li>
  <li><strong>Multi-Protocol Support</strong> - Native integration with A2A, MCP, and custom agent protocols</li>
</ul>

<p>This approach enables developers to publish agents once and make them discoverable across the entire ecosystem, while consumers can find the right agents for their needs through requirement- and capability-based queries rather than simple name lookups.</p>

<h3 id="capability-based-discovery">Capability-Based Discovery</h3>

<p>Agent Directory leverages the <a href="https://docs.agntcy.org/oasf/open-agentic-schema-framework/">Open Agentic Schema Framework (OASF)</a> to provide standardized, capability-based agent discovery. OASF defines a structured schema for describing AI agents through:</p>

<ul>
  <li><strong>Skills Taxonomy</strong> - Hierarchical organization of agent capabilities, from high-level categories like “natural language processing” down to specific skills like “sentiment analysis” or “named entity recognition”.</li>
  <li><strong>Domain Classification</strong> - Agents can be categorized by application domains such as “healthcare”, “finance”, or “manufacturing”, enabling domain-specific discovery.</li>
  <li><strong>Modular Extensions</strong> - OASF records support modular extensions for protocol-specific metadata (A2A cards, MCP server configurations, GitHub Copilot integrations), allowing agents to work seamlessly across different agent frameworks.</li>
</ul>

<p>The Directory indexes records by their skills and domains, enabling natural capability-based queries like:</p>

<ul>
  <li>“Find agents with image analysis capabilities”</li>
  <li>“Discover agents that can perform sentiment analysis”</li>
  <li>“Show me all agents in the healthcare domain with natural language processing skills”</li>
</ul>

<p>This semantic approach to discovery is far more powerful than simple name-based lookups, enabling developers to find the right agents based on what they need them to <em>do</em>.</p>

<h3 id="content-addressed-naming">Content-Addressed Naming</h3>

<p>Agent Directory uses <a href="https://github.com/multiformats/cid">Content Identifiers (CIDs)</a> for naming directory records. CIDs provide a self-describing, content-addressed naming scheme that ensures:</p>

<ul>
  <li><strong>Global Uniqueness</strong> - CIDs are cryptographic hashes of the record content, making them globally unique without requiring a central authority to assign identifiers.</li>
  <li><strong>Immutability</strong> - Any change to a record produces a different CID, providing a natural versioning mechanism and tamper detection.</li>
  <li><strong>Self-Verification</strong> - Anyone can verify that the content matches its CID by recomputing the hash, enabling trustless verification.</li>
</ul>

<p>In addition to CID-based addressing, Agent Directory supports <strong>verifiable domain-based names</strong> that enable human-readable references while maintaining cryptographic verification. For example, <code class="language-plaintext highlighter-rouge">example.org/my-agent</code> can be mapped to a CID while preserving the trust model through DNS-based verification.</p>

<p>This dual naming approach provides both the usability of human-readable names and the security guarantees of content-addressed identifiers.</p>

<h3 id="distributed-dht-based-routing">Distributed DHT-Based Routing</h3>

<p>Agent Directory uses <a href="https://github.com/libp2p/specs/tree/master/kad-dht">libp2p Kad-DHT</a> for both server and content discovery, providing a proven, scalable foundation for distributed routing. This architecture enables:</p>
<ul>
  <li><strong>Decentralization</strong> - No single point of failure or central authority</li>
  <li><strong>Fault Tolerance</strong> - The DHT can handle node churn and network partitions gracefully</li>
  <li><strong>Scalability</strong> - DHT-based routing scales to thousands of nodes and millions of records</li>
  <li><strong>Resilience</strong> - Multiple nodes can host the same records, providing redundancy</li>
  <li><strong>Efficient Lookup</strong> - Logarithmic search complexity for fast discovery even at scale</li>
</ul>

<p>Agent Directory implements a two-phase discovery process powered by distributed hash tables (DHT):</p>

<p><strong>1. Capability-to-Record Mapping</strong></p>

<p>When an agent publishes its capabilities, Agent Directory performs the following steps:</p>
<ul>
  <li>Extract taxonomy data from the OASF record</li>
  <li>Announces the record’s CID associated with those taxonomies to the DHT</li>
  <li>Updates routing tables that map taxonomies to record identifiers</li>
</ul>

<p><strong>2. Record-to-Server Mapping</strong></p>

<p>Agent Directory tracks which directory nodes store which records:</p>
<ul>
  <li>When a node stores a record, it announces its availability to the DHT</li>
  <li>The DHT maintains mappings from CIDs to server addresses</li>
  <li>Query resolution identifies optimal data sources</li>
</ul>

<p>Discovery queries execute this two-phase process:</p>

<pre><code class="language-mermaid">flowchart LR
    subgraph Query["Discovery Query"]
        Q["Query: agents with NLP skills"]
    end
    
    subgraph Phase1["Phase 1: Capability → CIDs"]
        DHT1[("DHT&lt;br/&gt;Capability Index")]
        CIDS["Matching CIDs:&lt;br/&gt;• bafkrei...abc&lt;br/&gt;• bafkrei...def&lt;br/&gt;• bafkrei...ghi"]
    end
    
    subgraph Phase2["Phase 2: CIDs → Servers"]
        DHT2[("DHT&lt;br/&gt;Provider Index")]
        MAPPING["Server → CID Mapping"]
    end
    
    subgraph Result["Result: Server → CIDs"]
        R1["dir-node-1.example.org&lt;br/&gt;  └─ bafkrei...abc&lt;br/&gt;  └─ bafkrei...def"]
        R2["dir-node-2.example.org&lt;br/&gt;  └─ bafkrei...ghi"]
    end
    
    Q --&gt;|"skill: NLP"| DHT1
    DHT1 --&gt;|"resolve taxonomy"| CIDS
    CIDS --&gt;|"lookup providers"| DHT2
    DHT2 --&gt;|"find hosting nodes"| MAPPING
    MAPPING --&gt; R1
    MAPPING --&gt; R2
    
    style Query fill:transparent,stroke:#6b9fc8,stroke-width:2px
    style Phase1 fill:transparent,stroke:#d4a054,stroke-width:2px
    style Phase2 fill:transparent,stroke:#5ba67a,stroke-width:2px
    style Result fill:transparent,stroke:#9a7bb0,stroke-width:2px
    style Q fill:#6b9fc8,stroke:#4a7a9c,stroke-width:2px
    style DHT1 fill:#d4a054,stroke:#b08540,stroke-width:2px
    style CIDS fill:#d4a054,stroke:#b08540,stroke-width:2px
    style DHT2 fill:#5ba67a,stroke:#478a60,stroke-width:2px
    style MAPPING fill:#5ba67a,stroke:#478a60,stroke-width:2px
    style R1 fill:#9a7bb0,stroke:#7a5f8c,stroke-width:2px
    style R2 fill:#9a7bb0,stroke:#7a5f8c,stroke-width:2px
</code></pre>

<p>The final result is a mapping of servers to their hosted CIDs, enabling the client to download records from optimal sources.</p>

<h3 id="security-and-trust">Security and Trust</h3>

<p>In distributed agentic systems, trust is paramount. Agent Directory provides a comprehensive security model that combines content integrity and content provenance via the publisher’s identity to enable secure agent discovery across organizational boundaries.</p>

<h4 id="content-integrity-and-provenance">Content Integrity and Provenance</h4>

<p>All records are identified by their CID, enabling anyone to verify that content hasn’t been tampered with by recomputing the hash. Records can be signed using OIDC flows or private keys, proving authorship and enabling trust decisions based on publisher reputation.</p>

<p>Agent Directory supports local trust chain verification, allowing organizations to:</p>
<ul>
  <li>Establish their own trust anchors</li>
  <li>Define verification policies for external records</li>
  <li>Validate complete provenance chains from publisher to record</li>
  <li>Make informed decisions about agent selection based on trust policies specific to their use case</li>
</ul>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Verify a record's signature and trust chain</span>
dirctl verify bafkreiabcd1234567890

<span class="c"># Output includes verification status, signer identity, and trust chain details</span>
</code></pre></div></div>

<p>This multi-layered security approach combines content-addressed integrity with cryptographic signing and trust chain verification, enabling secure discovery in a decentralized ecosystem without relying on centralized authority.</p>

<h4 id="zero-trust-security">Zero-Trust Security</h4>

<p>Agent Directory leverages <a href="https://spiffe.io/">SPIFFE (Secure Production Identity Framework For Everyone)</a> and its runtime implementation <a href="https://spiffe.io/docs/latest/spire-about/">SPIRE</a> to provide zero-trust security for the federation of different agent directory nodes. This enables:</p>

<ul>
  <li><strong>Workload Identity</strong> - Every Directory component receives a cryptographically verifiable identity in the form of a SPIFFE ID like <code class="language-plaintext highlighter-rouge">spiffe://directory.example/ns/production/sa/my-agent</code>. This identity is used for authentication and authorization across the system and network.</li>
  <li><strong>Mutual TLS (mTLS)</strong> - SPIRE issues SVIDs enabling automatic mTLS between all Directory components. Communication is encrypted and both parties are authenticated without manual certificate management.</li>
  <li><strong>Cross-Organization Federation</strong> - Organizations can federate their SPIRE deployments by exchanging trust bundles to form networks. This enables secure, authenticated communication between Directory nodes in different organizations without custom integration.</li>
  <li><strong>Policy-Based Access Control</strong> - SPIFFE IDs enable fine-grained authorization policies. Organizations can define which workloads can push records, which can query, and which external trust domains are authorized for federation and for which operations.</li>
</ul>

<h3 id="oci-based-distributed-storage">OCI-Based Distributed Storage</h3>

<p>Agent Directory differs from block storage systems like <a href="https://ipfs.tech/">IPFS</a> in its approach to distributed object storage, leveraging the <strong>OCI (Open Container Initiative) distribution specification</strong> for content storage and retrieval.</p>

<p><strong>Content Retrieval</strong></p>

<ul>
  <li>Agent Directory directly stores complete records rather than splitting them into blocks</li>
  <li>Records are retrieved as complete units using standard OCI protocols</li>
  <li>No complex block reassembly or multi-source optimization needed</li>
</ul>

<p><strong>Distribution Layer</strong></p>

<ul>
  <li>Records are stored and transferred using OCI artifacts</li>
  <li>Any OCI distribution-compliant server can participate in the Agent Directory network</li>
  <li>Servers retrieve records from each other using well-established OCI protocols</li>
  <li>Existing OCI infrastructure (registries, caching layers, CDNs) can be leveraged</li>
</ul>

<p><strong>Integration Support</strong></p>

<p>While Agent Directory uses <a href="https://zotregistry.dev/">zot</a> as its reference OCI store, the system works with <strong>any server that implements the OCI distribution specification</strong>. This means you can use existing container registries like Docker Registry, Harbor, or cloud-provider managed registries as Directory storage backends.</p>

<p>This OCI-based approach provides:</p>
<ul>
  <li><strong>Interoperability</strong> - Leverage existing OCI tooling and infrastructure</li>
  <li><strong>Simplicity</strong> - Standard HTTP-based protocol without custom networking requirements</li>
  <li><strong>Efficiency</strong> - Single-request record retrieval without block coordination overhead</li>
  <li><strong>Enterprise Integration</strong> - Use existing OCI registries and security policies</li>
</ul>

<h3 id="runtime-discovery-integration">Runtime Discovery Integration</h3>

<p>Agent Directory includes a <strong>runtime discovery service</strong> that automatically discovers and indexes agent workloads running in a specific environment, bridging the gap between deployed agents and directory records.
The runtime discovery system provides real-time visibility into active agents, enabling dynamic discovery and coordination in orchestrated environments like Kubernetes or Docker, but also
local environments such as developer machines or edge devices.</p>

<p>Workloads can be discovered through annotations that indicate they should be indexed by the Directory. For example, in Kubernetes, a pod can be annotated to enable discovery:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">translation-agent</span>
  <span class="na">annotations</span><span class="pi">:</span>
    <span class="na">org.agntcy/discover</span><span class="pi">:</span> <span class="s2">"</span><span class="s">true"</span> <span class="c1"># Enable discovery for this workload</span>
    <span class="na">org.agntcy/agent-type</span><span class="pi">:</span> <span class="s2">"</span><span class="s">a2a"</span> <span class="c1"># Indicate the protocol or framework used</span>
    <span class="na">org.agntcy/agent-record</span><span class="pi">:</span> <span class="s2">"</span><span class="s">my-agent/v1.0.0"</span> <span class="c1"># Indicate reference to a Directory record</span>
</code></pre></div></div>

<p>The Agent Directory runtime discovery is an optional component of the Directory stack that makes workloads queryable alongside statically published records. This enables hybrid discovery where some agents are published ahead of time while others are discovered dynamically as they’re deployed. The same discovery and security models apply to both, providing a unified experience for developers and users.</p>

<h2 id="real-world-use-cases">Real-World Use Cases</h2>

<h3 id="building-agent-marketplaces">Building Agent Marketplaces</h3>

<p>Agent Directory provides the foundation for building agent marketplaces where developers can publish agents and users can discover them:</p>

<p><strong>Publisher Workflow</strong>:</p>

<ol>
  <li>Developer creates an agent with specific capabilities</li>
  <li>Generates an OASF record describing skills, domains, and deployment details</li>
  <li>Signs the record with their identity for authenticity</li>
  <li>Publishes to the Directory which announces the CID and metadata to the DHT</li>
</ol>

<p><strong>Consumer Workflow</strong>:</p>

<ol>
  <li>User queries Agent Directory for agents with specific capabilities</li>
  <li>The Directory returns matching records with CIDs</li>
  <li>User reviews metadata, verifies signatures, and checks trust chains</li>
  <li>User pulls the complete record and retrieves deployment artifacts (Docker images, API endpoints)</li>
  <li>User deploys or invokes the agent</li>
</ol>

<p>This decentralized marketplace model enables anyone to publish and discover agents without requiring approval from a central authority or platform lock-in.</p>

<h3 id="multi-agent-system-coordination">Multi-Agent System Coordination</h3>

<p>In complex multi-agent systems, agents need to discover collaborators dynamically:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">agntcy.dir_sdk.client</span> <span class="kn">import</span> <span class="n">Config</span><span class="p">,</span> <span class="n">Client</span>
<span class="kn">from</span> <span class="nn">agntcy.dir_sdk.models</span> <span class="kn">import</span> <span class="n">search_v1</span>

<span class="c1"># Select either local/cache or remote Directory node based on context
</span><span class="n">client</span> <span class="o">=</span> <span class="n">Client</span><span class="p">(</span><span class="n">Config</span><span class="p">(</span><span class="n">server_address</span><span class="o">=</span><span class="s">"dir.example.org:8888"</span><span class="p">))</span>

<span class="c1"># Agent needs image analysis capability
# Use network or local discovery based on context
</span><span class="n">results</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">search_records</span><span class="p">(</span><span class="n">search_v1</span><span class="p">.</span><span class="n">ListRequest</span><span class="p">(</span>
  <span class="n">queries</span><span class="o">=</span><span class="p">[</span><span class="n">search_v1</span><span class="p">.</span><span class="n">RecordQuery</span><span class="p">(</span>
    <span class="nb">type</span><span class="o">=</span><span class="n">search_v1</span><span class="p">.</span><span class="n">RECORD_QUERY_TYPE_SKILL_NAME</span><span class="p">,</span>
    <span class="n">value</span><span class="o">=</span><span class="s">"images_computer_vision/object_detection"</span><span class="p">,</span>
  <span class="p">)]</span>
  <span class="n">limit</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span>
<span class="p">))</span>

<span class="c1"># Select the best candidate based on metadata
</span><span class="k">for</span> <span class="n">item</span> <span class="ow">in</span> <span class="n">results</span><span class="p">:</span>
    <span class="c1"># Verify signature, check version, evaluate endpoints
</span>    <span class="k">if</span> <span class="n">meets_requirements</span><span class="p">(</span><span class="n">item</span><span class="p">.</span><span class="n">record</span><span class="p">):</span>
        <span class="n">endpoint</span> <span class="o">=</span> <span class="n">item</span><span class="p">.</span><span class="n">record</span><span class="p">.</span><span class="n">data</span><span class="p">[</span><span class="s">'locators'</span><span class="p">][</span><span class="mi">0</span><span class="p">][</span><span class="s">'url'</span><span class="p">]</span>
        <span class="c1"># Invoke the discovered agent
</span>        <span class="n">invoke_agent</span><span class="p">(</span><span class="n">endpoint</span><span class="p">,</span> <span class="n">task_data</span><span class="p">)</span>
        <span class="k">break</span>
</code></pre></div></div>

<p>This pattern enables <strong>dynamic capability discovery</strong> where agents find and coordinate with each other based on current requirements rather than hardcoded integrations.</p>

<h3 id="federated-agent-networks">Federated Agent Networks</h3>

<p>Organizations can run their own Directory nodes while participating in a federated discovery network:</p>

<ul>
  <li><strong>Internal Directory Node</strong> - Organization runs an Agent Directory node behind their firewall, containing proprietary agents not shared externally.</li>
  <li><strong>Federation</strong> - The internal node connects to external Directory nodes via DHT, enabling discovery of public agents while keeping internal agents private.</li>
  <li><strong>Policy Enforcement</strong> - Organizations define trust policies determining which external agents can be discovered and used, based on publisher identity, signatures, and verification chains.</li>
</ul>

<p>This federated model enables both private agent catalogs and participation in the broader ecosystem, with fine-grained control over what’s shared and discovered.</p>

<h3 id="protocol-agnostic-discovery">Protocol-Agnostic Discovery</h3>

<p>Agent Directory’s OASF-based approach provides a unified discovery layer across different agent protocols:</p>

<ul>
  <li><strong>A2A Agents</strong> - Agents implementing the <a href="https://a2a-protocol.org/">Agent-to-Agent protocol</a> can be described in OASF records with A2A-specific metadata in modules.</li>
  <li><strong>MCP Servers</strong> - <a href="https://modelcontextprotocol.io/">Model Context Protocol</a> servers can be imported to OASF format, indexed by capabilities, and discovered through Agent Directory.</li>
  <li><strong>Custom Protocols</strong> - Organizations developing custom agent protocols can define public or private OASF modules for their specific metadata while leveraging Agent Directory’s discovery infrastructure.</li>
</ul>

<p>Example: A user searching for “text completion” might discover agents using A2A, MCP, or custom protocols - all through a single capability-based query. The OASF record clearly indicates which protocol each agent uses, enabling proper invocation.</p>

<h2 id="getting-started">Getting Started</h2>

<h3 id="installation">Installation</h3>

<p>Agent Directory provides a comprehensive suite of tools that make it easy to publish, discover, and work with agent records.</p>

<table>
  <thead>
    <tr>
      <th>Tool</th>
      <th>Install</th>
      <th>Prerequisite</th>
      <th>Usage</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>dirctl</td>
      <td><code class="language-plaintext highlighter-rouge">brew tap agntcy/dir https://github.com/agntcy/dir/</code><br /><code class="language-plaintext highlighter-rouge">brew install dirctl</code></td>
      <td>-</td>
      <td><a href="https://docs.agntcy.org/dir/directory-cli/">CLI Documentation</a></td>
    </tr>
    <tr>
      <td>Go SDK</td>
      <td><code class="language-plaintext highlighter-rouge">go get github.com/agntcy/dir/client</code></td>
      <td>-</td>
      <td><a href="https://docs.agntcy.org/dir/directory-sdk/#go-sdk">Go SDK Documentation</a></td>
    </tr>
    <tr>
      <td>Python SDK</td>
      <td><code class="language-plaintext highlighter-rouge">pip install agntcy-dir</code></td>
      <td>dirctl (sign/verify)</td>
      <td><a href="https://docs.agntcy.org/dir/directory-sdk/#python-sdk">Python SDK Documentation</a></td>
    </tr>
    <tr>
      <td>JS/TS SDK</td>
      <td><code class="language-plaintext highlighter-rouge">npm install agntcy-dir</code></td>
      <td>dirctl (sign/verify)</td>
      <td><a href="https://docs.agntcy.org/dir/directory-sdk/#javascript-sdk">JS/TS SDK Documentation</a></td>
    </tr>
    <tr>
      <td>MCP Server</td>
      <td><code class="language-plaintext highlighter-rouge">dirctl mcp serve</code></td>
      <td>dirctl</td>
      <td><a href="https://blogs.agntcy.org/technical/2026/02/19/directory-mcp-server.html">Directory MCP Server Blog Post</a> <br /> <a href="https://docs.agntcy.org/dir/directory-cli/#directory-mcp-server">MCP Integration Documentation</a></td>
    </tr>
    <tr>
      <td>Deployment</td>
      <td>-</td>
      <td>-</td>
      <td><a href="https://docs.agntcy.org/dir/getting-started/#deployment">Getting Started</a></td>
    </tr>
  </tbody>
</table>

<h3 id="quick-example">Quick Example</h3>

<p>Publish and discover your first agent in minutes:</p>

<ol>
  <li>Deploy an Agent Directory node locally</li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>helm pull oci://ghcr.io/agntcy/dir/helm-charts/dir <span class="nt">--version</span> v1.0.0
helm upgrade <span class="nt">--install</span> <span class="nb">dir </span>oci://ghcr.io/agntcy/dir/helm-charts/dir <span class="nt">--version</span> v1.0.0
kubectl port-forward svc/dir-apiserver 8888:8888
</code></pre></div></div>

<ol>
  <li>Download a sample agent record</li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-o</span> sample-record.json https://raw.githubusercontent.com/agntcy/dir/refs/heads/main/e2e/shared/testdata/record_100.json
</code></pre></div></div>

<ol>
  <li>Use CLI to publish and discover agents</li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Push and sign the agent record to Agent Directory</span>
dirctl push sample-record.json <span class="nt">--sign</span>

<span class="c"># Search for agents with specific capabilities</span>
dirctl search <span class="nt">--skill</span> <span class="s2">"natural_language_processing/*"</span>

<span class="c"># Pull a specific record by CID</span>
dirctl pull baeareiabbog2umgduqhlcb64fzt6adn34kblzvru3fdzkl75hjhwt6h3da

<span class="c"># Verify a record's signature and trust chain</span>
dirctl verify baeareiabbog2umgduqhlcb64fzt6adn34kblzvru3fdzkl75hjhwt6h3da
</code></pre></div></div>

<ol>
  <li>Use SDK to publish and discover agents</li>
</ol>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="nn">agntcy.dir_sdk.client</span> <span class="kn">import</span> <span class="n">Config</span><span class="p">,</span> <span class="n">Client</span>
<span class="kn">from</span> <span class="nn">agntcy.dir_sdk.models</span> <span class="kn">import</span> <span class="n">core_v1</span><span class="p">,</span> <span class="n">search_v1</span>

<span class="c1"># Connect to an Agent Directory node
</span><span class="n">client</span> <span class="o">=</span> <span class="n">Client</span><span class="p">(</span><span class="n">Config</span><span class="p">(</span><span class="n">server_address</span><span class="o">=</span><span class="s">"localhost:8888"</span><span class="p">))</span>

<span class="c1"># Push an agent record (requires signing credentials)
</span><span class="k">with</span> <span class="nb">open</span><span class="p">(</span><span class="s">"sample-record.json"</span><span class="p">,</span> <span class="s">"r"</span><span class="p">)</span> <span class="k">as</span> <span class="n">f</span><span class="p">:</span>
    <span class="n">record</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="n">load</span><span class="p">(</span><span class="n">f</span><span class="p">)</span>

<span class="n">cid</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="n">records</span><span class="o">=</span><span class="p">[</span><span class="n">core_v1</span><span class="p">.</span><span class="n">Record</span><span class="p">(</span><span class="n">data</span><span class="o">=</span><span class="n">record</span><span class="p">)])[</span><span class="mi">0</span><span class="p">]</span>
<span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"Published agent with CID: </span><span class="si">{</span><span class="n">cid</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>

<span class="c1"># Search for agents by capability
</span><span class="n">results</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">search_records</span><span class="p">(</span><span class="n">search_v1</span><span class="p">.</span><span class="n">ListRequest</span><span class="p">(</span>
    <span class="n">queries</span><span class="o">=</span><span class="p">[</span><span class="n">search_v1</span><span class="p">.</span><span class="n">RecordQuery</span><span class="p">(</span>
        <span class="nb">type</span><span class="o">=</span><span class="n">search_v1</span><span class="p">.</span><span class="n">RECORD_QUERY_TYPE_SKILL_NAME</span><span class="p">,</span>
        <span class="n">value</span><span class="o">=</span><span class="s">"natural_language_processing/*"</span><span class="p">,</span>
    <span class="p">)],</span>
    <span class="n">limit</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span>
<span class="p">))</span>

<span class="c1"># Retrieve and inspect matching agents
</span><span class="k">for</span> <span class="n">item</span> <span class="ow">in</span> <span class="n">results</span><span class="p">:</span>
    <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"Found: </span><span class="si">{</span><span class="n">item</span><span class="p">.</span><span class="n">record</span><span class="p">.</span><span class="n">name</span><span class="si">}</span><span class="s"> (CID: </span><span class="si">{</span><span class="n">item</span><span class="p">.</span><span class="n">cid</span><span class="si">}</span><span class="s">)"</span><span class="p">)</span>
    <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"  Skills: </span><span class="si">{</span><span class="n">item</span><span class="p">.</span><span class="n">record</span><span class="p">.</span><span class="n">data</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="s">'skills'</span><span class="p">,</span> <span class="p">[])</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
    <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"  Locator: </span><span class="si">{</span><span class="n">item</span><span class="p">.</span><span class="n">record</span><span class="p">.</span><span class="n">data</span><span class="p">[</span><span class="s">'locators'</span><span class="p">][</span><span class="mi">0</span><span class="p">][</span><span class="s">'url'</span><span class="p">]</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
</code></pre></div></div>

<h2 id="why-agent-directory-matters">Why Agent Directory Matters</h2>

<p>As AI systems evolve from isolated models to interconnected networks of specialized agents, discovery infrastructure becomes critical. Agent Directory provides a foundation that:</p>

<ul>
  <li><strong>Enables Semantic Discovery</strong> - Find agents by what they <em>can do</em>, not just by name or endpoint.</li>
  <li><strong>Ensures Data Integrity</strong> - Content-addressed naming and cryptographic signing guarantee authenticity.</li>
  <li><strong>Scales Without Centralization</strong> - DHT-based routing eliminates single points of failure.</li>
  <li><strong>Bridges Organizational Boundaries</strong> - Federated architecture enables cross-organization discovery with fine-grained trust policies.</li>
  <li><strong>Supports Protocol Diversity</strong> - Native integration with A2A, MCP, and custom agent protocols through OASF extensions.</li>
  <li><strong>Simplifies Development</strong> - Publish once, discover everywhere with intuitive CLI and SDK tooling.</li>
</ul>

<p>Whether you’re building a multi-agent AI system, an agent marketplace, or a platform that coordinates specialized AI services, Agent Directory provides the discovery infrastructure you need.</p>

<h2 id="the-road-ahead">The Road Ahead</h2>

<p>Agent Directory is actively developed as an open-source project under the Apache 2.0 License. The project has reached v1.0 with production-ready capabilities.</p>

<p><strong>Current Capabilities:</strong></p>

<ul>
  <li>OASF-based capability discovery with hierarchical taxonomies</li>
  <li>DHT-based distributed routing for scalable discovery</li>
  <li>Cryptographic signing with OIDC and private key support</li>
  <li>OCI distribution protocol for interoperable storage</li>
  <li>Data synchronization between Directory nodes</li>
  <li>Runtime discovery for Kubernetes and Docker workloads</li>
  <li>CLI, Go SDK, Python SDK, and JavaScript SDK</li>
  <li>MCP server integration for AI assistants</li>
</ul>

<p><strong>Under Active Development:</strong></p>

<ul>
  <li>Enhanced DHT performance and routing optimizations</li>
  <li>Extended SPIFFE/SPIRE integration for enterprise deployments</li>
  <li>Advanced trust chain verification policies</li>
  <li>Additional runtime discovery targets (edge devices, serverless)</li>
  <li>Deeper agentic protocol integrations</li>
  <li>Cross-network federation protocols</li>
</ul>

<h2 id="get-involved">Get Involved</h2>

<h3 id="public-instance">Public Instance</h3>

<p>A public Agent Directory instance is available for testing and experimentation at <code class="language-plaintext highlighter-rouge">prod.gateway.ads.outshift.io:443</code> in read-only mode.
This instance is open for anyone that is part of <a href="https://github.com/agntcy">github.com/agntcy</a> organization, making it a great place to get hands-on experience with Agent Directory.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">### Configuration</span>
<span class="c"># Set environment variables to connect to the public Directory instance</span>
<span class="nb">export </span><span class="nv">DIRECTORY_CLIENT_SERVER_ADDRESS</span><span class="o">=</span><span class="s2">"prod.gateway.ads.outshift.io:443"</span>
<span class="nb">export </span><span class="nv">DIRECTORY_CLIENT_AUTH_MODE</span><span class="o">=</span><span class="s2">"github"</span>

<span class="c">### Interactive Authentication with GitHub OAuth</span>
dirctl auth login 

<span class="c">### Non-Interactive Authentication with GitHub Token</span>
<span class="c"># The token must have `user:email` and `read:org` scope for authentication to work</span>
<span class="nb">export </span><span class="nv">DIRECTORY_CLIENT_GITHUB_TOKEN</span><span class="o">=</span><span class="s2">"ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"</span>

<span class="c">### Usage</span>
<span class="c"># Now you can use dirctl or the SDK to query the Agent Directory instance</span>
dirctl search <span class="nt">--skill</span> <span class="s2">"*"</span>
</code></pre></div></div>

<p>Check the <a href="https://blogs.agntcy.org/security/authentication/directory/2026/01/22/directory-authentication-dual-mode-security.html">Directory Testbed Authentication Modes</a> blog for detailed guides on integration
and authentication with the public Agent Directory instance.
If you’re interested in publishing your own agents, running a full Directory node, or joining the public Directory network, check <a href="https://github.com/agntcy/dir-staging">github.com/agntcy/dir-staging</a> on how to get started.</p>

<h3 id="community">Community</h3>

<p>Agent Directory is open source and welcomes contributions:</p>

<ul>
  <li><a href="https://github.com/agntcy/dir">GitHub</a></li>
  <li><a href="https://docs.agntcy.org/dir/overview/">Documentation</a></li>
  <li><a href="https://spec.dir.agntcy.org/">Specification</a></li>
</ul>

<p>Join the community building the discovery layer for agentic AI. 
Try Agent Directory in your next project, contribute to the codebase, or share your use cases.</p>

<hr />

<p><em>Agent Directory is developed by AGNTCY Contributors and released under the Apache 2.0 License.</em></p>]]></content><author><name>Ramiz Polic</name></author><category term="technical" /><category term="dir" /><category term="grpc" /><category term="distributed-systems" /><category term="announce" /><category term="discovery" /><category term="security" /><category term="ai" /><summary type="html"><![CDATA[As AI systems evolve from isolated models into interconnected networks of specialized agents, several challenges emerge: How do agents find each other? How does an AI application discover which agents can perform specific actions driven by the current context or task requirements? How do developers publish their agents so others can find and use them? Traditional service discovery systems weren’t designed for the agentic AI ecosystem. They lack semantic understanding of agent capabilities, don’t support capability-based discovery, and can’t handle the rich metadata that describes what agents can do. Developer platforms often resort to centralized marketplaces or proprietary directories, fragmenting the ecosystem and limiting agent reusability. The Problem: Agent Discovery at Scale Modern AI applications increasingly rely on specialized agents working together: Multi-agent Workflows where different agents handle specific tasks (data processing, analysis, synthesis) Agent Marketplaces where developers publish agents for others to discover and use Dynamic Agent Coordination where systems discover and invoke agents based on current requirements Federated AI Services spanning organizations, platforms, and deployment environments These scenarios require sophisticated discovery capabilities: Capability-based Search to find agents by what they can do, not just by name or identifier Semantic Discovery to match agent capabilities to requirements using hierarchical taxonomies Verifiable Metadata to trust that agent descriptions are authentic and haven’t been tampered with Distributed Architecture to enable discovery across organizational boundaries without centralized control Protocol Integrations to support various agent protocols (A2A, MCP) while maintaining semantic consistency Developer Experience to make it simple to publish and discover agents programmatically Existing solutions fall short. DNS-based service discovery provides naming but lacks semantic understanding. Cloud-native service meshes focus on network routing, not capability matching. Container registries store artifacts but don’t organize by functional attributes. Centralized marketplaces work within single platforms but don’t enable cross-platform discovery. The Agent Directory was built to solve exactly these challenges, providing a distributed, capability-aware directory service purpose-built for the agentic AI ecosystem. Agents announce their capabilities through structured metadata, while consumers discover them through rich capability-based queries. Whether you’re building a multi-agent AI system, an agent marketplace, or a platform that coordinates specialized AI services, Agent Directory provides the discovery infrastructure you need. Agent Directory is a secure, scalable, decentralized service that holds agent records. The software allows users to publish and discover agent records across the network of decentralized, federated directories. Agent Directory provides a cryptographic trust model that ensures authenticity and provenance of each record. It is available as open-source software and can be found on GitHub. What is Agent Directory? Agent Directory is a distributed directory service that enables agents to announce their capabilities and allows others to discover them through capability-based queries. Built on proven distributed systems principles, Directory uses content-addressing for global uniqueness, hierarchical taxonomies for semantic organization, and distributed hash tables (DHT) for scalable discovery across decentralized networks. At its core, Agent Directory combines: Capability-based Discovery - Agents publish structured metadata describing their functional characteristics Hierarchical Taxonomies - Capabilities are organized through semantic taxonomies for efficient matching Content-addressed Naming - Cryptographic hashes (CIDs) ensure global uniqueness and data integrity Distributed Architecture - DHT-based routing enables scalable discovery without centralized control Verifiable Claims - Cryptographic signing provides data integrity and provenance tracking OCI Integration - Standard OCI distribution protocol for interoperable storage and retrieval Multi-Protocol Support - Native integration with A2A, MCP, and custom agent protocols This approach enables developers to publish agents once and make them discoverable across the entire ecosystem, while consumers can find the right agents for their needs through requirement- and capability-based queries rather than simple name lookups. Capability-Based Discovery Agent Directory leverages the Open Agentic Schema Framework (OASF) to provide standardized, capability-based agent discovery. OASF defines a structured schema for describing AI agents through: Skills Taxonomy - Hierarchical organization of agent capabilities, from high-level categories like “natural language processing” down to specific skills like “sentiment analysis” or “named entity recognition”. Domain Classification - Agents can be categorized by application domains such as “healthcare”, “finance”, or “manufacturing”, enabling domain-specific discovery. Modular Extensions - OASF records support modular extensions for protocol-specific metadata (A2A cards, MCP server configurations, GitHub Copilot integrations), allowing agents to work seamlessly across different agent frameworks. The Directory indexes records by their skills and domains, enabling natural capability-based queries like: “Find agents with image analysis capabilities” “Discover agents that can perform sentiment analysis” “Show me all agents in the healthcare domain with natural language processing skills” This semantic approach to discovery is far more powerful than simple name-based lookups, enabling developers to find the right agents based on what they need them to do. Content-Addressed Naming Agent Directory uses Content Identifiers (CIDs) for naming directory records. CIDs provide a self-describing, content-addressed naming scheme that ensures: Global Uniqueness - CIDs are cryptographic hashes of the record content, making them globally unique without requiring a central authority to assign identifiers. Immutability - Any change to a record produces a different CID, providing a natural versioning mechanism and tamper detection. Self-Verification - Anyone can verify that the content matches its CID by recomputing the hash, enabling trustless verification. In addition to CID-based addressing, Agent Directory supports verifiable domain-based names that enable human-readable references while maintaining cryptographic verification. For example, example.org/my-agent can be mapped to a CID while preserving the trust model through DNS-based verification. This dual naming approach provides both the usability of human-readable names and the security guarantees of content-addressed identifiers. Distributed DHT-Based Routing Agent Directory uses libp2p Kad-DHT for both server and content discovery, providing a proven, scalable foundation for distributed routing. This architecture enables: Decentralization - No single point of failure or central authority Fault Tolerance - The DHT can handle node churn and network partitions gracefully Scalability - DHT-based routing scales to thousands of nodes and millions of records Resilience - Multiple nodes can host the same records, providing redundancy Efficient Lookup - Logarithmic search complexity for fast discovery even at scale Agent Directory implements a two-phase discovery process powered by distributed hash tables (DHT): 1. Capability-to-Record Mapping When an agent publishes its capabilities, Agent Directory performs the following steps: Extract taxonomy data from the OASF record Announces the record’s CID associated with those taxonomies to the DHT Updates routing tables that map taxonomies to record identifiers 2. Record-to-Server Mapping Agent Directory tracks which directory nodes store which records: When a node stores a record, it announces its availability to the DHT The DHT maintains mappings from CIDs to server addresses Query resolution identifies optimal data sources Discovery queries execute this two-phase process: flowchart LR subgraph Query["Discovery Query"] Q["Query: agents with NLP skills"] end subgraph Phase1["Phase 1: Capability → CIDs"] DHT1[("DHT&lt;br/&gt;Capability Index")] CIDS["Matching CIDs:&lt;br/&gt;• bafkrei...abc&lt;br/&gt;• bafkrei...def&lt;br/&gt;• bafkrei...ghi"] end subgraph Phase2["Phase 2: CIDs → Servers"] DHT2[("DHT&lt;br/&gt;Provider Index")] MAPPING["Server → CID Mapping"] end subgraph Result["Result: Server → CIDs"] R1["dir-node-1.example.org&lt;br/&gt; └─ bafkrei...abc&lt;br/&gt; └─ bafkrei...def"] R2["dir-node-2.example.org&lt;br/&gt; └─ bafkrei...ghi"] end Q --&gt;|"skill: NLP"| DHT1 DHT1 --&gt;|"resolve taxonomy"| CIDS CIDS --&gt;|"lookup providers"| DHT2 DHT2 --&gt;|"find hosting nodes"| MAPPING MAPPING --&gt; R1 MAPPING --&gt; R2 style Query fill:transparent,stroke:#6b9fc8,stroke-width:2px style Phase1 fill:transparent,stroke:#d4a054,stroke-width:2px style Phase2 fill:transparent,stroke:#5ba67a,stroke-width:2px style Result fill:transparent,stroke:#9a7bb0,stroke-width:2px style Q fill:#6b9fc8,stroke:#4a7a9c,stroke-width:2px style DHT1 fill:#d4a054,stroke:#b08540,stroke-width:2px style CIDS fill:#d4a054,stroke:#b08540,stroke-width:2px style DHT2 fill:#5ba67a,stroke:#478a60,stroke-width:2px style MAPPING fill:#5ba67a,stroke:#478a60,stroke-width:2px style R1 fill:#9a7bb0,stroke:#7a5f8c,stroke-width:2px style R2 fill:#9a7bb0,stroke:#7a5f8c,stroke-width:2px The final result is a mapping of servers to their hosted CIDs, enabling the client to download records from optimal sources. Security and Trust In distributed agentic systems, trust is paramount. Agent Directory provides a comprehensive security model that combines content integrity and content provenance via the publisher’s identity to enable secure agent discovery across organizational boundaries. Content Integrity and Provenance All records are identified by their CID, enabling anyone to verify that content hasn’t been tampered with by recomputing the hash. Records can be signed using OIDC flows or private keys, proving authorship and enabling trust decisions based on publisher reputation. Agent Directory supports local trust chain verification, allowing organizations to: Establish their own trust anchors Define verification policies for external records Validate complete provenance chains from publisher to record Make informed decisions about agent selection based on trust policies specific to their use case # Verify a record's signature and trust chain dirctl verify bafkreiabcd1234567890 # Output includes verification status, signer identity, and trust chain details This multi-layered security approach combines content-addressed integrity with cryptographic signing and trust chain verification, enabling secure discovery in a decentralized ecosystem without relying on centralized authority. Zero-Trust Security Agent Directory leverages SPIFFE (Secure Production Identity Framework For Everyone) and its runtime implementation SPIRE to provide zero-trust security for the federation of different agent directory nodes. This enables: Workload Identity - Every Directory component receives a cryptographically verifiable identity in the form of a SPIFFE ID like spiffe://directory.example/ns/production/sa/my-agent. This identity is used for authentication and authorization across the system and network. Mutual TLS (mTLS) - SPIRE issues SVIDs enabling automatic mTLS between all Directory components. Communication is encrypted and both parties are authenticated without manual certificate management. Cross-Organization Federation - Organizations can federate their SPIRE deployments by exchanging trust bundles to form networks. This enables secure, authenticated communication between Directory nodes in different organizations without custom integration. Policy-Based Access Control - SPIFFE IDs enable fine-grained authorization policies. Organizations can define which workloads can push records, which can query, and which external trust domains are authorized for federation and for which operations. OCI-Based Distributed Storage Agent Directory differs from block storage systems like IPFS in its approach to distributed object storage, leveraging the OCI (Open Container Initiative) distribution specification for content storage and retrieval. Content Retrieval Agent Directory directly stores complete records rather than splitting them into blocks Records are retrieved as complete units using standard OCI protocols No complex block reassembly or multi-source optimization needed Distribution Layer Records are stored and transferred using OCI artifacts Any OCI distribution-compliant server can participate in the Agent Directory network Servers retrieve records from each other using well-established OCI protocols Existing OCI infrastructure (registries, caching layers, CDNs) can be leveraged Integration Support While Agent Directory uses zot as its reference OCI store, the system works with any server that implements the OCI distribution specification. This means you can use existing container registries like Docker Registry, Harbor, or cloud-provider managed registries as Directory storage backends. This OCI-based approach provides: Interoperability - Leverage existing OCI tooling and infrastructure Simplicity - Standard HTTP-based protocol without custom networking requirements Efficiency - Single-request record retrieval without block coordination overhead Enterprise Integration - Use existing OCI registries and security policies Runtime Discovery Integration Agent Directory includes a runtime discovery service that automatically discovers and indexes agent workloads running in a specific environment, bridging the gap between deployed agents and directory records. The runtime discovery system provides real-time visibility into active agents, enabling dynamic discovery and coordination in orchestrated environments like Kubernetes or Docker, but also local environments such as developer machines or edge devices. Workloads can be discovered through annotations that indicate they should be indexed by the Directory. For example, in Kubernetes, a pod can be annotated to enable discovery: apiVersion: v1 kind: Pod metadata: name: translation-agent annotations: org.agntcy/discover: "true" # Enable discovery for this workload org.agntcy/agent-type: "a2a" # Indicate the protocol or framework used org.agntcy/agent-record: "my-agent/v1.0.0" # Indicate reference to a Directory record The Agent Directory runtime discovery is an optional component of the Directory stack that makes workloads queryable alongside statically published records. This enables hybrid discovery where some agents are published ahead of time while others are discovered dynamically as they’re deployed. The same discovery and security models apply to both, providing a unified experience for developers and users. Real-World Use Cases Building Agent Marketplaces Agent Directory provides the foundation for building agent marketplaces where developers can publish agents and users can discover them: Publisher Workflow: Developer creates an agent with specific capabilities Generates an OASF record describing skills, domains, and deployment details Signs the record with their identity for authenticity Publishes to the Directory which announces the CID and metadata to the DHT Consumer Workflow: User queries Agent Directory for agents with specific capabilities The Directory returns matching records with CIDs User reviews metadata, verifies signatures, and checks trust chains User pulls the complete record and retrieves deployment artifacts (Docker images, API endpoints) User deploys or invokes the agent This decentralized marketplace model enables anyone to publish and discover agents without requiring approval from a central authority or platform lock-in. Multi-Agent System Coordination In complex multi-agent systems, agents need to discover collaborators dynamically: from agntcy.dir_sdk.client import Config, Client from agntcy.dir_sdk.models import search_v1 # Select either local/cache or remote Directory node based on context client = Client(Config(server_address="dir.example.org:8888")) # Agent needs image analysis capability # Use network or local discovery based on context results = client.search_records(search_v1.ListRequest( queries=[search_v1.RecordQuery( type=search_v1.RECORD_QUERY_TYPE_SKILL_NAME, value="images_computer_vision/object_detection", )] limit=5, )) # Select the best candidate based on metadata for item in results: # Verify signature, check version, evaluate endpoints if meets_requirements(item.record): endpoint = item.record.data['locators'][0]['url'] # Invoke the discovered agent invoke_agent(endpoint, task_data) break This pattern enables dynamic capability discovery where agents find and coordinate with each other based on current requirements rather than hardcoded integrations. Federated Agent Networks Organizations can run their own Directory nodes while participating in a federated discovery network: Internal Directory Node - Organization runs an Agent Directory node behind their firewall, containing proprietary agents not shared externally. Federation - The internal node connects to external Directory nodes via DHT, enabling discovery of public agents while keeping internal agents private. Policy Enforcement - Organizations define trust policies determining which external agents can be discovered and used, based on publisher identity, signatures, and verification chains. This federated model enables both private agent catalogs and participation in the broader ecosystem, with fine-grained control over what’s shared and discovered. Protocol-Agnostic Discovery Agent Directory’s OASF-based approach provides a unified discovery layer across different agent protocols: A2A Agents - Agents implementing the Agent-to-Agent protocol can be described in OASF records with A2A-specific metadata in modules. MCP Servers - Model Context Protocol servers can be imported to OASF format, indexed by capabilities, and discovered through Agent Directory. Custom Protocols - Organizations developing custom agent protocols can define public or private OASF modules for their specific metadata while leveraging Agent Directory’s discovery infrastructure. Example: A user searching for “text completion” might discover agents using A2A, MCP, or custom protocols - all through a single capability-based query. The OASF record clearly indicates which protocol each agent uses, enabling proper invocation. Getting Started Installation Agent Directory provides a comprehensive suite of tools that make it easy to publish, discover, and work with agent records. Tool Install Prerequisite Usage dirctl brew tap agntcy/dir https://github.com/agntcy/dir/brew install dirctl - CLI Documentation Go SDK go get github.com/agntcy/dir/client - Go SDK Documentation Python SDK pip install agntcy-dir dirctl (sign/verify) Python SDK Documentation JS/TS SDK npm install agntcy-dir dirctl (sign/verify) JS/TS SDK Documentation MCP Server dirctl mcp serve dirctl Directory MCP Server Blog Post MCP Integration Documentation Deployment - - Getting Started Quick Example Publish and discover your first agent in minutes: Deploy an Agent Directory node locally helm pull oci://ghcr.io/agntcy/dir/helm-charts/dir --version v1.0.0 helm upgrade --install dir oci://ghcr.io/agntcy/dir/helm-charts/dir --version v1.0.0 kubectl port-forward svc/dir-apiserver 8888:8888 Download a sample agent record curl -o sample-record.json https://raw.githubusercontent.com/agntcy/dir/refs/heads/main/e2e/shared/testdata/record_100.json Use CLI to publish and discover agents # Push and sign the agent record to Agent Directory dirctl push sample-record.json --sign # Search for agents with specific capabilities dirctl search --skill "natural_language_processing/*" # Pull a specific record by CID dirctl pull baeareiabbog2umgduqhlcb64fzt6adn34kblzvru3fdzkl75hjhwt6h3da # Verify a record's signature and trust chain dirctl verify baeareiabbog2umgduqhlcb64fzt6adn34kblzvru3fdzkl75hjhwt6h3da Use SDK to publish and discover agents from agntcy.dir_sdk.client import Config, Client from agntcy.dir_sdk.models import core_v1, search_v1 # Connect to an Agent Directory node client = Client(Config(server_address="localhost:8888")) # Push an agent record (requires signing credentials) with open("sample-record.json", "r") as f: record = json.load(f) cid = client.push(records=[core_v1.Record(data=record)])[0] print(f"Published agent with CID: {cid}") # Search for agents by capability results = client.search_records(search_v1.ListRequest( queries=[search_v1.RecordQuery( type=search_v1.RECORD_QUERY_TYPE_SKILL_NAME, value="natural_language_processing/*", )], limit=10, )) # Retrieve and inspect matching agents for item in results: print(f"Found: {item.record.name} (CID: {item.cid})") print(f" Skills: {item.record.data.get('skills', [])}") print(f" Locator: {item.record.data['locators'][0]['url']}") Why Agent Directory Matters As AI systems evolve from isolated models to interconnected networks of specialized agents, discovery infrastructure becomes critical. Agent Directory provides a foundation that: Enables Semantic Discovery - Find agents by what they can do, not just by name or endpoint. Ensures Data Integrity - Content-addressed naming and cryptographic signing guarantee authenticity. Scales Without Centralization - DHT-based routing eliminates single points of failure. Bridges Organizational Boundaries - Federated architecture enables cross-organization discovery with fine-grained trust policies. Supports Protocol Diversity - Native integration with A2A, MCP, and custom agent protocols through OASF extensions. Simplifies Development - Publish once, discover everywhere with intuitive CLI and SDK tooling. Whether you’re building a multi-agent AI system, an agent marketplace, or a platform that coordinates specialized AI services, Agent Directory provides the discovery infrastructure you need. The Road Ahead Agent Directory is actively developed as an open-source project under the Apache 2.0 License. The project has reached v1.0 with production-ready capabilities. Current Capabilities: OASF-based capability discovery with hierarchical taxonomies DHT-based distributed routing for scalable discovery Cryptographic signing with OIDC and private key support OCI distribution protocol for interoperable storage Data synchronization between Directory nodes Runtime discovery for Kubernetes and Docker workloads CLI, Go SDK, Python SDK, and JavaScript SDK MCP server integration for AI assistants Under Active Development: Enhanced DHT performance and routing optimizations Extended SPIFFE/SPIRE integration for enterprise deployments Advanced trust chain verification policies Additional runtime discovery targets (edge devices, serverless) Deeper agentic protocol integrations Cross-network federation protocols Get Involved Public Instance A public Agent Directory instance is available for testing and experimentation at prod.gateway.ads.outshift.io:443 in read-only mode. This instance is open for anyone that is part of github.com/agntcy organization, making it a great place to get hands-on experience with Agent Directory. ### Configuration # Set environment variables to connect to the public Directory instance export DIRECTORY_CLIENT_SERVER_ADDRESS="prod.gateway.ads.outshift.io:443" export DIRECTORY_CLIENT_AUTH_MODE="github" ### Interactive Authentication with GitHub OAuth dirctl auth login ### Non-Interactive Authentication with GitHub Token # The token must have `user:email` and `read:org` scope for authentication to work export DIRECTORY_CLIENT_GITHUB_TOKEN="ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX" ### Usage # Now you can use dirctl or the SDK to query the Agent Directory instance dirctl search --skill "*" Check the Directory Testbed Authentication Modes blog for detailed guides on integration and authentication with the public Agent Directory instance. If you’re interested in publishing your own agents, running a full Directory node, or joining the public Directory network, check github.com/agntcy/dir-staging on how to get started. Community Agent Directory is open source and welcomes contributions: GitHub Documentation Specification Join the community building the discovery layer for agentic AI. Try Agent Directory in your next project, contribute to the codebase, or share your use cases. Agent Directory is developed by AGNTCY Contributors and released under the Apache 2.0 License.]]></summary></entry><entry><title type="html">SLIM v1.0: Secure Low-Latency Interactive Messaging for Agentic AI</title><link href="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/02/03/slim-v1.html" rel="alternate" type="text/html" title="SLIM v1.0: Secure Low-Latency Interactive Messaging for Agentic AI" /><published>2026-02-03T07:00:00+00:00</published><updated>2026-02-03T07:00:00+00:00</updated><id>https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/02/03/slim-v1</id><content type="html" xml:base="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/02/03/slim-v1.html"><![CDATA[<p>The AI landscape is rapidly evolving from isolated models to interconnected
networks of intelligent agents. As AI systems become more sophisticated, they
need to communicate, collaborate, and coordinate in real-time. Protocols like
<a href="https://a2a-protocol.org/latest/">A2A (Agent-to-Agent)</a> define <em>what</em>
agents say to each other: standardized messages, task delegation, and 
coordination semantics. But A2A needs a robust transport layer that defines 
<em>how</em> agents securely exchange these messages across network boundaries.</p>

<p>Existing messaging frameworks weren’t designed for the unique demands of
agentic AI. They either sacrifice security for performance, lack proper group
communication primitives, or can’t scale to support distributed multi-agent
systems. Others rely on centralized brokers that can’t be safely exposed across
network boundaries, limiting agents to run only where the broker is deployed.</p>

<p>Enter <a href="https://docs.agntcy.org/slim/overview/">SLIM (Secure Low-Latency Interactive Messaging)</a>, a next-generation
communication framework that provides the secure, scalable transport layer for
agentic AI protocols like A2A. SLIM handles the “piping”: secure connections,
message routing, group communication, and end-to-end encryption, so protocol
implementations can focus on agent semantics, not infrastructure.</p>

<h2 id="the-problem-agent-protocols-need-secure-transport">The Problem: Agent Protocols Need Secure Transport</h2>

<p>Modern AI applications are increasingly agentic. They don’t just respond to
prompts, they take action, use tools, coordinate with other agents, and make
autonomous decisions. The A2A protocol has emerged as a
standard for defining how agents communicate: specifying message formats, task
delegation patterns, and coordination semantics. A2A comes with SDKs in
multiple languages (Python, JavaScript, Java, C#, Go) that implement these
agent-level semantics.</p>

<p>However, A2A and similar agent protocols need a robust transport layer beneath
them. Whether you’re building:</p>

<ul>
  <li><strong>Multi-agent AI systems</strong> where specialized agents collaborate to solve
complex problems.</li>
  <li><strong>AI-powered workflows</strong> that orchestrate between LLMs, tools, and data
sources.</li>
  <li><strong>Real-time agent collaboration</strong> where agents need to share context and
coordinate actions.</li>
  <li><strong>Distributed AI services</strong> that span data centers, edge devices, and
browsers.</li>
</ul>

<p>You need a messaging infrastructure that can handle:</p>

<ol>
  <li><strong>Real-time, low-latency communication</strong> — AI agents can’t wait seconds for
responses.</li>
  <li><strong>End-to-end security</strong> — Agent communications must remain confidential even
through untrusted infrastructure.</li>
  <li><strong>Flexible communication patterns</strong> — From point-to-point tool calls to
group coordination.</li>
  <li><strong>Scale</strong> — Supporting thousands of concurrent agents across distributed
environments.</li>
  <li><strong>Protocol flexibility</strong> — Supporting A2A, MCP, and custom agent protocols</li>
  <li><strong>Simplicity</strong> — Developers shouldn’t need to be cryptography or distributed
systems experts.</li>
  <li><strong>Flexible deployment</strong> — Agents should be reachable without requiring
direct server exposure.</li>
</ol>

<p>Traditional messaging systems fall short. <a href="https://mqtt.org/">MQTT</a> and
<a href="https://www.amqp.org/">AMQP</a> weren’t designed for
interactive AI workflows. <a href="https://grpc.io/">gRPC</a> provides excellent RPC semantics but lacks
group capabilities and end-to-end encryption. Message queues add latency and
complexity. Rolling your own solution means reinventing security, scaling, and
reliability.</p>

<p>A critical networking challenge also emerges: when A2A runs on direct HTTP/gRPC
connections, servers must be continuously exposed and directly reachable.
However, A2A can instead use SLIM as its transport layer. SLIM’s
registration-based model allows agents to register with the SLIM network and
become reachable by peers without exposing server ports. While SLIM routing
nodes themselves need to be exposed, agent endpoints can run behind NATs and
firewalls while remaining fully accessible, significantly simplifying
deployment.</p>

<p>SLIM was built to solve exactly these challenges. It provides the secure,
scalable “piping” that agent protocols like A2A need to operate across
network boundaries.</p>

<h2 id="what-is-slim">What is SLIM?</h2>

<p>SLIM is a secure, scalable, and developer-friendly messaging framework that
provides the transport layer for agent communication protocols like A2A.</p>

<p>While A2A defines <em>what</em> agents say (message formats, task semantics, coordination
patterns), SLIM defines <em>how</em> these messages are securely delivered across
distributed networks.</p>

<p>At its core, SLIM combines:</p>

<ul>
  <li><strong>gRPC’s performance and reliability</strong> — Built on HTTP/2 for efficient,
multiplexed transport.</li>
  <li><strong>Messaging capabilities</strong> — Native support for channels and group
communication.</li>
  <li><strong>End-to-end encryption</strong> — Using Message Layer Security (MLS) protocol.</li>
  <li><strong>Native RPC support</strong> — SRPC (SLIM RPC) for request-response patterns
alongside messaging.</li>
  <li><strong>Distributed architecture</strong> — Separate control and data planes for
scalability and management.</li>
  <li><strong>Protocol flexibility</strong> — Transport layer for A2A, MCP, and custom agent
protocols.</li>
</ul>

<p>SLIM enables AI agents to communicate securely whether they’re running in a
data center, in a browser, on mobile devices, or across organizational
boundaries, all while maintaining sub-second latencies and strong security
guarantees.</p>

<h2 id="key-strengths-and-capabilities">Key Strengths and Capabilities</h2>

<p>The strengths and capabilities of SLIM are:</p>

<ul>
  <li><a href="#scalable-distributed-architecture">Scalable Distributed Architecture</a></li>
  <li><a href="#end-to-end-security-by-design">End-to-End Security by Design</a></li>
  <li><a href="#two-powerful-session-types">Two Powerful Session Types</a></li>
  <li><a href="#intelligent-routing-and-naming">Intelligent Routing and Naming</a></li>
  <li><a href="#low-latency-architecture">Low Latency Architecture</a></li>
  <li><a href="#developer-friendly-apis">Developer-Friendly APIs</a></li>
  <li><a href="#native-rpc-support-with-srpc">Native RPC Support with SRPC</a></li>
</ul>

<h3 id="scalable-distributed-architecture">Scalable Distributed Architecture</h3>

<p>SLIM is architected with three main components that cleanly separate
concerns:</p>

<h4 id="the-data-plane">The Data Plane</h4>

<p>The data plane’s sole purpose is to route messages across SLIM nodes. It
handles:</p>

<ul>
  <li>Efficient message forwarding based on hierarchical names.</li>
  <li>Connection management between SLIM nodes.</li>
  <li>Network topology and routing table maintenance.</li>
  <li>Transport optimization without needing to inspect message contents.</li>
</ul>

<p>The data plane operates purely on message metadata, forwarding packets without
understanding application-level sessions or encryption.</p>

<h4 id="the-session-layer">The Session Layer</h4>

<p>The session layer sits on top of the data plane and handles the complex
requirements of secure agent communication:</p>

<ul>
  <li>Reliable delivery: automatic retries, acknowledgments, and ordering
guarantees.</li>
  <li>Encryption: end-to-end encryption using the Message Layer Security
(MLS) protocol.</li>
  <li>Session management: group creation, invites, membership changes, and
state synchronization.</li>
</ul>

<p>While the session layer is required for applications to achieve secure,
reliable communication, it is not used by SLIM nodes themselves—they only need
the data plane.</p>

<h4 id="the-control-plane">The Control Plane</h4>

<p>The control plane manages the distributed network of SLIM nodes:</p>

<ul>
  <li>Configuration management for routing nodes.</li>
  <li>Monitoring and telemetry collection (planned for future releases).</li>
  <li>Network orchestration and service discovery.</li>
  <li>Administrative operations across the SLIM infrastructure.</li>
</ul>

<h4 id="architecture-diagram">Architecture Diagram</h4>

<p>The following diagram illustrates how these components are distributed across
applications and intermediate routing nodes:</p>

<pre><code class="language-mermaid">graph TB
    subgraph "Application Node (Agent A)"
        A1[Session Layer 1]
        A2[Session Layer 2]
        A3[Data Plane Client]
        A1 -.-&gt;|encrypted messages| A3
        A2 -.-&gt;|encrypted messages| A3
    end
    
    subgraph "Intermediate SLIM Node 1"
        I1[Data Plane]
    end
    
    subgraph "Intermediate SLIM Node 2"
        I2[Data Plane]
    end
    
    subgraph "Application Node (Agent B)"
        B1[Session Layer 1]
        B2[Session Layer 2]
        B3[Data Plane Client]
        B1 -.-&gt;|encrypted messages| B3
        B2 -.-&gt;|encrypted messages| B3
    end
    
    subgraph "Control Plane"
        CP[Configuration &amp; Monitoring]
    end
    
    A3 -.-&gt;|encrypted messages| I1
    I1 -.-&gt;|route| I2
    I2 -.-&gt;|encrypted messages| B3
    
    CP -.-&gt;|manages| I1
    CP -.-&gt;|manages| I2
    
    style A1 fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#fff
    style A2 fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#fff
    style A3 fill:#f39c12,stroke:#d68910,stroke-width:2px,color:#fff
    style B1 fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#fff
    style B2 fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#fff
    style B3 fill:#f39c12,stroke:#d68910,stroke-width:2px,color:#fff
    style I1 fill:#f39c12,stroke:#d68910,stroke-width:2px,color:#fff
    style I2 fill:#f39c12,stroke:#d68910,stroke-width:2px,color:#fff
    style CP fill:#7f8c8d,stroke:#5a6970,stroke-width:2px,color:#fff
</code></pre>

<h4 id="distributed-artifacts">Distributed Artifacts</h4>

<p>This architecture dictates how SLIM is distributed:</p>

<ul>
  <li>
    <p><strong>Pure Data Plane</strong></p>

    <p>We distribute the <code class="language-plaintext highlighter-rouge">slim</code> binary and Docker images as
pure data-plane artifacts. Since SLIM routing nodes only forward messages
and don’t participate in application sessions, they don’t need the session
layer. This keeps the infrastructure lightweight, fast, and simple to deploy.</p>
  </li>
  <li>
    <p><strong>Language Bindings</strong></p>

    <p>We distribute libraries (Python, Go, etc.)
that include both the data plane client and the session layer on top.
Applications use these bindings to get the full stack: secure, reliable,
encrypted communication with automatic session management.</p>
  </li>
</ul>

<p>This separation means you can run a global network of SLIM routing nodes
without any application logic, while your agents use the rich, full-featured
language bindings for their communication needs. The control plane manages the
routing infrastructure independently, ensuring the network operates efficiently.</p>

<h3 id="end-to-end-security-by-design">End-to-End Security by Design</h3>

<p>SLIM leverages the <strong>Message Layer Security (MLS)</strong> protocol
(<a href="https://www.rfc-editor.org/rfc/rfc9420.txt">RFC 9420</a>) to
provide end-to-end encryption for all agent communications. Unlike TLS, which
only secures point-to-point connections, MLS ensures that messages remain
encrypted even when passing through intermediate nodes or experiencing TLS
termination along the path.</p>

<p>This means:</p>

<ul>
  <li>Messages are confidential. Only authorized group members can decrypt
content.</li>
  <li>Authentication is built-in. Cryptographic identities verify agent
authenticity.</li>
  <li>Zero-trust architecture. You don’t need to trust intermediate
infrastructure</li>
  <li>Group security. Add or remove agents from secure groups without
rekeying everything.</li>
</ul>

<p>For AI applications, this is crucial. Your agent conversations, tool outputs,
and coordination messages stay private even when traversing cloud
infrastructure you don’t control.</p>

<h3 id="two-powerful-session-types">Two Powerful Session Types</h3>

<p>SLIM provides two session abstractions that map perfectly to agentic AI
patterns: point-to-point and group sessions.</p>

<h4 id="point-to-point-sessions">Point-to-Point Sessions</h4>

<p>Perfect for tool invocations and direct agent-to-agent communication:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Discover and connect to a specific service instance
</span><span class="n">session_config</span> <span class="o">=</span> <span class="n">slim_bindings</span><span class="p">.</span><span class="n">SessionConfig</span><span class="p">(</span>
    <span class="n">session_type</span><span class="o">=</span><span class="n">slim_bindings</span><span class="p">.</span><span class="n">SessionType</span><span class="p">.</span><span class="n">POINT_TO_POINT</span><span class="p">,</span>
    <span class="n">enable_mls</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
    <span class="n">max_retries</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span>
    <span class="n">interval</span><span class="o">=</span><span class="n">datetime</span><span class="p">.</span><span class="n">timedelta</span><span class="p">(</span><span class="n">seconds</span><span class="o">=</span><span class="mi">5</span><span class="p">),</span>
    <span class="n">metadata</span><span class="o">=</span><span class="p">{}</span>
<span class="p">)</span>

<span class="c1"># Create session and wait for establishment
</span><span class="n">session_context</span> <span class="o">=</span> <span class="k">await</span> <span class="n">app</span><span class="p">.</span><span class="n">create_session_async</span><span class="p">(</span><span class="n">session_config</span><span class="p">,</span> <span class="n">remote_name</span><span class="p">)</span>
<span class="k">await</span> <span class="n">session_context</span><span class="p">.</span><span class="n">completion</span><span class="p">.</span><span class="n">wait_async</span><span class="p">()</span>
<span class="n">session</span> <span class="o">=</span> <span class="n">session_context</span><span class="p">.</span><span class="n">session</span>

<span class="c1"># Send a request
</span><span class="k">await</span> <span class="n">session</span><span class="p">.</span><span class="n">publish_async</span><span class="p">(</span>
    <span class="sa">b</span><span class="s">"execute_tool: search(query='AI agents')"</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span>
<span class="p">)</span>

<span class="c1"># Get the response
</span><span class="n">received_msg</span> <span class="o">=</span> <span class="k">await</span> <span class="n">session</span><span class="p">.</span><span class="n">get_message_async</span><span class="p">(</span>
    <span class="n">timeout</span><span class="o">=</span><span class="n">datetime</span><span class="p">.</span><span class="n">timedelta</span><span class="p">(</span><span class="n">seconds</span><span class="o">=</span><span class="mi">30</span><span class="p">)</span>
<span class="p">)</span>
<span class="n">result</span> <span class="o">=</span> <span class="n">received_msg</span><span class="p">.</span><span class="n">payload</span>
</code></pre></div></div>

<p>The session automatically:</p>

<ul>
  <li>Discovers an available service instance.</li>
  <li>Establishes a secure MLS group (if enabled).</li>
  <li>Binds all subsequent communication to that instance.</li>
  <li>Provides reliable delivery with automatic retries.</li>
</ul>

<h4 id="group-sessions">Group Sessions</h4>

<p>Ideal for multi-agent coordination, broadcast updates, and
collaborative workflows:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Create a coordination channel
</span><span class="n">session_config</span> <span class="o">=</span> <span class="n">slim_bindings</span><span class="p">.</span><span class="n">SessionConfig</span><span class="p">(</span>
    <span class="n">session_type</span><span class="o">=</span><span class="n">slim_bindings</span><span class="p">.</span><span class="n">SessionType</span><span class="p">.</span><span class="n">GROUP</span><span class="p">,</span>
    <span class="n">enable_mls</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
    <span class="n">max_retries</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span>
    <span class="n">interval</span><span class="o">=</span><span class="n">datetime</span><span class="p">.</span><span class="n">timedelta</span><span class="p">(</span><span class="n">seconds</span><span class="o">=</span><span class="mi">5</span><span class="p">),</span>
    <span class="n">metadata</span><span class="o">=</span><span class="p">{}</span>
<span class="p">)</span>

<span class="c1"># Create group session and wait for establishment
</span><span class="n">session_context</span> <span class="o">=</span> <span class="k">await</span> <span class="n">app</span><span class="p">.</span><span class="n">create_session_async</span><span class="p">(</span><span class="n">session_config</span><span class="p">,</span> <span class="n">channel_name</span><span class="p">)</span>
<span class="k">await</span> <span class="n">session_context</span><span class="p">.</span><span class="n">completion</span><span class="p">.</span><span class="n">wait_async</span><span class="p">()</span>
<span class="n">session</span> <span class="o">=</span> <span class="n">session_context</span><span class="p">.</span><span class="n">session</span>

<span class="c1"># Invite other agents to join
</span><span class="n">handle</span> <span class="o">=</span> <span class="k">await</span> <span class="n">session</span><span class="p">.</span><span class="n">invite_async</span><span class="p">(</span><span class="n">agent_name</span><span class="p">)</span>
<span class="k">await</span> <span class="n">handle</span><span class="p">.</span><span class="n">wait_async</span><span class="p">()</span>

<span class="c1"># Broadcast to all participants
</span><span class="k">await</span> <span class="n">session</span><span class="p">.</span><span class="n">publish_async</span><span class="p">(</span>
    <span class="sa">b</span><span class="s">"task_update: analysis complete"</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span>
<span class="p">)</span>
</code></pre></div></div>

<p>Group sessions support:</p>

<ul>
  <li>Many-to-many communication: all messages delivered to all participants.</li>
  <li>Dynamic membership: add or remove agents on the fly.</li>
  <li>Moderation: channel creators control membership.</li>
  <li>Secure group state: MLS ensures all members share cryptographic context.</li>
</ul>

<h3 id="intelligent-routing-and-naming">Intelligent Routing and Naming</h3>

<p>SLIM uses a hierarchical naming system based on Decentralized Identifiers
(DIDs) that enables both human-readable names and secure, globally unique
identifiers:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>organization/namespace/service/instance
</code></pre></div></div>

<p>For example: <code class="language-plaintext highlighter-rouge">agntcy/production/search-agent/a3f9k2</code></p>

<p>This structure supports:</p>

<ul>
  <li>Anycast routing: send to <code class="language-plaintext highlighter-rouge">org/ns/service</code> and reach any available instance.</li>
  <li>Unicast routing: target a specific instance with the full four-part name.</li>
  <li>Service discovery: find agents by their organizational and service identifiers.</li>
  <li>Scalable forwarding: hierarchical names aggregate efficiently in routing tables.</li>
</ul>

<p>No more hardcoded endpoints or complex service registries. SLIM’s naming
system just works.</p>

<h3 id="low-latency-architecture">Low Latency Architecture</h3>

<p>SLIM is optimized for interactive AI workloads where every millisecond
counts:</p>

<ul>
  <li>Multiplexed streams: multiple logical streams over single connections.</li>
  <li>Efficient message routing: intelligent forwarding tables minimize hops.</li>
  <li>Zero-copy operations: where possible, avoid unnecessary data copying.</li>
  <li>Async-first design: non-blocking I/O throughout the stack.</li>
  <li>HTTP/2 transport: proven, efficient binary protocol.</li>
</ul>

<p>Real-world deployments show sub-second end-to-end latencies even with MLS
encryption enabled.</p>

<h3 id="developer-friendly-apis">Developer-Friendly APIs</h3>

<p>SLIM provides high-quality bindings for multiple languages that abstract
complexity. Here’s an example in Python:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">slim_bindings</span>
<span class="kn">import</span> <span class="nn">datetime</span>

<span class="c1"># Initialize the global service
</span><span class="n">service</span> <span class="o">=</span> <span class="n">slim_bindings</span><span class="p">.</span><span class="n">get_global_service</span><span class="p">()</span>

<span class="c1"># Create a SLIM instance with shared secret auth
</span><span class="n">local_name</span> <span class="o">=</span> <span class="n">slim_bindings</span><span class="p">.</span><span class="n">Name</span><span class="p">(</span><span class="s">"org"</span><span class="p">,</span> <span class="s">"namespace"</span><span class="p">,</span> <span class="s">"my-agent"</span><span class="p">)</span>
<span class="n">app</span> <span class="o">=</span> <span class="n">service</span><span class="p">.</span><span class="n">create_app_with_secret</span><span class="p">(</span>
    <span class="n">local_name</span><span class="p">,</span> <span class="s">"my-shared-secret"</span>
<span class="p">)</span>

<span class="c1"># Connect to SLIM node
</span><span class="n">client_config</span> <span class="o">=</span> <span class="n">slim_bindings</span><span class="p">.</span><span class="n">new_insecure_client_config</span><span class="p">(</span>
    <span class="s">"http://127.0.0.1:46357"</span>
<span class="p">)</span>
<span class="n">conn_id</span> <span class="o">=</span> <span class="k">await</span> <span class="n">service</span><span class="p">.</span><span class="n">connect_async</span><span class="p">(</span><span class="n">client_config</span><span class="p">)</span>

<span class="c1"># Subscribe to receive messages
</span><span class="k">await</span> <span class="n">app</span><span class="p">.</span><span class="n">subscribe_async</span><span class="p">(</span><span class="n">local_name</span><span class="p">,</span> <span class="n">conn_id</span><span class="p">)</span>

<span class="c1"># Listen for incoming session
</span><span class="n">session</span> <span class="o">=</span> <span class="k">await</span> <span class="n">app</span><span class="p">.</span><span class="n">listen_for_session_async</span><span class="p">(</span><span class="bp">None</span><span class="p">)</span>

<span class="c1"># Receive and reply to messages
</span><span class="n">received_msg</span> <span class="o">=</span> <span class="k">await</span> <span class="n">session</span><span class="p">.</span><span class="n">get_message_async</span><span class="p">(</span>
    <span class="n">timeout</span><span class="o">=</span><span class="n">datetime</span><span class="p">.</span><span class="n">timedelta</span><span class="p">(</span><span class="n">seconds</span><span class="o">=</span><span class="mi">30</span><span class="p">)</span>
<span class="p">)</span>
<span class="n">payload</span> <span class="o">=</span> <span class="n">received_msg</span><span class="p">.</span><span class="n">payload</span>
<span class="k">await</span> <span class="n">session</span><span class="p">.</span><span class="n">publish_async</span><span class="p">(</span><span class="sa">b</span><span class="s">"response"</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">)</span>
</code></pre></div></div>

<p>No need to manage MLS groups manually, handle connection pooling, or worry
about encryption keys — SLIM’s session layer handles it all.</p>

<h3 id="native-rpc-support-with-srpc">Native RPC Support with SRPC</h3>

<p>Many agent-facing protocols (like A2A and MCP) are RPC-oriented. SLIM includes
<strong>SRPC</strong> (SLIM RPC), which provides gRPC-like streaming RPC semantics directly
over the SLIM transport.</p>

<p>SRPC supports all four gRPC patterns:</p>

<ul>
  <li><strong>Unary → Unary</strong> — Simple request-response</li>
  <li><strong>Unary → Stream</strong> — Server streaming responses</li>
  <li><strong>Stream → Unary</strong> — Client streaming requests</li>
  <li><strong>Stream → Stream</strong> — Bidirectional streaming</li>
</ul>

<p>Define services with Protocol Buffers:</p>

<div class="language-proto highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">service</span> <span class="n">AgentService</span> <span class="p">{</span>
  <span class="k">rpc</span> <span class="n">ExecuteTool</span><span class="p">(</span><span class="n">ToolRequest</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">ToolResponse</span><span class="p">);</span>
  <span class="k">rpc</span> <span class="n">StreamResults</span><span class="p">(</span><span class="n">Query</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">stream</span> <span class="n">Result</span><span class="p">);</span>
  <span class="k">rpc</span> <span class="n">CollectInputs</span><span class="p">(</span><span class="n">stream</span> <span class="n">Input</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">Summary</span><span class="p">);</span>
  <span class="k">rpc</span> <span class="n">Coordinate</span><span class="p">(</span><span class="n">stream</span> <span class="n">Message</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">stream</span> <span class="n">Message</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p>The SLIM RPC compiler generates language-specific stubs that work seamlessly
with SLIM’s secure messaging layer. You get familiar RPC semantics with SLIM’s
security and group communication benefits. We are also studying how to extend
these RPC interaction patterns to support group communication scenarios,
enabling multi-agent RPC workflows.</p>

<h3 id="single-implementation-multiple-languages">Single Implementation, Multiple Languages</h3>

<p>SLIM is implemented in Rust, providing a high-performance, memory-safe
core that powers all language bindings. This approach ensures:</p>

<ul>
  <li>Consistent behavior: all language bindings use the same battle-tested
implementation.</li>
  <li>Performance: Rust’s zero-cost abstractions deliver native speed across
all platforms.</li>
  <li>Security: Memory safety guarantees prevent entire classes of
vulnerabilities.</li>
  <li>Maintainability: Bug fixes and improvements benefit all language
ecosystems simultaneously.</li>
</ul>

<p><strong>Currently Available:</strong></p>

<ul>
  <li><strong>Python</strong>. Full-featured bindings with async/await support</li>
  <li><strong>Go</strong>. Native Go bindings for seamless integration (see our technical
blog posts on <a href="https://blogs.agntcy.org/technical/2026/01/27/integrating-rust-and-go-with-uniffi.html">integrating Rust and Go with
UniFFI</a>
and <a href="https://blogs.agntcy.org/technical/2026/01/20/distributing-c-artifacts-for-go-modules.html">distributing C artifacts for Go
modules</a>)</li>
</ul>

<p><strong>Coming Soon:</strong></p>

<ul>
  <li><strong>C#</strong>. For .NET and Unity applications.</li>
  <li><strong>JavaScript/TypeScript</strong>. Browser and Node.js support.</li>
  <li><strong>Kotlin</strong>. Android and JVM applications.</li>
</ul>

<p>This multi-language strategy means you can build heterogeneous agent systems
where different components use their most appropriate language, all
communicating seamlessly through SLIM’s unified protocol.</p>

<p>One of the key reasons SLIM provides multiple language bindings is to
support agent communication protocols like A2A across different
programming ecosystems. A2A
comes with SDKs in multiple languages (Python, JavaScript, Java, C#, Go), and
SLIM’s language bindings enable these A2A SDKs to communicate securely. An A2A
Python agent can securely communicate with an A2A Java agent, with SLIM
handling the underlying transport transparently.</p>

<h2 id="real-world-use-cases">Real-World Use Cases</h2>

<h3 id="slim--a2a-agent-to-agent-communication">SLIM + A2A: Agent-to-Agent Communication</h3>

<p>SLIM and A2A are designed to work together:</p>

<ul>
  <li><a href="https://a2a-protocol.org/latest/sdk/">A2A SDKs</a> (Python, JavaScript,
Java, C#, Go) implement agent-level
semantics — task delegation, message formats, coordination patterns.</li>
  <li><a href="https://github.com/agntcy/slim/tree/main/data-plane/bindings">SLIM bindings</a>
provide the secure transport, handling connection
management, routing, encryption, and delivery guarantees.</li>
</ul>

<p>Currently, A2A sits on top of SLIM RPC (SRPC), enabling secure point-to-point agent interactions.
We provide <a href="https://github.com/agntcy/slim-a2a-python">slim-a2a-python</a>, a
Python A2A SDK implementation built on SLIMRPC that enables developers to
build A2A-compliant agents with SLIM as the transport layer. We are actively
working to add A2A SDK support for the remaining SLIM language bindings (Go,
C#, JavaScript/TypeScript, Kotlin).</p>

<p>Looking ahead, A2A is evolving to support bidirectional streaming for real-time, interactive agent conversations and group communication for multi-agent coordination and collaboration.</p>

<p>SLIM naturally provides both capabilities at the transport level through SRPC
and MLS-based end-to-end encryption for groups, which many pub-sub
systems lack. This positions SLIM as an ideal transport foundation for A2A’s
continued evolution toward richer, more secure agent communication
patterns.</p>

<p>The following diagram illustrates how A2A and SLIM capabilities combine:</p>

<pre><code class="language-mermaid">graph LR
    subgraph Protocol["Protocol Layer (Application Semantics)"]
        direction TB
        A1["A2A Go SDK&lt;br/&gt;Task Delegation&lt;br/&gt;Coordination"]
        B1["A2A C# SDK&lt;br/&gt;Task Delegation&lt;br/&gt;Coordination"]
        C1["A2A Python SDK&lt;br/&gt;Task Delegation&lt;br/&gt;Coordination"]
    end
    
    subgraph Transport["Transport Layer (SLIM)"]
        direction TB
        subgraph Endpoints["Agent Endpoints"]
            A2["SLIM Go Bindings&lt;br/&gt;SRPC + Session + Data Plane"]
            B2["SLIM C# Bindings&lt;br/&gt;SRPC + Session + Data Plane"]
            C2["SLIM Python Bindings&lt;br/&gt;SRPC + Session + Data Plane"]
        end
        
        subgraph Network["SLIM Network Infrastructure"]
            SN1[SLIM Node 1&lt;br/&gt;Data Plane]
            SN2[SLIM Node 2&lt;br/&gt;Data Plane]
        end
    end
    
    A1 --&gt;|uses| A2
    B1 --&gt;|uses| B2
    C1 --&gt;|uses| C2
    
    A2 -.-&gt;|"SRPC (P2P)&lt;br/&gt;encrypted via MLS"| SN1
    B2 -.-&gt;|"SRPC (P2P)&lt;br/&gt;encrypted via MLS"| SN1
    C2 -.-&gt;|"SRPC (Groups)&lt;br/&gt;encrypted via MLS"| SN2
    
    SN1 -.-&gt;|routes| SN2
    SN2 -.-&gt;|routes| SN1
    
    style Protocol fill:#e8f4f8,stroke:#2980b9,stroke-width:3px
    style Transport fill:#fef5e7,stroke:#f39c12,stroke-width:3px
    style Endpoints fill:#fff,stroke:#95a5a6,stroke-width:2px
    style Network fill:#fff,stroke:#95a5a6,stroke-width:2px
    
    style A1 fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#fff
    style B1 fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#fff
    style C1 fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#fff
    style A2 fill:#27ae60,stroke:#1e8449,stroke-width:2px,color:#fff
    style B2 fill:#27ae60,stroke:#1e8449,stroke-width:2px,color:#fff
    style C2 fill:#27ae60,stroke:#1e8449,stroke-width:2px,color:#fff
    style SN1 fill:#f39c12,stroke:#d68910,stroke-width:2px,color:#fff
    style SN2 fill:#f39c12,stroke:#d68910,stroke-width:2px,color:#fff
</code></pre>

<p><strong>Architecture Overview:</strong></p>

<ul>
  <li><strong>Protocol Layer</strong> (blue, left): A2A SDKs implement agent-level
semantics — defining <em>what</em> agents say through standardized task
delegation, message formats, and coordination patterns.</li>
  <li><strong>Transport Layer</strong> (green/orange, right): SLIM provides the secure
infrastructure for <em>how</em> messages are delivered:
    <ul>
      <li><strong>SLIM Bindings</strong> (green): Include SRPC for RPC semantics, Session
Layer for reliability and encryption, and Data Plane client for
connectivity.</li>
      <li><strong>SLIM Network</strong> (orange): Pure data plane nodes route encrypted
messages without understanding agent semantics.</li>
    </ul>
  </li>
</ul>

<p><strong>Communication Patterns:</strong></p>

<ul>
  <li><strong>Current</strong>: A2A uses SRPC for point-to-point (P2P) agent interactions
with MLS encryption.</li>
  <li><strong>Future</strong>: SLIM naturally provides SRPC group capabilities and
MLS-based group encryption, ready to support A2A’s evolution toward
groups and bidirectional streaming.</li>
</ul>

<p>SLIM also provides the transport layer for other agent communication
protocols:</p>

<ul>
  <li><strong>MCP (Model Context Protocol)</strong> for context sharing between agents and tools.</li>
  <li><strong>Custom protocols</strong> for domain-specific agent interactions.</li>
</ul>

<p>For more information about A2A, see the <a href="https://a2a-protocol.org/">official A2A
documentation</a>.</p>

<h2 id="getting-started">Getting Started</h2>

<h3 id="installation">Installation</h3>

<p>Install the SLIM node:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Docker</span>
docker pull ghcr.io/agntcy/slim:v1.0.0

<span class="c"># Cargo</span>
cargo <span class="nb">install </span>agntcy-slim

<span class="c"># Helm (Kubernetes)</span>
helm pull oci://ghcr.io/agntcy/slim/helm/slim <span class="nt">--version</span> v1.0.0
</code></pre></div></div>

<p>Install Python bindings:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>slim-bindings
</code></pre></div></div>

<h3 id="quick-example">Quick Example</h3>

<p>Create your first SLIM agent in minutes:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">asyncio</span>
<span class="kn">import</span> <span class="nn">slim_bindings</span>
<span class="kn">import</span> <span class="nn">datetime</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">main</span><span class="p">():</span>
    <span class="c1"># Initialize the global service
</span>    <span class="n">service</span> <span class="o">=</span> <span class="n">slim_bindings</span><span class="p">.</span><span class="n">get_global_service</span><span class="p">()</span>
    
    <span class="c1"># Create app with shared secret authentication
</span>    <span class="n">local_name</span> <span class="o">=</span> <span class="n">slim_bindings</span><span class="p">.</span><span class="n">Name</span><span class="p">(</span><span class="s">"org"</span><span class="p">,</span> <span class="s">"namespace"</span><span class="p">,</span> <span class="s">"agent"</span><span class="p">)</span>
    <span class="n">app</span> <span class="o">=</span> <span class="n">service</span><span class="p">.</span><span class="n">create_app_with_secret</span><span class="p">(</span><span class="n">local_name</span><span class="p">,</span> <span class="s">"my-shared-secret"</span><span class="p">)</span>
    
    <span class="c1"># Connect to SLIM node
</span>    <span class="n">client_config</span> <span class="o">=</span> <span class="n">slim_bindings</span><span class="p">.</span><span class="n">new_insecure_client_config</span><span class="p">(</span>
        <span class="s">"http://127.0.0.1:46357"</span>
    <span class="p">)</span>
    <span class="n">conn_id</span> <span class="o">=</span> <span class="k">await</span> <span class="n">service</span><span class="p">.</span><span class="n">connect_async</span><span class="p">(</span><span class="n">client_config</span><span class="p">)</span>
    
    <span class="c1"># Subscribe to local name
</span>    <span class="k">await</span> <span class="n">app</span><span class="p">.</span><span class="n">subscribe_async</span><span class="p">(</span><span class="n">local_name</span><span class="p">,</span> <span class="n">conn_id</span><span class="p">)</span>
    
    <span class="c1"># Listen for incoming session
</span>    <span class="n">session</span> <span class="o">=</span> <span class="k">await</span> <span class="n">app</span><span class="p">.</span><span class="n">listen_for_session_async</span><span class="p">(</span><span class="bp">None</span><span class="p">)</span>
    
    <span class="c1"># Handle messages
</span>    <span class="k">while</span> <span class="bp">True</span><span class="p">:</span>
        <span class="n">received_msg</span> <span class="o">=</span> <span class="k">await</span> <span class="n">session</span><span class="p">.</span><span class="n">get_message_async</span><span class="p">(</span>
            <span class="n">timeout</span><span class="o">=</span><span class="n">datetime</span><span class="p">.</span><span class="n">timedelta</span><span class="p">(</span><span class="n">seconds</span><span class="o">=</span><span class="mi">30</span><span class="p">)</span>
        <span class="p">)</span>
        <span class="n">payload</span> <span class="o">=</span> <span class="n">received_msg</span><span class="p">.</span><span class="n">payload</span>
        <span class="k">print</span><span class="p">(</span><span class="sa">f</span><span class="s">"Received: </span><span class="si">{</span><span class="n">payload</span><span class="p">.</span><span class="n">decode</span><span class="p">()</span><span class="si">}</span><span class="s">"</span><span class="p">)</span>
        <span class="k">await</span> <span class="n">session</span><span class="p">.</span><span class="n">publish_async</span><span class="p">(</span><span class="sa">b</span><span class="s">"Acknowledged: "</span> <span class="o">+</span> <span class="n">payload</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">)</span>

<span class="n">asyncio</span><span class="p">.</span><span class="n">run</span><span class="p">(</span><span class="n">main</span><span class="p">())</span>
</code></pre></div></div>

<h2 id="the-road-ahead">The Road Ahead</h2>

<p>SLIM is being actively developed as an open-source project under the Apache
2.0 license. The protocol specification has been submitted as an
Internet-Draft to
the IETF, reflecting its ambition to become a standard for agentic AI
communication.</p>

<p>Current capabilities include:</p>

<ul>
  <li>Rust core with multi-language bindings (Python, Go, and more coming)</li>
  <li>MLS-based end-to-end encryption</li>
  <li>Point-to-point and group sessions</li>
  <li>SRPC for RPC patterns</li>
  <li>Control plane for management</li>
  <li>Kubernetes deployment support</li>
</ul>

<p>The community is working on:</p>

<ul>
  <li>Additional language bindings (C#, JavaScript/TypeScript, Kotlin)</li>
  <li>Enhanced monitoring and observability</li>
  <li>Integration with popular AI frameworks</li>
  <li>Extended A2A support for groups and bidirectional streaming</li>
  <li>Reference implementations of MCP over SLIM</li>
  <li>Performance optimizations</li>
</ul>

<h2 id="why-slim-matters">Why SLIM Matters</h2>

<p>As AI systems evolve from monolithic models to distributed networks of
specialized agents, the communication infrastructure becomes critical. SLIM
provides a foundation that:</p>

<ul>
  <li>Ensures security: end-to-end encryption without complexity.</li>
  <li>Scales with demand: from prototype to production.</li>
  <li>Simplifies development: intuitive APIs for complex capabilities.</li>
  <li>Future-proofs architecture: standards-based, extensible design.</li>
</ul>

<p>Whether you’re building a multi-agent AI system, an agentic workflow platform,
or the next generation of AI applications, SLIM provides the messaging
infrastructure you need.</p>

<h2 id="get-involved">Get Involved</h2>

<p>SLIM is open source and welcomes contributions:</p>

<ul>
  <li><a href="https://github.com/agntcy/slim">GitHub</a></li>
  <li><a href="https://docs.agntcy.org/slim/overview">Documentation</a></li>
  <li><a href="https://spec.slim.agntcy.org/draft-mpsb-agntcy-slim.html">Specification</a></li>
</ul>

<p>You can find comprehensive <a href="https://github.com/agntcy/slim/tree/slim-v1.0.0/data-plane/bindings/python/examples">Python</a>
and <a href="https://github.com/agntcy/slim/tree/slim-v1.0.0/data-plane/bindings/go/examples">Go</a>
examples in the GitHub repository.</p>

<p>Join the community building the communication layer for agentic AI. Try
SLIM in your next project with our <a href="https://github.com/agntcy/slim/tree/main/data-plane/bindings">language binding
examples</a>,
contribute to the codebase, or share your use cases.</p>

<p>The future of AI is collaborative, distributed, and secure. SLIM makes it
possible.</p>

<hr />

<p><em>SLIM is developed by AGNTCY Contributors and released under the Apache 2.0
License.</em></p>]]></content><author><name>Mauro Sardara</name></author><category term="technical" /><category term="slim" /><category term="rust" /><category term="messaging" /><category term="groups" /><category term="grpc" /><summary type="html"><![CDATA[The AI landscape is rapidly evolving from isolated models to interconnected networks of intelligent agents. As AI systems become more sophisticated, they need to communicate, collaborate, and coordinate in real-time. Protocols like A2A (Agent-to-Agent) define what agents say to each other: standardized messages, task delegation, and coordination semantics. But A2A needs a robust transport layer that defines how agents securely exchange these messages across network boundaries. Existing messaging frameworks weren’t designed for the unique demands of agentic AI. They either sacrifice security for performance, lack proper group communication primitives, or can’t scale to support distributed multi-agent systems. Others rely on centralized brokers that can’t be safely exposed across network boundaries, limiting agents to run only where the broker is deployed. Enter SLIM (Secure Low-Latency Interactive Messaging), a next-generation communication framework that provides the secure, scalable transport layer for agentic AI protocols like A2A. SLIM handles the “piping”: secure connections, message routing, group communication, and end-to-end encryption, so protocol implementations can focus on agent semantics, not infrastructure. The Problem: Agent Protocols Need Secure Transport Modern AI applications are increasingly agentic. They don’t just respond to prompts, they take action, use tools, coordinate with other agents, and make autonomous decisions. The A2A protocol has emerged as a standard for defining how agents communicate: specifying message formats, task delegation patterns, and coordination semantics. A2A comes with SDKs in multiple languages (Python, JavaScript, Java, C#, Go) that implement these agent-level semantics. However, A2A and similar agent protocols need a robust transport layer beneath them. Whether you’re building: Multi-agent AI systems where specialized agents collaborate to solve complex problems. AI-powered workflows that orchestrate between LLMs, tools, and data sources. Real-time agent collaboration where agents need to share context and coordinate actions. Distributed AI services that span data centers, edge devices, and browsers. You need a messaging infrastructure that can handle: Real-time, low-latency communication — AI agents can’t wait seconds for responses. End-to-end security — Agent communications must remain confidential even through untrusted infrastructure. Flexible communication patterns — From point-to-point tool calls to group coordination. Scale — Supporting thousands of concurrent agents across distributed environments. Protocol flexibility — Supporting A2A, MCP, and custom agent protocols Simplicity — Developers shouldn’t need to be cryptography or distributed systems experts. Flexible deployment — Agents should be reachable without requiring direct server exposure. Traditional messaging systems fall short. MQTT and AMQP weren’t designed for interactive AI workflows. gRPC provides excellent RPC semantics but lacks group capabilities and end-to-end encryption. Message queues add latency and complexity. Rolling your own solution means reinventing security, scaling, and reliability. A critical networking challenge also emerges: when A2A runs on direct HTTP/gRPC connections, servers must be continuously exposed and directly reachable. However, A2A can instead use SLIM as its transport layer. SLIM’s registration-based model allows agents to register with the SLIM network and become reachable by peers without exposing server ports. While SLIM routing nodes themselves need to be exposed, agent endpoints can run behind NATs and firewalls while remaining fully accessible, significantly simplifying deployment. SLIM was built to solve exactly these challenges. It provides the secure, scalable “piping” that agent protocols like A2A need to operate across network boundaries. What is SLIM? SLIM is a secure, scalable, and developer-friendly messaging framework that provides the transport layer for agent communication protocols like A2A. While A2A defines what agents say (message formats, task semantics, coordination patterns), SLIM defines how these messages are securely delivered across distributed networks. At its core, SLIM combines: gRPC’s performance and reliability — Built on HTTP/2 for efficient, multiplexed transport. Messaging capabilities — Native support for channels and group communication. End-to-end encryption — Using Message Layer Security (MLS) protocol. Native RPC support — SRPC (SLIM RPC) for request-response patterns alongside messaging. Distributed architecture — Separate control and data planes for scalability and management. Protocol flexibility — Transport layer for A2A, MCP, and custom agent protocols. SLIM enables AI agents to communicate securely whether they’re running in a data center, in a browser, on mobile devices, or across organizational boundaries, all while maintaining sub-second latencies and strong security guarantees. Key Strengths and Capabilities The strengths and capabilities of SLIM are: Scalable Distributed Architecture End-to-End Security by Design Two Powerful Session Types Intelligent Routing and Naming Low Latency Architecture Developer-Friendly APIs Native RPC Support with SRPC Scalable Distributed Architecture SLIM is architected with three main components that cleanly separate concerns: The Data Plane The data plane’s sole purpose is to route messages across SLIM nodes. It handles: Efficient message forwarding based on hierarchical names. Connection management between SLIM nodes. Network topology and routing table maintenance. Transport optimization without needing to inspect message contents. The data plane operates purely on message metadata, forwarding packets without understanding application-level sessions or encryption. The Session Layer The session layer sits on top of the data plane and handles the complex requirements of secure agent communication: Reliable delivery: automatic retries, acknowledgments, and ordering guarantees. Encryption: end-to-end encryption using the Message Layer Security (MLS) protocol. Session management: group creation, invites, membership changes, and state synchronization. While the session layer is required for applications to achieve secure, reliable communication, it is not used by SLIM nodes themselves—they only need the data plane. The Control Plane The control plane manages the distributed network of SLIM nodes: Configuration management for routing nodes. Monitoring and telemetry collection (planned for future releases). Network orchestration and service discovery. Administrative operations across the SLIM infrastructure. Architecture Diagram The following diagram illustrates how these components are distributed across applications and intermediate routing nodes: graph TB subgraph "Application Node (Agent A)" A1[Session Layer 1] A2[Session Layer 2] A3[Data Plane Client] A1 -.-&gt;|encrypted messages| A3 A2 -.-&gt;|encrypted messages| A3 end subgraph "Intermediate SLIM Node 1" I1[Data Plane] end subgraph "Intermediate SLIM Node 2" I2[Data Plane] end subgraph "Application Node (Agent B)" B1[Session Layer 1] B2[Session Layer 2] B3[Data Plane Client] B1 -.-&gt;|encrypted messages| B3 B2 -.-&gt;|encrypted messages| B3 end subgraph "Control Plane" CP[Configuration &amp; Monitoring] end A3 -.-&gt;|encrypted messages| I1 I1 -.-&gt;|route| I2 I2 -.-&gt;|encrypted messages| B3 CP -.-&gt;|manages| I1 CP -.-&gt;|manages| I2 style A1 fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#fff style A2 fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#fff style A3 fill:#f39c12,stroke:#d68910,stroke-width:2px,color:#fff style B1 fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#fff style B2 fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#fff style B3 fill:#f39c12,stroke:#d68910,stroke-width:2px,color:#fff style I1 fill:#f39c12,stroke:#d68910,stroke-width:2px,color:#fff style I2 fill:#f39c12,stroke:#d68910,stroke-width:2px,color:#fff style CP fill:#7f8c8d,stroke:#5a6970,stroke-width:2px,color:#fff Distributed Artifacts This architecture dictates how SLIM is distributed: Pure Data Plane We distribute the slim binary and Docker images as pure data-plane artifacts. Since SLIM routing nodes only forward messages and don’t participate in application sessions, they don’t need the session layer. This keeps the infrastructure lightweight, fast, and simple to deploy. Language Bindings We distribute libraries (Python, Go, etc.) that include both the data plane client and the session layer on top. Applications use these bindings to get the full stack: secure, reliable, encrypted communication with automatic session management. This separation means you can run a global network of SLIM routing nodes without any application logic, while your agents use the rich, full-featured language bindings for their communication needs. The control plane manages the routing infrastructure independently, ensuring the network operates efficiently. End-to-End Security by Design SLIM leverages the Message Layer Security (MLS) protocol (RFC 9420) to provide end-to-end encryption for all agent communications. Unlike TLS, which only secures point-to-point connections, MLS ensures that messages remain encrypted even when passing through intermediate nodes or experiencing TLS termination along the path. This means: Messages are confidential. Only authorized group members can decrypt content. Authentication is built-in. Cryptographic identities verify agent authenticity. Zero-trust architecture. You don’t need to trust intermediate infrastructure Group security. Add or remove agents from secure groups without rekeying everything. For AI applications, this is crucial. Your agent conversations, tool outputs, and coordination messages stay private even when traversing cloud infrastructure you don’t control. Two Powerful Session Types SLIM provides two session abstractions that map perfectly to agentic AI patterns: point-to-point and group sessions. Point-to-Point Sessions Perfect for tool invocations and direct agent-to-agent communication: # Discover and connect to a specific service instance session_config = slim_bindings.SessionConfig( session_type=slim_bindings.SessionType.POINT_TO_POINT, enable_mls=True, max_retries=5, interval=datetime.timedelta(seconds=5), metadata={} ) # Create session and wait for establishment session_context = await app.create_session_async(session_config, remote_name) await session_context.completion.wait_async() session = session_context.session # Send a request await session.publish_async( b"execute_tool: search(query='AI agents')", None, None ) # Get the response received_msg = await session.get_message_async( timeout=datetime.timedelta(seconds=30) ) result = received_msg.payload The session automatically: Discovers an available service instance. Establishes a secure MLS group (if enabled). Binds all subsequent communication to that instance. Provides reliable delivery with automatic retries. Group Sessions Ideal for multi-agent coordination, broadcast updates, and collaborative workflows: # Create a coordination channel session_config = slim_bindings.SessionConfig( session_type=slim_bindings.SessionType.GROUP, enable_mls=True, max_retries=5, interval=datetime.timedelta(seconds=5), metadata={} ) # Create group session and wait for establishment session_context = await app.create_session_async(session_config, channel_name) await session_context.completion.wait_async() session = session_context.session # Invite other agents to join handle = await session.invite_async(agent_name) await handle.wait_async() # Broadcast to all participants await session.publish_async( b"task_update: analysis complete", None, None ) Group sessions support: Many-to-many communication: all messages delivered to all participants. Dynamic membership: add or remove agents on the fly. Moderation: channel creators control membership. Secure group state: MLS ensures all members share cryptographic context. Intelligent Routing and Naming SLIM uses a hierarchical naming system based on Decentralized Identifiers (DIDs) that enables both human-readable names and secure, globally unique identifiers: organization/namespace/service/instance For example: agntcy/production/search-agent/a3f9k2 This structure supports: Anycast routing: send to org/ns/service and reach any available instance. Unicast routing: target a specific instance with the full four-part name. Service discovery: find agents by their organizational and service identifiers. Scalable forwarding: hierarchical names aggregate efficiently in routing tables. No more hardcoded endpoints or complex service registries. SLIM’s naming system just works. Low Latency Architecture SLIM is optimized for interactive AI workloads where every millisecond counts: Multiplexed streams: multiple logical streams over single connections. Efficient message routing: intelligent forwarding tables minimize hops. Zero-copy operations: where possible, avoid unnecessary data copying. Async-first design: non-blocking I/O throughout the stack. HTTP/2 transport: proven, efficient binary protocol. Real-world deployments show sub-second end-to-end latencies even with MLS encryption enabled. Developer-Friendly APIs SLIM provides high-quality bindings for multiple languages that abstract complexity. Here’s an example in Python: import slim_bindings import datetime # Initialize the global service service = slim_bindings.get_global_service() # Create a SLIM instance with shared secret auth local_name = slim_bindings.Name("org", "namespace", "my-agent") app = service.create_app_with_secret( local_name, "my-shared-secret" ) # Connect to SLIM node client_config = slim_bindings.new_insecure_client_config( "http://127.0.0.1:46357" ) conn_id = await service.connect_async(client_config) # Subscribe to receive messages await app.subscribe_async(local_name, conn_id) # Listen for incoming session session = await app.listen_for_session_async(None) # Receive and reply to messages received_msg = await session.get_message_async( timeout=datetime.timedelta(seconds=30) ) payload = received_msg.payload await session.publish_async(b"response", None, None) No need to manage MLS groups manually, handle connection pooling, or worry about encryption keys — SLIM’s session layer handles it all. Native RPC Support with SRPC Many agent-facing protocols (like A2A and MCP) are RPC-oriented. SLIM includes SRPC (SLIM RPC), which provides gRPC-like streaming RPC semantics directly over the SLIM transport. SRPC supports all four gRPC patterns: Unary → Unary — Simple request-response Unary → Stream — Server streaming responses Stream → Unary — Client streaming requests Stream → Stream — Bidirectional streaming Define services with Protocol Buffers: service AgentService { rpc ExecuteTool(ToolRequest) returns (ToolResponse); rpc StreamResults(Query) returns (stream Result); rpc CollectInputs(stream Input) returns (Summary); rpc Coordinate(stream Message) returns (stream Message); } The SLIM RPC compiler generates language-specific stubs that work seamlessly with SLIM’s secure messaging layer. You get familiar RPC semantics with SLIM’s security and group communication benefits. We are also studying how to extend these RPC interaction patterns to support group communication scenarios, enabling multi-agent RPC workflows. Single Implementation, Multiple Languages SLIM is implemented in Rust, providing a high-performance, memory-safe core that powers all language bindings. This approach ensures: Consistent behavior: all language bindings use the same battle-tested implementation. Performance: Rust’s zero-cost abstractions deliver native speed across all platforms. Security: Memory safety guarantees prevent entire classes of vulnerabilities. Maintainability: Bug fixes and improvements benefit all language ecosystems simultaneously. Currently Available: Python. Full-featured bindings with async/await support Go. Native Go bindings for seamless integration (see our technical blog posts on integrating Rust and Go with UniFFI and distributing C artifacts for Go modules) Coming Soon: C#. For .NET and Unity applications. JavaScript/TypeScript. Browser and Node.js support. Kotlin. Android and JVM applications. This multi-language strategy means you can build heterogeneous agent systems where different components use their most appropriate language, all communicating seamlessly through SLIM’s unified protocol. One of the key reasons SLIM provides multiple language bindings is to support agent communication protocols like A2A across different programming ecosystems. A2A comes with SDKs in multiple languages (Python, JavaScript, Java, C#, Go), and SLIM’s language bindings enable these A2A SDKs to communicate securely. An A2A Python agent can securely communicate with an A2A Java agent, with SLIM handling the underlying transport transparently. Real-World Use Cases SLIM + A2A: Agent-to-Agent Communication SLIM and A2A are designed to work together: A2A SDKs (Python, JavaScript, Java, C#, Go) implement agent-level semantics — task delegation, message formats, coordination patterns. SLIM bindings provide the secure transport, handling connection management, routing, encryption, and delivery guarantees. Currently, A2A sits on top of SLIM RPC (SRPC), enabling secure point-to-point agent interactions. We provide slim-a2a-python, a Python A2A SDK implementation built on SLIMRPC that enables developers to build A2A-compliant agents with SLIM as the transport layer. We are actively working to add A2A SDK support for the remaining SLIM language bindings (Go, C#, JavaScript/TypeScript, Kotlin). Looking ahead, A2A is evolving to support bidirectional streaming for real-time, interactive agent conversations and group communication for multi-agent coordination and collaboration. SLIM naturally provides both capabilities at the transport level through SRPC and MLS-based end-to-end encryption for groups, which many pub-sub systems lack. This positions SLIM as an ideal transport foundation for A2A’s continued evolution toward richer, more secure agent communication patterns. The following diagram illustrates how A2A and SLIM capabilities combine: graph LR subgraph Protocol["Protocol Layer (Application Semantics)"] direction TB A1["A2A Go SDK&lt;br/&gt;Task Delegation&lt;br/&gt;Coordination"] B1["A2A C# SDK&lt;br/&gt;Task Delegation&lt;br/&gt;Coordination"] C1["A2A Python SDK&lt;br/&gt;Task Delegation&lt;br/&gt;Coordination"] end subgraph Transport["Transport Layer (SLIM)"] direction TB subgraph Endpoints["Agent Endpoints"] A2["SLIM Go Bindings&lt;br/&gt;SRPC + Session + Data Plane"] B2["SLIM C# Bindings&lt;br/&gt;SRPC + Session + Data Plane"] C2["SLIM Python Bindings&lt;br/&gt;SRPC + Session + Data Plane"] end subgraph Network["SLIM Network Infrastructure"] SN1[SLIM Node 1&lt;br/&gt;Data Plane] SN2[SLIM Node 2&lt;br/&gt;Data Plane] end end A1 --&gt;|uses| A2 B1 --&gt;|uses| B2 C1 --&gt;|uses| C2 A2 -.-&gt;|"SRPC (P2P)&lt;br/&gt;encrypted via MLS"| SN1 B2 -.-&gt;|"SRPC (P2P)&lt;br/&gt;encrypted via MLS"| SN1 C2 -.-&gt;|"SRPC (Groups)&lt;br/&gt;encrypted via MLS"| SN2 SN1 -.-&gt;|routes| SN2 SN2 -.-&gt;|routes| SN1 style Protocol fill:#e8f4f8,stroke:#2980b9,stroke-width:3px style Transport fill:#fef5e7,stroke:#f39c12,stroke-width:3px style Endpoints fill:#fff,stroke:#95a5a6,stroke-width:2px style Network fill:#fff,stroke:#95a5a6,stroke-width:2px style A1 fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#fff style B1 fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#fff style C1 fill:#4a90e2,stroke:#2e5c8a,stroke-width:2px,color:#fff style A2 fill:#27ae60,stroke:#1e8449,stroke-width:2px,color:#fff style B2 fill:#27ae60,stroke:#1e8449,stroke-width:2px,color:#fff style C2 fill:#27ae60,stroke:#1e8449,stroke-width:2px,color:#fff style SN1 fill:#f39c12,stroke:#d68910,stroke-width:2px,color:#fff style SN2 fill:#f39c12,stroke:#d68910,stroke-width:2px,color:#fff Architecture Overview: Protocol Layer (blue, left): A2A SDKs implement agent-level semantics — defining what agents say through standardized task delegation, message formats, and coordination patterns. Transport Layer (green/orange, right): SLIM provides the secure infrastructure for how messages are delivered: SLIM Bindings (green): Include SRPC for RPC semantics, Session Layer for reliability and encryption, and Data Plane client for connectivity. SLIM Network (orange): Pure data plane nodes route encrypted messages without understanding agent semantics. Communication Patterns: Current: A2A uses SRPC for point-to-point (P2P) agent interactions with MLS encryption. Future: SLIM naturally provides SRPC group capabilities and MLS-based group encryption, ready to support A2A’s evolution toward groups and bidirectional streaming. SLIM also provides the transport layer for other agent communication protocols: MCP (Model Context Protocol) for context sharing between agents and tools. Custom protocols for domain-specific agent interactions. For more information about A2A, see the official A2A documentation. Getting Started Installation Install the SLIM node: # Docker docker pull ghcr.io/agntcy/slim:v1.0.0 # Cargo cargo install agntcy-slim # Helm (Kubernetes) helm pull oci://ghcr.io/agntcy/slim/helm/slim --version v1.0.0 Install Python bindings: pip install slim-bindings Quick Example Create your first SLIM agent in minutes: import asyncio import slim_bindings import datetime async def main(): # Initialize the global service service = slim_bindings.get_global_service() # Create app with shared secret authentication local_name = slim_bindings.Name("org", "namespace", "agent") app = service.create_app_with_secret(local_name, "my-shared-secret") # Connect to SLIM node client_config = slim_bindings.new_insecure_client_config( "http://127.0.0.1:46357" ) conn_id = await service.connect_async(client_config) # Subscribe to local name await app.subscribe_async(local_name, conn_id) # Listen for incoming session session = await app.listen_for_session_async(None) # Handle messages while True: received_msg = await session.get_message_async( timeout=datetime.timedelta(seconds=30) ) payload = received_msg.payload print(f"Received: {payload.decode()}") await session.publish_async(b"Acknowledged: " + payload, None, None) asyncio.run(main()) The Road Ahead SLIM is being actively developed as an open-source project under the Apache 2.0 license. The protocol specification has been submitted as an Internet-Draft to the IETF, reflecting its ambition to become a standard for agentic AI communication. Current capabilities include: Rust core with multi-language bindings (Python, Go, and more coming) MLS-based end-to-end encryption Point-to-point and group sessions SRPC for RPC patterns Control plane for management Kubernetes deployment support The community is working on: Additional language bindings (C#, JavaScript/TypeScript, Kotlin) Enhanced monitoring and observability Integration with popular AI frameworks Extended A2A support for groups and bidirectional streaming Reference implementations of MCP over SLIM Performance optimizations Why SLIM Matters As AI systems evolve from monolithic models to distributed networks of specialized agents, the communication infrastructure becomes critical. SLIM provides a foundation that: Ensures security: end-to-end encryption without complexity. Scales with demand: from prototype to production. Simplifies development: intuitive APIs for complex capabilities. Future-proofs architecture: standards-based, extensible design. Whether you’re building a multi-agent AI system, an agentic workflow platform, or the next generation of AI applications, SLIM provides the messaging infrastructure you need. Get Involved SLIM is open source and welcomes contributions: GitHub Documentation Specification You can find comprehensive Python and Go examples in the GitHub repository. Join the community building the communication layer for agentic AI. Try SLIM in your next project with our language binding examples, contribute to the codebase, or share your use cases. The future of AI is collaborative, distributed, and secure. SLIM makes it possible. SLIM is developed by AGNTCY Contributors and released under the Apache 2.0 License.]]></summary></entry><entry><title type="html">Integrating Rust and Go with UniFFI: A Complete Guide</title><link href="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/01/27/integrating-rust-and-go-with-uniffi.html" rel="alternate" type="text/html" title="Integrating Rust and Go with UniFFI: A Complete Guide" /><published>2026-01-27T15:50:00+00:00</published><updated>2026-01-27T15:50:00+00:00</updated><id>https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/01/27/integrating-rust-and-go-with-uniffi</id><content type="html" xml:base="https://blogs.agntcy.org/drafts/docs-directory-oidc-auth/technical/2026/01/27/integrating-rust-and-go-with-uniffi.html"><![CDATA[<p>Rust and Go are both excellent languages, but they excel in different areas. Rust provides memory safety and zero-cost abstractions, while Go offers simplicity and fast compilation. What if you could combine the strengths of both? This post explores how we use UniFFI to create seamless Rust-Go integrations for the SLIM messaging library.</p>

<!--more-->

<h2 id="why-integrate-rust-and-go">Why Integrate Rust and Go?</h2>

<p>Before diving into the technical details, let’s understand when and why you might want to integrate Rust with Go:</p>

<p><strong>Rust’s Strengths:</strong></p>
<ul>
  <li>Memory safety without garbage collection</li>
  <li>Zero-cost abstractions</li>
  <li>Excellent cryptography libraries</li>
  <li>Strong type system with powerful error handling</li>
  <li>Performance-critical operations</li>
</ul>

<p><strong>Go’s Strengths:</strong></p>
<ul>
  <li>Simple, readable syntax</li>
  <li>Fast compilation times</li>
  <li>Excellent concurrency model</li>
  <li>Great standard library for networking and HTTP</li>
  <li>Easy deployment (single binary)</li>
</ul>

<p><strong>When to Integrate:</strong></p>
<ul>
  <li>You have performance-critical code that benefits from Rust’s zero-cost abstractions</li>
  <li>You need cryptographic operations best handled by Rust’s mature ecosystem</li>
  <li>You want Go’s deployment simplicity but need Rust’s safety guarantees</li>
  <li>You’re building a library that needs to support multiple languages</li>
</ul>

<h2 id="the-challenge-foreign-function-interface-ffi">The Challenge: Foreign Function Interface (FFI)</h2>

<p>Calling Rust from Go requires crossing the FFI boundary through C. The traditional approach involves:</p>

<ol>
  <li>Writing a C-compatible API in Rust using <code class="language-plaintext highlighter-rouge">extern "C"</code></li>
  <li>Creating C header files manually</li>
  <li>Writing Go code with CGO to call the C functions</li>
  <li>Manual memory management at the boundary</li>
  <li>Custom error handling and type conversions</li>
</ol>

<p>This is tedious, error-prone, and requires maintaining three separate codebases (Rust, C headers, and Go bindings).</p>

<h2 id="enter-uniffi-automated-binding-generation">Enter UniFFI: Automated Binding Generation</h2>

<p><a href="https://mozilla.github.io/uniffi-rs/">UniFFI</a> (Unified Foreign Function Interface) is Mozilla’s solution to this problem. Instead of manually writing FFI code, you:</p>

<ol>
  <li>Write your Rust library with normal Rust types</li>
  <li>Define an interface using UniFFI’s macro system</li>
  <li>UniFFI automatically generates:
    <ul>
      <li>C-compatible FFI layer</li>
      <li>C header files</li>
      <li>Language bindings (Go, Python, Swift, Kotlin, etc.)</li>
    </ul>
  </li>
</ol>

<p>For Go specifically, we use <a href="https://github.com/NordSecurity/uniffi-bindgen-go">uniffi-bindgen-go</a>, a Go binding generator maintained by NordSecurity.</p>

<h2 id="our-implementation-slim-go-bindings">Our Implementation: SLIM Go Bindings</h2>

<p>Let’s walk through how we integrated Rust and Go for the <a href="https://github.com/agntcy/slim">SLIM messaging library</a>.</p>

<h3 id="step-1-define-the-rust-api">Step 1: Define the Rust API</h3>

<p>First, we write our Rust library with normal Rust types and use UniFFI macros to expose the API:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// src/lib.rs</span>
<span class="k">use</span> <span class="n">uniffi</span><span class="p">;</span>

<span class="nd">#[uniffi::export]</span>
<span class="k">pub</span> <span class="k">fn</span> <span class="nf">create_identity</span><span class="p">(</span><span class="n">name</span><span class="p">:</span> <span class="nb">String</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="n">Identity</span><span class="p">,</span> <span class="n">SlimError</span><span class="o">&gt;</span> <span class="p">{</span>
    <span class="c1">// Implementation using normal Rust code</span>
    <span class="nn">Identity</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="n">name</span><span class="p">)</span>
<span class="p">}</span>

<span class="nd">#[derive(uniffi::Object)]</span>
<span class="k">pub</span> <span class="k">struct</span> <span class="n">Identity</span> <span class="p">{</span>
    <span class="n">inner</span><span class="p">:</span> <span class="nb">Arc</span><span class="o">&lt;</span><span class="n">IdentityInner</span><span class="o">&gt;</span><span class="p">,</span>
<span class="p">}</span>

<span class="nd">#[uniffi::export]</span>
<span class="k">impl</span> <span class="n">Identity</span> <span class="p">{</span>
    <span class="k">pub</span> <span class="k">fn</span> <span class="nf">public_key</span><span class="p">(</span><span class="o">&amp;</span><span class="k">self</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">u8</span><span class="o">&gt;</span> <span class="p">{</span>
        <span class="k">self</span><span class="py">.inner.public_key</span><span class="nf">.clone</span><span class="p">()</span>
    <span class="p">}</span>
    
    <span class="k">pub</span> <span class="k">fn</span> <span class="nf">sign</span><span class="p">(</span><span class="o">&amp;</span><span class="k">self</span><span class="p">,</span> <span class="n">data</span><span class="p">:</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">u8</span><span class="o">&gt;</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">u8</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">SlimError</span><span class="o">&gt;</span> <span class="p">{</span>
        <span class="k">self</span><span class="py">.inner</span><span class="nf">.sign</span><span class="p">(</span><span class="o">&amp;</span><span class="n">data</span><span class="p">)</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="nd">#[derive(uniffi::Error)]</span>
<span class="k">pub</span> <span class="k">enum</span> <span class="n">SlimError</span> <span class="p">{</span>
    <span class="n">InvalidInput</span> <span class="p">{</span> <span class="n">message</span><span class="p">:</span> <span class="nb">String</span> <span class="p">},</span>
    <span class="n">CryptoError</span> <span class="p">{</span> <span class="n">message</span><span class="p">:</span> <span class="nb">String</span> <span class="p">},</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Key Points:</strong></p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">#[uniffi::export]</code> marks functions for export</li>
  <li><code class="language-plaintext highlighter-rouge">#[derive(uniffi::Object)]</code> for complex types that need methods</li>
  <li><code class="language-plaintext highlighter-rouge">#[derive(uniffi::Error)]</code> for error enums</li>
  <li>Use standard Rust types like <code class="language-plaintext highlighter-rouge">String</code>, <code class="language-plaintext highlighter-rouge">Vec&lt;u8&gt;</code>, <code class="language-plaintext highlighter-rouge">Result</code></li>
  <li>UniFFI reads the Rust macros directly—no separate <code class="language-plaintext highlighter-rouge">.udl</code> file needed!</li>
</ul>

<h3 id="step-2-configure-the-rust-crate-to-use-uniffi">Step 2: Configure the Rust Crate to Use UniFFI</h3>

<p>Add UniFFI to your <code class="language-plaintext highlighter-rouge">Cargo.toml</code> with the CLI feature:</p>

<div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[lib]</span>
<span class="py">crate-type</span> <span class="p">=</span> <span class="nn">["staticlib"]</span>
<span class="py">name</span> <span class="p">=</span> <span class="s">"slim_bindings"</span>

<span class="nn">[dependencies]</span>
<span class="nn">uniffi</span> <span class="o">=</span> <span class="p">{</span> <span class="py">version</span> <span class="p">=</span> <span class="s">"0.28.3"</span><span class="p">,</span> <span class="py">features</span> <span class="p">=</span> <span class="nn">["cli"]</span> <span class="p">}</span>

<span class="nn">[dev-dependencies]</span>
<span class="nn">uniffi</span> <span class="o">=</span> <span class="p">{</span> <span class="py">version</span> <span class="p">=</span> <span class="s">"0.28.3"</span><span class="p">,</span> <span class="py">features</span> <span class="p">=</span> <span class="nn">["build"]</span> <span class="p">}</span>
</code></pre></div></div>

<p><strong>Important:</strong> We use <code class="language-plaintext highlighter-rouge">crate-type = ["staticlib"]</code> to produce a static library (<code class="language-plaintext highlighter-rouge">.a</code> archive) rather than a dynamic library. This allows the Go compiler to statically link the native code into the final binary, ensuring there are no external runtime dependencies.</p>

<p>Then add the scaffolding macro to your <code class="language-plaintext highlighter-rouge">lib.rs</code>:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// src/lib.rs</span>
<span class="nn">uniffi</span><span class="p">::</span><span class="nd">setup_scaffolding!</span><span class="p">();</span>
</code></pre></div></div>

<p>The <code class="language-plaintext highlighter-rouge">setup_scaffolding!()</code> macro generates all the FFI glue code at compile time—no build script needed when using proc-macros!</p>

<h3 id="step-3-build-the-rust-library">Step 3: Build the Rust Library</h3>

<p>Compile the Rust library to produce the static archive:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Build in release mode for optimal performance</span>
cargo build <span class="nt">--release</span>
</code></pre></div></div>

<p>This produces <code class="language-plaintext highlighter-rouge">target/release/libslim_bindings.a</code> - a static library archive that can be linked directly into Go binaries. Static linking means:</p>
<ul>
  <li><strong>At build time</strong>: The Go compiler embeds the native code into your binary</li>
  <li><strong>At runtime</strong>: No external <code class="language-plaintext highlighter-rouge">.so</code>, <code class="language-plaintext highlighter-rouge">.dylib</code>, or <code class="language-plaintext highlighter-rouge">.dll</code> files are needed</li>
  <li><strong>For end users</strong>: Just distribute a single, self-contained executable</li>
</ul>

<h3 id="step-4-generate-go-bindings">Step 4: Generate Go Bindings</h3>

<p>Use <code class="language-plaintext highlighter-rouge">uniffi-bindgen-go</code> to generate the Go code:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Install uniffi-bindgen-go</span>
cargo <span class="nb">install </span>uniffi-bindgen-go

<span class="c"># Generate Go bindings</span>
uniffi-bindgen-go <span class="se">\</span>
  <span class="nt">--library</span> target/release/libslim_bindings.a <span class="se">\</span>
  <span class="nt">--out-dir</span> ./bindings/go
</code></pre></div></div>

<p>UniFFI reads the macros directly from your Rust code to generate the bindings—no separate definition file required!</p>

<p>This generates two files:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">slim_bindings.go</code> - Go wrapper code</li>
  <li><code class="language-plaintext highlighter-rouge">slim_bindings.h</code> - C header file</li>
</ul>

<h3 id="step-5-distribution-strategy">Step 5: Distribution Strategy</h3>

<p>Now that you have the static library (<code class="language-plaintext highlighter-rouge">.a</code> file) and Go bindings, you need to distribute them to developers. This presents a challenge:</p>

<ul>
  <li>The static library is ~150 MB per platform</li>
  <li>GitHub has a 100 MB file size limit (without Git LFS)</li>
  <li><code class="language-plaintext highlighter-rouge">go get</code> doesn’t support Git LFS</li>
  <li><code class="language-plaintext highlighter-rouge">go get</code> has a hard-coded 500 MB size limit</li>
</ul>

<p><strong>Solution:</strong> Use GitHub Releases to host the pre-compiled static libraries, and provide a setup tool that downloads the appropriate library for the developer’s platform.</p>

<p>We cover this approach in detail in our blog post: <a href="/2026/01/16/distributing-c-artifacts-for-go-modules.html">Distributing C Artifacts for Go Modules</a></p>

<p>Key points:</p>
<ul>
  <li>Host static libraries on GitHub Releases (not in the Git repository)</li>
  <li>Provide a lightweight setup tool developers run once</li>
  <li>Cache libraries in <code class="language-plaintext highlighter-rouge">$GOPATH/.cgo-cache/</code> for CGO to find</li>
  <li>Configure CGO linker flags to reference the cached library</li>
</ul>

<h3 id="step-6-using-the-go-bindings">Step 6: Using the Go Bindings</h3>

<p>The generated Go code provides an idiomatic Go API. Here’s a complete example showing SLIM message encryption:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">package</span> <span class="n">main</span>

<span class="k">import</span> <span class="p">(</span>
    <span class="s">"fmt"</span>
    <span class="n">slim</span> <span class="s">"github.com/agntcy/slim-bindings-go"</span>
<span class="p">)</span>

<span class="k">func</span> <span class="n">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="c">// Create two identities</span>
    <span class="n">alice</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">slim</span><span class="o">.</span><span class="n">CreateIdentity</span><span class="p">(</span><span class="s">"Alice"</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="k">defer</span> <span class="n">alice</span><span class="o">.</span><span class="n">Destroy</span><span class="p">()</span> <span class="c">// Important: cleanup Rust resources</span>
    
    <span class="n">bob</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">slim</span><span class="o">.</span><span class="n">CreateIdentity</span><span class="p">(</span><span class="s">"Bob"</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="k">defer</span> <span class="n">bob</span><span class="o">.</span><span class="n">Destroy</span><span class="p">()</span>
    
    <span class="c">// Get public keys</span>
    <span class="n">alicePubKey</span> <span class="o">:=</span> <span class="n">alice</span><span class="o">.</span><span class="n">PublicKey</span><span class="p">()</span>
    <span class="n">bobPubKey</span> <span class="o">:=</span> <span class="n">bob</span><span class="o">.</span><span class="n">PublicKey</span><span class="p">()</span>
    <span class="n">fmt</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"Alice public key: %x</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">alicePubKey</span><span class="p">)</span>
    <span class="n">fmt</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"Bob public key: %x</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">bobPubKey</span><span class="p">)</span>
    
    <span class="c">// Alice creates an encrypted message for Bob</span>
    <span class="n">message</span> <span class="o">:=</span> <span class="p">[]</span><span class="kt">byte</span><span class="p">(</span><span class="s">"Secret message"</span><span class="p">)</span>
    <span class="n">encrypted</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">alice</span><span class="o">.</span><span class="n">Encrypt</span><span class="p">(</span><span class="n">message</span><span class="p">,</span> <span class="n">bobPubKey</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>
    
    <span class="c">// Bob decrypts the message</span>
    <span class="n">decrypted</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">bob</span><span class="o">.</span><span class="n">Decrypt</span><span class="p">(</span><span class="n">encrypted</span><span class="p">,</span> <span class="n">alicePubKey</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>
    
    <span class="n">fmt</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"Original:  %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">message</span><span class="p">)</span>
    <span class="n">fmt</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"Decrypted: %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">decrypted</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Key Features:</strong></p>
<ul>
  <li><strong>Idiomatic Go error handling</strong> with the <code class="language-plaintext highlighter-rouge">error</code> interface</li>
  <li><strong>Automatic type conversions</strong> (Rust <code class="language-plaintext highlighter-rouge">Vec&lt;u8&gt;</code> ↔ Go <code class="language-plaintext highlighter-rouge">[]byte</code>)</li>
  <li><strong>Memory management</strong> via <code class="language-plaintext highlighter-rouge">Destroy()</code> method—call this to free Rust resources</li>
  <li><strong>High-level API</strong> that hides all FFI complexity</li>
</ul>

<p>This high-level Go code is calling Rust crypto functions under the hood, with all the FFI complexity handled by UniFFI.</p>

<h2 id="how-uniffi-works-under-the-hood">How UniFFI Works: Under the Hood</h2>

<p>Let’s understand what UniFFI generates:</p>

<h3 id="the-rust-side-generated-by-uniffi">The Rust Side (Generated by UniFFI)</h3>

<p>UniFFI generates a C-compatible FFI layer:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Generated by UniFFI</span>
<span class="nd">#[no_mangle]</span>
<span class="k">pub</span> <span class="k">extern</span> <span class="s">"C"</span> <span class="k">fn</span> <span class="nf">slim_create_identity</span><span class="p">(</span>
    <span class="n">name</span><span class="p">:</span> <span class="n">RustBuffer</span><span class="p">,</span>
    <span class="n">err</span><span class="p">:</span> <span class="o">&amp;</span><span class="k">mut</span> <span class="n">RustCallStatus</span><span class="p">,</span>
<span class="p">)</span> <span class="k">-&gt;</span> <span class="o">*</span><span class="k">const</span> <span class="n">Identity</span> <span class="p">{</span>
    <span class="c1">// Conversion logic from C types to Rust types</span>
    <span class="c1">// Calls your actual Rust function</span>
    <span class="c1">// Converts result back to C-compatible types</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="the-go-side-generated-by-uniffi-bindgen-go">The Go Side (Generated by uniffi-bindgen-go)</h3>

<p>The Go bindings wrap the C functions:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// Generated by uniffi-bindgen-go</span>

<span class="c">// #include "slim_bindings.h"</span>
<span class="c">// #cgo LDFLAGS: -lslim_bindings</span>
<span class="k">import</span> <span class="s">"C"</span>

<span class="k">type</span> <span class="n">Identity</span> <span class="k">struct</span> <span class="p">{</span>
    <span class="n">pointer</span> <span class="n">unsafe</span><span class="o">.</span><span class="n">Pointer</span>
<span class="p">}</span>

<span class="k">func</span> <span class="n">CreateIdentity</span><span class="p">(</span><span class="n">name</span> <span class="kt">string</span><span class="p">)</span> <span class="p">(</span><span class="o">*</span><span class="n">Identity</span><span class="p">,</span> <span class="kt">error</span><span class="p">)</span> <span class="p">{</span>
    <span class="c">// Convert Go string to C string</span>
    <span class="n">cName</span> <span class="o">:=</span> <span class="n">C</span><span class="o">.</span><span class="n">CString</span><span class="p">(</span><span class="n">name</span><span class="p">)</span>
    <span class="k">defer</span> <span class="n">C</span><span class="o">.</span><span class="n">free</span><span class="p">(</span><span class="n">unsafe</span><span class="o">.</span><span class="n">Pointer</span><span class="p">(</span><span class="n">cName</span><span class="p">))</span>
    
    <span class="c">// Call C function</span>
    <span class="k">var</span> <span class="n">err</span> <span class="n">C</span><span class="o">.</span><span class="n">RustCallStatus</span>
    <span class="n">ptr</span> <span class="o">:=</span> <span class="n">C</span><span class="o">.</span><span class="n">slim_create_identity</span><span class="p">(</span><span class="n">cName</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">err</span><span class="p">)</span>
    
    <span class="c">// Handle errors</span>
    <span class="k">if</span> <span class="n">err</span><span class="o">.</span><span class="n">code</span> <span class="o">!=</span> <span class="m">0</span> <span class="p">{</span>
        <span class="k">return</span> <span class="no">nil</span><span class="p">,</span> <span class="n">convertError</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
    <span class="p">}</span>
    
    <span class="k">return</span> <span class="o">&amp;</span><span class="n">Identity</span><span class="p">{</span><span class="n">pointer</span><span class="o">:</span> <span class="n">ptr</span><span class="p">},</span> <span class="no">nil</span>
<span class="p">}</span>

<span class="k">func</span> <span class="p">(</span><span class="n">i</span> <span class="o">*</span><span class="n">Identity</span><span class="p">)</span> <span class="n">Destroy</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">C</span><span class="o">.</span><span class="n">slim_identity_free</span><span class="p">(</span><span class="n">i</span><span class="o">.</span><span class="n">pointer</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="memory-management">Memory Management</h3>

<p>UniFFI handles the complex memory management at the boundary:</p>

<ul>
  <li><strong>Rust → Go</strong>: Rust allocates, Go receives pointer, Go calls <code class="language-plaintext highlighter-rouge">Destroy()</code> to free</li>
  <li><strong>Go → Rust</strong>: Go data is copied into Rust-owned memory</li>
  <li><strong>Return values</strong>: Rust allocates, transfers ownership to Go</li>
</ul>

<p>The generated <code class="language-plaintext highlighter-rouge">Destroy()</code> method is crucial—it tells Rust to free its memory. Without it, you’d have memory leaks.</p>

<h2 id="advantages-of-the-uniffi-approach">Advantages of the UniFFI Approach</h2>

<ol>
  <li><strong>Type Safety</strong>: UniFFI generates type-safe bindings automatically</li>
  <li><strong>Error Handling</strong>: Rust <code class="language-plaintext highlighter-rouge">Result</code> types map to Go <code class="language-plaintext highlighter-rouge">error</code> naturally</li>
  <li><strong>Multiple Languages</strong>: Same Rust code can target Go, Python, Swift, Kotlin</li>
  <li><strong>Maintainability</strong>: Changes to Rust API automatically reflect in bindings</li>
  <li><strong>Idiomatic APIs</strong>: Generated code feels natural in each target language</li>
  <li><strong>Memory Safety</strong>: Automatic handling of ownership and lifetimes</li>
</ol>

<h2 id="challenges-and-limitations">Challenges and Limitations</h2>

<ol>
  <li><strong>Learning Curve</strong>: Understanding UniFFI’s macro system takes time</li>
  <li><strong>Build Complexity</strong>: Requires cargo, uniffi-bindgen-go, and CGO toolchain</li>
  <li><strong>Memory Management</strong>: Developers must remember to call <code class="language-plaintext highlighter-rouge">Destroy()</code></li>
  <li><strong>Type Limitations</strong>: Not all Rust types can cross FFI boundary (e.g., traits, complex generics)</li>
  <li><strong>Debugging</strong>: Stack traces cross language boundaries</li>
  <li><strong>Binary Size</strong>: Static linking increases final binary size</li>
</ol>

<h2 id="special-cases">Special Cases</h2>

<h3 id="native-async-functions">Native Async Functions</h3>

<p>UniFFI supports exposing Rust async functions through the FFI interface. The key thing to understand is that these async functions use the <strong>binding language’s async runtime</strong>, not Rust’s:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">#[uniffi::export]</span>
<span class="k">pub</span> <span class="k">async</span> <span class="k">fn</span> <span class="nf">fetch_data</span><span class="p">(</span><span class="n">url</span><span class="p">:</span> <span class="nb">String</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">u8</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">SlimError</span><span class="o">&gt;</span> <span class="p">{</span>
    <span class="c1">// This async function will be translated to the target language</span>
    <span class="nf">perform_http_request</span><span class="p">(</span><span class="n">url</span><span class="p">)</span><span class="k">.await</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>How it translates across languages:</strong></p>

<ul>
  <li><strong>Python</strong>: Directly becomes an awaitable function that integrates with Python’s <code class="language-plaintext highlighter-rouge">asyncio</code>
    <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">data</span> <span class="o">=</span> <span class="k">await</span> <span class="n">fetch_data</span><span class="p">(</span><span class="s">"https://example.com"</span><span class="p">)</span>
</code></pre></div>    </div>
  </li>
  <li><strong>Go</strong>: Becomes a regular blocking function (Go doesn’t have native async/await)
    <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">data</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">FetchData</span><span class="p">(</span><span class="s">"https://example.com"</span><span class="p">)</span> <span class="c">// Blocks until complete</span>
  
<span class="c">// To make it non-blocking, wrap it in a goroutine</span>
<span class="k">go</span> <span class="k">func</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">data</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">FetchData</span><span class="p">(</span><span class="s">"https://example.com"</span><span class="p">)</span>
    <span class="c">// Handle result...</span>
<span class="p">}()</span>
</code></pre></div>    </div>
  </li>
</ul>

<p><strong>Important Note About Futures:</strong></p>

<p>Rust <code class="language-plaintext highlighter-rouge">Future</code> types don’t translate well to Go. If you need to expose a custom future, you must provide a <code class="language-plaintext highlighter-rouge">.Wait()</code> method that handles the awaiting internally:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">#[derive(uniffi::Object)]</span>
<span class="k">pub</span> <span class="k">struct</span> <span class="n">DataFuture</span> <span class="p">{</span>
    <span class="n">inner</span><span class="p">:</span> <span class="cm">/* your future implementation */</span>
<span class="p">}</span>

<span class="nd">#[uniffi::export]</span>
<span class="k">impl</span> <span class="n">DataFuture</span> <span class="p">{</span>
    <span class="c1">// Provide an async wait method that UniFFI will translate</span>
    <span class="k">pub</span> <span class="k">async</span> <span class="k">fn</span> <span class="nf">wait</span><span class="p">(</span><span class="o">&amp;</span><span class="k">self</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">u8</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">SlimError</span><span class="o">&gt;</span> <span class="p">{</span>
        <span class="k">self</span><span class="py">.inner</span><span class="k">.await</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Then in Go:</p>
<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">future</span> <span class="o">:=</span> <span class="n">CreateDataFuture</span><span class="p">(</span><span class="n">url</span><span class="p">)</span>
<span class="k">defer</span> <span class="n">future</span><span class="o">.</span><span class="n">Destroy</span><span class="p">()</span>
<span class="n">result</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">future</span><span class="o">.</span><span class="n">Wait</span><span class="p">()</span> <span class="c">// Blocks until complete</span>
</code></pre></div></div>

<h3 id="tokio-and-external-async-runtimes">Tokio and External Async Runtimes</h3>

<p>If your Rust code uses an external async runtime like <strong>Tokio</strong>, you need to handle it differently. The runtime must live on the Rust side of the boundary, and you need to explicitly spawn or block on tasks:</p>

<div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">use</span> <span class="nn">tokio</span><span class="p">::</span><span class="nn">runtime</span><span class="p">::</span><span class="n">Runtime</span><span class="p">;</span>
<span class="k">use</span> <span class="nn">std</span><span class="p">::</span><span class="nn">sync</span><span class="p">::</span><span class="nb">Arc</span><span class="p">;</span>

<span class="nd">#[derive(uniffi::Object)]</span>
<span class="k">pub</span> <span class="k">struct</span> <span class="n">AsyncClient</span> <span class="p">{</span>
    <span class="n">runtime</span><span class="p">:</span> <span class="nb">Arc</span><span class="o">&lt;</span><span class="n">Runtime</span><span class="o">&gt;</span><span class="p">,</span>
    <span class="c1">// other fields...</span>
<span class="p">}</span>

<span class="nd">#[uniffi::export]</span>
<span class="k">impl</span> <span class="n">AsyncClient</span> <span class="p">{</span>
    <span class="nd">#[uniffi::constructor]</span>
    <span class="k">pub</span> <span class="k">fn</span> <span class="nf">new</span><span class="p">()</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="nb">Arc</span><span class="o">&lt;</span><span class="k">Self</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">SlimError</span><span class="o">&gt;</span> <span class="p">{</span>
        <span class="k">let</span> <span class="n">runtime</span> <span class="o">=</span> <span class="nn">Runtime</span><span class="p">::</span><span class="nf">new</span><span class="p">()</span>
            <span class="nf">.map_err</span><span class="p">(|</span><span class="n">e</span><span class="p">|</span> <span class="nn">SlimError</span><span class="p">::</span><span class="n">RuntimeError</span> <span class="p">{</span> 
                <span class="n">message</span><span class="p">:</span> <span class="n">e</span><span class="nf">.to_string</span><span class="p">()</span> 
            <span class="p">})</span><span class="o">?</span><span class="p">;</span>
        
        <span class="nf">Ok</span><span class="p">(</span><span class="nn">Arc</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="k">Self</span> <span class="p">{</span>
            <span class="n">runtime</span><span class="p">:</span> <span class="nn">Arc</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="n">runtime</span><span class="p">),</span>
        <span class="p">}))</span>
    <span class="p">}</span>
    
    <span class="c1">// Blocking operation that uses the Tokio runtime</span>
    <span class="k">pub</span> <span class="k">fn</span> <span class="nf">fetch_data</span><span class="p">(</span><span class="o">&amp;</span><span class="k">self</span><span class="p">,</span> <span class="n">url</span><span class="p">:</span> <span class="nb">String</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">u8</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">SlimError</span><span class="o">&gt;</span> <span class="p">{</span>
        <span class="c1">// Use block_on to await the async operation</span>
        <span class="k">self</span><span class="py">.runtime</span><span class="nf">.block_on</span><span class="p">(</span><span class="k">async</span> <span class="p">{</span>
            <span class="nf">perform_async_request</span><span class="p">(</span><span class="o">&amp;</span><span class="n">url</span><span class="p">)</span><span class="k">.await</span>
        <span class="p">})</span>
    <span class="p">}</span>
    
    <span class="c1">// Fire-and-forget background task</span>
    <span class="k">pub</span> <span class="k">fn</span> <span class="nf">start_background_task</span><span class="p">(</span><span class="o">&amp;</span><span class="k">self</span><span class="p">,</span> <span class="n">interval_ms</span><span class="p">:</span> <span class="nb">u64</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="p">(),</span> <span class="n">SlimError</span><span class="o">&gt;</span> <span class="p">{</span>
        <span class="k">let</span> <span class="n">runtime</span> <span class="o">=</span> <span class="k">self</span><span class="py">.runtime</span><span class="nf">.clone</span><span class="p">();</span>
        
        <span class="c1">// Spawn a task on the Tokio runtime</span>
        <span class="n">runtime</span><span class="nf">.spawn</span><span class="p">(</span><span class="k">async</span> <span class="k">move</span> <span class="p">{</span>
            <span class="k">loop</span> <span class="p">{</span>
                <span class="nn">tokio</span><span class="p">::</span><span class="nn">time</span><span class="p">::</span><span class="nf">sleep</span><span class="p">(</span>
                    <span class="nn">tokio</span><span class="p">::</span><span class="nn">time</span><span class="p">::</span><span class="nn">Duration</span><span class="p">::</span><span class="nf">from_millis</span><span class="p">(</span><span class="n">interval_ms</span><span class="p">)</span>
                <span class="p">)</span><span class="k">.await</span><span class="p">;</span>
                <span class="c1">// Do background work...</span>
            <span class="p">}</span>
        <span class="p">});</span>
        
        <span class="nf">Ok</span><span class="p">(())</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Key points for Tokio integration:</strong></p>

<ol>
  <li><strong>Create the runtime once</strong>: Store a reference to the Tokio runtime in your Rust object</li>
  <li><strong>Use <code class="language-plaintext highlighter-rouge">block_on</code></strong>: For synchronous operations that need to await async code</li>
  <li><strong>Use <code class="language-plaintext highlighter-rouge">spawn</code></strong>: For background tasks that shouldn’t block the caller</li>
  <li><strong>Runtime lifecycle</strong>: The runtime lives as long as your Rust object exists</li>
  <li><strong>Go integration</strong>: From Go’s perspective, these are just regular blocking functions</li>
</ol>

<p>Example usage in Go:</p>
<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">client</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">NewAsyncClient</span><span class="p">()</span>
<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
    <span class="nb">panic</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
<span class="p">}</span>
<span class="k">defer</span> <span class="n">client</span><span class="o">.</span><span class="n">Destroy</span><span class="p">()</span>

<span class="c">// This blocks until the async operation completes</span>
<span class="n">data</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">client</span><span class="o">.</span><span class="n">FetchData</span><span class="p">(</span><span class="s">"https://example.com"</span><span class="p">)</span>

<span class="c">// This starts a background task and returns immediately</span>
<span class="n">err</span> <span class="o">=</span> <span class="n">client</span><span class="o">.</span><span class="n">StartBackgroundTask</span><span class="p">(</span><span class="m">1000</span><span class="p">)</span>
</code></pre></div></div>

<p>The Tokio runtime handles all the async execution internally, while Go sees only synchronous function calls.</p>

<h2 id="performance-considerations">Performance Considerations</h2>

<p>Crossing the FFI boundary through CGO has overhead. While the exact performance impact depends on your use case, there are some general guidelines:</p>

<p><strong>Key Considerations:</strong></p>
<ul>
  <li><strong>Function call overhead</strong>: Each FFI call has a cost beyond a regular function call</li>
  <li><strong>Data copying</strong>: Data must be copied across the language boundary</li>
  <li><strong>Avoid chatty APIs</strong>: Design your API to minimize the number of FFI calls</li>
</ul>

<p><strong>Optimization tips:</strong></p>
<ul>
  <li>Batch operations when possible instead of making many small calls</li>
  <li>Keep data structures simple at the boundary</li>
  <li>Use byte slices (<code class="language-plaintext highlighter-rouge">Vec&lt;u8&gt;</code>) for efficient data transfer</li>
  <li>Do heavy processing on one side of the boundary, not back-and-forth</li>
</ul>

<p>For a detailed analysis of CGO performance overhead, see Shane O’Neill’s excellent post: <a href="https://shane.ai/posts/cgo-performance-in-go1.21/">CGO Performance in Go 1.21</a></p>

<h2 id="build-and-distribution">Build and Distribution</h2>

<p>For production use, you’ll need to:</p>

<ol>
  <li><strong>Cross-compile Rust libraries</strong> for target platforms</li>
  <li><strong>Distribute static libraries</strong> (see our <a href="https://blog.agntcy.com/2026/01/16/distributing-c-artifacts-for-go-modules.html">previous blog post</a>)</li>
  <li><strong>Configure CGO</strong> to link against the Rust library</li>
  <li><strong>Handle platform-specific differences</strong></li>
</ol>

<p>We cover this in detail in our blog post about <a href="https://blog.agntcy.com/2026/01/16/distributing-c-artifacts-for-go-modules.html">distributing C artifacts for Go modules</a>.</p>

<h2 id="automation-and-cicd">Automation and CI/CD</h2>

<p>Our release process automates the entire workflow for building Rust libraries, generating Go bindings, and distributing them across platforms. You can see our complete CI/CD setup in the <a href="https://github.com/agntcy/slim/blob/main/.github/workflows/release-bindings.yaml">SLIM repository’s release-bindings.yaml workflow</a>.</p>

<p>This workflow handles:</p>
<ul>
  <li>Cross-compilation for multiple platforms using Zig</li>
  <li>UniFFI binding generation</li>
  <li>Packaging and uploading to GitHub Releases</li>
  <li>Version tagging and coordination between repositories</li>
</ul>

<h2 id="conclusion">Conclusion</h2>

<p>UniFFI provides a powerful way to integrate Rust and Go, combining Rust’s safety and performance with Go’s simplicity and deployment model. While there’s complexity in the build process and memory management, the type safety and automated binding generation make it far superior to manual FFI code.</p>

<p>For the SLIM project, this approach allows us to:</p>
<ul>
  <li>Write cryptographic code once in Rust</li>
  <li>Expose it to Go developers with an idiomatic API</li>
  <li>Maintain type safety across the language boundary</li>
  <li>Support multiple languages from the same Rust codebase</li>
</ul>

<p>If you’re building performance-critical libraries or need Rust’s safety guarantees in your Go projects, UniFFI is worth the investment.</p>

<h2 id="additional-resources">Additional Resources</h2>

<ul>
  <li><a href="https://mozilla.github.io/uniffi-rs/">UniFFI Documentation</a></li>
  <li><a href="https://github.com/NordSecurity/uniffi-bindgen-go">uniffi-bindgen-go</a></li>
  <li><a href="https://github.com/agntcy/slim-bindings-go">SLIM Go Bindings Repository</a></li>
  <li><a href="https://github.com/agntcy/slim">SLIM Main Repository</a></li>
  <li><a href="https://pkg.go.dev/cmd/cgo">CGO Documentation</a></li>
</ul>

<hr />

<p><em>Have questions about Rust-Go integration or UniFFI? Join our <a href="https://join.slack.com/t/agntcy/shared_invite/zt-3hb4p7bo0-5H2otGjxGt9OQ1g5jzK_GQ">Slack community</a> or check out our <a href="https://github.com/agntcy">GitHub</a>.</em></p>]]></content><author><name>Sam Betts</name></author><category term="technical" /><category term="rust" /><category term="go" /><category term="uniffi" /><category term="bindings" /><category term="cgo" /><category term="interop" /><summary type="html"><![CDATA[Rust and Go are both excellent languages, but they excel in different areas. Rust provides memory safety and zero-cost abstractions, while Go offers simplicity and fast compilation. What if you could combine the strengths of both? This post explores how we use UniFFI to create seamless Rust-Go integrations for the SLIM messaging library.]]></summary></entry></feed>