<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://blogs.agntcy.org/drafts/feat-lazydir-v0.0.1-blog/feed.xml" rel="self" type="application/atom+xml" /><link href="https://blogs.agntcy.org/drafts/feat-lazydir-v0.0.1-blog/" rel="alternate" type="text/html" /><updated>2026-05-19T07:27:12+00:00</updated><id>https://blogs.agntcy.org/drafts/feat-lazydir-v0.0.1-blog/feed.xml</id><title type="html">AGNTCY Blogs</title><subtitle>Building infrastructure for the Internet of Agents</subtitle><entry><title type="html">lazydir: A Terminal UI for Browsing Agent Directory</title><link href="https://blogs.agntcy.org/drafts/feat-lazydir-v0.0.1-blog/technical/2026/05/18/lazydir-v0.0.1.html" rel="alternate" type="text/html" title="lazydir: A Terminal UI for Browsing Agent Directory" /><published>2026-05-18T07:00:00+00:00</published><updated>2026-05-18T07:00:00+00:00</updated><id>https://blogs.agntcy.org/drafts/feat-lazydir-v0.0.1-blog/technical/2026/05/18/lazydir-v0.0.1</id><content type="html" xml:base="https://blogs.agntcy.org/drafts/feat-lazydir-v0.0.1-blog/technical/2026/05/18/lazydir-v0.0.1.html"><![CDATA[<p>The tooling around <a href="https://github.com/agntcy/dir">Agent Directory</a> has evolved through several layers.
The <code class="language-plaintext highlighter-rouge">dirctl</code> CLI provides full programmatic control, and the
<a href="https://blogs.agntcy.org/technical/2026/02/19/directory-mcp-server.html">Directory MCP server</a> enables natural-language
interaction with directory nodes from within AI-powered IDEs.
These tools serve their respective workflows well, but neither is optimized for the common task of simply exploring what
a directory node contains — browsing records, narrowing results through filters, and inspecting individual entries.</p>

<p><a href="https://github.com/agntcy/lazydir">lazydir</a> addresses this gap.
It is a keyboard-driven terminal user interface (TUI) for Agent Directory, inspired by
<a href="https://github.com/jesseduffield/lazygit">lazygit</a> and <a href="https://github.com/jesseduffield/lazydocker">lazydocker</a>.
This post describes the motivation behind lazydir, the capabilities included in the first release, how to install and
configure it, and the direction of upcoming development.</p>

<!--more-->

<h2 id="the-problem-browsing-a-directory-node-is-a-multi-step-process">The Problem: Browsing a Directory Node Is a Multi-Step Process</h2>

<p>Discovering the contents of a directory node through <code class="language-plaintext highlighter-rouge">dirctl</code> requires constructing search queries, interpreting JSON
output, and often piping results through <code class="language-plaintext highlighter-rouge">jq</code> to extract the relevant fields.
Finding agents with a specific skill requires knowing the exact taxonomy name.
Comparing records across different filter criteria involves running multiple commands and cross-referencing the results
manually.</p>

<p>The MCP server improves this workflow considerably.
A developer can describe what they are looking for in natural language, and the AI assistant translates that into the
appropriate tool calls.
This approach works well for exploratory tasks and for workflows that combine discovery with other IDE activities.
However, each query still requires a round-trip through the assistant, and the interaction model is conversational
rather than navigational.</p>

<p>What was missing was a way to directly observe a directory node’s contents — to switch between filter categories, scan
through matching records, and inspect their details in a continuous flow.
The same pattern that lazygit provides for navigating a git repository without repeatedly typing <code class="language-plaintext highlighter-rouge">git log</code>, <code class="language-plaintext highlighter-rouge">git diff</code>,
and <code class="language-plaintext highlighter-rouge">git show</code>.</p>

<h2 id="lazydir-v001">lazydir v0.0.1</h2>

<p>lazydir presents the contents of a directory node in four navigable panels:
connections, filters, records, and a live preview.</p>

<div style="text-align: center; margin: 10px 0px;">
  <img src="/assets/figures/lazydir1.png" alt="Lazydir" />
</div>

<p>The interface is keyboard-driven.
Panels are cycled with <code class="language-plaintext highlighter-rouge">tab</code>, items are navigated with arrow keys (Vim-style <code class="language-plaintext highlighter-rouge">j</code>/<code class="language-plaintext highlighter-rouge">k</code> and <code class="language-plaintext highlighter-rouge">h</code>/<code class="language-plaintext highlighter-rouge">l</code> are also supported),
and details are expanded with <code class="language-plaintext highlighter-rouge">enter</code> or <code class="language-plaintext highlighter-rouge">i</code>.
Mouse clicks are supported as well for panel selection and item navigation.
The full set of available keybindings is accessible at any time by pressing <code class="language-plaintext highlighter-rouge">?</code>.
No prior knowledge of <code class="language-plaintext highlighter-rouge">dirctl</code> commands is required.</p>

<h3 id="connections-panel">Connections Panel</h3>

<p>The top-left panel displays the live health status of the Directory and OASF endpoints.
Pressing <code class="language-plaintext highlighter-rouge">c</code> (or <code class="language-plaintext highlighter-rouge">enter</code>) on either row opens a selection popup listing all servers of that type defined in the
configuration file.
The same popup also offers a “Custom…” option for entering a new address directly, without modifying any
configuration.
Connection details are viewable with <code class="language-plaintext highlighter-rouge">i</code>.
For servers that require authentication, lazydir supports OIDC device-flow login, displaying the authorization URL and
code inline within the TUI.</p>

<h3 id="filters-panel">Filters Panel</h3>

<p>The middle-left panel lists collapsible filter categories:
Skills, Domains, Modules, OASF version, Version, Author, and Trusted/Verified.
Toggling any option with <code class="language-plaintext highlighter-rouge">enter</code> or <code class="language-plaintext highlighter-rouge">space</code> updates the records panel instantly.
Most filters are applied client-side from a local cache, so the response is immediate — only the Trusted/Verified filter
requires a server round-trip.
A search across all categories is available with <code class="language-plaintext highlighter-rouge">/</code>, and pressing <code class="language-plaintext highlighter-rouge">i</code> on any skill, domain or module opens the OASF
class hierarchy and its description.</p>

<h3 id="records-panel">Records Panel</h3>

<p>The bottom-left panel presents the filtered list of records.
Records with multiple versions are grouped under collapsible headers.
A live name filter is available with <code class="language-plaintext highlighter-rouge">/</code>, a record info popup (showing CID, annotations, schema version, and created-at
timestamp) can be opened with <code class="language-plaintext highlighter-rouge">i</code>, and the CID or full JSON can be copied to the clipboard with <code class="language-plaintext highlighter-rouge">y</code>.</p>

<h3 id="preview-panel">Preview Panel</h3>

<p>The right-hand panel renders the selected record’s JSON with syntax highlighting via
<a href="https://github.com/alecthomas/chroma">Chroma</a>.
When focused, the content is fully scrollable.</p>

<h3 id="configuration-and-theming">Configuration and Theming</h3>

<p>lazydir reads an optional configuration file from <code class="language-plaintext highlighter-rouge">~/.config/lazydir/config.yml</code>.
The recommended approach for working with multiple directory nodes is to list the frequently used servers in this file.
All defined servers appear in the in-app server selection popup, with the first entry used as the default connection.
Environment variable overrides (<code class="language-plaintext highlighter-rouge">DIRECTORY_CLIENT_SERVER_ADDRESS</code>, <code class="language-plaintext highlighter-rouge">OASF_SERVER_ADDRESS</code>) are also supported.</p>

<p>The configuration file additionally supports color theme customization (10 abstract color slots accepting named colors,
256-color indices, and hex true-color values), GUI tuning parameters (scroll step, panel split ratio, input debounce
delay, popup dimming), and stream batch sizing for record loading.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">server</span><span class="pi">:</span>
  <span class="na">directoryServers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">address</span><span class="pi">:</span> <span class="s2">"</span><span class="s">localhost:8888"</span>
    <span class="pi">-</span> <span class="na">address</span><span class="pi">:</span> <span class="s2">"</span><span class="s">prod.gateway.ads.outshift.io:443"</span>
      <span class="na">oidcIssuer</span><span class="pi">:</span> <span class="s2">"</span><span class="s">https://auth.example.com"</span>
      <span class="na">oidcClientID</span><span class="pi">:</span> <span class="s2">"</span><span class="s">lazydir"</span>
  <span class="na">oasfServers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s2">"</span><span class="s">https://schema.oasf.outshift.com"</span>

<span class="na">gui</span><span class="pi">:</span>
  <span class="na">theme</span><span class="pi">:</span>
    <span class="na">activeBorderColor</span><span class="pi">:</span> <span class="s2">"</span><span class="s">green"</span>
    <span class="na">selectedRowBgColor</span><span class="pi">:</span> <span class="s2">"</span><span class="s">8"</span>
  <span class="na">splitRatio</span><span class="pi">:</span> <span class="m">0.33</span>
</code></pre></div></div>

<h2 id="getting-started">Getting Started</h2>

<p>A running Directory server is required.
The quickest way to start one locally is:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>dirctl daemon start
</code></pre></div></div>

<p>Install lazydir via Homebrew or Go:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Homebrew</span>
brew tap agntcy/lazydir https://github.com/agntcy/lazydir
brew <span class="nb">install </span>lazydir

<span class="c"># Or via Go</span>
go <span class="nb">install </span>github.com/agntcy/lazydir@latest
</code></pre></div></div>

<p>Then launch:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lazydir
</code></pre></div></div>

<p>The TUI connects to the configured directory, streams in the available records, and is ready for browsing.
Pre-built binaries for Linux, macOS, and Windows (amd64/arm64) are also available on the
<a href="https://github.com/agntcy/lazydir/releases">GitHub Releases</a> page.</p>

<h2 id="whats-next">What’s Next</h2>

<p>The first release covers read-only browsing.
Upcoming work is focused on CRUD operations for managing records directly from the TUI, searching across the network to
discover records announced by remote nodes, and copy-pasting records between directory nodes that are not in a
federation.
Some of these ideas already come from early user feedback, which has been valuable in shaping the roadmap.
If you have suggestions or feature requests, please open an issue on the
<a href="https://github.com/agntcy/lazydir/issues">GitHub project</a> — contributions and ideas are welcome.</p>

<h2 id="references">References</h2>

<ul>
  <li><a href="https://github.com/agntcy/lazydir">lazydir on GitHub</a></li>
  <li><a href="https://github.com/agntcy/dir">Agent Directory</a></li>
  <li><a href="https://docs.agntcy.org/dir/overview/">Directory Documentation</a></li>
  <li><a href="https://blogs.agntcy.org/technical/2026/02/19/directory-mcp-server.html">Directory MCP Server</a></li>
  <li><a href="https://github.com/agntcy/oasf">OASF (Open Agentic Schema Framework)</a></li>
</ul>

<hr />

<p><em>Have questions about lazydir?
Join our <a href="https://join.slack.com/t/agntcy/shared_invite/zt-3hb4p7bo0-5H2otGjxGt9OQ1g5jzK_GQ">Slack community</a> or check
out our <a href="https://github.com/agntcy">GitHub</a>.</em></p>]]></content><author><name>Andras Jaky</name></author><category term="technical" /><category term="dir" /><category term="tui" /><category term="go" /><category term="developer-experience" /><category term="agent-directory" /><summary type="html"><![CDATA[The tooling around Agent Directory has evolved through several layers. The dirctl CLI provides full programmatic control, and the Directory MCP server enables natural-language interaction with directory nodes from within AI-powered IDEs. These tools serve their respective workflows well, but neither is optimized for the common task of simply exploring what a directory node contains — browsing records, narrowing results through filters, and inspecting individual entries. lazydir addresses this gap. It is a keyboard-driven terminal user interface (TUI) for Agent Directory, inspired by lazygit and lazydocker. This post describes the motivation behind lazydir, the capabilities included in the first release, how to install and configure it, and the direction of upcoming development.]]></summary></entry></feed>