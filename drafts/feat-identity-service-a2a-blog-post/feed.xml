<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://blogs.agntcy.org/drafts/feat-identity-service-a2a-blog-post/feed.xml" rel="self" type="application/atom+xml" /><link href="https://blogs.agntcy.org/drafts/feat-identity-service-a2a-blog-post/" rel="alternate" type="text/html" /><updated>2026-08-20T12:20:05+00:00</updated><id>https://blogs.agntcy.org/drafts/feat-identity-service-a2a-blog-post/feed.xml</id><title type="html">AGNTCY Blogs</title><subtitle>Building infrastructure for the Internet of Agents</subtitle><entry><title type="html">Agent Identity and Task-Based Access Control: Closing the Authorization Gap A2A Leaves Open</title><link href="https://blogs.agntcy.org/drafts/feat-identity-service-a2a-blog-post/technical/identity/2026/08/20/agent-identity-tbac-a2a-authorization-gap.html" rel="alternate" type="text/html" title="Agent Identity and Task-Based Access Control: Closing the Authorization Gap A2A Leaves Open" /><published>2026-08-20T08:00:00+00:00</published><updated>2026-08-20T08:00:00+00:00</updated><id>https://blogs.agntcy.org/drafts/feat-identity-service-a2a-blog-post/technical/identity/2026/08/20/agent-identity-tbac-a2a-authorization-gap</id><content type="html" xml:base="https://blogs.agntcy.org/drafts/feat-identity-service-a2a-blog-post/technical/identity/2026/08/20/agent-identity-tbac-a2a-authorization-gap.html"><![CDATA[<p><a href="https://a2a-protocol.org/v1.0.0/specification/">Agent2Agent (A2A)</a> reached
v1.0.0 in March 2026 as a Linux Foundation project, and the
<a href="https://modelcontextprotocol.io/">Model Context Protocol</a> has become a common
way for agents to reach tools. Between them they specify, in detail, how one
agent asks another agent — or a tool — to do something.</p>

<p>A2A v1.0.0 also added something more interesting. An agent can now stop in the
middle of a task and ask for authorization: <code class="language-plaintext highlighter-rouge">TASK_STATE_AUTH_REQUIRED</code> is a
real lifecycle state, and the request propagates up a delegation chain until it
reaches something, ultimately someone, that can decide. It is the right
primitive. What the specification then does is decline, explicitly, to define
what the resulting permission covers, how long it lasts, or how it is revoked.</p>

<p>That is a deliberate boundary rather than an oversight, and it is where the
interesting work now sits. Closing it takes two things the protocol does not
supply: a cryptographically verifiable identity for the agent, and permissions
scoped to the task rather than to the agent. AGNTCY Identity ships the first
half today. This post walks through what A2A standardizes, what it leaves open,
what you can build on top of it right now, and what is still being designed in
the open.</p>

<!--more-->

<h3 id="what-youll-learn">What You’ll Learn</h3>

<p>In this post, you’ll learn:</p>

<ul>
  <li>Why traditional IAM assumptions break down for ephemeral, polymorphic,
autonomous agents.</li>
  <li>Exactly what A2A v1.0.0 standardizes around authorization — and the four
places the specification says the rest is up to you.</li>
  <li>How AGNTCY Identity establishes who an agent is, using resolvable
identifiers, <code class="language-plaintext highlighter-rouge">ResolverMetadata</code>, and W3C Verifiable Credential Agent Badges.</li>
  <li>Why OAuth client identity is the sticking point at organizational
boundaries, and how Client ID Metadata Documents change that.</li>
  <li>What Tool, Transaction and Task-Based Access Control (T-BAC) covers, which
layer enforces today, and which layers are still working-group drafts.</li>
  <li>What an end-to-end approval flow looks like, and precisely which two pieces
of it do not exist yet.</li>
</ul>

<h2 id="identity-is-no-longer-just-for-humans">Identity Is No Longer Just for Humans</h2>

<p>Traditional IAM rests on one stable assumption: whoever is asking for access is
a person, or a service account standing in for one. Agentic AI breaks that
assumption three ways. Agents can be ephemeral, spun up and torn down per task.
They can be polymorphic, taking different roles depending on context. And they
act on their own, with no human in the loop to vouch for each step.</p>

<p>The mismatch runs deeper than lifetime. Client registration establishes a
client’s identity and metadata, and it can constrain which scopes that client
may ask for. But the token’s real scope gets requested and granted at
authorization time, and nothing in that flow ties the resulting authority to a
task’s objective or its lifecycle. Agents work in tasks: bounded, short-lived,
different every time. Issue a broad credential to something doing narrow,
variable work and you get an agent that authenticates perfectly and can reach
far past anything it needs.</p>

<p>So an agent has to answer three questions with cryptography, not assertion:</p>

<ul>
  <li>Who created me, and what organization do I represent?</li>
  <li>What am I authorized to do, right now, for this specific piece of work?</li>
  <li>Can another system verify both before extending trust, without the two of us
sharing a central authority?</li>
</ul>

<h2 id="what-a2a-10-actually-standardizes">What A2A 1.0 Actually Standardizes</h2>

<p>A2A’s core abstraction is the Task. It carries a server-assigned <code class="language-plaintext highlighter-rouge">id</code>, a
<code class="language-plaintext highlighter-rouge">contextId</code> that groups related work, a <code class="language-plaintext highlighter-rouge">status</code> holding one of eight concrete
lifecycle states plus a <code class="language-plaintext highlighter-rouge">TASK_STATE_UNSPECIFIED</code> sentinel, and a <code class="language-plaintext highlighter-rouge">metadata</code> map
the spec reserves for extensions. Three parts of the specification matter if you
are designing the security model.</p>

<p><strong>Task operations are authorization-scoped.</strong> §13.1 requires authorization
checks on every A2A operation, scoped to the caller’s boundaries. <code class="language-plaintext highlighter-rouge">ListTasks</code>
returns only tasks that caller can see. <code class="language-plaintext highlighter-rouge">GetTask</code>, cancellation, and
subscription each verify access first, before any query that might leak whether
a resource exists.</p>

<p><strong>Agent Cards are discoverable and signable.</strong> An agent can publish its
capabilities at the registered well-known location
<code class="language-plaintext highlighter-rouge">/.well-known/agent-card.json</code>, though the spec accommodates other discovery
mechanisms. The card declares <code class="language-plaintext highlighter-rouge">securitySchemes</code> and <code class="language-plaintext highlighter-rouge">securityRequirements</code>, and
per skill, <code class="language-plaintext highlighter-rouge">AgentSkill.securityRequirements</code>, so an agent can state which scopes
each individual skill needs. Cards can also carry JWS signatures over a
canonicalized payload, which lets a client confirm the card came from the domain
it claims.</p>

<p><strong>There is a first-class extension point.</strong> An agent declares an
<code class="language-plaintext highlighter-rouge">AgentExtension</code> by URI. Clients activate it with the <code class="language-plaintext highlighter-rouge">A2A-Extensions</code> header.
Extension data rides in the <code class="language-plaintext highlighter-rouge">metadata</code> maps, keyed by that URI. The spec is
blunt that an extension must never become a way around an agent’s primary
security controls.</p>

<p>And in v1.0, an agent can stop mid-task and ask for authorization.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"5c2a9e10-3f42-4c8b-9d71-6f0b2a7e4c19"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"contextId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"b31f7c58-0a6d-4e93-8c22-1d5f9e7a3b40"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"status"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"state"</span><span class="p">:</span><span class="w"> </span><span class="s2">"TASK_STATE_AUTH_REQUIRED"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"message"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
      </span><span class="nl">"messageId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"9a1c7e33-5b08-4f6e-8a12-c47d0e9b2f65"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"role"</span><span class="p">:</span><span class="w"> </span><span class="s2">"ROLE_AGENT"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"parts"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
        </span><span class="p">{</span><span class="w"> </span><span class="nl">"text"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Restarting payments-api in production requires approval."</span><span class="w"> </span><span class="p">}</span><span class="w">
      </span><span class="p">]</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="nl">"timestamp"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2026-08-11T14:22:07Z"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p><em>An A2A Task interrupted for authorization. One migration note: v1.0 serializes
lifecycle states in SCREAMING_SNAKE_CASE on the JSON wire, so
<code class="language-plaintext highlighter-rouge">TASK_STATE_AUTH_REQUIRED</code> replaces the <code class="language-plaintext highlighter-rouge">auth-required</code> of v0.3.</em></p>

<h2 id="the-gap-a2a-deliberately-leaves-open">The Gap A2A Deliberately Leaves Open</h2>

<p><code class="language-plaintext highlighter-rouge">TASK_STATE_AUTH_REQUIRED</code> is where an agent lands when it hits a step it can’t
take on its own authority. Maybe it needs a token it doesn’t hold. Maybe a human
has to approve something destructive. Either way, the client picks up
responsibility for getting that authorization. And if the client is itself an
agent servicing an upstream task, §7.6.2 lets it flip its own task to
<code class="language-plaintext highlighter-rouge">TASK_STATE_AUTH_REQUIRED</code> too, so a chain of paused tasks propagates up to a
person.</p>

<p>Useful primitive — but §7.6.4 draws a hard line around it:</p>

<blockquote>
  <p>The A2A protocol does not define the scope, representation, validity, or
revocation semantics of the authorization decision or credential obtained in
response to this state. Agents MUST NOT treat the
<code class="language-plaintext highlighter-rouge">TASK_STATE_AUTH_REQUIRED</code> state transition, by itself, as authorization for
any particular operation. The meaning and scope of any resulting
authorization decision or credential MUST be defined by the agent’s
implementation, by the credential issuer, or by an A2A extension.</p>
</blockquote>

<p>The spec says the same thing in other places. §7.5: “Authorization logic is
implementation-specific.” §13.1: “Authorization boundaries are defined by each
agent’s authorization model, not prescribed by the protocol.”</p>

<p>Treat that as a work item, not a shortcoming. A2A standardized <em>when</em> to ask.
Somebody has to define what the answer means.</p>

<table>
  <thead>
    <tr>
      <th>Capability</th>
      <th>Status in A2A v1.0.0</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Authenticated caller can access a specific Task</td>
      <td>Standardized (§13.1)</td>
    </tr>
    <tr>
      <td>Authorization checks on task operations</td>
      <td>Required; the model itself is left to the implementation</td>
    </tr>
    <tr>
      <td>Pause a task to request approval or credentials</td>
      <td>Standardized (<code class="language-plaintext highlighter-rouge">TASK_STATE_AUTH_REQUIRED</code>)</td>
    </tr>
    <tr>
      <td>Propagate that request up a delegation chain</td>
      <td>Standardized (§7.6.2)</td>
    </tr>
    <tr>
      <td>Declare which scopes a skill requires</td>
      <td>Declarative only (<code class="language-plaintext highlighter-rouge">AgentSkill.securityRequirements</code>)</td>
    </tr>
    <tr>
      <td>Authorization based on the action attempted within a task</td>
      <td>Supported as implementation policy (§7.5)</td>
    </tr>
    <tr>
      <td>Credential cryptographically bound to a <code class="language-plaintext highlighter-rouge">taskId</code></td>
      <td>Not standardized</td>
    </tr>
    <tr>
      <td>Permission derived from the task’s semantic intent</td>
      <td>Not standardized</td>
    </tr>
    <tr>
      <td>Automatically narrow tools and scopes to what the task needs</td>
      <td>Not standardized</td>
    </tr>
    <tr>
      <td>Task-scoped validity and revocation semantics</td>
      <td>Not standardized (§7.6.4)</td>
    </tr>
    <tr>
      <td>A sanctioned place to carry all of the above</td>
      <td>Standardized (<code class="language-plaintext highlighter-rouge">metadata</code> + <code class="language-plaintext highlighter-rouge">AgentExtension</code>)</td>
    </tr>
  </tbody>
</table>

<h2 id="identity-first-establishing-who-the-agent-is">Identity First: Establishing Who the Agent Is</h2>

<p>Before you can scope an agent’s authority, you need to know whose agent it is.
<a href="https://spec.identity.agntcy.org">AGNTCY Identity</a> gives every entity, whether
that’s an Agent, an MCP Server, or a Multi-Agent System, an identifier with
three properties: open, so no central issuing authority is required;
collision-free; and verifiable by cryptographic proof instead of by claim.</p>

<p>Those identifiers root in whatever the organization already trusts. Connect Duo,
Microsoft Entra ID, Okta, Ory, or Ping, and the Identity Service derives the
agent’s ID from its registration in that provider. Organizations that would
rather skip an external IdP can register a self-issued identity instead, and
there is a <code class="language-plaintext highlighter-rouge">did:agntcy:</code> method for decentralized deployments.</p>

<pre><code class="language-mermaid">flowchart LR
    subgraph SRC["Trust roots"]
        direction TB
        IDP["Enterprise IdP&lt;br/&gt;Duo, Entra ID, Okta, Ory, Ping"]
        SELF["Self-issued identity"]
        DID["did:agntcy: method"]
    end

    ID["AGNTCY ID&lt;br/&gt;open, collision-free, verifiable"]

    RM["ResolverMetadata, 1:1&lt;br/&gt;keys, assertion methods, endpoints"]
    BADGE["Agent Badges, 1:many&lt;br/&gt;W3C VC wrapping an OASF document&lt;br/&gt;or an A2A Agent Card"]

    NODE["Identity Node&lt;br/&gt;trust anchor"]
    RP["Relying party&lt;br/&gt;verifies with no prior relationship"]

    IDP --&gt; ID
    SELF --&gt; ID
    DID --&gt; ID
    ID --&gt; RM
    ID --&gt; BADGE
    BADGE --&gt; NODE
    RM --&gt; RP
    NODE --&gt; RP

    style SRC fill:#eff3fc,stroke:#0251af,color:#1c1e21
    style IDP fill:#e8eefb,stroke:#0251af,color:#1c1e21
    style SELF fill:#e8eefb,stroke:#0251af,color:#1c1e21
    style DID fill:#e8eefb,stroke:#0251af,color:#1c1e21
    style ID fill:#0251af,stroke:#023a7a,color:#f3f6fd
    style RM fill:#cfe1fb,stroke:#0251af,color:#1c1e21
    style BADGE fill:#cfe1fb,stroke:#0251af,color:#1c1e21
    style NODE fill:#d4edda,stroke:#2d6a4f,color:#1c1e21
    style RP fill:#d4edda,stroke:#2d6a4f,color:#1c1e21
</code></pre>

<p>Each ID maps 1:1 to a <strong><code class="language-plaintext highlighter-rouge">ResolverMetadata</code></strong> object, a JSON-LD document holding
the key material, assertion methods, and service endpoints you need to resolve
the identifier and check anything signed under it. A relying party never has to
take an agent’s self-description on faith. One caveat worth keeping straight:
verifying a badge establishes provenance. Deciding you trust its issuer is a
separate policy call.</p>

<p>Each ID also maps 1:many to <strong>Agent Badges</strong>, <a href="https://www.w3.org/TR/vc-data-model-2.0/">W3C Verifiable
Credentials</a> whose
<code class="language-plaintext highlighter-rouge">credentialSubject.badge</code> holds the agent’s real definition: an
<a href="https://docs.agntcy.org/oasf/open-agentic-schema-framework/">OASF</a> schema document, or for A2A
agents, the Agent Card itself. Badges get published to an Identity Node acting
as a trust anchor, so any party can verify one without a prior relationship.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"@context"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"https://www.w3.org/ns/credentials/v2"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"urn:uuid:1f4b6c2e-9d31-4a75-b0e8-3c7a5d21f9b4"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"VerifiableCredential"</span><span class="p">,</span><span class="w"> </span><span class="s2">"AgentBadge"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"issuer"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://identity.example.com/issuers/examplecorp"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"validFrom"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2026-08-11T09:00:00Z"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"credentialSubject"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"OKTA-0oa9z8y7x6w5V4u3t2s1"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"badge"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"..."</span><span class="p">:</span><span class="w"> </span><span class="s2">"the agent's A2A Agent Card, verbatim"</span><span class="w"> </span><span class="p">}</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"credentialSchema"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://a2a-protocol.org/v1.0.0/spec/a2a.json#/definitions/Agent%20Card"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"JsonSchema"</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">],</span><span class="w">
  </span><span class="nl">"proof"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"DataIntegrityProof"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"proofPurpose"</span><span class="p">:</span><span class="w"> </span><span class="s2">"assertionMethod"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"proofValue"</span><span class="p">:</span><span class="w"> </span><span class="s2">"z58DAdFfa9SkqZMVPxAQp...jQCrfFPP2oumHKtz"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p><em>An A2A Agent Badge, abridged, in the form published in the AGNTCY Verifiable
Credential specification. The shipped Identity Service currently signs badges as
JOSE-enveloped credentials, meaning a JWS over the credential, and verification
accepts either form.</em></p>

<h2 id="where-the-agents-oauth-client-identity-comes-from">Where the Agent’s OAuth Client Identity Comes From</h2>

<p>That works because the <a href="https://identity-docs.outshift.com">Identity
Service</a> handles IdP registration for you.
Create an Agentic Service and it provisions a dedicated OAuth client in your
tenant. Microsoft’s Entra ID integration notes are blunt about the reason: each
agentic service gets “its own dedicated app registration with isolated
credentials, following the principle of least privilege.”</p>

<p>Inside one organization that model is excellent. At the organizational boundary
it runs out of road, which is unfortunate, because the boundary is where A2A is
built to operate. Calling a partner’s agent means somebody pre-registers a
client at the partner’s authorization server, exchanges a secret, and manages
its lifecycle. Multiply that by every partner and every agent.</p>

<p>The IETF is standardizing a way out. <strong><a href="https://datatracker.ietf.org/doc/draft-ietf-oauth-client-id-metadata-document/">Client ID Metadata
Documents</a></strong>
(<code class="language-plaintext highlighter-rouge">draft-ietf-oauth-client-id-metadata-document</code>, adopted by the OAuth working
group) let a client use an HTTPS URL as its <code class="language-plaintext highlighter-rouge">client_id</code>, and the authorization
server dereferences that URL to pull the client’s metadata. No prior
relationship required. In deployments that resolve those documents dynamically,
there is also no per-authorization-server registration transaction and no
registration state left behind, though the draft still permits preregistration
and expects it to stay common in enterprise environments. Shared-secret client
authentication is ruled out for these clients in favor of public-key methods
like <code class="language-plaintext highlighter-rouge">private_key_jwt</code>, with key material published through <code class="language-plaintext highlighter-rouge">jwks</code> or
<code class="language-plaintext highlighter-rouge">jwks_uri</code>, so the identity ends up key-bound and rooted in a domain someone
demonstrably controls. It is an Internet-Draft rather than a finished RFC, but
<a href="https://modelcontextprotocol.io/specification/draft/basic/authorization">MCP’s authorization
specification</a>
already recommends it for client onboarding where no prior registration exists,
and deprecates dynamic client registration in its favor.</p>

<p>The two models fit together. CIMD answers <em>which OAuth client is this</em>.
<code class="language-plaintext highlighter-rouge">ResolverMetadata</code> and the Agent Badge answer <em>who published it and what it was
built to do</em>. Both can anchor to the same key material, and AGNTCY has a working
subgroup on that seam.</p>

<h2 id="from-identity-to-task-scoped-authorization">From Identity to Task-Scoped Authorization</h2>

<p>Authentication tells you who the agent is. It says nothing about what work this
agent should be allowed to do right now. Tool, Transaction and Task-Based Access
Control (T-BAC) covers that gap in three layers, each one narrowing authority
from the agent down to the individual action.</p>

<pre><code class="language-mermaid">flowchart TB
    AGENT["Verified agent identity&lt;br/&gt;Agent Badge + ResolverMetadata"]

    subgraph TOOL["Tool: enforcing today"]
        direction TB
        T1["Task catalog derived from the agent's badge"]
        T2["Policy on the calling service:&lt;br/&gt;permitted tasks, Allow or Deny, Needs Approval"]
        T3["Decided when the call happens,&lt;br/&gt;not fixed when the token was issued"]
        T1 --&gt; T2 --&gt; T3
    end

    subgraph TXN["Transaction: IETF draft"]
        direction TB
        X1["Immutable call-chain context:&lt;br/&gt;who originally asked, for what, under what conditions"]
        X2["No intermediate agent can quietly widen it"]
        X1 --&gt; X2
    end

    subgraph TASK["Task: working-group proposal"]
        direction TB
        K1["Authority derived from the objective itself"]
        K2["Cryptographically bound to the taskId"]
        K3["Monotonic narrowing at every delegation hop"]
        K4["Expires with the task"]
        K1 --&gt; K2 --&gt; K3 --&gt; K4
    end

    AGENT --&gt; TOOL
    TOOL --&gt; TXN
    TXN --&gt; TASK

    style AGENT fill:#0251af,stroke:#023a7a,color:#f3f6fd
    style TOOL fill:#d4edda,stroke:#2d6a4f,color:#1c1e21
    style T1 fill:#eff3fc,stroke:#0251af,color:#1c1e21
    style T2 fill:#eff3fc,stroke:#0251af,color:#1c1e21
    style T3 fill:#eff3fc,stroke:#0251af,color:#1c1e21
    style TXN fill:#fff3cd,stroke:#856404,color:#1c1e21
    style X1 fill:#eff3fc,stroke:#0251af,color:#1c1e21
    style X2 fill:#eff3fc,stroke:#0251af,color:#1c1e21
    style TASK fill:#fff3cd,stroke:#856404,color:#1c1e21
    style K1 fill:#eff3fc,stroke:#0251af,color:#1c1e21
    style K2 fill:#eff3fc,stroke:#0251af,color:#1c1e21
    style K3 fill:#eff3fc,stroke:#0251af,color:#1c1e21
    style K4 fill:#eff3fc,stroke:#0251af,color:#1c1e21
</code></pre>

<p><strong>Tool</strong> is the layer that enforces today. The Identity Service builds a task
catalog from the agent’s badge, so administrators write policy against the
agent’s real tools and invocations instead of abstract scopes. Policies attach
to the <em>calling</em> service and hold Rules listing permitted tasks, an Allow or
Deny action, and a “Needs Approval” flag. The design intent is that
authorization gets decided when the call happens, not fixed when the token was
issued. Which is what turns Continuous Authorization from a marketing line into
a mechanism: a policy change can bite on the next call, not the next token
refresh.</p>

<p><strong>Transaction</strong> handles what per-hop least privilege can’t. Once work fans out
across agents, the chain has to carry who originally asked, for what, and under
what conditions, immutably, so no intermediate agent quietly widens it. The
IETF’s <a href="https://datatracker.ietf.org/doc/draft-ietf-oauth-transaction-tokens/">Transaction
Tokens</a>
draft builds that primitive for internal service call chains.</p>

<p><strong>Task</strong> is the layer that is not standardized or shipping in the architecture
described here: authority derived from the objective itself, cryptographically
bound to the task, expiring with it. Working-group drafts sketch it. A credential
profile carrying authorized scopes and delegation constraints. A narrowing rule
where a delegate’s effective authority is the intersection of what the delegator
can pass on, what the delegate holds intrinsically, and what this specific
delegation granted. Monotonic narrowing, enforced at every hop.</p>

<p>So that is the target architecture. Its status deserves a straight answer: it
lives in working-group proposals, not in shipped code. The example below shows
what such a credential could carry.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"iss"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://identity.example.com"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"sub"</span><span class="p">:</span><span class="w"> </span><span class="s2">"OKTA-0oa9z8y7x6w5V4u3t2s1"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"exp"</span><span class="p">:</span><span class="w"> </span><span class="mi">1786012800</span><span class="p">,</span><span class="w">
  </span><span class="nl">"cnf"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"jkt"</span><span class="p">:</span><span class="w"> </span><span class="s2">"0ZcOCORZNYy-DWpqq30jZyJGHTN0d2HglBV3uiguA4I"</span><span class="w"> </span><span class="p">},</span><span class="w">
  </span><span class="nl">"act"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"sub"</span><span class="p">:</span><span class="w"> </span><span class="s2">"OKTA-0oa1b2c3d4e5F6g7h8i9"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"act"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"sub"</span><span class="p">:</span><span class="w"> </span><span class="s2">"alex@example.com"</span><span class="w"> </span><span class="p">}</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"a2a"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"taskId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"5c2a9e10-3f42-4c8b-9d71-6f0b2a7e4c19"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"contextId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"b31f7c58-0a6d-4e93-8c22-1d5f9e7a3b40"</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"authorization_details"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"agentic_task"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"actions"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"restart"</span><span class="p">],</span><span class="w">
      </span><span class="nl">"locations"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"https://mcp.example.com/infra"</span><span class="p">],</span><span class="w">
      </span><span class="nl">"datatypes"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"service:payments-api"</span><span class="p">]</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p><em>Illustrative only. AGNTCY Identity does not issue this task-scoped credential
today. Today, Identity establishes who the agent is and TBAC evaluates whether
it may invoke a specific tool; the proposed next step is to bind that authority
to the A2A task itself. A future AGNTCY task-authorization profile would bind
the agent’s identity, <code class="language-plaintext highlighter-rouge">taskId</code>, permitted actions and resources, delegation
constraints, and a limited lifetime into one verifiable authorization object.
Existing standards can supply reusable building blocks for structured
authorization, delegation provenance, and proof of possession, but they do not
define this task-scoped model. The profile work is to specify issuance and
validation, how authority narrows through delegation, how the agent proves
possession of the authorized key, and how task-bound authority expires or is
revoked.</em></p>

<h2 id="two-names-for-the-same-primitive">Two Names for the Same Primitive</h2>

<p>A2A has <code class="language-plaintext highlighter-rouge">TASK_STATE_AUTH_REQUIRED</code>. An agent stops, says it needs authorization,
and the request propagates up the chain of tasks until it reaches something,
ultimately someone, that can decide. AGNTCY’s TBAC has “Needs Approval”: a rule
flag that halts an invocation, pushes a notification to a registered device
showing the caller, the callee, and the tool, then waits on a human.</p>

<p>Same primitive, built at different layers, and today they don’t talk to each
other. AGNTCY’s approval blocks synchronously inside a single hop, on a short
timer. A2A’s is asynchronous and composes across multiple task hops, so a
sub-agent’s request shows up as an interrupted state on the orchestrator’s task,
and on the task above that.</p>

<p>Combining them is what pays off. A person sees the objective they delegated
alongside the specific action now being requested against it, several agents
down, and the credential issued in response is scoped to both. A2A §7.6.4 names
three places where the meaning of such a credential can be defined: the agent’s
implementation, the credential issuer, or an A2A extension. All three are open
to the community.</p>

<h2 id="what-this-looks-like-end-to-end">What This Looks Like End to End</h2>

<p>Picture an infrastructure troubleshooting agent working for an on-call engineer.</p>

<ol>
  <li><strong>Delegation.</strong> The engineer hands an orchestrator an objective: find out why
checkout latency spiked, and fix it if that’s safe. The orchestrator opens an
A2A Task and gets back an <code class="language-plaintext highlighter-rouge">id</code> and a <code class="language-plaintext highlighter-rouge">contextId</code>.</li>
  <li><strong>Discovery and verification.</strong> It pulls the candidate specialist’s Agent Card
from <code class="language-plaintext highlighter-rouge">/.well-known/agent-card.json</code>, checks the card’s JWS signature,
resolves the agent’s identifier to its <code class="language-plaintext highlighter-rouge">ResolverMetadata</code>, and verifies the
Agent Badge: publisher, organization, declared capabilities. No shared
central authority needed.</li>
  <li><strong>Work begins.</strong> The specialist opens its own Task. Reading logs and querying
monitoring fall inside policy, so those calls go through, with policy
evaluated at the MCP server each time.</li>
  <li><strong>The sensitive step.</strong> Restarting <code class="language-plaintext highlighter-rouge">payments-api</code> will fix the incident, but
that task sits behind a rule marked Needs Approval. The specialist moves its
Task to <code class="language-plaintext highlighter-rouge">TASK_STATE_AUTH_REQUIRED</code> with a status message naming the action.</li>
  <li><strong>Propagation.</strong> The orchestrator, servicing the engineer’s task, moves its
own Task to <code class="language-plaintext highlighter-rouge">TASK_STATE_AUTH_REQUIRED</code>. The engineer sees one specific
action, in the context of the objective they delegated.</li>
  <li><strong>Approval and issuance.</strong> The engineer approves. The credential issuer mints
authority for that action and, in the target model, binds it to the <code class="language-plaintext highlighter-rouge">taskId</code>,
the specialist’s identity, and that one named service.</li>
  <li><strong>Enforcement.</strong> The restart call carries that authority to the MCP server,
which validates it independently and re-checks policy before acting.</li>
  <li><strong>Close.</strong> The task completes. The authority dies with it.</li>
</ol>

<pre><code class="language-mermaid">sequenceDiagram
    autonumber
    actor ENG as On-call engineer
    participant ORCH as Orchestrator agent
    participant SPEC as Specialist agent
    participant IDS as Identity Service and credential issuer
    participant MCP as MCP server for infrastructure tools

    ENG-&gt;&gt;ORCH: Delegate objective: diagnose and safely fix checkout latency
    Note over ORCH: Task A opens, id + contextId assigned
    ORCH-&gt;&gt;IDS: Resolve identifier, verify Agent Card JWS and Agent Badge
    IDS--&gt;&gt;ORCH: ResolverMetadata and verified badge
    ORCH-&gt;&gt;SPEC: Send message
    Note over SPEC: Task B opens
    SPEC-&gt;&gt;MCP: Read logs, query monitoring
    MCP--&gt;&gt;SPEC: Results, policy evaluated on every call
    SPEC-&gt;&gt;MCP: Restart payments-api
    MCP--&gt;&gt;SPEC: Blocked, rule marked Needs Approval

    rect rgba(251, 175, 70, 0.12)
    Note over SPEC: Task B moves to TASK_STATE_AUTH_REQUIRED
    SPEC--&gt;&gt;ORCH: Authorization required to restart payments-api
    Note over ORCH: Task A moves to TASK_STATE_AUTH_REQUIRED
    ORCH--&gt;&gt;ENG: One specific action, in the context of the delegated objective
    ENG-&gt;&gt;IDS: Approve this action
    IDS--&gt;&gt;SPEC: Task-scoped credential, proposed and not shipped today
    end

    SPEC-&gt;&gt;MCP: Restart carrying that authority
    MCP--&gt;&gt;SPEC: Validated independently, policy re-checked, restart executed
    SPEC--&gt;&gt;ORCH: Task B complete
    ORCH--&gt;&gt;ENG: Task A complete
    Note over ENG,MCP: Both tasks close, the authority dies with them
</code></pre>

<p>Most of these primitives exist today. Two don’t. Steps four and five assume a
bridge between a TBAC approval decision and A2A’s authorization-required task
state, and that bridge is integration work, not a shipped feature. Step six
assumes a task-bound credential plus the rules a resource server uses to
validate it, which is the profile work described above.</p>

<h2 id="what-you-can-build-today">What You Can Build Today</h2>

<p>Verifiable agent identity ships now: badges, resolvable identifiers, and
cryptographic verification that works across organizational boundaries. So does
tool-level authorization with continuous re-evaluation and human-in-the-loop
approval. A2A supplies the task lifecycle, the interruption state, the
propagation semantics, and a sanctioned place to carry extension data.</p>

<p>You supply the wiring and the binding: connecting an approval decision to A2A’s
authorization-required state, and defining what a task-scoped credential holds,
how each boundary validates it, and when it dies. A2A leaves that to
implementations, issuers, and extensions. The community’s job is making sure we
don’t all invent a different answer.</p>

<h2 id="why-this-has-to-be-open">Why This Has to Be Open</h2>

<p>A trust layer is only worth something if it’s interoperable, and the mechanisms
deciding whether one system should trust another have to be inspectable, not
proprietary. An identity and authorization layer that only one vendor can audit
isn’t a trust layer. It’s a dependency. That is why AGNTCY’s components are
built as open infrastructure, designed to complement existing protocols instead
of replacing them:</p>

<ul>
  <li><strong><a href="https://docs.agntcy.org/oasf/open-agentic-schema-framework/">OASF</a></strong> (Open Agentic Schema
Framework), a standard schema for an agent’s capabilities and metadata,
enabling discovery across a registry.</li>
  <li><strong><a href="https://docs.agntcy.org/dir/overview/">Agent Directory</a></strong>, a federated
registry for publishing and discovering agents across frameworks and
organizational boundaries.</li>
  <li><strong><a href="https://docs.agntcy.org/slim/overview/">SLIM</a></strong>, secure agent-to-agent
messaging with pub/sub and MLS encryption, protecting the communication itself
and not just the endpoints.</li>
  <li><strong><a href="https://spec.identity.agntcy.org">Identity</a></strong>, the identifier,
<code class="language-plaintext highlighter-rouge">ResolverMetadata</code>, Agent Badge, and policy model described above.</li>
</ul>

<p>A2A handles agent-to-agent messaging. MCP handles tool integration. AGNTCY
supplies the identity, discovery, and trust layer that lets those pieces work
together instead of forming silos. That work is happening in the open, with
partners including Cisco Duo, Skyfire, and Permit.io.</p>

<h2 id="closing-thoughts">Closing thoughts</h2>

<p>Identity tells us who an agent is, backed by a verifiable credential instead of
a claim. Task-Based Access Control defines what it may do, and for how long.
Continuous Authorization decides whether it should still be allowed as the work
evolves.</p>

<p>Together, they’re the foundation for multi-agent systems that can operate across
organizational boundaries — not because every party trusts every agent by
default, but because trust itself has become something that can be verified,
scoped, and revoked.</p>

<p>The future of AI won’t depend solely on more capable agents. It will depend on
agents whose identity and authorization can be checked, not assumed. A2A 1.0
made that possible by defining exactly where the check belongs. Filling it in is
the work in front of us.</p>

<p><strong>Get involved:</strong> the <a href="https://github.com/agntcy/governance/blob/main/working-groups/identity/CHARTER.md">AGNTCY Agent Identity Working
Group</a>
is an open forum for developers, security architects, and researchers building
toward this model.</p>

<h2 id="references">References</h2>

<ul>
  <li><a href="https://a2a-protocol.org/v1.0.0/specification/">A2A Protocol v1.0.0
specification</a> — §7.5 server
authorization responsibilities, §7.6 in-task authorization, §7.6.4 in-task
authorization scope, §13.1 data access and authorization scoping, §4.6
extensions, §8.4 Agent Card signing.</li>
  <li><a href="https://github.com/a2aproject/A2A/blob/v1.0.0/specification/a2a.proto"><code class="language-plaintext highlighter-rouge">specification/a2a.proto</code> at tag
v1.0.0</a>
— the canonical, normative data model for <code class="language-plaintext highlighter-rouge">TaskState</code>, <code class="language-plaintext highlighter-rouge">Task</code>, <code class="language-plaintext highlighter-rouge">Message</code>,
<code class="language-plaintext highlighter-rouge">AgentCard</code>, <code class="language-plaintext highlighter-rouge">AgentSkill</code>, <code class="language-plaintext highlighter-rouge">AgentExtension</code>. The JSON Schema bundle published
at <a href="https://a2a-protocol.org/v1.0.0/spec/a2a.json"><code class="language-plaintext highlighter-rouge">spec/a2a.json</code></a> is a
generated, non-normative artifact derived from it.</li>
  <li><a href="https://spec.identity.agntcy.org">AGNTCY Identity specification</a> — ID and
<code class="language-plaintext highlighter-rouge">ResolverMetadata</code> definitions, Agent Badge and MCP Server Badge, issuance and
verification architecture.</li>
  <li><a href="https://identity-docs.outshift.com">AGNTCY Identity Service
documentation</a> — identity provider
connection, Agentic Service creation, Policies and TBAC, development guide.</li>
  <li><a href="https://datatracker.ietf.org/doc/draft-ietf-oauth-client-id-metadata-document/">draft-ietf-oauth-client-id-metadata-document</a>,
OAuth working group.</li>
  <li><a href="https://datatracker.ietf.org/doc/draft-ietf-oauth-transaction-tokens/">draft-ietf-oauth-transaction-tokens</a>,
OAuth working group.</li>
  <li><a href="https://modelcontextprotocol.io/specification/draft/basic/authorization">Model Context Protocol authorization
specification</a>
— client registration and Client ID Metadata Documents.</li>
  <li><a href="https://www.rfc-editor.org/rfc/rfc9396.html">RFC 9396</a> (Rich Authorization
Requests), <a href="https://www.rfc-editor.org/rfc/rfc8693.html#section-4.1">RFC 8693</a>
(Token Exchange) §4.1 on the <code class="language-plaintext highlighter-rouge">act</code> claim,
<a href="https://www.rfc-editor.org/rfc/rfc9449.html">RFC 9449</a> (DPoP),
<a href="https://www.rfc-editor.org/rfc/rfc6749.html">RFC 6749</a> and
<a href="https://www.rfc-editor.org/rfc/rfc7591.html">RFC 7591</a> on client registration
and scope handling.</li>
  <li><a href="https://www.w3.org/TR/vc-data-model-2.0/">W3C Verifiable Credentials Data Model
v2.0</a> — issuer property
requirements.</li>
</ul>

<hr />

<p><em>Have questions? Join our <a href="https://join.slack.com/t/agntcy/shared_invite/zt-3xozr6nzq-i6LXv2P8l2kVW4_Prnny2w">Slack
community</a>
or check out our <a href="https://github.com/agntcy">GitHub</a>.</em></p>]]></content><author><name>Jean Diaconu</name></author><category term="technical" /><category term="identity" /><category term="identity" /><category term="a2a" /><category term="tbac" /><category term="authorization" /><category term="verifiable-credentials" /><category term="oauth" /><category term="mcp" /><category term="agents" /><summary type="html"><![CDATA[Agent2Agent (A2A) reached v1.0.0 in March 2026 as a Linux Foundation project, and the Model Context Protocol has become a common way for agents to reach tools. Between them they specify, in detail, how one agent asks another agent — or a tool — to do something. A2A v1.0.0 also added something more interesting. An agent can now stop in the middle of a task and ask for authorization: TASK_STATE_AUTH_REQUIRED is a real lifecycle state, and the request propagates up a delegation chain until it reaches something, ultimately someone, that can decide. It is the right primitive. What the specification then does is decline, explicitly, to define what the resulting permission covers, how long it lasts, or how it is revoked. That is a deliberate boundary rather than an oversight, and it is where the interesting work now sits. Closing it takes two things the protocol does not supply: a cryptographically verifiable identity for the agent, and permissions scoped to the task rather than to the agent. AGNTCY Identity ships the first half today. This post walks through what A2A standardizes, what it leaves open, what you can build on top of it right now, and what is still being designed in the open.]]></summary></entry></feed>