<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://blogs.agntcy.org/drafts/dir-ai-catalog/feed.xml" rel="self" type="application/atom+xml" /><link href="https://blogs.agntcy.org/drafts/dir-ai-catalog/" rel="alternate" type="text/html" /><updated>2026-06-16T20:14:31+00:00</updated><id>https://blogs.agntcy.org/drafts/dir-ai-catalog/feed.xml</id><title type="html">AGNTCY Blogs</title><subtitle>Building infrastructure for the Internet of Agents</subtitle><entry><title type="html">ARD over Agent Directory: Interoperability by Design</title><link href="https://blogs.agntcy.org/drafts/dir-ai-catalog/technical/2026/06/16/ai-catalog-over-directory.html" rel="alternate" type="text/html" title="ARD over Agent Directory: Interoperability by Design" /><published>2026-06-16T12:00:00+00:00</published><updated>2026-06-16T12:00:00+00:00</updated><id>https://blogs.agntcy.org/drafts/dir-ai-catalog/technical/2026/06/16/ai-catalog-over-directory</id><content type="html" xml:base="https://blogs.agntcy.org/drafts/dir-ai-catalog/technical/2026/06/16/ai-catalog-over-directory.html"><![CDATA[<p>A developer integrating agentic capabilities today faces a discovery problem that looks deceptively like a solved one. We have standards, registries, catalogs, and well-knowns; surely finding the right tool is just a lookup? In practice it is not, because the ecosystem has fragmented into protocols that each describe the <em>same</em> artifact differently; be it a resource, tool, agent, or a full agentic system. And while the artifact is one thing; its discovery representations are many.</p>

<p>The question of discovery and interoperability is not achieved by picking a winning format or specification. <a href="https://agent-card.github.io/ai-catalog">AI Catalog</a> and <a href="https://agenticresourcediscovery.org/">Agentic Resource Discovery</a> are not competing standards; they are complementary lenses on the same underlying problem. Each one tries to answers a shared set of questions - <em>what is this artifact</em>, <em>what can it do</em>, <em>how to use it</em>, <em>who published it</em>, and <em>can I trust it</em>.</p>

<p>Most of these formats are <strong>higher-level discovery protocols</strong>: they define a vocabulary and an envelope. What they do <em>not</em> define is the hard part – a secure, federated, capability-driven discovery layer that actually links these artifacts, routes queries across organizational boundaries, and provides security and control policies embedded in. That layer is exactly what <a href="https://github.com/agntcy/dir">Agent Directory Service (ADS)</a> provides - not another competing registry, but a <strong>low-level protocol</strong> the others can be built on.</p>

<!-- **TL;DR:** AI Catalog and ARD are intentionally artifact-agnostic envelopes; they identify *what* an agentic thing is and delegate *how* to invoke it to the native protocol. ADS supplies the shared schema, identity, routing, and security layer beneath them. A consumer can therefore discover through ARD, verify through ADS, and invoke through MCP or A2A -- one flow, many protocols. -->

<h2 id="three-layers-one-stack">Three layers, one stack</h2>

<p>A recurring design mistake is to collapse “the format”, “the query API”, and “the system” into one monolith. The agentic ecosystem deliberately keeps them apart, and the separation is the whole reason it interoperates.</p>

<pre><code class="language-mermaid">flowchart BT
    subgraph L3["ADS -- Discovery &amp; Control Plane"]
        OASF["Records"]
        API["API (gRPC / REST)"]
        CID["Identifiers (CIDs / DIDs)"]
        DHT["Routing (DHT)"]
        OCI["Storage (OCI)"]
        SIGN["Security / Policies"]
    end
    subgraph L2["ARD -- Consumption API"]
        WK["GET /.well-known/ai-catalog.json"]
        SEARCH["POST /search"]
        LIST["GET /v1/agents"]
    end
    subgraph L1["AI Catalog -- Index / Content Standard"]
        ENTRY["Catalog Entry Schema"]
        TRUST["Trust Manifest Schema"]
        TYPES["IANA media types"]
    end

    L1 --&gt;|"mapped by"| L2
    L2 --&gt;|"implemented by"| L3
    L3 --&gt;|"consumed by"| CLIENT["AI clients / applications / orchestrators"]
</code></pre>

<h3 id="layer-1-ai-catalog-the-index-standard">Layer 1: AI Catalog: the index standard</h3>

<p>The <a href="https://agent-card.github.io/ai-catalog/">AI Catalog</a> is a <em>typed, nestable JSON container</em> for describing heterogeneous AI artifacts. Each entry identifies an artifact by an <strong>IANA media type</strong> and a <strong>domain-anchored URN</strong>. The catalog is therefore <em>protocol-agnostic</em>: it can describe an MCP server, an A2A agent, a custom tool, or a set of catalogs in the same way.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"identifier"</span><span class="p">:</span><span class="w"> </span><span class="s2">"urn:ai:acme.com:agents:weather"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"displayName"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Weather Agent"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"application/mcp-server+json"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"url"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://api.acme.com/mcp/weather.json"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"capabilities"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"WeatherTool"</span><span class="p">,</span><span class="w"> </span><span class="s2">"ForecastTool"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"representativeQueries"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"current wind speed in Chicago"</span><span class="p">,</span><span class="w"> </span><span class="s2">"5-day forecast for Seattle"</span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<h3 id="layer-2-ard-the-consumption-api">Layer 2: ARD: the consumption API</h3>

<p>The <a href="https://agenticresourcediscovery.org/">Agentic Resource Discovery</a> is a <em>read-only discovery API layer</em> on top of the AI Catalog. The API is intentionally minimal and provides only static (<code class="language-plaintext highlighter-rouge">GET /.well-known/ai-catalog.json</code>) and dynamic (<code class="language-plaintext highlighter-rouge">GET /agents</code>, <code class="language-plaintext highlighter-rouge">POST /search</code>) discovery endpoints as entire consumption surface. It sits <em>before</em> invocation and never speaks other protocols; it just tells which resource to use and where they live.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"specVersion"</span><span class="p">:</span><span class="w"> </span><span class="s2">"1.0"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"host"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"displayName"</span><span class="p">:</span><span class="w"> </span><span class="s2">"AGNTCY Directory"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"identifier"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://ai-catalog.outshift.io"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"trustManifest"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
      </span><span class="nl">"identity"</span><span class="p">:</span><span class="w"> </span><span class="s2">"urn:ai:org.agntcy:host:12D3KooWLf9p3cedc86xGQBaqak6rAFmQk1HxKAK1yh7umHE3amu"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"identityType"</span><span class="p">:</span><span class="w"> </span><span class="s2">"did"</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"collections"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"displayName"</span><span class="p">:</span><span class="w"> </span><span class="s2">"A2A Agents"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"url"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://ai-catalog.outshift.io/v1/agents?filter=type%3Dapplication%2Fa2a-agent-card%2Bjson"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Agents that publish an A2A agent card."</span><span class="p">,</span><span class="w">
      </span><span class="nl">"mediaType"</span><span class="p">:</span><span class="w"> </span><span class="s2">"application/a2a-agent-card+json"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"tags"</span><span class="p">:</span><span class="w"> </span><span class="p">[],</span><span class="w">
      </span><span class="nl">"metadata"</span><span class="p">:</span><span class="w"> </span><span class="p">{}</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"displayName"</span><span class="p">:</span><span class="w"> </span><span class="s2">"MCP Servers"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"url"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://ai-catalog.outshift.io/v1/agents?filter=type%3Dapplication%2Fmcp-server-card%2Bjson"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Agents that expose an MCP server connection."</span><span class="p">,</span><span class="w">
      </span><span class="nl">"mediaType"</span><span class="p">:</span><span class="w"> </span><span class="s2">"application/mcp-server-card+json"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"tags"</span><span class="p">:</span><span class="w"> </span><span class="p">[],</span><span class="w">
      </span><span class="nl">"metadata"</span><span class="p">:</span><span class="w"> </span><span class="p">{}</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="p">{</span><span class="w">
      </span><span class="nl">"displayName"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Agent Skills"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"url"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://ai-catalog.outshift.io/v1/agents?filter=type%3Dapplication%2Fagentskill%2Bmd"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Agents that publish a reusable Agent Skill definition."</span><span class="p">,</span><span class="w">
      </span><span class="nl">"mediaType"</span><span class="p">:</span><span class="w"> </span><span class="s2">"application/agentskill+md"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"tags"</span><span class="p">:</span><span class="w"> </span><span class="p">[],</span><span class="w">
      </span><span class="nl">"metadata"</span><span class="p">:</span><span class="w"> </span><span class="p">{}</span><span class="w">
    </span><span class="p">}</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<h3 id="layer-3-ads-the-discovery-and-control-plane">Layer 3: ADS: the discovery and control plane</h3>

<p>ARD is a contract; something has to <em>fill and manage</em> the real records, real storage, and real trust. That role is performed by <a href="https://docs.agntcy.org/dir/overview/">ADS</a>, the only layer with a <strong>control plane</strong>: it ingests records, content-addresses them with CIDs, signs and verifies them, announces them to a DHT, replicates them over OCI, and enforces zero-trust identity between data, clients, and nodes. Where ARD answers “what fits this task?”, ADS owns “what exists, best fit, who vouches for it, and where it physically lives.”</p>

<p>The clean consequence is that <strong>ADS is a reference implementation of ARD.</strong> A directory node exposes its <a href="https://docs.agntcy.org/oasf/open-agentic-schema-framework/">OASF</a> records as AI Catalog entries and serves them over the ARD endpoints so the same inventory is reachable by any ARD client integration.</p>

<h2 id="what-the-specs-leave-unsolved">What the specs leave unsolved</h2>

<p>Both the AI Catalog and ARD specifications are scrupulous about scope. They define the <em>what</em> and the <em>how</em>, but not the <em>where</em> or the <em>who</em>. They describe a trust model, but do not ship a trust plane. They describe federation, but they do not ship a routing plane. They describe distribution, but they do not ship a storage plane.</p>

<h3 id="identity">Identity</h3>

<p>AI Catalog’s <a href="https://agent-card.github.io/ai-catalog/#trust-manifest">Trust Manifest</a> carries an <code class="language-plaintext highlighter-rouge">identity</code> URI, <code class="language-plaintext highlighter-rouge">attestations</code>, <code class="language-plaintext highlighter-rouge">provenance</code> and an optional <code class="language-plaintext highlighter-rouge">signature</code> fields. ARD reuses this object and additionally <em>requires</em> that the cryptographic identity align with the domain embedded in the resource’s URN. Both specs describe verification procedures, but neither defines how.</p>

<p>ADS already runs one. Records are addressed by <a href="https://github.com/multiformats/cid">CID</a>, so <code class="language-plaintext highlighter-rouge">provenance</code> models are native, not bolted on. Workload identity comes from <a href="https://spiffe.io/">SPIFFE</a> and <a href="https://datatracker.ietf.org/doc/html/rfc1035/">DNS/HTTPS</a> where every component gets a verifiable ID which is precisely the <code class="language-plaintext highlighter-rouge">identity</code> ARD wants anchored to a publisher domain. ADS also ships a signing and verification plane, so the <code class="language-plaintext highlighter-rouge">signature</code> field is not just a placeholder; it is a verifiable signature over the record’s CID.</p>

<h3 id="federation">Federation</h3>

<p>ARD’s federation model (<code class="language-plaintext highlighter-rouge">auto</code>, <code class="language-plaintext highlighter-rouge">referrals</code>, <code class="language-plaintext highlighter-rouge">none</code>) is elegant precisely because it assumes the implementations can route. <em>“Query upstream registries and merge”</em> is one HTTP line in the spec and a distributed-systems problem in practice. ADS solves the routing problem with a <a href="https://github.com/libp2p/specs/tree/master/kad-dht">libp2p Kad-DHT</a> and a two-phase lookup: <em>capability to CIDs</em>, then <em>CID to servers</em>. An ARD <code class="language-plaintext highlighter-rouge">referrals</code> response is, underneath, a projection of that routing table.</p>

<h3 id="distribution">Distribution</h3>

<p>AI Catalog’s maps the logical format onto OCI registries for content-addressed storage. ADS uses the <a href="https://github.com/opencontainers/distribution-spec">OCI distribution spec</a> as its reference storage model. The mapping the spec describes as future work is the mechanism ADS already ships today.</p>

<h2 id="end-to-end-discovery-to-invocation-flow-with-ard-over-ads">End-to-End Discovery-to-Invocation Flow with ARD over ADS</h2>

<p><a href="https://github.com/agntcy/dir">ADS v1.5</a> ships with a reference implementation of the ARD specification. A publisher can push a record to ADS and have it automatically available through any ARD client, regardless of whether the client speaks MCP, A2A, or any other protocol. The publisher does not need to implement multiple discovery protocols; they can rely on ADS to bridge the gap.</p>

<p>Below is a sequence diagram showing how a user request flows through the orchestrator defining an agentic workflow, ADS, and the federated DHT layer to discover and invoke the appropriate capability.</p>

<pre><code class="language-mermaid">sequenceDiagram
    autonumber
    participant U as User
    participant O as Orchestrator (Go)
    participant ADS as ADS node (ARD endpoint)
	participant DHT as DHT federation layer
	participant FADS as Federated ADS node
    participant P as Publisher
    participant T as Tool (MCP / A2A / REST)

	P-&gt;&gt;ADS: push signed OASF record
	ADS-&gt;&gt;DHT: announce capability + CID
	DHT--&gt;&gt;ADS: discoverable in federation

    U-&gt;&gt;O: "Book a flight to Tokyo and file the expense"
    O-&gt;&gt;ADS: POST /search { text, filter: type in [a2a, mcp] }
	ADS-&gt;&gt;DHT: federated lookup (capability -&gt; CID -&gt; providers)
	DHT--&gt;&gt;ADS: provider nodes + matching CIDs
	ADS-&gt;&gt;FADS: fetch federated records by CID
	FADS--&gt;&gt;ADS: sync federated records
    ADS--&gt;&gt;O: ranked CatalogEntries (type + url + score)
    O-&gt;&gt;O: select top entry per sub-task
    O-&gt;&gt;O: verify bindings + signature
    O-&gt;&gt;T: dispatch by media type → native protocol
    T--&gt;&gt;O: result
    O--&gt;&gt;U: task complete
</code></pre>

<p>The decisive line is where the orchestrator dispatches runs <strong>by media type</strong>. ADS doesn’t invoke anything; it tells the orchestrator <em>which</em> protocol each capability speaks, and the orchestrator routes accordingly. This is how one discovery query spans heterogeneous protocols and domains, exactly as envisioned in the agnostic discovery model of ADS. One caveat here is that the orchestrator must manually map ARD POST request (e.g. via LLM) to the appropriate GET request since ADS only supports deterministic search over ARD.</p>

<h2 id="building-the-orchestrator-in-go">Building the orchestrator in Go</h2>

<p>We use the recommended split: <strong>plain <code class="language-plaintext highlighter-rouge">net/http</code> for the ARD consumption API</strong> and the <strong>ADS control-plane SDK</strong> when we need to push, sign, or verify records. The code below is illustrative, modeled on the published <a href="https://dir.agntcy.org/latest/dir/dir-sdk/#go-sdk">ADS Go SDK</a> and the <a href="https://agenticresourcediscovery.org/">ARD spec</a>.</p>

<h3 id="step-1-publish-an-agentic-capability-to-ads">Step 1: Publish an agentic capability to ADS</h3>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">package</span> <span class="n">publisher</span>

<span class="k">import</span> <span class="p">(</span>
	<span class="n">adsclient</span> <span class="s">"github.com/agntcy/dir/client"</span>
	<span class="n">corev1</span> <span class="s">"github.com/agntcy/dir/api/core/v1"</span>
<span class="p">)</span>

<span class="c">// Publish an OASF record to ADS, sign it, then announce to DHT for federated discovery.</span>
<span class="c">// Local node indexes it and can serve it locally over ADS or ARD right away.</span>
<span class="k">func</span> <span class="n">Publish</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">client</span> <span class="o">*</span><span class="n">adsclient</span><span class="o">.</span><span class="n">Client</span><span class="p">,</span> <span class="n">record</span> <span class="o">*</span><span class="n">corev1</span><span class="o">.</span><span class="n">Record</span><span class="p">)</span> <span class="kt">error</span> <span class="p">{</span>
    <span class="c">// Push locally</span>
	<span class="n">ref</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">client</span><span class="o">.</span><span class="n">Push</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">corev1</span><span class="o">.</span><span class="n">New</span><span class="p">(</span><span class="o">&amp;</span><span class="n">typesv1</span><span class="o">.</span><span class="n">Record</span><span class="p">{</span>
        <span class="n">Name</span><span class="o">:</span> <span class="s">"weather-agent"</span><span class="p">,</span>
        <span class="n">Version</span><span class="o">:</span> <span class="s">"1.0.0"</span><span class="p">,</span>
        <span class="c">// ...</span>
    <span class="p">}))</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="k">return</span> <span class="n">err</span>
	<span class="p">}</span>

    <span class="c">// Sign the record to prove authenticity and integrity.</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">client</span><span class="o">.</span><span class="n">Sign</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">ref</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">err</span>
    <span class="p">}</span>

    <span class="c">// Publish to DHT for federated discovery, so other nodes can find it.</span>
    <span class="c">// Optional if only local discovery is needed.</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">client</span><span class="o">.</span><span class="n">Publish</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">ref</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
        <span class="k">return</span> <span class="n">err</span>
    <span class="p">}</span>

	<span class="k">return</span> <span class="no">nil</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="step-2-discover-over-ard">Step 2: Discover over ARD</h3>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">package</span> <span class="n">orchestrator</span>

<span class="k">import</span> <span class="p">(</span>
    <span class="n">catalogv1</span> <span class="s">"github.com/agntcy/dir/api/catalog/v1"</span>
<span class="p">)</span>

<span class="c">// Discover asks a local ADS node (via its ARD endpoint) for capabilities matching a task.</span>
<span class="k">func</span> <span class="n">Discover</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">registry</span><span class="p">,</span> <span class="n">taskQuery</span> <span class="kt">string</span><span class="p">)</span> <span class="p">([]</span><span class="o">*</span><span class="n">catalogv1</span><span class="o">.</span><span class="n">CatalogEntry</span><span class="p">,</span> <span class="kt">error</span><span class="p">)</span> <span class="p">{</span>
	<span class="n">httpReq</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">http</span><span class="o">.</span><span class="n">NewRequestWithContext</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">http</span><span class="o">.</span><span class="n">MethodGet</span><span class="p">,</span> <span class="n">registry</span><span class="o">+</span><span class="s">"/v1/agents"</span><span class="p">,</span> <span class="n">bytes</span><span class="o">.</span><span class="n">NewReader</span><span class="p">([]</span><span class="kt">byte</span><span class="p">(</span><span class="n">taskQuery</span><span class="p">)))</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="k">return</span> <span class="no">nil</span><span class="p">,</span> <span class="n">err</span>
	<span class="p">}</span>
	<span class="n">httpReq</span><span class="o">.</span><span class="n">Header</span><span class="o">.</span><span class="n">Set</span><span class="p">(</span><span class="s">"Content-Type"</span><span class="p">,</span> <span class="s">"application/json"</span><span class="p">)</span>

	<span class="n">resp</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="p">(</span><span class="o">&amp;</span><span class="n">http</span><span class="o">.</span><span class="n">Client</span><span class="p">{</span><span class="n">Timeout</span><span class="o">:</span> <span class="m">10</span> <span class="o">*</span> <span class="n">time</span><span class="o">.</span><span class="n">Second</span><span class="p">})</span><span class="o">.</span><span class="n">Do</span><span class="p">(</span><span class="n">httpReq</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="k">return</span> <span class="no">nil</span><span class="p">,</span> <span class="n">err</span>
	<span class="p">}</span>
	<span class="k">defer</span> <span class="n">resp</span><span class="o">.</span><span class="n">Body</span><span class="o">.</span><span class="n">Close</span><span class="p">()</span>

	<span class="k">var</span> <span class="n">out</span> <span class="n">catalogv1</span><span class="o">.</span><span class="n">AICatalog</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">json</span><span class="o">.</span><span class="n">NewDecoder</span><span class="p">(</span><span class="n">resp</span><span class="o">.</span><span class="n">Body</span><span class="p">)</span><span class="o">.</span><span class="n">Decode</span><span class="p">(</span><span class="o">&amp;</span><span class="n">out</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="k">return</span> <span class="no">nil</span><span class="p">,</span> <span class="n">err</span>
	<span class="p">}</span>
	<span class="k">return</span> <span class="n">out</span><span class="o">.</span><span class="n">Entries</span><span class="p">,</span> <span class="no">nil</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="step-2-dispatch-by-media-type-to-the-native-protocol">Step 2: Dispatch by media type to the native protocol</h3>

<p>Discovery hands us a <code class="language-plaintext highlighter-rouge">type</code> and a <code class="language-plaintext highlighter-rouge">url</code>. The orchestrator’s job is to route to the right execution path and not to “guess” a protocol:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// Dispatch routes a discovered entry to its native invocation path.</span>
<span class="k">func</span> <span class="n">Dispatch</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">e</span> <span class="o">*</span><span class="n">catalogv1</span><span class="o">.</span><span class="n">CatalogEntry</span><span class="p">,</span> <span class="n">task</span> <span class="k">map</span><span class="p">[</span><span class="kt">string</span><span class="p">]</span><span class="n">any</span><span class="p">)</span> <span class="p">(</span><span class="n">any</span><span class="p">,</span> <span class="kt">error</span><span class="p">)</span> <span class="p">{</span>
	<span class="k">switch</span> <span class="n">e</span><span class="o">.</span><span class="n">Type</span> <span class="p">{</span>
	<span class="k">case</span> <span class="s">"application/mcp-server-card+json"</span><span class="o">:</span>
		<span class="c">// Fetch the MCP server descriptor at e.URL, then speak JSON-RPC.</span>
		<span class="k">return</span> <span class="n">invokeMCP</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">e</span><span class="o">.</span><span class="n">URL</span><span class="p">,</span> <span class="n">task</span><span class="p">)</span>
	<span class="k">case</span> <span class="s">"application/a2a-agent-card+json"</span><span class="o">:</span>
		<span class="c">// Load the A2A agent card at e.URL, then speak A2A.</span>
		<span class="k">return</span> <span class="n">invokeA2A</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">e</span><span class="o">.</span><span class="n">URL</span><span class="p">,</span> <span class="n">task</span><span class="p">)</span>
	<span class="k">case</span> <span class="s">"application/agentskill+md"</span><span class="o">:</span>
		<span class="c">// Install or load the skill referenced by e.URL.</span>
		<span class="k">return</span> <span class="n">loadSkill</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">e</span><span class="o">.</span><span class="n">URL</span><span class="p">)</span>
	<span class="k">default</span><span class="o">:</span>
		<span class="k">return</span> <span class="no">nil</span><span class="p">,</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"unsupported media type: %s"</span><span class="p">,</span> <span class="n">e</span><span class="o">.</span><span class="n">Type</span><span class="p">)</span>
	<span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>This <code class="language-plaintext highlighter-rouge">switch</code> <em>is</em> the interoperability boundary. ADS guarantees the <code class="language-plaintext highlighter-rouge">type</code> is a well-known IANA media type; the orchestrator owns the mapping from type to protocol client. Add a new artifact type to your stack, and you add one <code class="language-plaintext highlighter-rouge">case</code> while the discovery itself is unchanged.</p>

<h3 id="step-3-verify-trust-before-you-invoke">Step 3: Verify trust before you invoke</h3>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// VerifyTrust confirms the entry owns its claimed identity and trust signals.</span>
<span class="k">func</span> <span class="n">VerifyTrust</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">client</span> <span class="o">*</span><span class="n">adsclient</span><span class="o">.</span><span class="n">Client</span><span class="p">,</span> <span class="n">e</span> <span class="o">*</span><span class="n">catalogv1</span><span class="o">.</span><span class="n">CatalogEntry</span><span class="p">)</span> <span class="kt">error</span> <span class="p">{</span>
	<span class="n">did</span><span class="p">,</span> <span class="n">cid</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">publisherFromURN</span><span class="p">(</span><span class="n">e</span><span class="o">.</span><span class="n">Identifier</span><span class="p">)</span> <span class="c">// urn:ai:org.agntcy:cid:&lt;cid&gt;</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="k">return</span> <span class="n">err</span>
	<span class="p">}</span>
	<span class="k">return</span> <span class="n">client</span><span class="o">.</span><span class="n">Verify</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">did</span><span class="p">,</span> <span class="n">cid</span><span class="p">)</span> <span class="c">// checks signature, provenance, and identity alignment</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="step-4-tie-it-together">Step 4: Tie it together</h3>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">Handle</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">registry</span><span class="p">,</span> <span class="n">taskQuery</span> <span class="kt">string</span><span class="p">)</span> <span class="kt">error</span> <span class="p">{</span>
	<span class="n">entries</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">Discover</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">registry</span><span class="p">,</span> <span class="n">taskQuery</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="k">return</span> <span class="n">err</span>
	<span class="p">}</span>
	<span class="k">for</span> <span class="n">_</span><span class="p">,</span> <span class="n">e</span> <span class="o">:=</span> <span class="k">range</span> <span class="n">entries</span> <span class="p">{</span>
		<span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">VerifyTrust</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">client</span><span class="p">,</span> <span class="n">e</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
			<span class="k">continue</span> <span class="c">// skip anything we can't verify</span>
		<span class="p">}</span>
		<span class="k">if</span> <span class="n">_</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">Dispatch</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">e</span><span class="p">,</span> <span class="k">map</span><span class="p">[</span><span class="kt">string</span><span class="p">]</span><span class="n">any</span><span class="p">{</span><span class="s">"task"</span><span class="o">:</span> <span class="n">taskQuery</span><span class="p">});</span> <span class="n">err</span> <span class="o">==</span> <span class="no">nil</span> <span class="p">{</span>
			<span class="k">return</span> <span class="no">nil</span> <span class="c">// first trusted, working capability wins</span>
		<span class="p">}</span>
	<span class="p">}</span>
	<span class="k">return</span> <span class="n">errors</span><span class="o">.</span><span class="n">New</span><span class="p">(</span><span class="s">"no trusted capability satisfied the task"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="where-the-ads-control-plane-earns-its-keep">Where the ADS control plane earns its keep</h2>

<p>The consumption side is plain HTTP, but the <em>security gate</em> of what comes back depends entirely on the ADS control plane that produced it. When you publish the agents your orchestrator will later discover, you use the ADS SDK, not raw HTTP. This is the division of labor that makes the whole stack work:</p>

<pre><code class="language-mermaid">flowchart LR
    subgraph Control["ADS control plane (ADS SDK)"]
        PUSH["Push + sign OASF record"]
        INDEX["OCI store + DHT announce"]
    end
    subgraph Consume["ARD consumption (net/http)"]
        SEARCH["GET /agents"]
        PROJECT["OASF → AI Catalog entry"]
    end
    PUSH --&gt; INDEX --&gt; PROJECT --&gt; SEARCH --&gt; ORCH["Orchestrator dispatch"]

    style Control fill:#5ba67a,stroke:#478a60,color:#fff
    style Consume fill:#d4a054,stroke:#b08540,color:#fff
    style ORCH fill:#9a7bb0,stroke:#7a5f8c,color:#fff
</code></pre>

<p>Publishers and platform teams drive the <strong>control plane</strong> (push, sign, index, federate). Orchestrators and clients ride the <strong>consumption plane</strong> (search, verify, dispatch). The contract between them is the AI Catalog entry, which is why an agentic resource published and discovered regardless of the protocol or domain.</p>

<h3 id="why-this-matters">Why this matters</h3>

<p>Here is the dependency between ARD and ADS, made concrete as a mapping table.</p>

<table>
  <thead>
    <tr>
      <th>Higher-level concept</th>
      <th>Spec field</th>
      <th>ADS primitives</th>
      <th>Supported by ADS</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Artifact identity</td>
      <td><code class="language-plaintext highlighter-rouge">identifier</code></td>
      <td>OASF record + verifiable domain name</td>
      <td>✅</td>
    </tr>
    <tr>
      <td>Content integrity</td>
      <td><code class="language-plaintext highlighter-rouge">provenance.sourceDigest</code></td>
      <td>CID (content addressing)</td>
      <td>✅</td>
    </tr>
    <tr>
      <td>Publisher identity</td>
      <td><code class="language-plaintext highlighter-rouge">trustManifest.identity</code></td>
      <td>SPIFFE + DID identity</td>
      <td>✅</td>
    </tr>
    <tr>
      <td>Authenticity</td>
      <td><code class="language-plaintext highlighter-rouge">trustManifest.signature</code></td>
      <td>OIDC / key-based sign + verify</td>
      <td>✅</td>
    </tr>
    <tr>
      <td>Search</td>
      <td>ARD REST API</td>
      <td>ARD REST API without <code class="language-plaintext highlighter-rouge">POST /search</code></td>
      <td>ADS only supports deterministic search due to security and complexity concerns</td>
    </tr>
    <tr>
      <td>Federation</td>
      <td><code class="language-plaintext highlighter-rouge">federation: referrals \| auto</code></td>
      <td>DHT two-phase routing + sync</td>
      <td>✅</td>
    </tr>
    <tr>
      <td>Distribution</td>
      <td>OCI mapping</td>
      <td>OCI v1.1 distribution spec</td>
      <td>✅</td>
    </tr>
  </tbody>
</table>

<ul>
  <li><strong>No protocol lock-in for publishers.</strong> A record pushed to ADS is reachable through AI Catalog resolution, an ARD search registry, or a direct gRPC query. The publisher does not need to worry.</li>
  <li><strong>Security is solved once, at the bottom.</strong> SPIFFE identity, DID resolution, CID integrity, and signing available directly. Higher layers reference them through <code class="language-plaintext highlighter-rouge">trustManifest</code> fields instead of reinventing key management.</li>
  <li><strong>Federation is real, not only desirable.</strong> ARD’s <code class="language-plaintext highlighter-rouge">referrals</code> and <code class="language-plaintext highlighter-rouge">auto</code> modes need a routing fabric to be more than a single registry. The DHT can be that fabric.</li>
</ul>

<hr />

<p><em>ADS and its components are developed by AGNTCY Contributors and released under the Apache 2.0 License.</em></p>]]></content><author><name>Ramiz Polic</name></author><category term="technical" /><category term="ads" /><category term="a2a" /><category term="mcp" /><category term="agent-skills" /><category term="discovery" /><category term="ai-catalog" /><category term="ard" /><summary type="html"><![CDATA[A developer integrating agentic capabilities today faces a discovery problem that looks deceptively like a solved one. We have standards, registries, catalogs, and well-knowns; surely finding the right tool is just a lookup? In practice it is not, because the ecosystem has fragmented into protocols that each describe the same artifact differently; be it a resource, tool, agent, or a full agentic system. And while the artifact is one thing; its discovery representations are many. The question of discovery and interoperability is not achieved by picking a winning format or specification. AI Catalog and Agentic Resource Discovery are not competing standards; they are complementary lenses on the same underlying problem. Each one tries to answers a shared set of questions - what is this artifact, what can it do, how to use it, who published it, and can I trust it. Most of these formats are higher-level discovery protocols: they define a vocabulary and an envelope. What they do not define is the hard part – a secure, federated, capability-driven discovery layer that actually links these artifacts, routes queries across organizational boundaries, and provides security and control policies embedded in. That layer is exactly what Agent Directory Service (ADS) provides - not another competing registry, but a low-level protocol the others can be built on. Three layers, one stack A recurring design mistake is to collapse “the format”, “the query API”, and “the system” into one monolith. The agentic ecosystem deliberately keeps them apart, and the separation is the whole reason it interoperates. flowchart BT subgraph L3["ADS -- Discovery &amp; Control Plane"] OASF["Records"] API["API (gRPC / REST)"] CID["Identifiers (CIDs / DIDs)"] DHT["Routing (DHT)"] OCI["Storage (OCI)"] SIGN["Security / Policies"] end subgraph L2["ARD -- Consumption API"] WK["GET /.well-known/ai-catalog.json"] SEARCH["POST /search"] LIST["GET /v1/agents"] end subgraph L1["AI Catalog -- Index / Content Standard"] ENTRY["Catalog Entry Schema"] TRUST["Trust Manifest Schema"] TYPES["IANA media types"] end L1 --&gt;|"mapped by"| L2 L2 --&gt;|"implemented by"| L3 L3 --&gt;|"consumed by"| CLIENT["AI clients / applications / orchestrators"] Layer 1: AI Catalog: the index standard The AI Catalog is a typed, nestable JSON container for describing heterogeneous AI artifacts. Each entry identifies an artifact by an IANA media type and a domain-anchored URN. The catalog is therefore protocol-agnostic: it can describe an MCP server, an A2A agent, a custom tool, or a set of catalogs in the same way. { "identifier": "urn:ai:acme.com:agents:weather", "displayName": "Weather Agent", "type": "application/mcp-server+json", "url": "https://api.acme.com/mcp/weather.json", "capabilities": ["WeatherTool", "ForecastTool"], "representativeQueries": ["current wind speed in Chicago", "5-day forecast for Seattle"] } Layer 2: ARD: the consumption API The Agentic Resource Discovery is a read-only discovery API layer on top of the AI Catalog. The API is intentionally minimal and provides only static (GET /.well-known/ai-catalog.json) and dynamic (GET /agents, POST /search) discovery endpoints as entire consumption surface. It sits before invocation and never speaks other protocols; it just tells which resource to use and where they live. { "specVersion": "1.0", "host": { "displayName": "AGNTCY Directory", "identifier": "https://ai-catalog.outshift.io", "trustManifest": { "identity": "urn:ai:org.agntcy:host:12D3KooWLf9p3cedc86xGQBaqak6rAFmQk1HxKAK1yh7umHE3amu", "identityType": "did" } }, "collections": [ { "displayName": "A2A Agents", "url": "https://ai-catalog.outshift.io/v1/agents?filter=type%3Dapplication%2Fa2a-agent-card%2Bjson", "description": "Agents that publish an A2A agent card.", "mediaType": "application/a2a-agent-card+json", "tags": [], "metadata": {} }, { "displayName": "MCP Servers", "url": "https://ai-catalog.outshift.io/v1/agents?filter=type%3Dapplication%2Fmcp-server-card%2Bjson", "description": "Agents that expose an MCP server connection.", "mediaType": "application/mcp-server-card+json", "tags": [], "metadata": {} }, { "displayName": "Agent Skills", "url": "https://ai-catalog.outshift.io/v1/agents?filter=type%3Dapplication%2Fagentskill%2Bmd", "description": "Agents that publish a reusable Agent Skill definition.", "mediaType": "application/agentskill+md", "tags": [], "metadata": {} } ] } Layer 3: ADS: the discovery and control plane ARD is a contract; something has to fill and manage the real records, real storage, and real trust. That role is performed by ADS, the only layer with a control plane: it ingests records, content-addresses them with CIDs, signs and verifies them, announces them to a DHT, replicates them over OCI, and enforces zero-trust identity between data, clients, and nodes. Where ARD answers “what fits this task?”, ADS owns “what exists, best fit, who vouches for it, and where it physically lives.” The clean consequence is that ADS is a reference implementation of ARD. A directory node exposes its OASF records as AI Catalog entries and serves them over the ARD endpoints so the same inventory is reachable by any ARD client integration. What the specs leave unsolved Both the AI Catalog and ARD specifications are scrupulous about scope. They define the what and the how, but not the where or the who. They describe a trust model, but do not ship a trust plane. They describe federation, but they do not ship a routing plane. They describe distribution, but they do not ship a storage plane. Identity AI Catalog’s Trust Manifest carries an identity URI, attestations, provenance and an optional signature fields. ARD reuses this object and additionally requires that the cryptographic identity align with the domain embedded in the resource’s URN. Both specs describe verification procedures, but neither defines how. ADS already runs one. Records are addressed by CID, so provenance models are native, not bolted on. Workload identity comes from SPIFFE and DNS/HTTPS where every component gets a verifiable ID which is precisely the identity ARD wants anchored to a publisher domain. ADS also ships a signing and verification plane, so the signature field is not just a placeholder; it is a verifiable signature over the record’s CID. Federation ARD’s federation model (auto, referrals, none) is elegant precisely because it assumes the implementations can route. “Query upstream registries and merge” is one HTTP line in the spec and a distributed-systems problem in practice. ADS solves the routing problem with a libp2p Kad-DHT and a two-phase lookup: capability to CIDs, then CID to servers. An ARD referrals response is, underneath, a projection of that routing table. Distribution AI Catalog’s maps the logical format onto OCI registries for content-addressed storage. ADS uses the OCI distribution spec as its reference storage model. The mapping the spec describes as future work is the mechanism ADS already ships today. End-to-End Discovery-to-Invocation Flow with ARD over ADS ADS v1.5 ships with a reference implementation of the ARD specification. A publisher can push a record to ADS and have it automatically available through any ARD client, regardless of whether the client speaks MCP, A2A, or any other protocol. The publisher does not need to implement multiple discovery protocols; they can rely on ADS to bridge the gap. Below is a sequence diagram showing how a user request flows through the orchestrator defining an agentic workflow, ADS, and the federated DHT layer to discover and invoke the appropriate capability. sequenceDiagram autonumber participant U as User participant O as Orchestrator (Go) participant ADS as ADS node (ARD endpoint) participant DHT as DHT federation layer participant FADS as Federated ADS node participant P as Publisher participant T as Tool (MCP / A2A / REST) P-&gt;&gt;ADS: push signed OASF record ADS-&gt;&gt;DHT: announce capability + CID DHT--&gt;&gt;ADS: discoverable in federation U-&gt;&gt;O: "Book a flight to Tokyo and file the expense" O-&gt;&gt;ADS: POST /search { text, filter: type in [a2a, mcp] } ADS-&gt;&gt;DHT: federated lookup (capability -&gt; CID -&gt; providers) DHT--&gt;&gt;ADS: provider nodes + matching CIDs ADS-&gt;&gt;FADS: fetch federated records by CID FADS--&gt;&gt;ADS: sync federated records ADS--&gt;&gt;O: ranked CatalogEntries (type + url + score) O-&gt;&gt;O: select top entry per sub-task O-&gt;&gt;O: verify bindings + signature O-&gt;&gt;T: dispatch by media type → native protocol T--&gt;&gt;O: result O--&gt;&gt;U: task complete The decisive line is where the orchestrator dispatches runs by media type. ADS doesn’t invoke anything; it tells the orchestrator which protocol each capability speaks, and the orchestrator routes accordingly. This is how one discovery query spans heterogeneous protocols and domains, exactly as envisioned in the agnostic discovery model of ADS. One caveat here is that the orchestrator must manually map ARD POST request (e.g. via LLM) to the appropriate GET request since ADS only supports deterministic search over ARD. Building the orchestrator in Go We use the recommended split: plain net/http for the ARD consumption API and the ADS control-plane SDK when we need to push, sign, or verify records. The code below is illustrative, modeled on the published ADS Go SDK and the ARD spec. Step 1: Publish an agentic capability to ADS package publisher import ( adsclient "github.com/agntcy/dir/client" corev1 "github.com/agntcy/dir/api/core/v1" ) // Publish an OASF record to ADS, sign it, then announce to DHT for federated discovery. // Local node indexes it and can serve it locally over ADS or ARD right away. func Publish(ctx context.Context, client *adsclient.Client, record *corev1.Record) error { // Push locally ref, err := client.Push(ctx, &amp;corev1.New(&amp;typesv1.Record{ Name: "weather-agent", Version: "1.0.0", // ... })) if err != nil { return err } // Sign the record to prove authenticity and integrity. if err := client.Sign(ctx, ref); err != nil { return err } // Publish to DHT for federated discovery, so other nodes can find it. // Optional if only local discovery is needed. if err := client.Publish(ctx, ref); err != nil { return err } return nil } Step 2: Discover over ARD package orchestrator import ( catalogv1 "github.com/agntcy/dir/api/catalog/v1" ) // Discover asks a local ADS node (via its ARD endpoint) for capabilities matching a task. func Discover(ctx context.Context, registry, taskQuery string) ([]*catalogv1.CatalogEntry, error) { httpReq, err := http.NewRequestWithContext(ctx, http.MethodGet, registry+"/v1/agents", bytes.NewReader([]byte(taskQuery))) if err != nil { return nil, err } httpReq.Header.Set("Content-Type", "application/json") resp, err := (&amp;http.Client{Timeout: 10 * time.Second}).Do(httpReq) if err != nil { return nil, err } defer resp.Body.Close() var out catalogv1.AICatalog if err := json.NewDecoder(resp.Body).Decode(&amp;out); err != nil { return nil, err } return out.Entries, nil } Step 2: Dispatch by media type to the native protocol Discovery hands us a type and a url. The orchestrator’s job is to route to the right execution path and not to “guess” a protocol: // Dispatch routes a discovered entry to its native invocation path. func Dispatch(ctx context.Context, e *catalogv1.CatalogEntry, task map[string]any) (any, error) { switch e.Type { case "application/mcp-server-card+json": // Fetch the MCP server descriptor at e.URL, then speak JSON-RPC. return invokeMCP(ctx, e.URL, task) case "application/a2a-agent-card+json": // Load the A2A agent card at e.URL, then speak A2A. return invokeA2A(ctx, e.URL, task) case "application/agentskill+md": // Install or load the skill referenced by e.URL. return loadSkill(ctx, e.URL) default: return nil, fmt.Errorf("unsupported media type: %s", e.Type) } } This switch is the interoperability boundary. ADS guarantees the type is a well-known IANA media type; the orchestrator owns the mapping from type to protocol client. Add a new artifact type to your stack, and you add one case while the discovery itself is unchanged. Step 3: Verify trust before you invoke // VerifyTrust confirms the entry owns its claimed identity and trust signals. func VerifyTrust(ctx context.Context, client *adsclient.Client, e *catalogv1.CatalogEntry) error { did, cid, err := publisherFromURN(e.Identifier) // urn:ai:org.agntcy:cid:&lt;cid&gt; if err != nil { return err } return client.Verify(ctx, did, cid) // checks signature, provenance, and identity alignment } Step 4: Tie it together func Handle(ctx context.Context, registry, taskQuery string) error { entries, err := Discover(ctx, registry, taskQuery) if err != nil { return err } for _, e := range entries { if err := VerifyTrust(ctx, client, e); err != nil { continue // skip anything we can't verify } if _, err := Dispatch(ctx, e, map[string]any{"task": taskQuery}); err == nil { return nil // first trusted, working capability wins } } return errors.New("no trusted capability satisfied the task") } Where the ADS control plane earns its keep The consumption side is plain HTTP, but the security gate of what comes back depends entirely on the ADS control plane that produced it. When you publish the agents your orchestrator will later discover, you use the ADS SDK, not raw HTTP. This is the division of labor that makes the whole stack work: flowchart LR subgraph Control["ADS control plane (ADS SDK)"] PUSH["Push + sign OASF record"] INDEX["OCI store + DHT announce"] end subgraph Consume["ARD consumption (net/http)"] SEARCH["GET /agents"] PROJECT["OASF → AI Catalog entry"] end PUSH --&gt; INDEX --&gt; PROJECT --&gt; SEARCH --&gt; ORCH["Orchestrator dispatch"] style Control fill:#5ba67a,stroke:#478a60,color:#fff style Consume fill:#d4a054,stroke:#b08540,color:#fff style ORCH fill:#9a7bb0,stroke:#7a5f8c,color:#fff Publishers and platform teams drive the control plane (push, sign, index, federate). Orchestrators and clients ride the consumption plane (search, verify, dispatch). The contract between them is the AI Catalog entry, which is why an agentic resource published and discovered regardless of the protocol or domain. Why this matters Here is the dependency between ARD and ADS, made concrete as a mapping table. Higher-level concept Spec field ADS primitives Supported by ADS Artifact identity identifier OASF record + verifiable domain name ✅ Content integrity provenance.sourceDigest CID (content addressing) ✅ Publisher identity trustManifest.identity SPIFFE + DID identity ✅ Authenticity trustManifest.signature OIDC / key-based sign + verify ✅ Search ARD REST API ARD REST API without POST /search ADS only supports deterministic search due to security and complexity concerns Federation federation: referrals \| auto DHT two-phase routing + sync ✅ Distribution OCI mapping OCI v1.1 distribution spec ✅ No protocol lock-in for publishers. A record pushed to ADS is reachable through AI Catalog resolution, an ARD search registry, or a direct gRPC query. The publisher does not need to worry. Security is solved once, at the bottom. SPIFFE identity, DID resolution, CID integrity, and signing available directly. Higher layers reference them through trustManifest fields instead of reinventing key management. Federation is real, not only desirable. ARD’s referrals and auto modes need a routing fabric to be more than a single registry. The DHT can be that fabric. ADS and its components are developed by AGNTCY Contributors and released under the Apache 2.0 License.]]></summary></entry></feed>