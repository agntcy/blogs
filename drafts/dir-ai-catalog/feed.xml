<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://blogs.agntcy.org/drafts/dir-ai-catalog/feed.xml" rel="self" type="application/atom+xml" /><link href="https://blogs.agntcy.org/drafts/dir-ai-catalog/" rel="alternate" type="text/html" /><updated>2026-06-17T14:01:04+00:00</updated><id>https://blogs.agntcy.org/drafts/dir-ai-catalog/feed.xml</id><title type="html">AGNTCY Blogs</title><subtitle>Building infrastructure for the Internet of Agents</subtitle><entry><title type="html">ARD over Agent Directory: Interoperability by Design</title><link href="https://blogs.agntcy.org/drafts/dir-ai-catalog/technical/2026/06/16/ai-catalog-over-directory.html" rel="alternate" type="text/html" title="ARD over Agent Directory: Interoperability by Design" /><published>2026-06-16T12:00:00+00:00</published><updated>2026-06-16T12:00:00+00:00</updated><id>https://blogs.agntcy.org/drafts/dir-ai-catalog/technical/2026/06/16/ai-catalog-over-directory</id><content type="html" xml:base="https://blogs.agntcy.org/drafts/dir-ai-catalog/technical/2026/06/16/ai-catalog-over-directory.html"><![CDATA[<p>A developer integrating agentic capabilities today faces a discovery problem that looks deceptively like a solved one. We have standards, registries, catalogs, and well-knowns; surely finding the right tool is just a lookup? In practice it is not, because the ecosystem has fragmented into protocols that each describe the <em>same</em> artifact differently; be it a resource, tool, agent, or a full agentic system. And while the artifact is one thing; its discovery representations are many.</p>

<p>The question of discovery and interoperability is not achieved by picking a winning format or specification. <a href="https://agent-card.github.io/ai-catalog">AI Catalog</a> and <a href="https://agenticresourcediscovery.org/">Agentic Resource Discovery (ARD)</a> are not competing standards; they are complementary lenses on the same underlying problem. Each one tries to answers a shared set of questions - <em>what is this artifact</em>, <em>what can it do</em>, <em>how to use it</em>, <em>who published it</em>, and <em>can I trust it</em>.</p>

<p>Most of these formats are <strong>higher-level discovery protocols</strong>: they define a vocabulary and an envelope. What they do <em>not</em> define is the hard part – a secure, federated, capability-driven discovery layer that actually links these artifacts, routes queries across organizational boundaries, and provides security and control policies embedded in. That layer is exactly what <a href="https://github.com/agntcy/dir">Agent Directory Service (ADS)</a> provides - not another competing registry, but a <strong>low-level protocol</strong> the others can be built on.</p>

<!-- **TL;DR:** AI Catalog and ARD are intentionally artifact-agnostic envelopes; they identify *what* an agentic thing is and delegate *how* to invoke it to the native protocol. ADS supplies the shared schema, identity, routing, and security layer beneath them. A consumer can therefore discover through ARD, verify through ADS, and invoke through MCP or A2A -- one flow, many protocols. -->

<h2 id="three-layers-one-stack">Three layers, one stack</h2>

<p>A recurring design mistake is to collapse “the format”, “the query API”, and “the system” into one monolith. The agentic ecosystem deliberately keeps them apart, and the separation is the whole reason it interoperates.</p>

<pre><code class="language-mermaid">flowchart BT
    subgraph L3["ADS -- Discovery &amp; Control Plane"]
        OASF["Records"]
        API["API (gRPC / REST)"]
        CID["Identifiers (CIDs / DIDs)"]
        DHT["Routing (DHT)"]
        OCI["Storage (OCI)"]
        SIGN["Security / Policies"]
    end
    subgraph L2["ARD -- Consumption API"]
        WK["GET /.well-known/ai-catalog.json"]
        SEARCH["POST /search"]
        LIST["GET /agents"]
    end
    subgraph L1["AI Catalog -- Index / Content Standard"]
        ENTRY["Catalog Entry Schema"]
        TRUST["Trust Manifest Schema"]
        TYPES["IANA media types"]
    end

    L1 --&gt;|"mapped by"| L2
    L2 --&gt;|"implemented by"| L3
    L3 --&gt;|"consumed by"| CLIENT["AI clients / applications / systems"]

    style CLIENT fill:#800080,stroke:#800080,color:#fff
    style OASF fill:#023a7a,color:#f3f6fd
    style API fill:#023a7a,color:#f3f6fd
    style CID fill:#023a7a,color:#f3f6fd
    style DHT fill:#023a7a,color:#f3f6fd
    style OCI fill:#023a7a,color:#f3f6fd
    style SIGN fill:#023a7a,color:#f3f6fd
    style WK fill:#023a7a,color:#f3f6fd
    style SEARCH fill:#023a7a,color:#f3f6fd
    style LIST fill:#023a7a,color:#f3f6fd
    style ENTRY fill:#023a7a,color:#f3f6fd
    style TRUST fill:#023a7a,color:#f3f6fd
    style TYPES fill:#023a7a,color:#f3f6fd
</code></pre>

<h3 id="layer-1-ai-catalog-as-the-index-standard">Layer 1: AI Catalog as the index standard</h3>

<p>The <a href="https://agent-card.github.io/ai-catalog/">AI Catalog</a> is a <em>typed, nestable JSON container</em> for describing heterogeneous AI artifacts. Each entry identifies an artifact by an <strong>IANA media type</strong> and a <strong>domain-anchored URN</strong>. The catalog is therefore <em>protocol-agnostic</em>: it can describe an MCP server, an A2A agent, a custom tool, or a set of catalogs in the same way. On its own, the catalog is a static index intended to be consumed by AI applications and served over higher-level discovery protocols like ARD.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"identifier"</span><span class="p">:</span><span class="w"> </span><span class="s2">"urn:ai:acme.com:agents:weather"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"displayName"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Weather Agent"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"application/mcp-server+json"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"url"</span><span class="p">:</span><span class="w"> </span><span class="s2">"https://api.acme.com/mcp/weather.json"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"capabilities"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"WeatherTool"</span><span class="p">,</span><span class="w"> </span><span class="s2">"ForecastTool"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"representativeQueries"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="s2">"current wind speed in Chicago"</span><span class="p">,</span><span class="w">
    </span><span class="s2">"5-day forecast for Seattle"</span><span class="p">,</span><span class="w">
  </span><span class="p">]</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<h3 id="layer-2-ard-as-the-consumption-api">Layer 2: ARD as the consumption API</h3>

<p>The <a href="https://agenticresourcediscovery.org/">Agentic Resource Discovery</a> is a <em>read-only discovery API layer</em> on top of the AI Catalog. It defines how AI artifacts are cataloged, discovered, and searched within and across registries. It promotes the search-first discovery where registries maintain a shared index while the clients find capabilities over standard HTTP REST APIs instead of loading every tool description into an LLM context window. The ARD spec is intentionally agnostic about the underlying storage, identity, and routing mechanisms; it only defines the <em>contract</em> but does not ship discovery or control plane.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>curl <span class="nt">-s</span> https://your-discovery-service/search <span class="se">\</span>
  <span class="nt">-H</span> <span class="s1">'content-type: application/json'</span> <span class="se">\</span>
  <span class="nt">-d</span> <span class="s1">'{"query": {"text":"book me a flight to Tokyo"}, "pageSize":3}'</span>
<span class="o">{</span>
  <span class="s2">"results"</span>: <span class="o">[</span>
    <span class="o">{</span>
      <span class="s2">"identifier"</span>: <span class="s2">"urn:ai:acme.com:travel:concierge"</span>,
      <span class="s2">"displayName"</span>: <span class="s2">"Travel Concierge"</span>,
      <span class="s2">"type"</span>: <span class="s2">"application/mcp-server+json"</span>,
      <span class="s2">"url"</span>: <span class="s2">"https://api.acme.com/mcp/travel.json"</span>,
      <span class="s2">"score"</span>: 95,
      <span class="s2">"source"</span>: <span class="s2">"https://registry.acme.com/api/v1/"</span>
    <span class="o">}</span>
  <span class="o">]</span>,
  <span class="s2">"referrals"</span>: <span class="o">[</span>
    <span class="o">{</span>
      <span class="s2">"identifier"</span>: <span class="s2">"urn:ai:example.org:registry"</span>,
      <span class="s2">"type"</span>: <span class="s2">"application/ai-registry"</span>,
      <span class="s2">"url"</span>: <span class="s2">"https://registry.acme.com/search"</span>
    <span class="o">}</span>
  <span class="o">]</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="layer-3-ads-as-the-discovery-and-control-plane">Layer 3: ADS as the discovery and control plane</h3>

<p>ARD is a contract; something has to <em>fill and manage</em> the data, storage, routing, and trust. That role is performed by <a href="https://docs.agntcy.org/dir/overview/">ADS</a>, the only layer with a <strong>control plane</strong>: it ingests records, content-addresses them with CIDs, signs and verifies them, announces them to a DHT, replicates them over OCI, and enforces zero-trust identity between data, clients, and nodes. Where ARD answers “what fits this task?”, ADS owns “what exists, best fit, who vouches for it, and where it physically lives.”</p>

<p>The clean consequence is that <strong>ADS is a reference implementation of ARD.</strong> ADS is a distributed registry that exposes its <a href="https://docs.agntcy.org/oasf/open-agentic-schema-framework/">OASF</a> records as AI Catalog entries and serves them securely over the ARD endpoints so the same inventory is reachable by both ADS and ARD client integrations. Operators publish records to ADS while the consumers discover through ARD, verify through ADS, and invoke through MCP or A2A – one flow, many protocols.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Start local ADS node in the background</span>
<span class="nv">$ </span>dirctl daemon start &amp;

<span class="c"># Define sample agent skill data</span>
<span class="nv">$ </span><span class="nb">export </span><span class="nv">SKILL_DATA</span><span class="o">=</span><span class="s2">"This is a sample agent skill for weather details over AccuWeather."</span>

<span class="c"># Push and sign a sample record</span>
<span class="nv">$ </span><span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | dirctl push --sign --stdin
{
  "name": "Weather Agent",
  "schema_version": "1.0.0",
  "version": "1.0.0",
  "description": "Helps with tracking weather information",
  "authors": ["Example Organization"],
  "created_at": "2025-01-01T00:00:00Z",
  "skills": [
    {"name": "natural_language_processing/natural_language_understanding/contextual_comprehension"}
  ],
  "modules": [
    {
      "name": "core/language_model/agentskills",
      "data": {
        "skill_file": "SKILL.md",
        "skill_manifest": {
          "name": "Weather Agent",
          "description": "Helps with tracking weather information",
          "version": "1.0.0"
        }
      },
      "artifact": {
        "data": "</span><span class="si">$(</span><span class="nb">echo</span> <span class="s2">"</span><span class="nv">$SKILL_DATA</span><span class="s2">"</span> | <span class="nb">base64</span><span class="si">)</span><span class="sh">",
        "media_type": "application/agentskill+md",
        "digest": "sha256:</span><span class="si">$(</span><span class="nb">echo</span> <span class="nt">-n</span> <span class="s2">"</span><span class="nv">$SKILL_DATA</span><span class="s2">"</span> | <span class="nb">sha256sum</span> | <span class="nb">awk</span> <span class="s1">'{print $1}'</span><span class="si">)</span><span class="sh">",
        "size": </span><span class="si">$(</span><span class="nb">echo</span> <span class="nt">-n</span> <span class="s2">"</span><span class="nv">$SKILL_DATA</span><span class="s2">"</span> | <span class="nb">wc</span> <span class="nt">-c</span><span class="si">)</span><span class="sh">
      }
    }
  ]
}
</span><span class="no">EOF

</span><span class="c"># Discover via ARD over ADS</span>
<span class="nv">$ </span>curl <span class="nt">-s</span> http://localhost:8889/v1/agents | <span class="se">\</span>
    jq <span class="nt">-r</span> <span class="s1">'.results[] | "\(.displayName) \(.identifier) \(.mediaType)"'</span>

<span class="c"># Verify the record</span>
<span class="nv">$ </span>dirctl verify baeareieojr2mtkdtddnllqhanf3aev4qcsd5rz6oj4by6f563l3mmnro5y

<span class="c"># Stop the ADS daemon</span>
<span class="nv">$ </span>dirctl daemon stop
</code></pre></div></div>

<h2 id="what-the-specs-leave-unsolved">What the specs leave unsolved</h2>

<p>Both the AI Catalog and ARD specifications are scrupulous about scope. They define the <em>what</em> and the <em>how</em>, but not the <em>where</em> or the <em>who</em>. They describe identity, federation, distribution, and security but do not ship a control plane that supports it. The table below summarizes how the higher-level specification concepts map to the underlying ADS primitives and what is solved by ADS.</p>

<table>
  <thead>
    <tr>
      <th>Spec concept</th>
      <th>ADS supported primitives</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Artifact/Publisher identity</td>
      <td>SPIFFE + DNS/HTTPS identity</td>
    </tr>
    <tr>
      <td>Content integrity</td>
      <td>CID (content addressing)</td>
    </tr>
    <tr>
      <td>Authenticity</td>
      <td>OIDC / key-based sign + verify</td>
    </tr>
    <tr>
      <td>Search</td>
      <td>ARD REST API (without <code class="language-plaintext highlighter-rouge">POST /search</code>)</td>
    </tr>
    <tr>
      <td>Federation</td>
      <td>libp2p DHT two-phase routing + sync</td>
    </tr>
    <tr>
      <td>Distribution</td>
      <td>OCI v1.1 distribution spec</td>
    </tr>
  </tbody>
</table>

<h3 id="identity">Identity</h3>

<p>AI Catalog’s <a href="https://agent-card.github.io/ai-catalog/#trust-manifest">Trust Manifest</a> carries <code class="language-plaintext highlighter-rouge">identity</code>, <code class="language-plaintext highlighter-rouge">attestations</code>, <code class="language-plaintext highlighter-rouge">provenance</code> and an optional <code class="language-plaintext highlighter-rouge">signature</code> field for security policies. ARD reuses this object and additionally <em>requires</em> that the cryptographic identity aligns with the domain embedded in the resource’s URN. Both specs describe verification procedures, but neither defines how. ADS already runs one. Records are addressed by <a href="https://github.com/multiformats/cid">CID</a>, so <code class="language-plaintext highlighter-rouge">provenance</code> models are native, not bolted on. Workload identity comes from <a href="https://spiffe.io/">SPIFFE</a> and <a href="https://datatracker.ietf.org/doc/html/rfc1035/">DNS/HTTPS</a> where every component gets a verifiable ID, which is precisely the <code class="language-plaintext highlighter-rouge">identity</code> ARD wants anchored to a publisher. ADS also ships a signing and verification plane, so the <code class="language-plaintext highlighter-rouge">signature</code> is not just a placeholder but actually a verifiable field over the data.</p>

<h3 id="federation">Federation</h3>

<p>ARD’s federation model is elegant precisely because it assumes the implementations can route. <em>“Query upstream registries and merge”</em> is one HTTP line in the spec and a distributed-systems problem in practice. ADS solves the routing problem with a <a href="https://github.com/libp2p/specs/tree/master/kad-dht">libp2p Kad-DHT</a> and a two-phase lookup: <em>capability to CIDs</em>, then <em>CID to servers</em>. An ARD <code class="language-plaintext highlighter-rouge">referrals</code> response is, underneath, a projection of that routing table.</p>

<h3 id="distribution">Distribution</h3>

<p>AI Catalog maps the logical format onto OCI registries for content-addressed storage. ADS uses the <a href="https://github.com/opencontainers/distribution-spec">OCI distribution spec</a> as its reference storage model. The mapping the spec describes as future work is the mechanism ADS already ships today. When a record is pushed to ADS, it is automatically stored in the given OCI registry and made available for fetch by CID. The ARD <code class="language-plaintext highlighter-rouge">url</code> field can directly point to the OCI location, or it can point to the ADS node which proxies the fetch.</p>

<h2 id="end-to-end-discovery-to-invocation-with-ard-over-ads">End-to-End Discovery-to-Invocation with ARD over ADS</h2>

<p><a href="https://github.com/agntcy/dir/releases/tag/v1.5.0">ADS v1.5</a> ships with a reference implementation of the ARD specification. A publisher can push an artifact to ADS and have it automatically available through any ARD client, regardless of whether the client speaks MCP, A2A, or any other protocol. The publisher does not need to implement multiple discovery protocols; they can rely on ADS to bridge the gap.</p>

<p>Below is a sequence diagram showing a sample end-to-end flow, from discovery to invocation, across heterogeneous protocols. The idea is simple: an agentic application queries ADS over ARD, discovers capabilities, verifies trust, and dispatches work to the appropriate protocol based on its requirements.</p>

<blockquote>
  <p>Full code samples are available in <a href="https://github.com/agntcy/dir/tree/main/samples">github.com/agntcy/dir</a> repository.</p>
</blockquote>

<pre><code class="language-mermaid">sequenceDiagram
    autonumber
    participant U as User
    participant O as Application (Go/CLI)
    participant ADS as ADS node (ARD endpoint)
	participant DHT as DHT federation layer
	participant FADS as Federated ADS node
    participant P as Publisher
    participant T as Tool (MCP / A2A / REST)

	P-&gt;&gt;ADS: push signed OASF record
	ADS-&gt;&gt;DHT: announce capability + CID
	DHT--&gt;&gt;ADS: discoverable in federation

    U-&gt;&gt;O: "Book a flight to Tokyo and file the expense"
    O-&gt;&gt;ADS: POST /search { text, filter: type in [a2a, mcp] }
	ADS-&gt;&gt;DHT: federated lookup (capability -&gt; CID -&gt; providers)
	DHT--&gt;&gt;ADS: provider nodes + matching CIDs
	ADS-&gt;&gt;FADS: fetch federated records by CID
	FADS--&gt;&gt;ADS: sync federated records
    ADS--&gt;&gt;O: ranked CatalogEntries (type + url + score)
    O-&gt;&gt;O: select top entry per sub-task
    O-&gt;&gt;O: verify bindings + signature
    O-&gt;&gt;T: dispatch by media type → native protocol
    T--&gt;&gt;O: result
    O--&gt;&gt;U: task complete
</code></pre>

<p>The decisive step is where the application dispatches runs <strong>by media type</strong>. ADS doesn’t invoke anything; it tells the application <em>which</em> protocol each capability speaks, and the application routes accordingly. This is how one discovery query spans heterogeneous protocols and domains, exactly as envisioned in the agnostic discovery model of ADS.</p>

<h2 id="building-the-application">Building the application</h2>

<p>We use the following tools and libraries to build a sample producer/consumer application:</p>
<ul>
  <li><strong>ARD consumption API</strong>: plain <code class="language-plaintext highlighter-rouge">net/http</code> or <code class="language-plaintext highlighter-rouge">curl</code> for the ARD consumption API used for discovery.</li>
  <li><strong>ADS client tools</strong>: <a href="https://github.com/agntcy/dir/tree/main/client">Go SDK</a> or <a href="https://github.com/agntcy/dir/tree/main/cli">dirctl</a> CLI for pushing, signing, and verifying records.</li>
</ul>

<p>Code snippets below are simplified for clarity and focus on ideas. The samples in the repository include full working implementations.</p>

<h3 id="step-1-publish-an-agentic-capability-to-ads">Step 1: Publish an agentic capability to ADS</h3>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">(</span>
  <span class="n">adsclient</span> <span class="s">"github.com/agntcy/dir/client"</span>
  <span class="n">corev1</span> <span class="s">"github.com/agntcy/dir/api/core/v1"</span>
<span class="p">)</span>

<span class="c">// Publish an OASF record to ADS, sign it, then announce to DHT for federated discovery.</span>
<span class="c">// Local node indexes it and can serve it locally over ADS or ARD right away.</span>
<span class="k">func</span> <span class="n">Publish</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">client</span> <span class="o">*</span><span class="n">adsclient</span><span class="o">.</span><span class="n">Client</span><span class="p">)</span> <span class="kt">error</span> <span class="p">{</span>
  <span class="c">// Push locally</span>
  <span class="n">ref</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">client</span><span class="o">.</span><span class="n">Push</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">corev1</span><span class="o">.</span><span class="n">New</span><span class="p">(</span><span class="o">&amp;</span><span class="n">typesv1</span><span class="o">.</span><span class="n">Record</span><span class="p">{</span>
      <span class="n">Name</span><span class="o">:</span> <span class="s">"weather-agent"</span><span class="p">,</span>
      <span class="n">Version</span><span class="o">:</span> <span class="s">"1.0.0"</span><span class="p">,</span>
      <span class="c">// ...</span>
  <span class="p">}))</span>
  <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
    <span class="k">return</span> <span class="n">err</span>
  <span class="p">}</span>

  <span class="c">// Sign the record to prove authenticity and integrity.</span>
  <span class="c">// Provider can be a private key or OIDC token.</span>
  <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">client</span><span class="o">.</span><span class="n">Sign</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="p">{</span><span class="n">ref</span><span class="p">,</span> <span class="n">provider</span><span class="p">});</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
      <span class="k">return</span> <span class="n">err</span>
  <span class="p">}</span>

  <span class="c">// Publish to DHT for federated discovery, so other nodes can find it.</span>
  <span class="c">// Optional if only local discovery is needed.</span>
  <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">client</span><span class="o">.</span><span class="n">Publish</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="p">{</span><span class="n">ref</span><span class="p">});</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
      <span class="k">return</span> <span class="n">err</span>
  <span class="p">}</span>

  <span class="k">return</span> <span class="no">nil</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="step-2-discover-over-ard">Step 2: Discover over ARD</h3>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">(</span>
  <span class="n">catalogv1</span> <span class="s">"github.com/agntcy/dir/api/catalog/v1"</span>
<span class="p">)</span>

<span class="c">// Discover asks a local ADS node via its ARD endpoint for capabilities matching a task.</span>
<span class="c">// The consumer application can extend this with more complex query matching and ranking logic as needed.</span>
<span class="k">func</span> <span class="n">Discover</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">taskQuery</span> <span class="kt">string</span><span class="p">)</span> <span class="p">([]</span><span class="o">*</span><span class="n">catalogv1</span><span class="o">.</span><span class="n">CatalogEntry</span><span class="p">,</span> <span class="kt">error</span><span class="p">)</span> <span class="p">{</span>
  <span class="c">// Find relevant capabilities</span>
  <span class="n">httpReq</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">http</span><span class="o">.</span><span class="n">NewRequestWithContext</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">http</span><span class="o">.</span><span class="n">MethodGet</span><span class="p">,</span> <span class="s">"http://localhost:8889/v1/agents"</span><span class="p">,</span> <span class="n">bytes</span><span class="o">.</span><span class="n">NewReader</span><span class="p">([]</span><span class="kt">byte</span><span class="p">(</span><span class="n">taskQuery</span><span class="p">)))</span>
  <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
    <span class="k">return</span> <span class="no">nil</span><span class="p">,</span> <span class="n">err</span>
  <span class="p">}</span>
  <span class="n">httpReq</span><span class="o">.</span><span class="n">Header</span><span class="o">.</span><span class="n">Set</span><span class="p">(</span><span class="s">"Content-Type"</span><span class="p">,</span> <span class="s">"application/json"</span><span class="p">)</span>

  <span class="n">resp</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="p">(</span><span class="o">&amp;</span><span class="n">http</span><span class="o">.</span><span class="n">Client</span><span class="p">{</span><span class="n">Timeout</span><span class="o">:</span> <span class="m">10</span> <span class="o">*</span> <span class="n">time</span><span class="o">.</span><span class="n">Second</span><span class="p">})</span><span class="o">.</span><span class="n">Do</span><span class="p">(</span><span class="n">httpReq</span><span class="p">)</span>
  <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
    <span class="k">return</span> <span class="no">nil</span><span class="p">,</span> <span class="n">err</span>
  <span class="p">}</span>
  <span class="k">defer</span> <span class="n">resp</span><span class="o">.</span><span class="n">Body</span><span class="o">.</span><span class="n">Close</span><span class="p">()</span>

  <span class="c">// Read the response</span>
  <span class="n">outBytes</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">io</span><span class="o">.</span><span class="n">ReadAll</span><span class="p">(</span><span class="n">resp</span><span class="o">.</span><span class="n">Body</span><span class="p">)</span>
  <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
    <span class="k">return</span> <span class="no">nil</span><span class="p">,</span> <span class="n">err</span>
  <span class="p">}</span>

  <span class="c">// Unmarshal the response into a ListAgentsResponse</span>
  <span class="c">// Note: use protojson since the response is a protobuf message</span>
  <span class="c">// and we're communicating over HTTP with JSON encoding</span>
  <span class="k">var</span> <span class="n">out</span> <span class="n">catalogv1</span><span class="o">.</span><span class="n">ListAgentsResponse</span>
  <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">protojson</span><span class="o">.</span><span class="n">Unmarshal</span><span class="p">(</span><span class="n">outBytes</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">out</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
    <span class="k">return</span> <span class="no">nil</span><span class="p">,</span> <span class="n">err</span>
  <span class="p">}</span>

  <span class="k">return</span> <span class="n">out</span><span class="o">.</span><span class="n">Results</span><span class="p">,</span> <span class="no">nil</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="step-2-dispatch-by-media-type-to-the-native-protocol">Step 2: Dispatch by media type to the native protocol</h3>

<p>Discovery hands us a <code class="language-plaintext highlighter-rouge">mediaType</code> and other relevant details such as <code class="language-plaintext highlighter-rouge">url</code> about the discovered capabilities. The application’s job is now to route to the right execution path and not to “guess” a protocol:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">(</span>
  <span class="n">catalogv1</span> <span class="s">"github.com/agntcy/dir/api/catalog/v1"</span>
<span class="p">)</span>

<span class="c">// Dispatch routes a discovered entry to its native invocation path.</span>
<span class="k">func</span> <span class="n">Dispatch</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">entry</span> <span class="o">*</span><span class="n">catalogv1</span><span class="o">.</span><span class="n">CatalogEntry</span><span class="p">,</span> <span class="n">task</span> <span class="k">map</span><span class="p">[</span><span class="kt">string</span><span class="p">]</span><span class="n">any</span><span class="p">)</span> <span class="kt">error</span> <span class="p">{</span>
  <span class="k">switch</span> <span class="n">entry</span><span class="o">.</span><span class="n">MediaType</span> <span class="p">{</span>
  <span class="k">case</span> <span class="s">"application/mcp-server-card+json"</span><span class="o">:</span>
    <span class="c">// Fetch the MCP server descriptor at entry.URL, then speak JSON-RPC.</span>
    <span class="k">return</span> <span class="n">invokeMCP</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">entry</span><span class="o">.</span><span class="n">URL</span><span class="p">,</span> <span class="n">task</span><span class="p">)</span>
  <span class="k">case</span> <span class="s">"application/a2a-agent-card+json"</span><span class="o">:</span>
    <span class="c">// Load the A2A agent card at entry.URL, then speak A2A.</span>
    <span class="k">return</span> <span class="n">invokeA2A</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">entry</span><span class="o">.</span><span class="n">URL</span><span class="p">,</span> <span class="n">task</span><span class="p">)</span>
  <span class="k">case</span> <span class="s">"application/agentskill+md"</span><span class="o">:</span>
    <span class="c">// Pull the agent skill markdown at entry.URL, then execute.</span>
    <span class="k">return</span> <span class="n">invokeAgentSkill</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">entry</span><span class="o">.</span><span class="n">URL</span><span class="p">,</span> <span class="n">task</span><span class="p">)</span>
  <span class="k">default</span><span class="o">:</span>
    <span class="k">return</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"unsupported media type: %s"</span><span class="p">,</span> <span class="n">entry</span><span class="o">.</span><span class="n">MediaType</span><span class="p">)</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>This <code class="language-plaintext highlighter-rouge">switch</code> is the interoperability boundary.</strong> ARD guarantees the <code class="language-plaintext highlighter-rouge">mediaType</code> is a well-known IANA media type; the consumer owns the mapping logic and decides what to do with each type. Add a new artifact type to your stack, and you add one <code class="language-plaintext highlighter-rouge">case</code> while the discovery itself is unchanged.</p>

<h3 id="step-3-verify-trust-before-you-invoke">Step 3: Verify trust before you invoke</h3>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">(</span>
  <span class="n">adsclient</span> <span class="s">"github.com/agntcy/dir/client"</span>
  <span class="n">corev1</span> <span class="s">"github.com/agntcy/dir/api/core/v1"</span>
<span class="p">)</span>

<span class="c">// Verify confirms the entry owns its claimed identity and trust signals.</span>
<span class="c">// Identifier format is expected to be urn:ai:org.agntcy:cid:&lt;cid&gt;</span>
<span class="k">func</span> <span class="n">Verify</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">client</span> <span class="o">*</span><span class="n">adsclient</span><span class="o">.</span><span class="n">Client</span><span class="p">,</span> <span class="n">e</span> <span class="o">*</span><span class="n">catalogv1</span><span class="o">.</span><span class="n">CatalogEntry</span><span class="p">)</span> <span class="kt">error</span> <span class="p">{</span>
  <span class="c">// Get AGNTYCY identifier</span>
  <span class="n">cid</span><span class="p">,</span> <span class="n">found</span> <span class="o">:=</span> <span class="n">strings</span><span class="o">.</span><span class="n">CutPrefix</span><span class="p">(</span><span class="n">e</span><span class="o">.</span><span class="n">Identifier</span><span class="p">,</span> <span class="s">"urn:ai:org.agntcy:cid:"</span><span class="p">)</span>
  <span class="k">if</span> <span class="o">!</span><span class="n">found</span> <span class="p">{</span>
    <span class="k">return</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"invalid identifier format: %s"</span><span class="p">,</span> <span class="n">e</span><span class="o">.</span><span class="n">Identifier</span><span class="p">)</span>
  <span class="p">}</span>

  <span class="c">// Verify provenance</span>
  <span class="k">if</span> <span class="n">verified</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">client</span><span class="o">.</span><span class="n">Verify</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">adssignv1</span><span class="o">.</span><span class="n">VerifyRequest</span><span class="p">{</span>
    <span class="n">RecordRef</span><span class="o">:</span> <span class="o">&amp;</span><span class="n">adscorev1</span><span class="o">.</span><span class="n">RecordRef</span><span class="p">{</span><span class="n">Cid</span><span class="o">:</span> <span class="n">cid</span><span class="p">},</span>
  <span class="p">});</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
    <span class="k">return</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"failed to verify provenance: %w"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
  <span class="p">}</span> <span class="k">else</span> <span class="k">if</span> <span class="o">!</span><span class="n">verified</span><span class="o">.</span><span class="n">Success</span> <span class="p">{</span>
    <span class="k">return</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"provenance verification failed: %s"</span><span class="p">,</span> <span class="o">*</span><span class="n">verified</span><span class="o">.</span><span class="n">ErrorMessage</span><span class="p">)</span>
  <span class="p">}</span>

  <span class="c">// Verify naming</span>
  <span class="c">// We do not fail here, simply log a warning</span>
  <span class="k">if</span> <span class="n">verified</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">client</span><span class="o">.</span><span class="n">GetVerificationInfo</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">cid</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
    <span class="n">fmt</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"Warning: failed to get verification info for CID %s: %v</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">cid</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
  <span class="p">}</span> <span class="k">else</span> <span class="k">if</span> <span class="o">!</span><span class="n">verified</span><span class="o">.</span><span class="n">Verified</span> <span class="p">{</span>
    <span class="n">fmt</span><span class="o">.</span><span class="n">Printf</span><span class="p">(</span><span class="s">"Warning: identity verification failed for CID %s: %+v</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">cid</span><span class="p">,</span> <span class="n">verified</span><span class="p">)</span>
  <span class="p">}</span>

  <span class="k">return</span> <span class="no">nil</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="step-4-tie-it-together">Step 4: Tie it together</h3>

<p>Once the discovery, verification, and dispatch functions are implemented, the consumer application can orchestrate them in a single flow. The following function demonstrates how to handle a task query by discovering capabilities, verifying trust, and dispatching to the appropriate protocol:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">(</span>
  <span class="n">adsclient</span> <span class="s">"github.com/agntcy/dir/client"</span>
  <span class="n">corev1</span> <span class="s">"github.com/agntcy/dir/api/core/v1"</span>
<span class="p">)</span>

<span class="k">func</span> <span class="n">Handle</span><span class="p">(</span><span class="n">ctx</span> <span class="n">context</span><span class="o">.</span><span class="n">Context</span><span class="p">,</span> <span class="n">client</span> <span class="o">*</span><span class="n">adsclient</span><span class="o">.</span><span class="n">Client</span><span class="p">,</span> <span class="n">taskQuery</span> <span class="kt">string</span><span class="p">)</span> <span class="kt">error</span> <span class="p">{</span>
  <span class="n">entries</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">Discover</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">taskQuery</span><span class="p">)</span>
  <span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
    <span class="k">return</span> <span class="n">err</span>
  <span class="p">}</span>

  <span class="k">for</span> <span class="n">_</span><span class="p">,</span> <span class="n">e</span> <span class="o">:=</span> <span class="k">range</span> <span class="n">entries</span> <span class="p">{</span>
    <span class="c">// Skip anything we can't verify</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">Verify</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">client</span><span class="p">,</span> <span class="n">e</span><span class="p">);</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
      <span class="k">continue</span>
    <span class="p">}</span>

    <span class="c">// First trusted, working capability wins</span>
    <span class="k">if</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">Dispatch</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">e</span><span class="p">,</span> <span class="k">map</span><span class="p">[</span><span class="kt">string</span><span class="p">]</span><span class="n">any</span><span class="p">{</span><span class="s">"task"</span><span class="o">:</span> <span class="n">taskQuery</span><span class="p">});</span> <span class="n">err</span> <span class="o">==</span> <span class="no">nil</span> <span class="p">{</span>
      <span class="k">return</span> <span class="no">nil</span>
    <span class="p">}</span>
  <span class="p">}</span>

  <span class="k">return</span> <span class="n">errors</span><span class="o">.</span><span class="n">New</span><span class="p">(</span><span class="s">"no trusted capability satisfied the task"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="where-the-ads-control-plane-earns-its-keep">Where the ADS control plane earns its keep</h2>

<p>The consumption side is plain HTTP, but the <em>security gate</em> of what comes back depends entirely on the ADS control plane that produced it. When you publish the agents your application later discovers, you use the ADS SDK, not raw HTTP. This is the division of labor that makes the whole stack work:</p>

<pre><code class="language-mermaid">flowchart LR
    subgraph Control["ADS control plane (ADS SDK)"]
        PUSH["Push + Sign"]
        INDEX["Announce + Discover"]
    end
    subgraph Consume["ARD consumption (net/http)"]
        SEARCH["GET /agents"]
        PROJECT["OASF → AI Catalog entry"]
    end
    PUSH --&gt; INDEX --&gt; PROJECT --&gt; SEARCH --&gt; ORCH["AI workflows"]

    style PUSH fill:#e8eefb,stroke:#0251af,color:#1c1e21
    style INDEX fill:#e8eefb,stroke:#0251af,color:#1c1e21
    style SEARCH fill:#0251af,color:#f3f6fd
    style PROJECT fill:#0251af,color:#f3f6fd
    style ORCH fill:#cfe1fb,color:#1c1e21
</code></pre>

<p>Publishers and platform teams drive the <strong>control plane</strong> (push, sign, announce). Clients ride the <strong>consumption plane</strong> (discover, verify, dispatch). The contract between them is the AI Catalog and ARD, which is why an agentic resource can be published and discovered regardless of the protocol or domain.</p>

<h3 id="why-this-matters">Why this matters</h3>

<p>ADS is not just another registry; it is the shared control plane that makes the discovery work. The AI Catalog and ARD specifications define the primitives, but without a control plane, they are just static formats. ADS provides a secure, federated, capability-driven discovery layer that makes them work, ensuring:</p>

<ul>
  <li><strong>No protocol lock-in for publishers.</strong> A record pushed to ADS is reachable through an ARD search registry, local gRPC query, or across the network. The publisher does not need to worry.</li>
  <li><strong>Security is solved once, at the bottom.</strong> SPIFFE identity, HTTPS/DNS resolution, CID integrity, and signing are available directly. Higher layers can focus on the discovery without re-implementing security.</li>
  <li><strong>Federation is real, not only desirable.</strong> ARD’s discovery modes need a routing fabric to be more than a single registry. The DHT can be that fabric.</li>
</ul>

<h2 id="whats-next">What’s Next</h2>

<p>The current ARD implementation in ADS is a reference implementation of the spec, but there are many features we plan to add in future releases of ADS:</p>

<ul>
  <li><strong>ARD POST /search</strong> endpoint with advanced query capabilities and relevance ranking. Currently available through DIR MCP server only.</li>
  <li><strong>DID-based identity</strong> for decentralized identifiers and verifiable credentials.</li>
  <li><strong>Client tooling and SDKs</strong> to simplify integration with ADS and ARD, both for publishers and consumers.</li>
</ul>

<p>If you’re building multi-agent systems, we’d love to hear your use cases.
Join our <a href="https://join.slack.com/t/agntcy/shared_invite/zt-3hb4p7bo0-5H2otGjxGt9OQ1g5jzK_GQ">Slack community</a>
or open an issue on <a href="https://github.com/agntcy/dir">GitHub</a>.</p>

<hr />

<p><em>ADS and its components are developed by AGNTCY Contributors and released under the Apache 2.0 License.</em></p>]]></content><author><name>Ramiz Polic</name></author><category term="technical" /><category term="ads" /><category term="a2a" /><category term="mcp" /><category term="agent-skills" /><category term="discovery" /><category term="ai-catalog" /><category term="ard" /><summary type="html"><![CDATA[A developer integrating agentic capabilities today faces a discovery problem that looks deceptively like a solved one. We have standards, registries, catalogs, and well-knowns; surely finding the right tool is just a lookup? In practice it is not, because the ecosystem has fragmented into protocols that each describe the same artifact differently; be it a resource, tool, agent, or a full agentic system. And while the artifact is one thing; its discovery representations are many. The question of discovery and interoperability is not achieved by picking a winning format or specification. AI Catalog and Agentic Resource Discovery (ARD) are not competing standards; they are complementary lenses on the same underlying problem. Each one tries to answers a shared set of questions - what is this artifact, what can it do, how to use it, who published it, and can I trust it. Most of these formats are higher-level discovery protocols: they define a vocabulary and an envelope. What they do not define is the hard part – a secure, federated, capability-driven discovery layer that actually links these artifacts, routes queries across organizational boundaries, and provides security and control policies embedded in. That layer is exactly what Agent Directory Service (ADS) provides - not another competing registry, but a low-level protocol the others can be built on. Three layers, one stack A recurring design mistake is to collapse “the format”, “the query API”, and “the system” into one monolith. The agentic ecosystem deliberately keeps them apart, and the separation is the whole reason it interoperates. flowchart BT subgraph L3["ADS -- Discovery &amp; Control Plane"] OASF["Records"] API["API (gRPC / REST)"] CID["Identifiers (CIDs / DIDs)"] DHT["Routing (DHT)"] OCI["Storage (OCI)"] SIGN["Security / Policies"] end subgraph L2["ARD -- Consumption API"] WK["GET /.well-known/ai-catalog.json"] SEARCH["POST /search"] LIST["GET /agents"] end subgraph L1["AI Catalog -- Index / Content Standard"] ENTRY["Catalog Entry Schema"] TRUST["Trust Manifest Schema"] TYPES["IANA media types"] end L1 --&gt;|"mapped by"| L2 L2 --&gt;|"implemented by"| L3 L3 --&gt;|"consumed by"| CLIENT["AI clients / applications / systems"] style CLIENT fill:#800080,stroke:#800080,color:#fff style OASF fill:#023a7a,color:#f3f6fd style API fill:#023a7a,color:#f3f6fd style CID fill:#023a7a,color:#f3f6fd style DHT fill:#023a7a,color:#f3f6fd style OCI fill:#023a7a,color:#f3f6fd style SIGN fill:#023a7a,color:#f3f6fd style WK fill:#023a7a,color:#f3f6fd style SEARCH fill:#023a7a,color:#f3f6fd style LIST fill:#023a7a,color:#f3f6fd style ENTRY fill:#023a7a,color:#f3f6fd style TRUST fill:#023a7a,color:#f3f6fd style TYPES fill:#023a7a,color:#f3f6fd Layer 1: AI Catalog as the index standard The AI Catalog is a typed, nestable JSON container for describing heterogeneous AI artifacts. Each entry identifies an artifact by an IANA media type and a domain-anchored URN. The catalog is therefore protocol-agnostic: it can describe an MCP server, an A2A agent, a custom tool, or a set of catalogs in the same way. On its own, the catalog is a static index intended to be consumed by AI applications and served over higher-level discovery protocols like ARD. { "identifier": "urn:ai:acme.com:agents:weather", "displayName": "Weather Agent", "type": "application/mcp-server+json", "url": "https://api.acme.com/mcp/weather.json", "capabilities": ["WeatherTool", "ForecastTool"], "representativeQueries": [ "current wind speed in Chicago", "5-day forecast for Seattle", ] } Layer 2: ARD as the consumption API The Agentic Resource Discovery is a read-only discovery API layer on top of the AI Catalog. It defines how AI artifacts are cataloged, discovered, and searched within and across registries. It promotes the search-first discovery where registries maintain a shared index while the clients find capabilities over standard HTTP REST APIs instead of loading every tool description into an LLM context window. The ARD spec is intentionally agnostic about the underlying storage, identity, and routing mechanisms; it only defines the contract but does not ship discovery or control plane. $ curl -s https://your-discovery-service/search \ -H 'content-type: application/json' \ -d '{"query": {"text":"book me a flight to Tokyo"}, "pageSize":3}' { "results": [ { "identifier": "urn:ai:acme.com:travel:concierge", "displayName": "Travel Concierge", "type": "application/mcp-server+json", "url": "https://api.acme.com/mcp/travel.json", "score": 95, "source": "https://registry.acme.com/api/v1/" } ], "referrals": [ { "identifier": "urn:ai:example.org:registry", "type": "application/ai-registry", "url": "https://registry.acme.com/search" } ] } Layer 3: ADS as the discovery and control plane ARD is a contract; something has to fill and manage the data, storage, routing, and trust. That role is performed by ADS, the only layer with a control plane: it ingests records, content-addresses them with CIDs, signs and verifies them, announces them to a DHT, replicates them over OCI, and enforces zero-trust identity between data, clients, and nodes. Where ARD answers “what fits this task?”, ADS owns “what exists, best fit, who vouches for it, and where it physically lives.” The clean consequence is that ADS is a reference implementation of ARD. ADS is a distributed registry that exposes its OASF records as AI Catalog entries and serves them securely over the ARD endpoints so the same inventory is reachable by both ADS and ARD client integrations. Operators publish records to ADS while the consumers discover through ARD, verify through ADS, and invoke through MCP or A2A – one flow, many protocols. # Start local ADS node in the background $ dirctl daemon start &amp; # Define sample agent skill data $ export SKILL_DATA="This is a sample agent skill for weather details over AccuWeather." # Push and sign a sample record $ cat &lt;&lt;EOF | dirctl push --sign --stdin { "name": "Weather Agent", "schema_version": "1.0.0", "version": "1.0.0", "description": "Helps with tracking weather information", "authors": ["Example Organization"], "created_at": "2025-01-01T00:00:00Z", "skills": [ {"name": "natural_language_processing/natural_language_understanding/contextual_comprehension"} ], "modules": [ { "name": "core/language_model/agentskills", "data": { "skill_file": "SKILL.md", "skill_manifest": { "name": "Weather Agent", "description": "Helps with tracking weather information", "version": "1.0.0" } }, "artifact": { "data": "$(echo "$SKILL_DATA" | base64)", "media_type": "application/agentskill+md", "digest": "sha256:$(echo -n "$SKILL_DATA" | sha256sum | awk '{print $1}')", "size": $(echo -n "$SKILL_DATA" | wc -c) } } ] } EOF # Discover via ARD over ADS $ curl -s http://localhost:8889/v1/agents | \ jq -r '.results[] | "\(.displayName) \(.identifier) \(.mediaType)"' # Verify the record $ dirctl verify baeareieojr2mtkdtddnllqhanf3aev4qcsd5rz6oj4by6f563l3mmnro5y # Stop the ADS daemon $ dirctl daemon stop What the specs leave unsolved Both the AI Catalog and ARD specifications are scrupulous about scope. They define the what and the how, but not the where or the who. They describe identity, federation, distribution, and security but do not ship a control plane that supports it. The table below summarizes how the higher-level specification concepts map to the underlying ADS primitives and what is solved by ADS. Spec concept ADS supported primitives Artifact/Publisher identity SPIFFE + DNS/HTTPS identity Content integrity CID (content addressing) Authenticity OIDC / key-based sign + verify Search ARD REST API (without POST /search) Federation libp2p DHT two-phase routing + sync Distribution OCI v1.1 distribution spec Identity AI Catalog’s Trust Manifest carries identity, attestations, provenance and an optional signature field for security policies. ARD reuses this object and additionally requires that the cryptographic identity aligns with the domain embedded in the resource’s URN. Both specs describe verification procedures, but neither defines how. ADS already runs one. Records are addressed by CID, so provenance models are native, not bolted on. Workload identity comes from SPIFFE and DNS/HTTPS where every component gets a verifiable ID, which is precisely the identity ARD wants anchored to a publisher. ADS also ships a signing and verification plane, so the signature is not just a placeholder but actually a verifiable field over the data. Federation ARD’s federation model is elegant precisely because it assumes the implementations can route. “Query upstream registries and merge” is one HTTP line in the spec and a distributed-systems problem in practice. ADS solves the routing problem with a libp2p Kad-DHT and a two-phase lookup: capability to CIDs, then CID to servers. An ARD referrals response is, underneath, a projection of that routing table. Distribution AI Catalog maps the logical format onto OCI registries for content-addressed storage. ADS uses the OCI distribution spec as its reference storage model. The mapping the spec describes as future work is the mechanism ADS already ships today. When a record is pushed to ADS, it is automatically stored in the given OCI registry and made available for fetch by CID. The ARD url field can directly point to the OCI location, or it can point to the ADS node which proxies the fetch. End-to-End Discovery-to-Invocation with ARD over ADS ADS v1.5 ships with a reference implementation of the ARD specification. A publisher can push an artifact to ADS and have it automatically available through any ARD client, regardless of whether the client speaks MCP, A2A, or any other protocol. The publisher does not need to implement multiple discovery protocols; they can rely on ADS to bridge the gap. Below is a sequence diagram showing a sample end-to-end flow, from discovery to invocation, across heterogeneous protocols. The idea is simple: an agentic application queries ADS over ARD, discovers capabilities, verifies trust, and dispatches work to the appropriate protocol based on its requirements. Full code samples are available in github.com/agntcy/dir repository. sequenceDiagram autonumber participant U as User participant O as Application (Go/CLI) participant ADS as ADS node (ARD endpoint) participant DHT as DHT federation layer participant FADS as Federated ADS node participant P as Publisher participant T as Tool (MCP / A2A / REST) P-&gt;&gt;ADS: push signed OASF record ADS-&gt;&gt;DHT: announce capability + CID DHT--&gt;&gt;ADS: discoverable in federation U-&gt;&gt;O: "Book a flight to Tokyo and file the expense" O-&gt;&gt;ADS: POST /search { text, filter: type in [a2a, mcp] } ADS-&gt;&gt;DHT: federated lookup (capability -&gt; CID -&gt; providers) DHT--&gt;&gt;ADS: provider nodes + matching CIDs ADS-&gt;&gt;FADS: fetch federated records by CID FADS--&gt;&gt;ADS: sync federated records ADS--&gt;&gt;O: ranked CatalogEntries (type + url + score) O-&gt;&gt;O: select top entry per sub-task O-&gt;&gt;O: verify bindings + signature O-&gt;&gt;T: dispatch by media type → native protocol T--&gt;&gt;O: result O--&gt;&gt;U: task complete The decisive step is where the application dispatches runs by media type. ADS doesn’t invoke anything; it tells the application which protocol each capability speaks, and the application routes accordingly. This is how one discovery query spans heterogeneous protocols and domains, exactly as envisioned in the agnostic discovery model of ADS. Building the application We use the following tools and libraries to build a sample producer/consumer application: ARD consumption API: plain net/http or curl for the ARD consumption API used for discovery. ADS client tools: Go SDK or dirctl CLI for pushing, signing, and verifying records. Code snippets below are simplified for clarity and focus on ideas. The samples in the repository include full working implementations. Step 1: Publish an agentic capability to ADS import ( adsclient "github.com/agntcy/dir/client" corev1 "github.com/agntcy/dir/api/core/v1" ) // Publish an OASF record to ADS, sign it, then announce to DHT for federated discovery. // Local node indexes it and can serve it locally over ADS or ARD right away. func Publish(ctx context.Context, client *adsclient.Client) error { // Push locally ref, err := client.Push(ctx, corev1.New(&amp;typesv1.Record{ Name: "weather-agent", Version: "1.0.0", // ... })) if err != nil { return err } // Sign the record to prove authenticity and integrity. // Provider can be a private key or OIDC token. if err := client.Sign(ctx, {ref, provider}); err != nil { return err } // Publish to DHT for federated discovery, so other nodes can find it. // Optional if only local discovery is needed. if err := client.Publish(ctx, {ref}); err != nil { return err } return nil } Step 2: Discover over ARD import ( catalogv1 "github.com/agntcy/dir/api/catalog/v1" ) // Discover asks a local ADS node via its ARD endpoint for capabilities matching a task. // The consumer application can extend this with more complex query matching and ranking logic as needed. func Discover(ctx context.Context, taskQuery string) ([]*catalogv1.CatalogEntry, error) { // Find relevant capabilities httpReq, err := http.NewRequestWithContext(ctx, http.MethodGet, "http://localhost:8889/v1/agents", bytes.NewReader([]byte(taskQuery))) if err != nil { return nil, err } httpReq.Header.Set("Content-Type", "application/json") resp, err := (&amp;http.Client{Timeout: 10 * time.Second}).Do(httpReq) if err != nil { return nil, err } defer resp.Body.Close() // Read the response outBytes, err := io.ReadAll(resp.Body) if err != nil { return nil, err } // Unmarshal the response into a ListAgentsResponse // Note: use protojson since the response is a protobuf message // and we're communicating over HTTP with JSON encoding var out catalogv1.ListAgentsResponse if err := protojson.Unmarshal(outBytes, &amp;out); err != nil { return nil, err } return out.Results, nil } Step 2: Dispatch by media type to the native protocol Discovery hands us a mediaType and other relevant details such as url about the discovered capabilities. The application’s job is now to route to the right execution path and not to “guess” a protocol: import ( catalogv1 "github.com/agntcy/dir/api/catalog/v1" ) // Dispatch routes a discovered entry to its native invocation path. func Dispatch(ctx context.Context, entry *catalogv1.CatalogEntry, task map[string]any) error { switch entry.MediaType { case "application/mcp-server-card+json": // Fetch the MCP server descriptor at entry.URL, then speak JSON-RPC. return invokeMCP(ctx, entry.URL, task) case "application/a2a-agent-card+json": // Load the A2A agent card at entry.URL, then speak A2A. return invokeA2A(ctx, entry.URL, task) case "application/agentskill+md": // Pull the agent skill markdown at entry.URL, then execute. return invokeAgentSkill(ctx, entry.URL, task) default: return fmt.Errorf("unsupported media type: %s", entry.MediaType) } } This switch is the interoperability boundary. ARD guarantees the mediaType is a well-known IANA media type; the consumer owns the mapping logic and decides what to do with each type. Add a new artifact type to your stack, and you add one case while the discovery itself is unchanged. Step 3: Verify trust before you invoke import ( adsclient "github.com/agntcy/dir/client" corev1 "github.com/agntcy/dir/api/core/v1" ) // Verify confirms the entry owns its claimed identity and trust signals. // Identifier format is expected to be urn:ai:org.agntcy:cid:&lt;cid&gt; func Verify(ctx context.Context, client *adsclient.Client, e *catalogv1.CatalogEntry) error { // Get AGNTYCY identifier cid, found := strings.CutPrefix(e.Identifier, "urn:ai:org.agntcy:cid:") if !found { return fmt.Errorf("invalid identifier format: %s", e.Identifier) } // Verify provenance if verified, err := client.Verify(ctx, &amp;adssignv1.VerifyRequest{ RecordRef: &amp;adscorev1.RecordRef{Cid: cid}, }); err != nil { return fmt.Errorf("failed to verify provenance: %w", err) } else if !verified.Success { return fmt.Errorf("provenance verification failed: %s", *verified.ErrorMessage) } // Verify naming // We do not fail here, simply log a warning if verified, err := client.GetVerificationInfo(ctx, cid); err != nil { fmt.Printf("Warning: failed to get verification info for CID %s: %v\n", cid, err) } else if !verified.Verified { fmt.Printf("Warning: identity verification failed for CID %s: %+v\n", cid, verified) } return nil } Step 4: Tie it together Once the discovery, verification, and dispatch functions are implemented, the consumer application can orchestrate them in a single flow. The following function demonstrates how to handle a task query by discovering capabilities, verifying trust, and dispatching to the appropriate protocol: import ( adsclient "github.com/agntcy/dir/client" corev1 "github.com/agntcy/dir/api/core/v1" ) func Handle(ctx context.Context, client *adsclient.Client, taskQuery string) error { entries, err := Discover(ctx, taskQuery) if err != nil { return err } for _, e := range entries { // Skip anything we can't verify if err := Verify(ctx, client, e); err != nil { continue } // First trusted, working capability wins if err := Dispatch(ctx, e, map[string]any{"task": taskQuery}); err == nil { return nil } } return errors.New("no trusted capability satisfied the task") } Where the ADS control plane earns its keep The consumption side is plain HTTP, but the security gate of what comes back depends entirely on the ADS control plane that produced it. When you publish the agents your application later discovers, you use the ADS SDK, not raw HTTP. This is the division of labor that makes the whole stack work: flowchart LR subgraph Control["ADS control plane (ADS SDK)"] PUSH["Push + Sign"] INDEX["Announce + Discover"] end subgraph Consume["ARD consumption (net/http)"] SEARCH["GET /agents"] PROJECT["OASF → AI Catalog entry"] end PUSH --&gt; INDEX --&gt; PROJECT --&gt; SEARCH --&gt; ORCH["AI workflows"] style PUSH fill:#e8eefb,stroke:#0251af,color:#1c1e21 style INDEX fill:#e8eefb,stroke:#0251af,color:#1c1e21 style SEARCH fill:#0251af,color:#f3f6fd style PROJECT fill:#0251af,color:#f3f6fd style ORCH fill:#cfe1fb,color:#1c1e21 Publishers and platform teams drive the control plane (push, sign, announce). Clients ride the consumption plane (discover, verify, dispatch). The contract between them is the AI Catalog and ARD, which is why an agentic resource can be published and discovered regardless of the protocol or domain. Why this matters ADS is not just another registry; it is the shared control plane that makes the discovery work. The AI Catalog and ARD specifications define the primitives, but without a control plane, they are just static formats. ADS provides a secure, federated, capability-driven discovery layer that makes them work, ensuring: No protocol lock-in for publishers. A record pushed to ADS is reachable through an ARD search registry, local gRPC query, or across the network. The publisher does not need to worry. Security is solved once, at the bottom. SPIFFE identity, HTTPS/DNS resolution, CID integrity, and signing are available directly. Higher layers can focus on the discovery without re-implementing security. Federation is real, not only desirable. ARD’s discovery modes need a routing fabric to be more than a single registry. The DHT can be that fabric. What’s Next The current ARD implementation in ADS is a reference implementation of the spec, but there are many features we plan to add in future releases of ADS: ARD POST /search endpoint with advanced query capabilities and relevance ranking. Currently available through DIR MCP server only. DID-based identity for decentralized identifiers and verifiable credentials. Client tooling and SDKs to simplify integration with ADS and ARD, both for publishers and consumers. If you’re building multi-agent systems, we’d love to hear your use cases. Join our Slack community or open an issue on GitHub. ADS and its components are developed by AGNTCY Contributors and released under the Apache 2.0 License.]]></summary></entry></feed>